= TODO — capsule-gradle (Détroit)
:last_update: 2026-08-25 (Session 122 — **EPIC CAP-ANIM TERMINÉ 3/3 US** — cadrage rétroactif code Remotion livré PR #1 (S-121) + tests unit manquants 40 unit (AudioCodecs/RemotionConfig/RemotionTemplate/RemotionCaptureImpl/CaptureResolver REMOTION) + cucumber BDD `capsule_remotion.feature` 3 scénarios + runner dédié 16ème + 2 functional. 977 unit VERT (+40 net baseline 937), 69 functional VERT (+2 net baseline 67). `check` BUILD SUCCESSFUL. Zéro régression, zéro secret, zéro bump BOM.)

'''
== ✅ EPIC CAP-MODEL — TERMINE (session 069)

. [x] Unification modèle `capsule.feed` ↔ `capsule.CapsuleScript` (7/7 US)
. [x] `SlideType` enum migré vers `capsule.feed`
. [x] `SlideSegment` étendu (type+manimScene)
. [x] `CapsuleScriptReader` remplace `CapsuleManager.parseScript` (-64 lignes)
. [x] 6 consommateurs migrés
. [x] `capsule.CapsuleModels` purgé
. [x] 361 unit tests VERT (+124)
. [x] Cucumber boundary `consolidated_capsule_model.feature` (5 scénarios)

'''
== ✅ Sync slider 0.0.16 — TERMINE (session 070)

. [x] Bump catalogue `slider = "0.0.16"` (was 0.0.15)
. [x] `./gradlew compileKotlin` VERT avec slider 0.0.16 (mavenLocal)

'''
== ✅ CR-2 Thread safety + CR-3 TTS error handling + CR-4 XSS + CR-6 CLI + CR-7 YAML — TERMINE (session 071)

. [x] CR-2 — Thread safety + timeout `captureSlideParallel` (executor.shutdownNow finally, it.get(5, MIN))
. [x] CR-3 — Error handling TTS (retry 1× + accumulation `failedSlides` + logger.error)
. [x] CR-4 — HTML escape XSS `injectSubtitleTrack` (HtmlEscape object)
. [x] CR-6 — Robust CLI parsing (cliInt/cliDouble/cliBoolean helpers)
. [x] CR-7 — Validation YAML (déjà implémenté + test couverture)
. [x] Fix cucumber consolidated (DuplicateStepDefinitionException + feature quotes + temp file)

'''
== ✅ CR-5 DRY CapsuleConfigMerger — TERMINE (session 072)

. [x] `merge()` unifié — `yaml: T?` nullable dispatch
. [x] 10 méthodes `mergeXxxConfig`/`mergeXxxConfigNoYaml` → 5 unifiées
. [x] 4 helpers génériques `mergeStr/mergeInt/mergeDouble/mergeBoolean`
. [x] Sémantique préservée (`toBoolean()` lenient, `isNotBlank()` heuristic)
. [x] +13 unit tests NoYaml (48 total ConfigMergerTest)
. [x] +1 scénario cucumber `@config @no-yaml` + 1 step Given
. [x] 322→302 lignes (-19 net), 396 unit VERT (+12)

'''
== ✅ CR-8 Logging structuré — TERMINE (session 073)

. [x] `CapsuleConfigLogger` object pur extrait (domaine `capsule.config`)
. [x] `formatConfigLog(config): List<String>` → 4 lignes structurées (TTS/Capture/Subtitles/Manim)
. [x] Tokens préservés verbatim (engine=, quality=, subtitle=, subtitleFormat=, burnIn=, scriptsDir=)
. [x] `CapsulePlugin.kt` câblage (l.40-48 monolithic → 3 lignes forEach)
. [x] +9 unit tests `CapsuleConfigLoggerTest`
. [x] +1 scénario cucumber `@config @logging` (5 steps) + 5 step defs
. [x] 405 unit VERT (+9), 0 failures, 0 errors

'''
== ✅ CAP-29.1 MultiLanguageResolver + VoiceMapping 10 langues — TERMINE (session 077)

. [x] BOM `workspace-bom:0.0.16` + `i18n-contracts:0.0.2` ajoutés au build
. [x] Domaine `capsule.multilang` créé (7ème) — `VoiceMapping.kt` (62 lignes, 11 Piper + 11 espeak)
. [x] `MultiLanguageResolver.kt` (51 lignes) — `ResolvedLanguage` data class + resolver object
. [x] `Language` enum supprimé — `LanguageVoiceMapping.kt` détruit (40 lignes)
. [x] `TtsEngine.language(): SupportedLanguage?` — `PiperTtsEngine`/`EspeakTtsEngine` migrés
. [x] `CapsuleVideoTask.kt:197` + `CapsuleBuildTask.kt:40` — `Language.fromCode` → `MultiLanguageResolver.resolve`
. [x] 22 unit `MultiLanguageResolverTest` (resolve 10 codes, unknown→null, RTL ar/ur, piperModel/espeakVoice 11 codes)
. [x] 2 tests `LanguageVoiceMappingTest` adaptés + 4 tests `PiperTtsEngine`/`EspeakTtsEngine` migrés
. [x] 436 unit VERT (+15 net), `check` BUILD SUCCESSFUL, zéro secret

'''
== ✅ CAP-29.2 CapsuleVideoPlan + CapsuleVideoPlanner — TERMINE (session 078)

. [x] `CapsuleVideoPlan.kt` — `CapsuleVideoEntry` (deckFile, scriptFile, language: SupportedLanguage, outputVideo) + invariants fail-fast (deck exists, script exists, language in LanguageCatalog)
. [x] `CapsuleVideoPlan` — invariant non-empty, accès `size()`/`isEmpty()`/`languages()`
. [x] `CapsuleVideoPlanner.kt` — object pur `plan(deckDir, scriptDir, outputDir, targetLanguages)`, skip langues sans deck+script, fail-fast plan vide
. [x] Nommage aligné slider — `<deckName>_<lang>-deck.html` + `<deckName>_<lang>-script.txt` → `<deckName>_<lang>.webm`
. [x] 10 unit `CapsuleVideoPlanTest` + 8 unit `CapsuleVideoPlannerTest` (avec `@TempDir`)
. [x] 454 unit VERT (+18 net), `check` BUILD SUCCESSFUL, zéro secret

'''
== ✅ CAP-29.3 GenerateCapsuleVideoAllLanguagesTask — TERMINE (session 079)

. [x] Domaine `capsule.multilang` : `CapsuleVideoAllLanguagesRunner` (object pur, `run(plan, probeDuration, render)` + `shouldRender(entry, probeDuration)`)
. [x] Économie d'encre — skip si WebM existe ET probe > 0, re-render si probe ≤ 0 (artefact invalide)
. [x] `GenerateCapsuleVideoAllLanguagesTask : CapsuleVideoTask` — pipeline mono-langue réutilisé (CR-1), TTS par langue via `MultiLanguageResolver`
. [x] Refactor base `CapsuleVideoTask` — `execute()` → `open`, `resolveTtsEngine()`/`injectAudio()` → `internal` (zéro changement de comportement)
. [x] Enregistrement `generateCapsuleVideoAllLanguages` (groupe generate, CapsuleManager)
. [x] 6 unit `CapsuleVideoAllLanguagesRunnerTest` (RED→GREEN) — correction : `LanguageCatalog` n'a pas "de" (10 langues)
. [x] 1 functional `GenerateCapsuleVideoAllLanguagesFunctionalTest` (3 langues fr/en/ar NoOp → 3 WebM + log `CAPSULE MULTILANG`)
. [x] 460 unit VERT (+6 net), functional PASS, `check` BUILD SUCCESSFUL, zéro secret

'''
== ✅ CAP-29.4 Câblage translateAndGenerateCapsuleVideos — TERMINE (session 080)

. [x] Tâche composite `translateAndGenerateCapsuleVideos` — dependsOn `translateAndExtractSpeakerNotes` + `generateCapsuleVideoAllLanguages`
. [x] Constantes `TRANSLATE_AND_GENERATE_CAPSULE_VIDEOS` + description (domaine `capsule.feed`)
. [x] `CapsuleFeedTaskRegistrar.registerTranslateAndGenerateCapsuleVideos` (group capsule)
. [x] Câblage 3ème appel registrar dans `CapsuleManager.registerExtractSpeakerNotesTask()`
. [x] 3 functional tests RED→GREEN (registration groupe, description, dependsOn chain `--dry-run` avec stub translateDeck)
. [x] 460 unit VERT (inchangé) + 25 functional (+3), `check` BUILD SUCCESSFUL, zéro secret

'''
== ✅ CAP-29.5 Cucumber BDD E2E 10 langues — TERMINE (session 081)

. [x] `multi_language_capsule.feature` — 6 scénarios `@multilang @i18n`
. [x] `MultiLanguageCapsuleSteps` — step defs (plan pur `CapsuleVideoPlanner` + E2E GradleRunner NoOp)
. [x] Plan 10 langues (LanguageCatalog), nommage `<deckName>_<lang>.webm`, skip langue sans paire, fail-fast plan vide
. [x] E2E NoOp 10 WebM + log `CAPSULE MULTILANG → n/n languages rendered` + RTL ar/ur
. [x] `cucumberTest -PrunCucumber` : 6/6 multilang GREEN (suite 154 tests, 1 FAIL préexistant burn-in @integration — hors périmètre)
. [x] 460 unit VERT (inchangé) + 25 functional VERT + kover VERT, `check` BUILD SUCCESSFUL, zéro secret

'''
== ✅ CAP-29.6 Documentation + dogfooding + clôture EPIC — TERMINE (session 082)

. [x] README master EN + README plugin mis à jour (domaine `capsule.multilang`, table des tâches, convention nommage)
. [x] Dogfooding réel consommateur composite `includeBuild` : `extractSpeakerNotes` réel → `generateCapsuleVideoAllLanguages` réel
. [x] 3 WebM générés (fr/en/ar dont 1 RTL) en 3m07 (Piper fr/en + espeak, chromium Playwright)
. [x] Défaut nommage découvert : rendu `<deckName>_<lang>-deck.webm` au lieu du plan `<deckName>_<lang>.webm` (brise économie d'encre)
. [x] Fix TDD `CapsuleVideoOutputRenamer` (4 unit) + wiring post-capture `renderEntry`
. [x] Re-dogfooding : alignement `-deck.webm` → `_<lang>.webm` (3 langues) + économie d'encre prouvée (0/3 rendered, 3 skipped)
. [x] 464 unit VERT (+4 net) + 25 functional VERT + 6 cucumber multilang GREEN, zéro secret
. [x] EPIC CAP-29 TERMINÉ 7/7 US — domaine `capsule.multilang` clos (7 fichiers prod)

'''
== 🟡 EPIC CAP-ARCH — Capsule LLM-driven piloté par codebase (koog + langchain4j + RAG + graphify) — EN COURS (CAP-ARCH-0 ✅ S-083, CAP-ARCH-1 ✅ S-084, CAP-ARCH-2 ✅ S-085, CAP-ARCH-3 ✅ S-086, CAP-ARCH-4 ✅ S-088, CAP-ARCH-7 ✅ S-087+S-088)

. [x] CAP-ARCH-0 — Cadrage EPIC refondu (audit code réel : zéro LLM, koog 0.8.0 mort, i18n-contracts déjà consommé) ✅ S-083
. [x] CAP-ARCH-1 — Socle LLM bridge (pattern SLD-8, domaine `capsule.ai`, `CapsuleLlmService` register/resolve/adapter, bump koog 1.0.0, purge dépendance morte, tâche `capsuleAiSmokeTest`, 477 unit + 28 functional + 3 cucumber `@ai`) ✅ S-084
. [x] CAP-ARCH-2 — Contexte augmenté CompositeContext (domaine `capsule.context`, `CapsuleContextBuilder`, `CapsuleContext` invariant fail-fast, tâche `collectCapsuleAugmentedContext`, 491 unit + 31 functional + 4 cucumber `@context @rag`) ✅ S-085
. [x] CAP-ARCH-3 — Koog pipeline génération contenu (domaine `capsule.pipeline`, `CapsulePipelineGraph`, `generateCapsuleContent`, 540 unit + 34 functional) ✅ S-086
. [x] CAP-ARCH-4 — Wiring backend (tâche composite `generateCapsuleContentAndVideos`, 3 functional + 3 cucumber `@architecture @wiring`) ✅ S-088
. [x] CAP-ARCH-7 US-1 — `OutputConfig` + `VersioningStrategy` étend `CapsuleConfig` section `output`, `CapsuleConfigMerger` 4 sources, 10 unit tests ✅ S-087
. [x] CAP-ARCH-7 US-2 — `VideoDestinationResolver` (object pur, timestamp/incremental), 10 unit tests ✅ S-087
. [x] CAP-ARCH-7 US-3 — `DistributeCapsuleVideoTask` (tâche `distributeCapsuleVideo`), 3 functional tests ✅ S-087
. [x] CAP-ARCH-7 US-4 — Cucumber BDD `@distrib @versioning` (feature `capsule_distrib.feature` 5 scénarios + `CapsuleDistribSteps` + `CapsuleDistribCucumberRunner`) ✅ S-088
. [x] CAP-ARCH-5 — i18n aligné sur le socle (domaine `capsule.i18n`, `CapsuleMessages` 10 langues) ✅ S-089
. [x] CAP-ARCH-6 — Cucumber BDD boundary cross-borough (@i18n @cross-borough @architecture @ai) ✅ S-090

. [x] **Code review S-087** — Bug burn-in fix : `CapsuleOutputDirResolver` (package `capsule.support`), 3 steps Cucumber migrés (résolution dynamique `outputDir` au lieu de hardcoder `build/capsule/`), runner `cucumberTestIntegration` câblé. Scénario E2E burn-in `@burnin @integration` VERT (était FAIL depuis S-069). ✅ S-087
. [ ] **Dette technique** — 15+ steps Cucumber restants hardcodent `build/capsule/` (lignes 43, 171, 181, 192, 202, 212, 214, 226, 228, 535, 553, 575, 603, 663, 689, 715, 739, 768, 978). Migrer vers `CapsuleOutputDirResolver` progressivement.
. [ ] **EPIC extraction pipeline-contracts (MEMPHIS)** — à cadrer si un 3e borough adopte le pattern pipeline koog. `CapsulePipelineGraph` (capsule) ≈ `DeckPipelineGraph` (slider) = duplication verbatim acceptée par boundary N2, extraction N0 future.

'''
== 🟡 Backlog restant — À planifier

. [x] **CR-9** (robust HTML parsing) — à coder ✅ S-074
. [x] **CR-10** (CI optimization cucumberTest onlyIf) — à coder ✅ S-075
. [x] **CAP-29** (pipeline traduction vidéo multi-langue) — TERMINÉ 7/7 US (CAP-29.0 ✅ S-076, CAP-29.1 ✅ S-077, CAP-29.2 ✅ S-078, CAP-29.3 ✅ S-079, CAP-29.4 ✅ S-080, CAP-29.5 ✅ S-081, CAP-29.6 ✅ S-082)
. [x] **CAP-ARCH-0** (cadrage refondu LLM-driven via codebase) — ✅ S-083
. [x] **CAP-ARCH-1** (socle LLM bridge codebase, domaine `capsule.ai`) — ✅ S-084
. [x] **CAP-ARCH-2** (contexte augmenté CompositeContext, domaine `capsule.context`) — ✅ S-085
. [x] **CAP-ARCH-3** (koog pipeline génération contenu, domaine `capsule.pipeline`) — ✅ S-086
. [x] **CAP-ARCH-4** (wiring backend `generateCapsuleContentAndVideos`) — ✅ S-088
. [x] **CAP-ARCH-7** (video destination versioning, 4 US) — ✅ S-087+S-088
. [x] CAP-ARCH-5→6 (i18n aligné socle, cucumber boundary cross-borough) — ✅ S-089 + S-090

'''
== ✅ CAP-DOCCONTEXT — Injection corpus documentaire (EPIC TERMINÉ 5/5 US) — TERMINE (session 093)

. [x] `ContextConfig` data class (`docsGlobs: List<String>`) 8ème section `CapsuleConfig`
. [x] `CapsuleConfigMerger.mergeContextConfig` + helper `mergeStrList` (comma-separated, 4 sources)
. [x] `CapsuleExtension.docsGlobs: ListProperty<String>` + `pushConfigIntoExtension`
. [x] `DocContextLoader` (object pur, domaine `capsule.context`) — concatène + tronque `budget.docsTokens`
. [x] `CollectCapsuleAugmentedContextTask` — `docsFiles` + `docsGlobs` + `resolveDocsSection`
. [x] Cucumber `@context @docs` (4 scénarios) + runner `CapsuleDocContextCucumberRunner`
. [x] 694 unit VERT (+20) + 47 functional (+3) + 4 cucumber GREEN

'''
== ✅ CAP-SPD — Consommation métadonnées SPD / PedagogicalScenario (EPIC TERMINÉ 5/5 US) — TERMINE (session 095)

. [x] CAP-SPD-0 — Cadrage EPIC (audit CapsulePipelineGraph/CapsuleContextBuilder, contrat SPD format-agnostic, 7 décisions architecturales BACKLOG) ✅ S-094
. [x] CAP-SPD-1 — `PedagogicalScenario` data class (was `SpdContext`) + `PedagogicalScenarioLoader` (object pur mirror `DocContextLoader`, valide `type=="SPD"` pas `source`, parse AsciiDoc, 10/10 unit GREEN) ✅ S-094
. [x] CAP-SPD-2 — `CapsuleContext.scenarioSection` (was `spdSection`) + `CapsuleContextBuilder` étendu + `DefaultCapsulePromptBuilder` étendu (invariant ajusté, append scenario après channels, instruction line scenario conditionnelle, 9/9 unit GREEN) ✅ S-094
. [x] CAP-SPD-3 — Wiring `collectCapsuleAugmentedContext -Pcapsule.context.scenarioFile` (`ContextConfig.scenarioFile`, `mergeContextConfig` étendu `mergeStr`, 4 sources ENV < props < YAML < CLI, `resolveScenarioSection()` 5% budget, 2 functional GREEN) ✅ S-095
. [x] CAP-SPD-4 — Cucumber BDD `capsule_scenario_context.feature` `@context @scenario` (3 scénarios + runner dédié `CapsuleScenarioContextCucumberRunner` + steps préfixés "scenario context" + `CucumberTaskSpec("cucumberTestScenarioContext")`, 3/3 PASSED) ✅ S-095
. [x] Renommage SPD→PedagogicalScenario (3 fichiers `git mv`, header `"==== Pedagogical Scenario (scenario)"`, instruction `contains("Pedagogical Scenario")`, `metadata.json` `type=="SPD"` inchangé format externe K-2) ✅ S-095

'''
== ✅ README i18n — TERMINE (session 041)

. [x] 20 fichiers README créés (10 langues × consumer + plugin)
. [x] `README.consommateurs/` — guide utilisateur (10 langues)
. [x] `README.plugin/` — guide développeur/contributeur (10 langues)
. [x] Badges shields.io (Maven Central 0.0.1-SNAPSHOT, Plugin Portal, CI, License)
. [x] Marqueurs : 195 unit + 22 Cucumber PASS, version 0.0.1-SNAPSHOT (non publié)
. [x] Switcher i18n 10 langues (EN/FR/ES/PT/RU/AR/UR/ZH/HI/BN)
. [x] AR/UR RTL (Right-To-Left)

'''
== ✅ CAP-26 Style burn-in configurable — TERMINE

. [x] **DSL `subtitleBurnInStyle`** — data class `SubtitleBurnInStyle(fontSize, fontColor, outlineColor, position)` + validation + `toForceStyle()`
. [x] **CapsuleExtension** — 4 nouvelles Property (subtitleBurnInFontSize, subtitleBurnInFontColor, subtitleBurnInOutlineColor, subtitleBurnInPosition)
. [x] **CapsuleConfig** — 4 nouveaux champs dans `CaptureConfig`
. [x] **CapsuleConfigMerger** — merge 4 sources (ENV, props, YAML, CLI) pour les 4 champs style
. [x] **CapsuleScaffoldTask** — scaffold style dans `capsule-context.yml`
. [x] **SubtitleBurnInServiceImpl** — paramètre `style: SubtitleBurnInStyle`, `toForceStyle()` dans commande FFmpeg
. [x] **CapsuleVideoTask** — `resolveSubtitleBurnInService()` construit le style depuis l'extension
. [x] **Tests unitaires** — `SubtitleBurnInStyleTest` (16 tests : defaults, validation, toForceStyle, alignments)
. [x] **Cucumber** — 2 scénarios @subtitles @burnin @style (defaults + custom style)
. [x] **Compile + unit tests PASS**

'''
== ✅ CR-1 Refactor CapsuleVideoTask.execute() — TERMINE (session 040)

. [x] Extraire `synthesizeTtsForScript()` (14 lignes) + 3 tests unitaires
. [x] Extraire `renderManimSlides()` (40 lignes) + 2 tests unitaires
. [x] Extraire `replaceManimSlidesInDeck()` (45 lignes) + 2 tests unitaires
. [x] Extraire `captureDeckSequential()` (30 lignes) + 1 test unitaire
. [x] Extraire `captureDeckParallel()` (20 lignes) + 1 test unitaire
. [x] execute() → orchestration pure (76 lignes, vs 218 avant)
. [x] Compile + unit tests PASS

== ✅ CR-9 Robust HTML parsing — TERMINE (session 074)

. [x] `HtmlSectionParser.findTopLevelSectionOpenTags(html): List<MatchResult>` (38 lignes)
. [x] Depth-tracking existant réutilisé (skip nested + self-closing)
. [x] `injectAudioSequentialFallback` : 2 lignes regex raw supprimées → 1 ligne `HtmlSectionParser.findTopLevelSectionOpenTags`
. [x] DRY — réutilise `HtmlSectionParser` (DRY-2 S-031)
. [x] Bug fix : vertical stacks (sections imbriquées) n'étaient pas skippées → `data-audio` injecté sur sous-sections
. [x] +8 unit tests `HtmlSectionParserTest.findTopLevelSectionOpenTags` (flat, nested, self-closing, attributes, empty, malformed, positions, multiline)
. [x] +1 unit test `CapsuleVideoTaskTest.sequential fallback skips nested sections in vertical stack` (data-audio count = 2, pas 4)
. [x] +1 scénario cucumber `@audio @sequential` + 2 step defs (Given nested, Then exactly N)
. [x] 414 unit tests VERT (+9), 0 FAIL, 0 SKIP

'''
== ✅ CR-10 CI optimization cucumberTest onlyIf — TERMINE (session 075)

. [x] Domaine `capsule.ci.CucumberTestGuard` (data class pure, 4 méthodes : shouldRun/shouldSkip/skipReason)
. [x] 7 unit tests `CucumberTestGuardTest` (runCucumber active, CI active, both, neither, shouldSkip negation, skipReason mention, skipReason blank)
. [x] Câblage `onlyIf { shouldRun }` dans `build.gradle.kts` (logique inlinée — script ne peut pas importer classpath `main`)
. [x] BDD `cucumber_skip.feature` (4 scénarios `@ci`) + `CucumberSkipSteps` (5 step defs)
. [x] `./gradlew check` VERT — cucumberTest SKIPPED sans flag (garde active)
. [x] `./gradlew cucumberTest -PrunCucumber` s'exécute (garde désactivée)
. [x] 421 unit tests VERT (+7), 0 FAIL, 0 SKIP
. [x] Bug préexistant découvert : `CucumberTestRunner` ne lance que `capsule.feature` (à investiguer session future)

'''
== ✅ EPIC CAP-GLOSSARY — Injection terminologique (contexte augmenté) — TERMINÉ 4/4 US (S-109 → S-112)

. [x] CAP-GLOSSARY-0 — Cadrage EPIC (audit code 16 constats + 12 décisions architecturales : extension capsule-local `glossarySection` pas 6ème canal N0, `GlossaryEntry`/`GlossaryContext` data classes immutable, `GlossaryLoader` object pur mirror `PedagogicalScenarioLoader`, format AsciiDoc `== Glossary` + bullets, budget 5% = 400 tokens, `CapsuleContextBuilder.build()` 4ème paramètre, `DefaultCapsulePromptBuilder` instruction `Official Glossary`, provenance `GLOSSARY_CHANNEL`, 4 sources `context.glossaryFile`, pas de nouvelle tâche Gradle, runner dédié `CapsuleGlossaryContextCucumberRunner` + `CucumberTaskSpec("cucumberTestGlossaryContext")` 14ème) ✅ S-109
. [x] CAP-GLOSSARY-1 — `GlossaryEntry` + `GlossaryContext` + `GlossaryLoader` (object pur) + `CapsuleContext.glossarySection` + `CapsuleContextBuilder.build()` étendu + `DefaultCapsulePromptBuilder` étendu. Baby-step TDD ~10 unit (RED → GREEN → REFACTOR). ✅ S-110
. [x] CAP-GLOSSARY-2 — Wiring `CollectCapsuleAugmentedContextTask` + `ContextConfig` 3ème champ `glossaryFile` + merger 4 sources + `CapsuleExtension.glossaryFile` + `pushConfigIntoExtension` + `CapsuleManager` provider + provenance tracking `GLOSSARY` channel. Baby-step TDD 2 functional. ✅ S-111
. [x] CAP-GLOSSARY-3 — Cucumber BDD `capsule_glossary_context.feature` `@context @glossary` (3 scénarios) + runner dédié `CapsuleGlossaryContextCucumberRunner` (pattern S-082) + steps préfixés "glossary context" (bug S-088) + `CucumberTaskSpec("cucumberTestGlossaryContext")` 14ème. Baby-step BDD RED → GREEN (3/3 PASSED en 55s) → REFACTOR. ✅ S-112

'''
== 🟡 Prochaine session — Backlog capsule restant (CAP-28, CAP-CR3-4, CAP-CHAPITRE, CAP-PODCAST, CAP-PREVIEW)

'''
== ✅ EPIC CAP-27 — VTT burn-in (3 US ~5 pts, TERMINÉ 3/3 US S-113)

. [x] CAP-27-0 — Cadrage EPIC (constat prémisse caduque : compat VTT déjà fonctionnelle vérifiée par test réel ffmpeg 6.1.1 ; audit 10 constats + 8 décisions architecturales) ✅ S-113
. [x] CAP-27-1 — `SubtitleBurnInCommand` object pur `buildArgv(...)` (mirror `AudioPostCommand`) + refactor `SubtitleBurnInServiceImpl.burnIn` + 13 unit tests SRT/VTT. Baby-step TDD RED → GREEN → REFACTOR. 866 unit VERT 0 FAIL. ✅ S-113
. [x] CAP-27-2 — Cucumber BDD `@subtitles @burnin @vtt` — 2 scénarios dans `capsule.feature` (non-integration PASSED 32s + E2E `@integration` real ffmpeg PASSED 32s WebM valide EBML + `.vtt` + burn-in success) + step defs VTT uniques (S-088). `cucumberTestIntegration` 256 tests 0 failures. Baby-step BDD RED → GREEN → REFACTOR. ✅ S-113

'''

== Backlog priorisé (ordre d'attaque)

. [x] 🔴 CR-1 — Refactor CapsuleVideoTask.execute() (218→76 lignes orchestration) ✅ S-040
. [x] 🔴 CR-2 — Thread safety + timeout captureSlideParallel ✅ S-071
. [x] 🔴 CR-3 — Error handling TTS — dégradation signalée + retry ✅ S-071
. [x] 🟠 CR-4 — Sécurité HTML escape injectSubtitleTrack ✅ S-071
. [x] 🟠 CR-5 — DRY CapsuleConfigMerger (~76 lignes dupliquées) ✅ S-072
. [x] 🟠 CR-6 — Robust CLI parsing (`as? Int` → `toIntOrNull()`) ✅ S-071
. [x] 🟠 CR-7 — Validation YAML avec try/catch gracieux ✅ S-071
. [x] 🟡 CR-8 — Logging structuré CapsulePlugin ✅ S-073
. [x] 🟡 CR-9 — Robust HTML parsing (HtmlSectionParser au lieu de regex raw) ✅ S-074
. [x] 🟡 CR-10 — CI optimization cucumberTest onlyIf ✅ S-075
. [x] 🟢 CAP-27 — VTT burn-in (TERMINÉ 3/3 US S-113 : US-0 cadrage, US-1 `SubtitleBurnInCommand` + 13 unit, US-2 Cucumber 2 scénarios `@subtitles @burnin @vtt` non-integration + E2E real ffmpeg)
. [ ] 🟢 CAP-28 — Coverage gaps (PiperTtsEngine 31%, ManimEngineImpl 25%, ScreenshotCaptureImpl 26%)
. [x] 🔴 CAP-CR3-1 — CI `full` avec vrais binaires + probe de durée vidéo ✅ S-101 (5/5 US, 782 unit + 57 functional + 11 cucumber dédiés)
. [x] 🔴 CAP-CR3-2 — Mode strict anti-NoOp (`capsule.strictMode`) ✅ S-091
. [x] 🔴 CAP-CR3-3 — Décision `ScreenshotCaptureImpl` vs `PlaywrightCaptureImpl` ✅ S-096 (branché comme alternative configurable `capture.strategy: playwright|screenshot`, 5/5 US, 742 unit + 52 functional + 3 cucumber)
. [ ] 🟡 CAP-CR3-4 — Convention Gradle `resolveScriptDir/resolveDeckDir`
. [x] 🟢 CAP-29 — Pipeline Traduction Vidéo Multi-Langue (7 US ~20 pts, TERMINÉ 7/7 US : CAP-29.0 ✅ S-076, CAP-29.1 ✅ S-077, CAP-29.2 ✅ S-078, CAP-29.3 ✅ S-079, CAP-29.4 ✅ S-080, CAP-29.5 ✅ S-081, CAP-29.6 ✅ S-082)
. [x] 🟢 CAP-ARCH — Collaboration cross-borough slider+codebase (8 US ~25 pts, refondu S-083 LLM-driven via codebase — CAP-ARCH-0 ✅ S-083, CAP-ARCH-1 ✅ S-084, CAP-ARCH-2 ✅ S-085, CAP-ARCH-3 ✅ S-086, CAP-ARCH-4 ✅ S-088, CAP-ARCH-7 ✅ S-087+S-088, reste CAP-ARCH-5→6)
. [x] 🟠 CAP-GLOSSARY — Injection terminologique (4 US ~6 pts, TERMINÉ 4/4 US : US-0 ✅ S-109 cadrage, US-1 ✅ S-110 data classes + loader + builder + prompt builder, US-2 ✅ S-111 wiring + 2 functional, US-3 ✅ S-112 cucumber 3 scénarios + clôture)
. [x] 🟣 CAP-ANIM — Stratégie capture Remotion (3 US ~7 pts, TERMINÉ 3/3 US : US-0 ✅ S-122 cadrage, US-1 ✅ S-122 40 unit, US-2 ✅ S-122 3 cucumber + 2 functional)

'''
== ✅ EPIC CAP-MP4 — Export MP4/H.264 + format distribution (TERMINÉ 4/4 US)

. [x] CAP-MP4-0 — Cadrage EPIC (10 décisions architecturales) ✅ S-097
. [x] CAP-MP4-1 — `OutputFormat` enum + `OutputConfig.format` + merger 4 sources ✅ S-098 (17 unit)
. [x] CAP-MP4-2 — `VideoFormatConverter` + `FormatConversion` + wiring post-burnIn + `DistributeCapsuleVideoTask` étendu ✅ S-099 (13 unit + 2 functional)
. [x] 🟢 CAP-MP4-3 — Cucumber BDD `capsule_format.feature` 3 scénarios + runner dédié `CapsuleFormatCucumberRunner` + steps préfixés "format distribution" + `CucumberTaskSpec("cucumberTestFormat")` ✅ S-100

'''
== ✅ EPIC CAP-AUDIO — Post-production audio (BGM + loudnorm EBU R128 + ducking) (TERMINÉ 5/5 US)

. [x] CAP-AUDIO-0 — Cadrage EPIC (12 décisions architecturales, domaine `capsule.audio` 10ème, `AudioPostConfig` 5 champs, `AudioPostProcessor` interface + Impl + NoOp + factory + `AudioPostException`, wiring `applyAudioPostIfEnabled` post-burnIn pre-convertFormat, runner dédié `CapsuleAudioPostCucumberRunner`) ✅ S-102
. [x] CAP-AUDIO-1 — `AudioPostConfig` + merger 4 sources (5 champs `bgmEnabled`/`bgmFile`/`bgmLevel`/`loudnessTarget`/`duckingEnabled`, section `audioPost` 10ème `CapsuleConfig`, `mergeAudioPostConfig`, `CapsuleExtension` 5 properties + conventions + push) ✅ S-103
. [x] CAP-AUDIO-2 — `AudioPostProcessor` interface + `AudioPostProcessorImpl` (FFmpeg loudnorm + amix + sidechaincompress) + `NoOpAudioPostProcessor` + `AudioPostException` + `AudioPostCommand` (object pur) + factory `resolveAudioPostProcessor` 5ème ✅ S-104 (17 unit)
. [x] CAP-AUDIO-3 — Wiring `CapsuleVideoTask.applyAudioPostIfEnabled` post-burnIn pre-convertFormat (L493/L524) ✅ S-105 (3 functional)
. [x] CAP-AUDIO-4 — Cucumber BDD `capsule_audio_post.feature` 4 scénarios + runner dédié `CapsuleAudioPostCucumberRunner` + `CucumberTaskSpec("cucumberTestAudioPost")` 12ème ✅ S-106 (4 cucumber)

'''
== ✅ EPIC CAP-PROVENANCE — Audit context anti-hallucination (TERMINÉ 3/3 US)

. [x] CAP-PROVENANCE-0 — Cadrage EPIC (audit `CapsuleContextBuilder.build`/`CollectCapsuleAugmentedContextTask`/`ContextChannel` N0 sealed, 11 décisions architecturales : pas de nouveau domaine, `ProvenanceSource`/`ProvenanceChannel`/`ContextProvenance` data classes immutable, `ProvenanceTracker` mutable accumulator, `CapsuleContextBuilder.build()` tracker optional backward compat, wiring `@OutputFile provenanceFile` always-on cheap, pas de config/DSL, économie d'encre, JSON Jackson INDENT_OUTPUT, runner dédié `CapsuleProvenanceCucumberRunner` + `CucumberTaskSpec("cucumberTestProvenance")` 13ème) ✅ S-107
. [x] CAP-PROVENANCE-1 — `ProvenanceSource` + `ProvenanceChannel` + `ContextProvenance` + `ProvenanceTracker` + `CapsuleContextBuilder.build()` étendu tracker optional. Baby-step TDD 15 unit (12 `ContextProvenanceTest` + 3 builder étendu). Backward compat `tracker = null` (défaut) → aucun tracking, tests existants inchangés. ✅ S-108 (834 unit VERT)
. [x] CAP-PROVENANCE-2 — Wiring `CollectCapsuleAugmentedContextTask` `@OutputFile provenanceFile` + `ProvenanceTracker` construit avant `build()` + tracking EAGER/RAG/Graphify/DOCS/SCENARIO sources + log `CAPSULE PROVENANCE →` 2ème ligne + 2 functional `CollectProvenanceFunctionalTest` + 3 cucumber `capsule_provenance.feature` `@context @provenance` + runner dédié `CapsuleProvenanceCucumberRunner` (pattern S-082) + `CucumberTaskSpec("cucumberTestProvenance")` 13ème. ✅ S-108

'''
== 🟡 EPIC CAP-TRANSCRIPT — Reverse pipeline capsule → article AsciiDoc (EN COURS 5/6 US)

. [x] CAP-TRANSCRIPT-0 — Cadrage EPIC (audit code 12 constats + 12 décisions architecturales : domaine `capsule.transcript` 11ème, `TranscriptConfig` 11ème section 2 champs defaults backward compat, `TranscriptStrategy` enum TEMPLATE/LLM défaut TEMPLATE, `TranscriptPlan` consomme `SlideSegment` existant, `TranscriptBuilder` object pur, `TranscriptLlmEnhancer` port/adapter fallback degraded, `TranscriptPromptBuilder` object pur, tâche `generateCapsuleTranscript` groupe generate dependsOn `generateCapsuleContent`, 4 sources DSL, pas de factory `resolve*`, runner dédié 15ème `CucumberTaskSpec`, `CapsuleMessages` 24ème key) ✅ S-114
. [x] CAP-TRANSCRIPT-1 — `TranscriptConfig` data class 2 champs (`enabled: Boolean = false` + `strategy: TranscriptStrategy = TEMPLATE`) + `TranscriptStrategy` enum 2 valeurs companion `fromString` case-insensitive fallback TEMPLATE + `mergeTranscriptConfig` (pattern `mergeValidationConfig`) + `mergeTranscriptStrategy` helper enum + `loadFromEnvironment` (`CAPSULE_TRANSCRIPT_*`) + `buildConfigFromProperties` (`capsule.transcript.*`) + `CapsuleExtension` 2 properties + helper Groovy + `CapsuleConventions` 2 champs + `pushConfigIntoExtension` transcript block (Variant B). Baby-step TDD 22 unit (7 `TranscriptStrategyTest` + 5 `TranscriptConfigTest` + 10 merger). 888 unit VERT +22 net. ✅ S-115
. [x] CAP-TRANSCRIPT-2 — `TranscriptPlan` data class immutable 4 champs (`deckName` + `segments: List<SlideSegment>` + `language` + `outputPath`, invariants `isNotBlank` × 3, segments vide autorisé, pattern `CapsuleVideoPlan`) + `TranscriptBuilder` object pur `build(plan): String` (template AsciiDoc déterministe `= <deckName>` level-0 + `:language:` attribute + `INTRO`/`OUTRO` const + une section `== title` par segment HTML avec speakerNote body, MANIM skipped, pattern `CapsuleScriptWriter`) + `TranscriptPromptBuilder` object pur `buildEnhancePrompt(plan): String` (embed `TranscriptBuilder.build(plan)` + rules keep headings + transitions + AsciiDoc only, pattern `DefaultCapsulePromptBuilder`). Baby-step TDD 23 unit (7 `TranscriptPlanTest` + 11 `TranscriptBuilderTest` + 5 `TranscriptPromptBuilderTest`). 911 unit VERT +23 net. `check` BUILD SUCCESSFUL 22m49s. ✅ S-116
. [x] CAP-TRANSCRIPT-3 — `TranscriptLlmEnhancer` port (interface sync `enhance(template, plan): String`, pattern `CapsuleLlm`) + `ChatModelTranscriptEnhancer` adapter (bridge `ChatModel` langchain4j pattern `ChatModelCapsuleLlm`). Fallback degraded LLM blank → template original (pattern `AudioPostProcessor.process`). Short-circuit si template blank. Baby-step TDD 6 unit (`TranscriptLlmEnhancerTest` + FakeChatModel override `doChat`). 917 unit VERT +6 net. `check` BUILD SUCCESSFUL 23m33s. ✅ S-117
. [x] CAP-TRANSCRIPT-4 — Tâche `generateCapsuleTranscript` + wiring. `GenerateCapsuleTranscriptTask` groupe `generate` `@InputFile speakerNotesFile` `@PathSensitive(RELATIVE)` + `@Input language` + `@Input strategy: Property<TranscriptStrategy>` + `@OutputFile transcriptOutput` (`build/capsule/<deckName>-transcript.adoc`) + `@Optional @ServiceReference llmService` (strategy=LLM → `project.aiProvider` + `resolveModel` + `ChatModelTranscriptEnhancer`). Économie d'encre skip si output existe ET non-blank. `dependsOn("generateCapsuleContent")` (tests `-x generateCapsuleContent` standalone). Clé `task.generateCapsuleTranscript.description` 24ème 11 `Messages*.properties`. Baby-step TDD 3 functional `GenerateCapsuleTranscriptFunctionalTest` (registration, TEMPLATE déterministe, LLM mock `/api/chat`). 917 unit VERT baseline S-117, 67 functional VERT +3 net. `check` BUILD SUCCESSFUL 26m03s. ✅ S-118
. [x] CAP-TRANSCRIPT-5 — Cucumber BDD `capsule_transcript.feature` `@transcript` (4 scénarios) + runner dédié `CapsuleTranscriptCucumberRunner` 15ème ✅ S-119

'''
== ✅ EPIC CAP-ANIM — Stratégie capture Remotion (TERMINÉ 3/3 US S-122)

. [x] CAP-ANIM-0 — Cadrage EPIC rétroactif (audit 5 fichiers prod + 10 unit existants, 10 constats + 9 décisions, EPIC "EN COURS" puis TERMINÉ) ✅ S-122
. [x] CAP-ANIM-1 — Tests unit manquants (AudioCodecs 7 unit + RemotionConfig 8 unit + RemotionTemplate 7 unit + RemotionCaptureImpl 14 unit + CaptureResolver REMOTION 4 unit = 40 unit, 977 VERT 0 FAIL) ✅ S-122
. [x] CAP-ANIM-2 — Functional + Cucumber BDD (capsule_remotion.feature 3 scénarios + runner dédié CapsuleRemotionCucumberRunner 16ème + 2 functional RemotionCaptureFunctionalTest, 69 functional VERT +2) ✅ S-122
