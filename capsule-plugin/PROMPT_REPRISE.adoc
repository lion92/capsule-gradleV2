= PROMPT_REPRISE — Capsule Gradle (Détroit)
:session: 128
:borough: Détroit
:session_precedente: 127 — CAP-PODCAST US-2 tâche+wiring+cucumber (EPIC TERMINÉ)

== Contexte Hérité

Session 127 — **CAP-PODCAST US-2 TERMINÉ** (EPIC CAP-PODCAST 3/3 US) :
- `GenerateCapsulePodcastTask.kt` (113 lignes, `capsule/podcast/`) — tâche
  Gradle `@DisableCachingByDefault` : `@InputFiles audioFiles`,
  `@Input deckName`, `@OutputFile podcastOutput`, `@Input podcastEnabled`.
  TaskAction : skip si `!enabled` ; skip si output existe ET
  `MediaProbeUtil.probeDuration > 0.0` (économie d'encre) ; skip si
  `audioFiles` vide → warn ; factory `resolvePodcastConcatenator`.
- `CapsuleManager.registerGeneratePodcastTask` (16ème, L371) — pattern
  `registerGenerateTranscriptTask`. `audioFiles` résolus depuis
  `build/capsule/<deckName>/slide-*.mp3` (provider lazy), `podcastOutput`
  convention `build/capsule/<deckName>-podcast.mp3`,
  `dependsOn("generateCapsuleVideo")`.
- `CapsuleMessages` 25ème key (11 `Messages*.properties`).
- Cucumber `capsule_podcast.feature` 3 scénarios `@podcast` + runner dédié
  17ème `CucumberTaskSpec("cucumberTestPodcast")` + `CapsulePodcastSteps`
  préfixés "podcast" (bug S-088).
- `GeneratePodcastFunctionalTest` 3 tests (task registered, disabled skip,
  enabled NoOp → degraded no MP3).
- `check` VERT 4m25s — 1023 unit (+9 i18n), 72 functional (+3), 17 cucumber.
- Rapport : `.agents/sessions/127-cap-podcast-us2-task-wiring-cucumber.adoc`
- EPIC CAP-PODCAST TERMINÉ 3/3 US. Domaine `capsule.podcast` (13ème) clos.

== Mission Session 128

=== CAP-PREVIEW — Mode dry-run (3 US ~4 pts)

. `CaptureConfig.previewOnly: Boolean = false` (14ème section `CapsuleConfig`)
  + `mergePreviewConfig` 4 sources + `CapsuleExtension` property
  `previewOnly` + `pushConfigIntoExtension` preview block.
. `ScreenshotCaptureImpl` : si `previewOnly` → capture PNG + stop (skip
  mux WebM, skip audio post, skip format conversion). Économie d'encre :
  si PNG existe → skip capture.
. Cucumber `capsule_preview.feature` `@preview` (2-3 scénarios : default
  full pipeline, previewOnly → PNG only no WebM, previewOnly + PNG existe
  → skip). Runner dédié 18ème + steps préfixés "preview".

Baby-step TDD/BDD : ~5 unit (PreviewConfig) + 1 functional (previewOnly →
PNG only) + 2-3 cucumber scénarios GREEN.

=== Reste P2 (après CAP-PREVIEW)

* CAP-CHAPITRE — Intro/outro + chapter markers. 5 US ~10 pts.
* CAP-CR3-4 — Convention Gradle `resolveScriptDir/resolveDeckDir`. ~2 pts.

== Points d'attention

* 1023 unit VERT (S-127 baseline), 72 functional VERT, 17 cucumber dédiés GREEN
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082) + steps
  préfixés par feature (bug S-088 glue `capsule.scenarios` partagée)
* English only : code, Gherkin, commentaires
* Économie d'encre = skip si artefact existe et valide (AGENT.adoc Loi)
* `build.gradle.kts` — 17 `CucumberTaskSpec` actifs (18ème
  `cucumberTestPreview` à câbler CAP-PREVIEW)
* Conventions 0.0.3 (mavenLocal), BOM 0.0.27, codebase 0.0.10, slider 0.0.18
* CaptureStrategy 3 valeurs : PLAYWRIGHT (default), SCREENSHOT, REMOTION
* 13 domaines DDD : feed, pipeline, ai, context, multilang, validation, audio,
  i18n, ci, transcript, remotion, podcast + support. `capsule.podcast` clos S-127.
* `ScreenshotCaptureImpl` = point d'insertion CAP-PREVIEW (skip mux après PNG)

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 1023 unit VERT (S-127 baseline), 72 functional VERT, cucumber dédiés GREEN
* `cucumberTest` skippé par défaut (CR-10), `cucumberTestIntegration` skippé
  par défaut (Playwright gate S-121)
* DSL 4 sources pattern : ENV < props < YAML < CLI
* Pattern object pur pour commande ffmpeg : `AudioPostCommand`,
  `SubtitleBurnInCommand`, `FormatConversion`, `PodcastConcatCommand`
* `compileOnly(libs.slider)` conservé (runtime). `testImplementation(libs.slider)`
  RETIRÉ S-120 (Groovy 3.x conflit ProjectBuilder)
* Catalogue : `slider-plugin`, slider 0.0.18, codebase 0.0.10, BOM 0.0.27
* CAP-PREVIEW = mode dry-run (previewOnly → PNG only, skip mux WebM)