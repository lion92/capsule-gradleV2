= PROMPT_REPRISE — Capsule Gradle (Détroit)
:session: 124
:borough: Détroit
:session_precedente: 123 — CAP-28 US-0 audit partiel (clôturé anticipé)

== Contexte Hérité

Session 123 — **CAP-28 US-0 audit partiel** :
- Audit 3 fichiers low-coverage (PiperTtsEngine 31%, ManimEngineImpl 25%,
  ScreenshotCaptureImpl 26%) — gaps identifiés par fichier
- Décisions préliminaires (pas de nouveau domaine, baby-step TDD strict,
  tests sans I/O uniquement, 3 US ~11 unit)
- Clôture anticipée par pilote — US-0 cadrage non finalisé (pas de MàJ
  BACKLOG détaillé), US-1/2/3 non démarrés
- Zéro code modifié — session gouvernance pure
- Baseline 977 unit VERT préservée, 69 functional VERT, cucumber GREEN

== Mission Session 124

=== CAP-28 — Finalisation + exécution

. US-0 finalisation cadrage — formaliser décisions + MàJ BACKLOG détaillé
  (table US-1/2/3 + gaps précis par fichier)
. US-1 PiperTtsEngine — ~4 unit : `language()` 2 branches (null → null,
  non-null → resolveByPiperModel), `resolvedModel` resolution (language
  null → model default, language non-null → piperModel)
. US-2 ManimEngineImpl — ~3 unit : `probeDuration` delegation ffprobe
  absent → 0.0, `name()` invariant, `isAvailable` memoization (2 appels)
. US-3 ScreenshotCaptureImpl — ~4 unit : `capture()` empty durations
  throws, `isAvailable()` ffmpeg absent → false, `close()` resets probe,
  `encodeSlides` parallelism coerceIn

Baby-step TDD strict RED → GREEN → REFACTOR. Tests sans I/O uniquement
(branches gated piper/manim/ffmpeg/Playwright hors scope).

=== Reste P2 (après CAP-28)

* CAP-CR3-4 — Convention Gradle `resolveScriptDir/resolveDeckDir`
* CAP-CHAPITRE — Découpage chapitres vidéo (markers temporels)
* CAP-PODCAST — Export audio-only (MP3 podcast)
* CAP-PREVIEW — Aperçu rapide (thumbnails + first-frame)

== Points d'attention

* 977 unit VERT (S-122 baseline), 69 functional VERT, cucumber dédiés GREEN
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082) + steps
  préfixés par feature (bug S-088 glue `capsule.scenarios` partagée)
* English only : code, Gherkin, commentaires
* Économie d'encre = skip si artefact existe et valide (AGENT.adoc Loi)
* `build.gradle.kts` restauré S-122 — 16 `CucumberTaskSpec` actifs
* Conventions 0.0.3 (mavenLocal), BOM 0.0.27, codebase 0.0.10, slider 0.0.18
* CaptureStrategy 3 valeurs : PLAYWRIGHT (default), SCREENSHOT, REMOTION
* 12 domaines DDD : feed, pipeline, ai, context, multilang, validation, audio,
  i18n, ci, transcript, remotion + support

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 977 unit VERT (S-122 baseline), 69 functional VERT, cucumber dédiés GREEN
* `cucumberTest` skippé par défaut (CR-10), `cucumberTestIntegration` skippé
  par défaut (Playwright gate S-121)
* DSL 4 sources pattern : ENV < props < YAML < CLI
* Pattern object pur pour commande ffmpeg : `AudioPostCommand`,
  `SubtitleBurnInCommand`, `FormatConversion`
* `compileOnly(libs.slider)` conservé (runtime). `testImplementation(libs.slider)`
  RETIRÉ S-120 (Groovy 3.x conflit ProjectBuilder)
* Catalogue : `slider-plugin`, slider 0.0.18, codebase 0.0.10, BOM 0.0.27
* CAP-28 = coverage gaps, pas de nouveau domaine, étend tests existants