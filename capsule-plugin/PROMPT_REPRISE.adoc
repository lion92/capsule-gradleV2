= PROMPT_REPRISE — Capsule Gradle (Détroit)
:session: 122
:borough: Détroit
:session_precedente: 121 — Intégration PR #1 (lion92) + fix ScreenshotCaptureImpl

== Contexte Hérité

Session 121 — **Intégration PR #1 "Fix/build toolchain et pipeline" (lion92)** :
- Merge branche `pr-1` (8 commits) dans main
- Conflit INDEX.adoc résolu (version S-120 conservée)
- Contenu PR : fix build (conventions 0.0.3, JDK 25, exclusion Groovy 3),
  fix i18n (namespace `capsule/i18n/Messages`), fix 4 défauts capture +
  capture conteneur (Chromium --no-sandbox), feat CAP-ANIM
  (`CaptureStrategy.REMOTION` + RemotionPlanner/RemotionCaptureImpl/
  RemotionTemplate + Manim incrusté OffthreadVideo + AudioCodecs),
  fix TTS économie d'encre
- Fix post-merge baby-step TDD/BDD : `ScreenshotCaptureImpl.isAvailable()`
  étendu pour vérifier ffmpeg (`isFfmpegAvailable()` probe `ffmpeg -version`)
- Test functional + cucumber scénario #3 corrigés → "strictMode fails when
  ffmpeg is unavailable"
- Gate : 930 unit 0 FAIL (+13 RemotionPlannerTest), 67 functional 0 FAIL,
  cucumberTestCaptureStrategy 3/3, cucumberTestTranscript 4/4

== Mission Session 122

=== Cadrage EPIC CAP-ANIM (Remotion) post-merge

La PR #1 a introduit un nouveau domaine `capsule.remotion` (4 fichiers prod :
`RemotionCapture.kt`, `RemotionPlanner.kt`, `RemotionTemplate.kt`,
`AudioCodecs.kt`) + `CaptureStrategy.REMOTION` (3ème valeur enum) +
`RemotionConfig` dans `CapsuleConfig` (section `capsule.remotion.*` DSL 4
sources). Ce code est **fonctionnel mais non cadré** — aucun EPIC BACKLOG ne
le documente. Mission S-122 : cadrer formellement EPIC CAP-ANIM.

*Étapes* :
. Audit code Remotion (4 fichiers prod + RemotionPlannerTest + RemotionConfig)
. Documenter dans BACKLOG : vision, domaine, US rétroactives (le code existe
  déjà, l'EPIC documente ce qui a été livré par la PR)
. Évaluer gaps : tests unit manquants sur RemotionCaptureImpl/RemotionTemplate/
  AudioCodecs, pas de cucumber dédié `@anim @remotion`, pas de functional test
. Décider : EPIC CAP-ANIM "TERMINÉ par PR #1" (documentation rétroactive) ou
  EPIC CAP-ANIM "EN COURS" (gaps à combler : unit + cucumber + functional)

=== Maintenance P2 (post-cadrage si temps restant)

Reste P2 : CAP-CHAPITRE, CAP-PODCAST, CAP-PREVIEW, CAP-CR3-4, CAP-28.

== Points d'attention

* Conventions 0.0.3 pas sur Central (mavenLocal seulement)
* Namespace i18n : `capsule/i18n/Messages.properties` (PR #1 a déplacé)
* `CaptureStrategy` a maintenant 3 valeurs : PLAYWRIGHT, SCREENSHOT, REMOTION
* `ScreenshotCaptureImpl.isAvailable()` vérifie Chromium + ffmpeg (fix S-121)
* `AudioCodecs` : codec selon conteneur (WebM=libvorbis, MP4=aac)
* `PlaywrightCapture.outputExtension()` : abstrait l'extension de sortie
* 930 unit VERT (baseline S-121), 67 functional VERT, cucumber dédiés GREEN

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 930 unit VERT (S-121 baseline), 67 functional VERT, cucumber dédiés GREEN
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082) + steps
  préfixés par feature (bug S-088 glue `capsule.scenarios` partagée)
* English only : code, Gherkin, commentaires
* Économie d'encre = skip si artefact existe et valide (AGENT.adoc Loi)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
* `CapsuleMessages` object (domaine `capsule.i18n`) — 11
  `Messages*.properties` 10 langues, 24 keys (namespace `capsule/i18n/`)
* DSL 4 sources pattern : ENV < props < YAML < CLI
* 3 loaders domaine `capsule.context` : `DocContextLoader`,
  `PedagogicalScenarioLoader`, `GlossaryLoader`
* Pattern object pur pour commande ffmpeg : `AudioPostCommand`,
  `SubtitleBurnInCommand`, `FormatConversion`
* `capsule.feed.SlideSegment` — source de vérité contenu pédagogique
* `compileOnly(libs.slider)` conservé (runtime). `testImplementation(libs.slider)`
  RETIRÉ S-120 (Groovy 3.x conflit ProjectBuilder)
* Catalogue : `slider-plugin`, slider 0.0.18, codebase 0.0.10, BOM 0.0.27
* Domaine `capsule.transcript` (11ème) : 8 fichiers prod (CAP-TRANSCRIPT TERMINÉ)
* Domaine `capsule.remotion` (12ème, PR #1) : 4 fichiers prod à documenter
* CaptureStrategy 3 valeurs : PLAYWRIGHT (default), SCREENSHOT, REMOTION