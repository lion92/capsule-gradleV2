= INDEX — Capsule Gradle
:toc:

_Règles absolues_ : voir `AGENT.adoc`
_DAG_ : N2 — Plugin Gradle de generation de capsules video
_Dependance N2_ : slider-gradle (compileOnly dans build.gradle.kts, runtime apply dans CapsulePlugin)

_Derniere mise a jour_ : 2026-08-25
_Session terminee_ : 121 — **Intégration PR #1 (lion92)** — merge branche `pr-1` (8 commits) : fix build toolchain (conventions 0.0.3, JDK 25, exclusion Groovy 3), fix i18n (namespace `capsule/i18n/Messages`), fix 4 défauts capture vidéo + capture conteneur fonctionnelle (Chromium --no-sandbox), feat CAP-ANIM (CaptureStrategy.REMOTION + RemotionPlanner/RemotionCaptureImpl/RemotionTemplate + Manim incrusté OffthreadVideo + AudioCodecs), fix TTS économie d'encre (narration existante non réécrite). Conflit INDEX.adoc résolu (version S-120 conservée). Gate post-merge à valider.
_Prochaine session_ : 122 — Cadrage EPIC CAP-ANIM (Remotion) post-merge + maintenance P2 (CAP-CHAPITRE, CAP-PODCAST, CAP-PREVIEW, CAP-CR3-4, CAP-28).
_Session en cours_ : — (S-121 terminée).

'''
== REGLES ABSOLUES (resume)

=== 1. COMMITS/GIT — INTERDICTION FORMELLE sans permission
=== 2. PUBLISH — INTERDICTION FORMELLE sans permission
=== 3. DAG N2 — importable par N3 runner-gradle, jamais importe N3
=== 4. PROCEDURE DE FIN DE SESSION — 6 ETAPES OBLIGATOIRES
=== 5. HOOK OUVERTURE — Checklist SESSION_CHECKLIST.adoc obligatoire
=== 6. ZERO secret/token/api key en dur dans le code source
=== 7. CAPSULE CONSUME SLIDER — lecture seule du deck

'''
== EPIC K — Taxonomie Workspace (Transverse)

[cols="1,3,2", options="header"]
|===
| Phase | Livrable | Statut
| K-0 | Document Jardin Secret — TAXONOMIE_WORKSPACE.adoc | ✅ 2026-05-18
| K-1 | Renommage 4 verbes (GENERER/COLLECTER/TRANSFORMER/DÉPLOYER) | ✅ 2026-05-19
| K-2 | Format pivot — metadata.json obligatoire | ✅ 2026-05-19
| K-3 | Configuration par domaine — par borough | ✅ 2026-05-18
| K-4 | Convention over configuration SPG→SPD | ✅ 2026-05-19
| K-5 | Documentation — MàJ INDEX.adoc boroughs | ✅ TERMINE (2026-05-19)
|===

'''
== EPIC CAP-13 — Config Multi-Source (4 sources : ENV < props < YAML < CLI)

[cols="1,3,2", options="header"]
|===
| Phase | Livrable | Statut
| CAP-13.1 | CapsuleConfig.kt — data class immutable 5 sections | ✅ TERMINE (session 019)
| CAP-13.2 | CapsuleConfigLoader.kt — YAML + `${VAR}` env resolution | ✅ TERMINE (session 019)
| CAP-13.3 | CapsuleConfigMerger.kt — merge 4 sources | ✅ TERMINE (session 020)
| CAP-13.4 | CapsuleExtension revisitée — configPath + wiring | ✅ TERMINE (session 020)
| CAP-13.5 | capsule-context.yml scaffold task | ✅ TERMINE (session 020)
| CAP-13.6 | Plugin wiring afterEvaluate → ConfigMerger | ✅ TERMINE (session 020)
| CAP-13.7 | Cucumber @integration tag + ManimEngine wiring | ✅ TERMINE (session 022)
|===

_Pattern aligné sur plantuml-gradle (ConfigLoader + ConfigMerger + 4 sources documentées)._

'''
== Sessions

|===
| # | Date | Type | Sujet | Score
| 120 | 2026-08-25 | fix+cross-borough | **EPIC MEMPHIS-BOM-FIX US-5 TERMINÉ** (cross-borough MEMPHIS + Queens) — Catalogue capsule fixé : `slider` → `slider-plugin` (artifactId réel depuis aplatissement S-067), slider 0.0.16 → 0.0.18, codebase 0.0.8 → 0.0.10, BOM 0.0.16 → 0.0.27. Bug discovery : `testImplementation(libs.slider)` amenait Groovy 3.0.17 (via asciidoctor-gradle-jvm-slides:5.0.0-alpha.1 → jrubygradle-resolver:2.1.2) sur le classpath de test, cassant ProjectBuilder (Groovy 4 vs 3.x, `NoClassDefFoundError InvokerHelper`). 79/917 tests unitaires FAILED préexistantes depuis bump codebase 0.0.8 S-119. Fix : retrait `testImplementation(libs.slider)` — aucun test n'importait slider. `compileOnly(libs.slider)` conservé (runtime). Gate : `test` 917 tests 0 FAIL (baseline restaurée) + `cucumberTestTranscript` 4/4 PASSED. CAP-TRANSCRIPT EPIC TERMINÉ 6/6 US (sync BACKLOG US-5 `⬜ TODO` → `✅ TERMINÉ S-119`). EPIC MEMPHIS-BOM-FIX TERMINÉ 5/5 US cross-borough. | 10/10
| 118 | 2026-08-14 | feat+ddd+tdd | **CAP-TRANSCRIPT US-4 tâche `generateCapsuleTranscript` + wiring** — Domaine `capsule.transcript` (11ème) enrichi avec `GenerateCapsuleTranscriptTask.kt` (tâche Gradle `@DisableCachingByDefault` : groupe `generate`, `@InputFile speakerNotesFile` `@PathSensitive(RELATIVE)`, `@Input language`, `@Input strategy: Property<TranscriptStrategy>`, `@OutputFile transcriptOutput`, `@Optional @ServiceReference llmService: Property<LlmBuildService>`. TaskAction : skip si output existe ET non-blank (économie d'encre) ; check speakerNotesFile.exists() message actionable ; deckName = filename removeSuffix("-speaker-notes") ; parse `AsciidocSpeakerNoteParser` → `CapsuleScript` → `TranscriptPlan(deckName, segments, language, outputPath)` ; TEMPLATE → `TranscriptBuilder.build(plan)` ; LLM → `project.aiProvider` + `project.resolveModel(provider, llmService)` + `ChatModelTranscriptEnhancer(model).enhance(template, plan)` ; write + log `CAPSULE TRANSCRIPT →`). Wiring `CapsuleManager.registerGenerateTranscriptTask` (après `registerGenerateCapsuleContentTask`, groupe generate, description i18n, `dependsOn("generateCapsuleContent")`, conventions language `deck.language`?:`fr`, strategy extension?:CLI, speakerNotesFile 1er `*-speaker-notes.adoc` dans build/capsule/, transcriptOutput map `capsule/<deckName>-transcript.adoc`, llmService + usesService). Décision GREEN : dependsOn préservé en prod, tests utilisent `-x generateCapsuleContent` (pré-écrivent les speaker notes, standalone + zéro LLM involontaire). Clé `task.generateCapsuleTranscript.description` 24ème ajoutée 11 `Messages*.properties`. Baby-step TDD strict RED (3 functional → UnexpectedBuildFailure task not found) → GREEN (1 fichier prod + wiring + `-x` tests, 3/3 PASSED) → REFACTOR (`:functionalTest` BUILD SUCCESSFUL 4m38s 67 tests 0 fail ; `check` BUILD SUCCESSFUL 26m03s, 917 unit VERT baseline S-117 préservée, 67 functional VERT +3 net baseline 64 → 67, cucumber dédiés GREEN baseline préservée, Kover VERT, validatePlugins VERT, zéro régression, zéro secret, zéro bump BOM). 1 fichier prod créé + 1 wiring + 1 test créé + 11 i18n modifiés. EPIC CAP-TRANSCRIPT 5/6 US (US-0 ✅ S-114, US-1 ✅ S-115, US-2 ✅ S-116, US-3 ✅ S-117, US-4 ✅ S-118, US-5 TODO). | 9/10
| 119 | 2026-08-16 | feat+bdd+cucumber+cloture | **CAP-TRANSCRIPT US-5 Cucumber BDD `capsule_transcript.feature`** (4 scénarios : TEMPLATE strategy, LLM strategy mocked, empty plan, skip-on-existing-output) + runner dédié `CapsuleTranscriptCucumberRunner` + étapes `CapsuleTranscriptSteps`. Tous les scénarios passent : BUILD SUCCESSFUL. EPIC CAP-TRANSCRIPT maintenant à 6/6 US terminées. | 9/10
| 117 | 2026-08-14 | feat+ddd+tdd | **CAP-TRANSCRIPT US-3 TranscriptLlmEnhancer port + ChatModelTranscriptEnhancer adapter** — Domaine `capsule.transcript` (11ème) enrichi avec `TranscriptLlmEnhancer.kt` (interface port sync `fun enhance(template: String, plan: TranscriptPlan): String`, pattern `CapsuleLlm` — le domaine reste Gradle-free/coroutine-free, unit-testable avec un plain fake, l'adapter bridge langchain4j vit hors du domaine) + `ChatModelTranscriptEnhancer.kt` (adapter `ChatModel` langchain4j pattern `ChatModelCapsuleLlm` — `model.chat(UserMessage.from(prompt)).aiMessage().text()`, prompt construit via `TranscriptPromptBuilder.buildEnhancePrompt`). Fallback degraded pattern `AudioPostProcessor.process` : LLM blank/whitespace → template original (économie d'encre, le caller garde un AsciiDoc valide). Short-circuit si template blank (no LLM call). Baby-step TDD strict RED (6 tests → `Unresolved reference 'ChatModelTranscriptEnhancer'` × 5 sites) → GREEN (2 fichiers prod créés) → REFACTOR (`check` BUILD SUCCESSFUL 23m33s, 917 unit VERT +6 net baseline S-116 911 → 917, 64 functional VERT baseline préservée, 13 cucumber dédiés GREEN baseline préservée, Kover VERT, validatePlugins VERT, zéro régression, zéro secret, zéro bump BOM). 6 unit tests créés (`TranscriptLlmEnhancerTest` + FakeChatModel minimal override `doChat` langchain4j 1.16.3 single abstract entry point). 2 fichiers prod créés + 1 fichier test créé (zéro fichier existant modifié). EPIC CAP-TRANSCRIPT 4/6 US (US-0 ✅ S-114, US-1 ✅ S-115, US-2 ✅ S-116, US-3 ✅ S-117, US-4→5 TODO). | 9/10
| 116 | 2026-08-13 | feat+ddd+tdd | **CAP-TRANSCRIPT US-2 TranscriptPlan + TranscriptBuilder + TranscriptPromptBuilder** — Domaine `capsule.transcript` (11ème) enrichi avec `TranscriptPlan.kt` (data class immutable 4 champs `deckName` + `segments: List<SlideSegment>` + `language` + `outputPath`, invariants fail-fast `isNotBlank` × 3, segments vide autorisé, pattern `CapsuleVideoPlan` CAP-29.2 `capsule.multilang`) + `TranscriptBuilder.kt` (object pur `build(plan): String` — template AsciiDoc déterministe `= <deckName>` level-0 + `:language: <lang>` attribute + `INTRO` boilerplate const + une section `== <title>` par segment HTML avec speakerNote body + `OUTRO` boilerplate const, MANIM segments skipped `if (segment.type == SlideType.MANIM) continue`, constantes `INTRO`/`OUTRO` publiques `const val` testabilité, pattern `CapsuleScriptWriter` object pur `write(script): String`) + `TranscriptPromptBuilder.kt` (object pur `buildEnhancePrompt(plan): String` — prompt pédagogique enrichissement LLM embed `TranscriptBuilder.build(plan)` + rules keep `== ` level-2 headings + preserve speaker note content + add pedagogical transitions + add intro+outro paragraphs + respond AsciiDoc only, pattern `DefaultCapsulePromptBuilder`). Baby-step TDD strict RED (23 tests → `Unresolved reference 'TranscriptPlan'`/`'TranscriptBuilder'`/`'TranscriptPromptBuilder'` 15 errors) → GREEN (3 fichiers prod créés) → REFACTOR (`check` BUILD SUCCESSFUL 22m49s, 911 unit VERT +23 net baseline S-115 888 → 911, 64 functional VERT baseline préservée, 13 cucumber dédiés GREEN baseline préservée, Kover VERT, validatePlugins VERT, zéro régression, zéro secret, zéro bump BOM). 23 unit tests créés (7 `TranscriptPlanTest` + 11 `TranscriptBuilderTest` + 5 `TranscriptPromptBuilderTest`). 3 fichiers prod créés + 3 fichiers test créés (zéro fichier existant modifié). `publishToMavenLocal` VERT. EPIC CAP-TRANSCRIPT 3/6 US (US-0 ✅ S-114, US-1 ✅ S-115, US-2 ✅ S-116, US-3→5 TODO). | 9/10
| 115 | 2026-08-13 | feat+ddd+tdd | **CAP-TRANSCRIPT US-1 TranscriptConfig + TranscriptStrategy enum + merger 4 sources** — Domaine `capsule.transcript` (11ème) créé avec `TranscriptConfig.kt` (data class 2 champs `enabled: Boolean = false` + `strategy: TranscriptStrategy = TEMPLATE`, defaults backward compat) + `TranscriptStrategy` enum type-safe 2 valeurs `TEMPLATE`/`LLM` companion `fromString` case-insensitive fallback `TEMPLATE` (pattern `OutputFormat`/`CaptureStrategy`). `CapsuleConfig` 11ème section `transcript`. `CapsuleConfigMerger` `mergeTranscriptConfig` (pattern `mergeValidationConfig` — `mergeBoolean` enabled + `mergeTranscriptStrategy` strategy) + `loadFromEnvironment` (`CAPSULE_TRANSCRIPT_ENABLED`/`_STRATEGY`) + `buildConfigFromProperties` (`capsule.transcript.enabled`/`.strategy`) + `mergeTranscriptStrategy` helper enum (pattern `mergeVersioning`/`mergeOutputFormat` — CLI > YAML > props). `CapsuleExtension` 2 properties (`transcriptEnabled` convention `false` + `transcriptStrategy` convention `TEMPLATE` + helper Groovy `transcriptStrategy(String)` case-insensitive) + `CapsuleConventions` 2 champs + `pushConfigIntoExtension` transcript block (Variant B `isPresent` + equality) après AudioPost block. Baby-step TDD strict RED (22 tests → `Unresolved reference 'TranscriptStrategy'`/`'TranscriptConfig'` 14 errors) → GREEN (TranscriptConfig.kt + CapsuleConfig + Merger + Models + Plugin) → REFACTOR (`check` BUILD SUCCESSFUL 18m53s, 888 unit VERT +22 net baseline S-113 866 → 888, 64 functional VERT baseline préservée, 13 cucumber dédiés GREEN baseline préservée, Kover VERT, validatePlugins VERT, zéro régression, zéro secret, zéro bump BOM). 22 unit tests créés (7 `TranscriptStrategyTest` + 5 `TranscriptConfigTest` + 10 merger transcript `CapsuleConfigMergerTest`). 5 fichiers prod modifiés + 3 fichiers test (2 nouveaux + 1 modifié). EPIC CAP-TRANSCRIPT 2/6 US (US-0 ✅ S-114, US-1 ✅ S-115, US-2→5 TODO). | 9/10
| 114 | 2026-08-13 | cadrage+gouvernance | **CAP-TRANSCRIPT US-0 Cadrage EPIC** (gouvernance pure, zéro code) — Audit code : `capsule.feed` (`SlideSegment`/`CapsuleScript`/`AsciidocSpeakerNoteParser`/`CapsuleScriptReader`), `capsule.pipeline` (`CapsuleLlm` port + `ChatModelCapsuleLlm` adapter + `DefaultCapsulePromptBuilder` + `CapsuleState` + `TtsScriptDeriver`), `GenerateCapsuleContentTask` (`@OutputFile speakerNotesOutput` source transcript), `CapsuleConfig` 10 sections, `CapsuleConfigMerger` 5 helpers + 3 enum helpers, `CapsuleExtension` 48 properties + `CapsuleConventions` mirror, `CapsulePlugin.pushConfigIntoExtension` 10 blocks, `CapsuleManager` 14 registrations + 5 `resolve*` factories, `CapsuleMessages` 23 keys + 10 `Messages_*.properties`, 14 `CucumberTaskSpec`. 12 décisions architecturales : domaine `capsule.transcript` 11ème, `TranscriptConfig` 11ème section 2 champs defaults backward compat, `TranscriptStrategy` enum TEMPLATE/LLM défaut TEMPLATE, `TranscriptPlan` consomme `SlideSegment` existant, `TranscriptBuilder` object pur, `TranscriptLlmEnhancer` port/adapter fallback degraded, `TranscriptPromptBuilder` object pur, tâche `generateCapsuleTranscript` groupe generate dependsOn `generateCapsuleContent`, 4 sources DSL, pas de factory `resolve*`, runner dédié 15ème `CucumberTaskSpec`, `CapsuleMessages` 24ème key. EPIC redécoupé 6 US ~12 pts (US-0 ✅, US-1→5 TODO). MàJ BACKLOG (section CAP-TRANSCRIPT refondue 12 décisions + 6 US détaillées + synthèse 48 US/90 pts), INDEX (en-tête + roadmap + table sessions), PROMPT_REPRISE, AGENTS racine. Pattern S-057/S-063. | 9/10
| 113 | 2026-08-13 | cadrage+feat+tdd+bdd+cloture | **EPIC CAP-27 VTT burn-in TERMINÉ 3/3 US** — Constat prémisse caduque (compat VTT déjà fonctionnelle, test réel ffmpeg 6.1.1). US-0 cadrage (8 décisions). US-1 `SubtitleBurnInCommand` object pur (pattern `AudioPostCommand`) + refactor `burnIn` + 13 unit SRT/VTT (866 unit VERT +9 net). US-2 Cucumber BDD 2 scénarios `@subtitles @burnin @vtt` dans `capsule.feature` + 2 step defs VTT (S-088) : non-integration PASSED + E2E `@integration` real ffmpeg PASSED (WebM valide EBML + `.vtt` + burn-in success). `cucumberTestIntegration` 256 tests 0 failures. `check` BUILD SUCCESSFUL, Kover VERT, 64 functional VERT, zéro régression, zéro secret, zéro bump BOM. EPIC clos en 1 session. | 9/10
| 112 | 2026-08-13 | feat+bdd+cucumber+cloture | **CAP-GLOSSARY US-3 Cucumber BDD + clôture EPIC CAP-GLOSSARY TERMINÉ 4/4 US** — Feature `capsule_glossary_context.feature` `@context @glossary` (3 scénarios : glossary injecté → section `==== Official Glossary (glossary)` + termes `competence transversale`/`evaluation formative` présents + bullet `unrelated` ignoré ; absent → fallback no-op ; malformed sans `== Glossary` → gracefully skipped no-op). Runner dédié `CapsuleGlossaryContextCucumberRunner` (pattern S-082) + `CapsuleGlossaryContextSteps` (10 step defs préfixés "glossary context", bug S-088) + `CucumberTaskSpec("cucumberTestGlossaryContext")` 14ème câblé `build.gradle.kts` (13ème cucumber dédié). Baby-step BDD RED → GREEN (3/3 PASSED en 55s) → REFACTOR (`check` BUILD SUCCESSFUL in 28m52s, 857 unit VERT baseline S-111 préservée — pure BDD zéro production modifié, 64 functional VERT baseline préservée, cucumberTest global SKIPPED par CR-10, Kover VERT, zéro régression, zéro secret). Domaine `capsule.context` clos (13 fichiers prod, 3 loaders). EPIC CAP-GLOSSARY TERMINÉ 4/4 US. | 9/10
| 111 | 2026-08-12 | feat+ddd+tdd | **CAP-GLOSSARY US-2 Wiring `CollectCapsuleAugmentedContextTask` + `ContextConfig` + merger 4 sources + `CapsuleExtension` + `CapsuleManager` + provenance tracking** — `ContextConfig` 3ème champ `glossaryFile: String? = null` (pattern `scenarioFile`). `CapsuleConfigMerger.mergeContextConfig` étendu `mergeStr` pour `glossaryFile` + `loadFromEnvironment` (`CAPSULE_CONTEXT_GLOSSARY_FILE`) + `buildConfigFromProperties` (`capsule.context.glossaryFile`). `CapsuleExtension.glossaryFile: Property<String>` convention `""`. `pushConfigIntoExtension` push `glossaryFile` (guard `isPresent || blank`, pattern `scenarioFile` L124-126). `CollectCapsuleAugmentedContextTask.glossaryFile: ConfigurableFileCollection @InputFiles @PathSensitive(RELATIVE) @Optional` + `resolveGlossarySection(budget)` (5% budget = 400 tokens/8000, `GlossaryLoader.load(target, glossaryBudget)`, pattern `resolveScenarioSection` L281-296) + `trackSources` étendu tracking GLOSSARY (1 `ProvenanceSource` si `glossaryTarget` existe, pattern SCENARIO L230-249) + appel `CapsuleContextBuilder.build()` étendu `glossarySection = resolveGlossarySection(budget)`. `CapsuleManager` wiring lazy provider `glossaryFile.from(project.provider {...})` (extension afterEvaluate > CLI, pattern `scenarioFile` L229-237). Baby-step TDD strict RED (1 FAIL + 1 passed par accident) → GREEN (2/2 `GlossaryContextFunctionalTest` PASSED — glossary fourni → `==== Official Glossary (glossary)` + termes `compétence transversale`/`évaluation formative` présents + bullet `unrelated` de section non-Glossary ignoré ; absent → pas de section glossary backward compat) → REFACTOR (`check` BUILD SUCCESSFUL in 26m25s, 857 unit VERT inchangé baseline S-110, 64 functional VERT +2 net, Kover VERT, validatePlugins VERT, zéro régression, zéro secret). Backward compat strict (`glossaryFile = null` défaut → `resolveGlossarySection` retourne `""` → tests existants inchangés). EPIC CAP-GLOSSARY 3/4 US (reste US-3 cucumber 3 scénarios). | 9/10
| 110 | 2026-08-12 | feat+ddd+tdd | **CAP-GLOSSARY US-1 `GlossaryEntry` + `GlossaryContext` + `GlossaryLoader` + `CapsuleContext.glossarySection` + Builder + PromptBuilder étendus** — Domaine `capsule.context` étendu (3 nouveaux fichiers prod : `GlossaryEntry.kt` data class immutable invariant fail-fast blank term/definition, `GlossaryContext.kt` data class immutable liste ordonnée, `GlossaryLoader.kt` object pur mirror `PedagogicalScenarioLoader` — `load(glossaryFile: File, maxTokens: Int): String` parse AsciiDoc `== Glossary` + bullets `- term: definition` split premier `:`, rend `==== Official Glossary (glossary)\n<term>: <definition>\n...`, tronque via `ContextChannel.Docs.truncateToTokens(maxTokens)` variant Docs sémantiquement proche, missing file → empty, malformed bullet skipped, non-Glossary section ignored, zero budget → empty). `CapsuleContext` 4ème champ `glossarySection: String = ""` invariant ajusté `rendered.isBlank() == (channels.isEmpty() && scenarioSection.isBlank() && glossarySection.isBlank())` backward compat. `CapsuleContextBuilder.build(composite, budget, scenarioSection="", glossarySection="", tracker=null)` 4ème paramètre + `buildRendered` private helper refactor `when` 4 cas → `listOf(channelBlock, trimmedScenario, trimmedGlossary).filter { isNotBlank }.joinToString("\n\n")` 3 sections conditionnelles + `tracker.retainOnly` étendu `+= GLOSSARY_CHANNEL` si `trimmedGlossary.isNotBlank()` pattern `SCENARIO_CHANNEL` + `CapsuleContext` retour étendu `glossarySection = trimmedGlossary`. `DefaultCapsulePromptBuilder.buildGeneratePrompt` instruction conditionnelle `if (state.augmentedContext.contains("Official Glossary"))` → "Use the official terminology defined in the Official Glossary section. Prefer glossary terms over generic synonyms." pattern `Pedagogical Scenario` L50-55. `ContextProvenance.GLOSSARY_CHANNEL = "GLOSSARY"` const pattern `SCENARIO_CHANNEL`. `CollectCapsuleAugmentedContextTask` L132 appel `build()` migré arguments nommés (`composite = `, `budget = `, `scenarioSection = `, `tracker = `) pour robustesse. Baby-step TDD strict RED (23 tests → `Unresolved reference 'GlossaryLoader'`/`'GlossaryEntry'`/`'GlossaryContext'`/`'glossarySection'` 14 sites) → GREEN (857 unit VERT +23 net : 15 `GlossaryLoaderTest` + 7 `CapsuleContextBuilderTest` glossarySection + 2 `DefaultCapsulePromptBuilderTest` glossary) → REFACTOR (`check` BUILD SUCCESSFUL in 18m48s, Kover VERT, validatePlugins VERT, 62 functional VERT inchangé, zéro régression, zéro secret). Backward compat strict (`glossarySection = ""` défaut → tests existants inchangés, 7 tests scenarioSection préservés 100%). EPIC CAP-GLOSSARY 2/4 US. | 9/10
| 109 | 2026-08-12 | cadrage+gouvernance | **CAP-GLOSSARY US-0 Cadrage EPIC Injection terminologique** — Audit code : `CapsuleContextBuilder.build(composite, budget, scenarioSection, tracker)` L69-92 (pure object, 3 params optionnels, `scenarioSection` extension capsule-local pattern CAP-SPD-2 pas canal N0, `tracker.retainOnly` prunage après budget+filter, `survivingChannels += SCENARIO_CHANNEL` si non-blank) ; `CapsuleContext` L26-45 (3 champs `channels`/`rendered`/`scenarioSection`, invariant `rendered.isBlank() == (channels.isEmpty() && scenarioSection.isBlank())`) ; `CollectCapsuleAugmentedContextTask` L49-292 (`scenarioFile: ConfigurableFileCollection @Optional` L89-92, `resolveScenarioSection(budget)` L276-291 5% budget, `trackSources(tracker)` L165-245 tracking 5 canaux EAGER/RAG/Graphify/DOCS/SCENARIO) ; `ContextConfig` L31-34 (2 champs `docsGlobs`/`scenarioFile`) ; `CapsuleConfigMerger.mergeContextConfig` L326-331 (`mergeStr` pour `scenarioFile`) ; `DefaultCapsulePromptBuilder` L34-65 (instruction conditionnelle `if (state.augmentedContext.contains("Pedagogical Scenario"))` L50-55) ; `ContextProvenance.SCENARIO_CHANNEL = "SCENARIO"` L41 ; `ProvenanceTracker.retainOnly` L39-41 ; `CapsuleExtension.scenarioFile: Property<String>` convention `""` L27 ; `pushConfigIntoExtension` scenarioFile push L124-126 ; `CapsuleManager.registerCollectAugmentedContextTask` scenarioFile provider L229-237 ; 13 `CucumberTaskSpec` existants (12 cucumber dédiés + integration) ; 20 features existantes ; 11 `Messages*.properties` (base + en + 9 langues). 12 décisions architecturales : (1) extension capsule-local `glossarySection` pas 6ème canal N0 (pattern CAP-SPD-2, pas de bump codebase-contracts) ; (2) `GlossaryEntry(term, definition)` data class immutable (invariant fail-fast si blank) ; (3) `GlossaryContext(terms: List<GlossaryEntry>)` data class immutable (liste ordonnée, pas map — l'ordre a une valeur pédagogique) ; (4) `GlossaryLoader` object pur mirror `PedagogicalScenarioLoader` — `load(file, maxTokens): String`, parse AsciiDoc `== Glossary` + bullets `- term: definition`, rend `==== Official Glossary (glossary)\n...`, tronque via `ContextChannel.Docs.truncateToTokens` ; (5) format AsciiDoc natif (pas de nouvelle dépendance parsing, MVP0 workspace AsciiDoc-native) ; (6) budget 5% = 400 tokens (pattern `scenarioSection`, glossaire AFNOR/REAC typique 30-50 termes × 10-20 tokens) ; (7) `CapsuleContextBuilder.build()` 4ème paramètre `glossarySection`, `tracker.retainOnly` étendu `+= GLOSSARY_CHANNEL` ; (8) `DefaultCapsulePromptBuilder` instruction conditionnelle `Official Glossary` "Use the official terminology... Prefer glossary terms over generic synonyms." ; (9) provenance `GLOSSARY_CHANNEL = "GLOSSARY"` const + `trackSources` étendu ; (10) 4 sources `context.glossaryFile` (ENV `CAPSULE_CONTEXT_GLOSSARY_FILE` < props `capsule.context.glossaryFile` < YAML < CLI) ; (11) pas de nouvelle tâche Gradle ni clé CapsuleMessages (glossaire = input de `collectCapsuleAugmentedContext` existant) ; (12) runner dédié `CapsuleGlossaryContextCucumberRunner` + `CucumberTaskSpec("cucumberTestGlossaryContext")` 14ème (13ème cucumber dédié). 4 US ~6 pts (US-0 ✅, US-1 data classes + loader + builder + prompt builder ~10 unit, US-2 wiring + 2 functional, US-3 cucumber 3 scénarios). Zéro code modifié — gouvernance pure pattern S-057/S-063/S-076/S-083/S-092/S-094/S-097/S-102/S-107. MàJ BACKLOG/INDEX/PROMPT_REPRISE/TODO. | 9/10
| 108 | 2026-08-11 | feat+ddd+tdd+bdd+cucumber+cloture | **CAP-PROVENANCE US-1 + US-2 + Clôture EPIC CAP-PROVENANCE (3/3 US TERMINÉ)** — US-1 : `ProvenanceSource.kt`/`ProvenanceChannel.kt`/`ContextProvenance.kt`/`ProvenanceTracker.kt` (4 fichiers prod, domaine `capsule.context`) + `CapsuleContextBuilder.build()` étendu `tracker: ProvenanceTracker? = null` (prune aux canaux survivants `it.type.name` + `SCENARIO_CHANNEL` si scenario non-blank, backward compat null). Jackson : `totalChars`/`totalTokens` exposés `@get:JsonProperty` (les `fun` ne sérialisent pas — itération GREEN 2 tests fail NPE). `ProvenanceTracker` : replace semantics + ignore empty source list + `retainOnly` prune + `build()` snapshot immutable. Baby-step TDD : RED (15 tests → `Unresolved reference 'ProvenanceSource'`) → GREEN (12/12 `ContextProvenanceTest` + 3/3 builder étendu) → REFACTOR (834 unit VERT +15 net). US-2 : `CollectCapsuleAugmentedContextTask` `@OutputFile provenanceFile` (`build/capsule/context-provenance.json`) + `trackSources(tracker)` (EAGER = eagerFiles `.filter{it.exists()}` → N sources fileName + chars `readText().length` + tokens `ContextChannel.estimateTokens` ; RAG/Graphify = 1 source `rag-injected`/`graphify-injected` si non-blank ; DOCS = docsFiles ou `docs-injected` legacy ; SCENARIO = compagnon `.adoc` résolu depuis répertoire ou fichier direct) + log `CAPSULE PROVENANCE → N channels, M sources → <path>` 2ème ligne. `CapsuleManager.registerCollectAugmentedContextTask` wiring `task.provenanceFile.set(...)`. 2 fix TDD découverts en GREEN : (a) `FileNotFoundException` AGENT.adoc absent — KDoc disait "missing files skipped" mais code lisait sans vérifier → `.filter { it.exists() }` sur eagerFiles (run + trackSources) + docsFiles ; (b) `scenarioFile` répertoire (CAP-SPD-3) → `readText()` crash `ScenarioContextFunctionalTest` → résolution compagnon `.adoc`. 2/2 functional `CollectProvenanceFunctionalTest` GREEN (provenance JSON generated EAGER+RAG+Graphify, empty context → empty provenance). Cucumber `capsule_provenance.feature` 3 scénarios `@context @provenance` (provenance JSON generated with EAGER+RAG+Graphify tracked, channels carry file names + chars + tokens, empty context → empty provenance JSON) + `CapsuleProvenanceSteps` (14 step defs préfixés "provenance" — bug S-088 glue `capsule.scenarios` partagée) + `CapsuleProvenanceCucumberRunner` (pattern S-082 `@SelectClasspathResource` + filter `@context and @provenance and not @wip and not @integration`) + `CucumberTaskSpec("cucumberTestProvenance")` 13ème câblé `build.gradle.kts` (12ème cucumber dédié, après `cucumberTestAudioPost`, avant `cucumberTestIntegration`). 3/3 scénarios PASSED (~1m55s). Baby-step TDD/BDD RED → GREEN → REFACTOR. `./gradlew check` BUILD SUCCESSFUL in 15m52s : 834 unit VERT (+15 net) + 62 functional VERT (+2) + 3 cucumber GREEN. Zéro régression, zéro secret (`rg "TOKEN|SECRET|PASSWORD|API_KEY|gho_|ghp_"` clean — seul match `DEFAULT_TOKEN_BUDGET` faux positif). EPIC CAP-PROVENANCE TERMINÉ 3/3 US (US-0 cadrage S-107, US-1 unit S-108, US-2 wiring+functional+cucumber S-108). Domaine `capsule.context` clos (10 fichiers prod). | 9/10
| 107 | 2026-08-11 | cadrage+gouvernance | **CAP-PROVENANCE US-0 Cadrage EPIC audit context anti-hallucination** — Audit code : `CapsuleContextBuilder.build(composite, budget, scenarioSection)` L60-75 (pure object, retourne `CapsuleContext(channels, rendered, scenarioSection)`, les `channels` sont les `ContextChannel` N0 budgetés + filtrés, aucun tracking du nom de fichier source — le `composite.eagerSection` est déjà flatté en un seul string) ; `CollectCapsuleAugmentedContextTask.run()` L98-132 (assemble `composite` EAGER + RAG + Graphify + Docs + config, appelle `CapsuleContextBuilder.build()`, écrit `context.rendered` dans `outputFile` `augmented-context.txt`, aucun artefact de provenance — le task connaît les sources `eagerFiles.files`/`docsFiles.files`/`scenarioFile.files`/`ragContent`/`graphifyContent` mais ne les exporte pas) ; `ContextChannel` N0 sealed 5 variants (Eager/Rag/Graphify/Resource/Docs) — `type`/`content`/`sectionHeader`/`name`/`isNotEmpty()`/`truncateToTokens()`/`estimateTokens()`, aucun champ source file dans le contrat N0 = bump codebase-contracts multi-borough si touché → extension capsule-local (pattern CAP-SPD-2 `scenarioSection`) ; `ChannelBudget` N0 (`totalTokenBudget` + 5 fractions 0.40/0.30/0.20/0.10/0, pas de tracking per-source) ; `ContextConfig` (2 champs `docsGlobs`/`scenarioFile`) pas de config provenance ; `CapsuleConfigMerger` pas de `mergeProvenanceConfig` ; `CapsuleExtension`/`CapsuleConventions`/`pushConfigIntoExtension` pas de properties provenance ; `CapsuleMessages` 31 keys pas de clé provenance (pas de nouvelle tâche) ; Jackson déjà dépendance (`jacksonObjectMapper` `CapsuleCompositeContextTask` L88, `ObjectMapper` `PedagogicalScenarioLoader` L36 + `ContentPlanValidator` L37, `YAMLFactory` `CapsuleConfigLoader` L21, `jackson-module-kotlin` + `jackson-databind` + `jackson-dataformat-yaml` en `implementation`) ; 12 `CucumberTaskSpec` existants (11 cucumber dédiés + `cucumberTestIntegration`) ; `CapsuleContextCucumberRunner` (S-082) runner de référence pour `capsule_context.feature` `@context`, `CapsuleContextSteps` (GradleRunner, `artefact()` lit `build/capsule/augmented-context.txt`) ; `CapsuleScenarioContextCucumberRunner` 2ème runner de référence (`@context @scenario`, steps préfixés "scenario context", bug S-088 glue partagée), `capsule_scenario_context.feature` 3 scénarios. 11 décisions architecturales : (1) pas de nouveau domaine — `ContextProvenance` + `ProvenanceSource` + `ProvenanceTracker` rejoignent `capsule.context` existant (6 fichiers), provenance est artefact compagnon du contexte augmenté (pattern CAP-SPD extension capsule-local pas N0) ; (2) `ProvenanceSource(fileName: String, chars: Int, tokens: Int)` data class immutable, un canal peut avoir N sources (EAGER = N fichiers, DOCS = N fichiers, RAG/Graphify = 1 source "injected", SCENARIO = 1 fichier), pas de `init` fail-fast (valeurs calculées) ; (3) `ProvenanceChannel(channel: String, sources: List<ProvenanceSource>)` data class + `totalChars()` + `totalTokens()` ; (4) `ContextProvenance(channels: List<ProvenanceChannel>)` data class + `totalChars()` + `totalTokens()` + `toJson(): String` (Jackson `ObjectMapper` + `INDENT_OUTPUT`, schema `{ "channels": [...], "totalChars": N, "totalTokens": M }`), pas d'invariant fail-fast (provenance vide légitime) ; (5) `ProvenanceTracker` mutable accumulator PAS object pur (accumule state pendant build), `trackChannel(channel, sources)` + `build(): ContextProvenance` snapshot immutable, construit par le task passé au builder, pattern tracker mutable + snapshot immutable (`build()` gèle) ; (6) `CapsuleContextBuilder.build()` étendu `tracker: ProvenanceTracker? = null` optional, quand non-null le builder signale les canaux non-empty après budget+filter, backward compat `null` → aucun tracking tests existants inchangés, le builder ne calcule PAS les sources (n'a pas accès aux File objects, le task fournit les sources) ; (7) wiring `CollectCapsuleAugmentedContextTask` construit `ProvenanceTracker` avant `build()`, passe au builder, `tracker.build().toJson()` écrit dans nouveau `@OutputFile provenanceFile: RegularFileProperty` (`build/capsule/context-provenance.json`), tracking EAGER=eagerFiles.sortedBy{name} → N sources, RAG=1 source "rag-injected" si non-blank, Graphify=1 source "graphify-injected" si non-blank, DOCS=docsFiles ou docsContent legacy, SCENARIO=scenarioFile.firstOrNull() si résolu, ne tracker QUE canaux non-empty après build ; (8) pas de config/DSL/4 sources — provenance always-on (cheap : pas de LLM/réseau/FFmpeg, Jackson serialize < 1ms), pas de `CapsuleExtension` property ni `CapsuleConventions` ni `pushConfigIntoExtension` ni `mergeProvenanceConfig` ni `ContextConfig` champ, l'auteur peut l'ignorer pas la désactiver ; (9) économie d'encre `provenanceFile` `@OutputFile` déclaré, task UP-TO-DATE quand `augmented-context.txt` l'est (même task, mêmes inputs), pas de re-serialize inutile, pas de probe ; (10) JSON format Jackson `INDENT_OUTPUT` (lisibilité auteur), pas de YAML, pas de timestamp (provenance régénérée à chaque build pas archive historique) ; (11) runner dédié `CapsuleProvenanceCucumberRunner` (pattern S-082 `@SelectClasspathResource("features/capsule_provenance.feature")` + filter `@context and @provenance and not @wip and not @integration`), steps `CapsuleProvenanceSteps` préfixés "provenance" (bug S-088 glue `capsule.scenarios` partagée — step texts uniques "provenance JSON contains channel" vs context "augmented context artefact contains"), `CucumberTaskSpec("cucumberTestProvenance")` 13ème câblé `build.gradle.kts` (12ème cucumber dédié), `CapsuleMessages` non étendu — log `CAPSULE PROVENANCE → N channels, M sources → <path>` 2ème ligne après `CAPSULE CONTEXT →` existant. Zéro code modifié — session de gouvernance pure (pattern S-057/S-063/S-076/S-083/S-092/S-094/S-097/S-102). INDEX/BACKLOG/PROMPT_REPRISE/TODO mis à jour. | 9/10
| 106 | 2026-08-11 | feat+bdd+cucumber+cloture | **CAP-AUDIO US-4 Cucumber BDD + clôture EPIC CAP-AUDIO (5/5 US)** — Feature `capsule_audio_post.feature` `@audio @post` (4 scénarios : default NoOp skip économie d'encre, BGM enabled NoOp → degraded, ducking only NoOp → degraded, ducking + BGM NoOp → degraded) + `CapsuleAudioPostSteps` (13 step defs préfixés "audio post", pattern `CapsuleCaptureStrategySteps` — étape interne `generateCapsuleVideo`, pas tâche standalone — `applyAudioPostIfEnabled` est câblée dans `CapsuleVideoTask` post-burnIn pre-convertFormat, pas une tâche Gradle) + `CapsuleAudioPostCucumberRunner` (pattern S-082 `@SelectClasspathResource("features/capsule_audio_post.feature")` + filter `@audio and @post and not @wip and not @integration` + glue `capsule.scenarios` + plugin pretty/html/json reports `cucumber-audio-post.html/.json`) + `CucumberTaskSpec("cucumberTestAudioPost")` 12ème câblé `build.gradle.kts` dans `cucumberConventions.additionalTasks` (11ème cucumber dédié, 12ème `CucumberTaskSpec` total avec `cucumberTestIntegration`). Position : après `cucumberTestDurationValidation`, avant `cucumberTestIntegration`. NoOp processor (pas de vrai FFmpeg — `ffmpegExecutablePath = "noop"` routes through `NoOpAudioPostProcessor` which returns `false`, keeping the original video) — valide le wiring + economy-of-ink guard + factory dispatch, pas le rendu audio (couvert par US-2 unit tests `AudioPostProcessorTest` 17 unit). Baby-step BDD RED → GREEN → REFACTOR : RED (feature + steps + runner + CucumberTaskSpec écrits) → GREEN (4/4 scénarios PASSED en 7m21s, BUILD SUCCESSFUL) → REFACTOR (`./gradlew check` BUILD SUCCESSFUL in 24m12s, 819 unit VERT baseline S-105 préservée, 60 functional VERT baseline S-105 préservée, `validatePlugins` VERT, Kover VERT, zéro régression, zéro secret). EPIC CAP-AUDIO TERMINÉ 5/5 US (US-0 cadrage S-102, US-1 config+merger S-103, US-2 processor+factory S-104, US-3 wiring S-105, US-4 cucumber S-106). Domaine `capsule.audio` (10ème) clos (6 fichiers prod : `AudioPostConfig`, `AudioPostProcessor` interface, `AudioPostProcessorImpl`, `NoOpAudioPostProcessor`, `AudioPostCommand`, `AudioPostException`). | 9/10
| 105 | 2026-08-11 | feat+ddd+tdd | **CAP-AUDIO US-3 Wiring `CapsuleVideoTask.applyAudioPostIfEnabled` + câblage** — Nouvelle méthode `internal fun applyAudioPostIfEnabled(finalVideo: File)` (20 lignes, pattern `burnInSubtitlesIfEnabled` + `convertFormatIfEnabled`) : skip si `!bgmEnabled && !duckingEnabled` (économie d'encre défaut false → no-op backward compat), skip si `finalVideo` n'existe pas → warn + return, factory `CapsuleManager.resolveAudioPostProcessor(ffmpegPath, strict = capsuleExtension.strictMode.get())` (5ème factory), log lifecycle si processor non-NoOp, construction `AudioPostConfig` depuis `capsuleExtension.audioPost*` (5 champs), `processor.process(finalVideo, tmpFile, config)` retourne Boolean — si succès rename tmpFile → finalVideo + log "Audio post: BGM + loudness + ducking applied", si échec (`false`) warn + garde original (degraded), `catch (AudioPostException)` warn + garde original (pattern `BurnInException`), `finally` delete tmpFile. Câblage `captureDeckSequential` L493 (après `burnInSubtitlesIfEnabled`, avant `convertFormatIfEnabled`) + `captureDeckParallel` L524 (même position). Imports `capsule.audio.AudioPostConfig` + `capsule.audio.NoOpAudioPostProcessor` ajoutés. Baby-step TDD RED → GREEN → REFACTOR : RED (3 functional tests — 2 failed: log "Audio post:" absent car méthode inexistante, 1 passed par accident: default skip valide absence) → GREEN (3/3 `AudioPostFunctionalTest` PASSED en 2m37s — default skip, BGM enabled NoOp degraded, ducking enabled NoOp degraded) → REFACTOR (`compileKotlin` + `compileTestKotlin` BUILD SUCCESSFUL, `test` 819 unit VERT 0 failures, `check` BUILD SUCCESSFUL 9m20s, 819 unit + 60 functional = 879 tests 0 FAIL, cucumber SKIPPED par CR-10). 819 unit VERT (inchangé baseline S-104), 60 functional VERT (+3 net), zéro régression, zéro secret. | 9/10
| 104 | 2026-08-11 | feat+ddd+tdd | **CAP-AUDIO US-2 `AudioPostProcessor` + Impl + NoOp + Exception + `AudioPostCommand` + factory** — Domaine `capsule.audio` enrichi (4 fichiers prod + 1 exception). `AudioPostProcessor` interface (`process(videoFile: File, outputFile: File, config: AudioPostConfig): Boolean` + `isAvailable(): Boolean` + `name(): String`) — `process` retourne Boolean degraded mode (pattern `VideoFormatConverter`), `AudioPostException(message: String)` erreurs critiques (pattern `BurnInException`). `AudioPostProcessorImpl(ffmpegPath: String)` — FFmpeg complex filter `loudnorm=I=${loudnessTarget}:TP=-1.5:LRA=11` (EBU R128) + `volume=${bgmLevel}dB + amix=inputs=2:duration=first:dropout_transition=0` BGM mix (si `bgmEnabled && bgmFile exists`) + `sidechaincompress=threshold=0.1:ratio=4:attack=5:release=300` ducking (si `duckingEnabled && bgmEnabled` — ducking sans BGM ignoré, meaningless). `NoOpAudioPostProcessor` — `process()` retourne `false` (degraded garde original, pattern `NoOpVideoFormatConverter`). `AudioPostCommand` object pur (domaine `capsule.audio`, pattern `FormatConversion`) — `buildFilterGraph(config, hasBgmInput): String` (pure, loudness-only / loudness+BGM `[0:a]loudnorm[voice];[1:a]volume[bgm];[voice][bgm]amix[aout]` / loudness+BGM+ducking `...sidechaincompress[ducked];[voice][ducked]amix[aout]` / ducking-without-BGM ignored → loudness-only fallback) + `buildArgv(videoFile, bgmFile, outputFile, config, ffmpegPath): List<String>` (BGM = 2ème `-i` + `-filter_complex` + `-map 0:v -map [aout]`, no-BGM = `-af` loudnorm only, `-c:v copy -c:a libvorbis -shortest`). `CapsuleManager.resolveAudioPostProcessor(ffmpegPath, strict)` 5ème factory `resolve*` (pattern `resolveFormatConverter` — `noop`→NoOp, unavailable+non-strict→NoOp, unavailable+strict→throw via `StrictModeGuard.requireAvailable`, available→Impl). Baby-step TDD RED → GREEN → REFACTOR : RED (17 tests → `Unresolved reference 'AudioPostProcessorImpl'`/`'NoOpAudioPostProcessor'`/`'resolveAudioPostProcessor'`) → GREEN (4 fichiers prod `AudioPostProcessor.kt` + `AudioPostProcessorImpl.kt` + `AudioPostCommand.kt` + `AudioPostException.kt` + factory `CapsuleManager`) → REFACTOR (2 itérations : `buildArgv` pure ne vérifie pas existence BGM — laissé à Impl, test `assertEquals(1, ...)` corrigé 2→1 `-i` flags no-BGM). 819 unit VERT (+17 net : 5 `buildFilterGraph` + 2 `buildArgv` + 4 Impl + 2 NoOp + 4 factory), zéro régression, zéro secret. | 9/10
| 103 | 2026-08-11 | feat+ddd+tdd | **CAP-AUDIO US-1 `AudioPostConfig` + merger 4 sources** — Nouveau domaine `capsule.audio` (10ème) créé avec `AudioPostConfig` data class 5 champs (`bgmEnabled: Boolean = false`, `bgmFile: String = ""`, `bgmLevel: Double = -18.0`, `loudnessTarget: Double = -16.0`, `duckingEnabled: Boolean = false`), KDoc EBU R128 + backward compat. Section `audioPost` 10ème dans `CapsuleConfig` (import `capsule.audio.AudioPostConfig`). `CapsuleConfigMerger` : `merge()` étendu `audioPost = mergeAudioPostConfig(...)` + `loadFromEnvironment` 5 ENV vars `CAPSULE_AUDIO_POST_BGM_ENABLED`/`_BGM_FILE`/`_BGM_LEVEL`/`_LOUDNESS_TARGET`/`_DUCKING_ENABLED` + `buildConfigFromProperties` 5 props `capsule.audioPost.*` + `mergeAudioPostConfig` (pattern `mergeValidationConfig` — `mergeBoolean` bgmEnabled/duckingEnabled, `mergeStr` bgmFile avec env fallback, `mergeDouble` bgmLevel/loudnessTarget). `CapsuleModels` : `CapsuleExtension` +5 properties (`audioPostBgmEnabled` convention `false`, `audioPostBgmFile` convention `""`, `audioPostBgmLevel` convention `-18.0`, `audioPostLoudnessTarget` convention `-16.0`, `audioPostDuckingEnabled` convention `false`) + `CapsuleConventions` +5 champs (mêmes défauts) + instance `conventions` initialisée. `CapsulePlugin.pushConfigIntoExtension` : section AudioPost 5 guards `isPresent` + convention (pattern `durationValidation`). Baby-step TDD RED → GREEN → REFACTOR : RED (4 `AudioPostConfigTest` + 12 merger `audioPost` tests → `Unresolved reference: AudioPostConfig`) → GREEN (AudioPostConfig.kt + CapsuleConfig + Merger + Models + Plugin) → REFACTOR (802 unit VERT +20 net, `CapsulePluginFunctionalTest` VERT, zéro régression, zéro secret). | 9/10
| 102 | 2026-08-11 | cadrage+gouvernance | **CAP-AUDIO US-0 Cadrage EPIC post-production audio** — Audit code : `CapsuleVideoTask.captureDeckSequential` (L461-498) + `captureDeckParallel` (L500-529) — point d'insertion entre `burnInSubtitlesIfEnabled` (L492/523) et `convertFormatIfEnabled` (L493/524) ; `SubtitleBurnInService` (interface + Impl + NoOp + factory + `BurnInException`) pattern de référence ; `VideoFormatConverter` (interface + Impl + NoOp + factory, retourne Boolean degraded) 2ème pattern ; `CapsuleConfig` 9 sections pas de `audioPost` ; `CapsuleConfigMerger` 9 mergeXxxConfig + 5 helpers scalaires (`mergeStr/mergeInt/mergeDouble/mergeBoolean/mergeStrList`) + 3 helpers enum (`mergeVersioning`/`mergeCaptureStrategy`/`mergeOutputFormat`) ; `CapsuleExtension`/`CapsuleConventions`/`pushConfigIntoExtension` pas de audioPost ; `CapsuleManager` 4 factories `resolve*` (`resolveManimVideoMixer`/`resolveManimEngine`/`resolveSubtitleBurnInService`/`resolveFormatConverter`) ; `StrictModeGuard.requireAvailable` 7 branchés ; `MediaProbeUtil.probeDuration` réutilisable ; 11 `CucumberTaskSpec` existants ; `CapsuleMessages` 31 keys pas de audioPost ; `mixAudioWithVideo` (L891-932) TTS brut mixé — CAP-AUDIO intervient après + après burn-in, ne remplace pas. 12 décisions architecturales : (1) domaine `capsule.audio` 10ème, `AudioPostProcessor` interface + Impl + NoOp + `AudioPostException` ; (2) `AudioPostConfig` 5 champs defaults backward compat (`bgmEnabled=false`, `bgmFile=""`, `bgmLevel=-18.0` dB, `loudnessTarget=-16.0` LUFS EBU R128, `duckingEnabled=false`) ; (3) section `audioPost` 10ème `CapsuleConfig` ; (4) 4 sources `mergeAudioPostConfig` (`mergeBoolean` bgmEnabled/duckingEnabled, `mergeStr` bgmFile, `mergeDouble` bgmLevel/loudnessTarget) ; (5) `AudioPostProcessor.process()` retourne Boolean degraded (pattern `VideoFormatConverter`) + `AudioPostException` ; (6) `AudioPostProcessorImpl` FFmpeg `loudnorm=I=-16:TP=-1.5:LRA=11` + `amix` BGM + `sidechaincompress` ducking (threshold -20dB, ratio 4:1) ; (7) `NoOpAudioPostProcessor` test fallback ; (8) factory `resolveAudioPostProcessor` 5ème pattern `resolveFormatConverter` ; (9) wiring `applyAudioPostIfEnabled` post-burnIn pre-convertFormat (L492.5/L523.5) ; (10) `CapsuleExtension` 5 properties + conventions + push ; (11) économie d'encre skip si disabled/missing ; (12) runner dédié `CapsuleAudioPostCucumberRunner` + `CucumberTaskSpec("cucumberTestAudioPost")` 12ème + steps préfixés "audio post" + pas de nouvelle tâche Gradle (CAP-AUDIO est une étape de `CapsuleVideoTask`, pas une tâche séparée, pas de clé CapsuleMessages). 5 US ~12 pts (US-0 ✅, US-1 config+merger, US-2 processor+factory, US-3 wiring, US-4 cucumber). Zéro code modifié, `compileKotlin` VERT. MàJ BACKLOG/PROMPT_REPRISE/INDEX/TODO. | 9/10
| 101 | 2026-08-11 | feat+ddd+tdd+bdd+ci+cloture | **CAP-CR3-1 TERMINÉ 5/5 US — CI `full` + probe durée** — US-0 cadrage (10 décisions architecturales). US-1 `VideoDurationValidator` (domaine `capsule.validation` 9ème) + `ValidationResult` sealed (Valid/Invalid, pattern `DeckContextValidator` slider SLD-8.3, `message()` Locale.US pour point décimal) — 10 unit GREEN. US-2 `ValidationConfig(durationEnabled=false, toleranceSecs=2.0)` 9ème section `CapsuleConfig` + `mergeValidationConfig` 4 sources (pattern `mergeStrictModeConfig`) + `CapsuleExtension` 2 properties + `CapsuleConventions` 2 champs + `pushConfigIntoExtension` + tâche `validateCapsuleVideoDuration` (groupe `verification`, `@Internal videoFile` existence runtime, `@InputFiles @PathSensitive(RELATIVE) audioFiles`, `@Input toleranceSecs/durationEnabled`, skip si disabled/missing, `throw GradleException` si Invalid) + wiring `CapsuleManager` + `CapsuleMessages` clé `task.validateCapsuleVideoDuration.description` + groupe `task.group.verification` — 5 unit + 3 functional GREEN. US-3 Cucumber `capsule_duration_validation.feature` `@validation @duration` 3 scénarios + runner dédié `CapsuleDurationValidationCucumberRunner` (pattern S-082) + steps `CapsuleDurationValidationSteps` préfixés "duration validation" (bug S-088) + `CucumberTaskSpec("cucumberTestDurationValidation")` 11ème — 3/3 scénarios GREEN. US-4 Workflow CI `.github/workflows/full-tests.yml` job `full-tests` (main only, installe Playwright+ffmpeg+espeak, `test functionalTest`, `validateCapsuleVideoDuration`, upload rapports). Baby-step TDD/BDD RED → GREEN → REFACTOR. 782 unit VERT (+10 net : 10 `VideoDurationValidatorTest` + 5 merger validation) + 57 functional VERT (+3 `ValidateCapsuleVideoDurationFunctionalTest`) + 11 cucumber dédiés GREEN (+1). `check` BUILD SUCCESSFUL (validatePlugins `@PathSensitive` fix). EPIC CAP-CR3-1 TERMINÉ 5/5 US. | 9/10
| 100 | 2026-08-11 | feat+bdd+cucumber+cloture | **CAP-MP4 US-3 Cucumber BDD + clôture EPIC CAP-MP4 (4/4 US TERMINÉ)** — `capsule_format.feature` 3 scénarios `@distrib @format` (Default WEBM distributes only WebM, MP4 distributes only MP4, BOTH distributes WebM+MP4) + runner dédié `CapsuleFormatCucumberRunner` (pattern S-082 `@SelectClasspathResource("features/capsule_format.feature")` + filter `@distrib and @format and not @wip and not @integration`) + `CapsuleFormatSteps` (6 step defs self-contained GradleRunner, pattern `CapsuleDistribSteps` mirror, préfixés "format distribution" — bug S-088 glue `capsule.scenarios` partagée, step texts uniques "format distribution copies video file" vs distrib "the video file is copied") + `CucumberTaskSpec("cucumberTestFormat")` câblé `build.gradle.kts` (9ème, 10 cucumber dédiés total). NoOp converter (pas de vrai FFmpeg — valide la distribution filter, pas le transcode déjà couvert US-2). Baby-step BDD RED → GREEN → REFACTOR : RED (6 tests failed, undefined steps) → GREEN (3/3 scénarios PASSED, 12 steps, 1m22s) → REFACTOR (`check` BUILD SUCCESSFUL 15m38s). 772 unit VERT (baseline S-099 préservée) + 54 functional VERT (baseline préservée) + 10 cucumber dédiés GREEN (66 tests cucumber dédiés, 0 FAIL). 2 `NoTestsDiscoveredException` dans `cucumberTest` global préexistantes (bug S-082 — runners dédiés ContentAndVideos/Context ne découvrent pas via feature global, hors CAP-MP4, `cucumberTest` skippé en CI par CR-10). EPIC CAP-MP4 TERMINÉ 4/4 US (US-0 cadrage S-097, US-1 enum+merger S-098 17 unit, US-2 converter+wiring S-099 13 unit + 2 functional, US-3 cucumber S-100 3 scénarios). | 9/10
| 099 | 2026-08-11 | feat+ddd+tdd | **CAP-MP4 US-2 `VideoFormatConverter` + `FormatConversion` + wiring** — `VideoFormatConverter.kt` (interface + `VideoFormatConverterImpl` FFmpeg `-c:v libx264 -c:a aac -movflags +faststart -y` + `NoOpVideoFormatConverter` returns `false`, pattern `SubtitleBurnInService`). `FormatConversion.kt` (object pur `convertIfNeeded(finalVideo, format, converter, probeDuration): File` — WEBM → no-op, MP4 → transcode + supprime WebM, BOTH → transcode + garde WebM, skip si `.mp4` existe ET probe>0 économie d'encre, échec transcode → garde WebM degraded). `CapsuleManager.resolveFormatConverter(ffmpegPath, strict)` factory (4ème factory `resolve*`, pattern `resolveSubtitleBurnInService` — "noop"→NoOp, unavailable+non-strict→NoOp, unavailable+strict→throw via `StrictModeGuard`). `CapsuleVideoTask.convertFormatIfEnabled(finalVideo)` post-burnIn (L693-712) câblé `captureDeckSequential` L493 + `captureDeckParallel` L523, log `CAPSULE →` pointe vers fichier produit. `DistributeCapsuleVideoTask.format: Property<String>` + filtre `.webm`/`.mp4`/both selon `OutputFormat.fromString` (CLI `-Poutput.format` override). `registerDistributeCapsuleVideoTask` push `merged.output.format.name`. Baby-step TDD RED → GREEN → REFACTOR : RED (13 tests → `Unresolved reference 'VideoFormatConverterImpl'`/`'resolveFormatConverter'`) → GREEN (converter + FormatConversion + factory + wiring + DistributeCapsuleVideoTask) → REFACTOR (patterns `CaptureResolver`/`SubtitleBurnInService` respectés, 4 factory `resolve*` + 7 `StrictModeGuard` cohérents). 772 unit VERT (+13 : 7 `VideoFormatConverterTest` + 6 `FormatConversionTest`), 54 functional VERT (+2 `FormatConversionFunctionalTest`), zéro régression. | 9/10
| 098 | 2026-08-11 | feat+ddd+tdd | **CAP-MP4 US-1 `OutputFormat` enum + `OutputConfig.format` + merger 4 sources** — `OutputFormat` enum type-safe 3 valeurs `WEBM`/`MP4`/`BOTH` (companion `fromString` case-insensitive fallback `WEBM` backward compat, pattern `CaptureStrategy`/`VersioningStrategy`) — `CapsuleConfig.kt:108-134`. `OutputConfig` étendu 4ème champ `format: OutputFormat = OutputFormat.WEBM` (backward compat) — `CapsuleConfig.kt:148`. `mergeOutputFormat` helper dédié (pattern `mergeVersioning`/`mergeCaptureStrategy`, CLI > YAML > props, fallback `WEBM`) — `CapsuleConfigMerger.kt:312-320`. `loadFromEnvironment` (`CAPSULE_OUTPUT_FORMAT`) + `buildConfigFromProperties` (`capsule.output.format`) étendus. `mergeOutputConfig` étendu avec `format`. `CapsuleExtension.outputFormat: Property<OutputFormat>` (convention `WEBM`) + helper Groovy `outputFormat(String)` (case-insensitive) — `CapsuleModels.kt:143-149`. `CapsuleConventions.outputFormat = OutputFormat.WEBM` (champ data class + instance) — `CapsuleModels.kt:175,214`. `pushConfigIntoExtension` push format (guard `isPresent` + convention, pattern `captureStrategy`) — `CapsulePlugin.kt:102`. Baby-step TDD RED → GREEN → REFACTOR : RED (8 `OutputFormatTest` + 9 merger `output.format` → `Unresolved reference 'OutputFormat'`) → GREEN (enum + OutputConfig.format + merger + Extension + Conventions + push) → REFACTOR (aucun — patterns établis respectés, 3 enums/3 helpers/3 Property/3 push cohérents). 759 unit VERT (+17 : 8 `OutputFormatTest` + 9 merger `output.format`), zéro régression, `compileKotlin` + `compileTestKotlin` VERT. US-1 config pure (pas de tâche Gradle, pas de cucumber — US-2/3 restantes). | 9/10
| 097 | 2026-08-11 | cadrage+gouvernance | **CAP-MP4 US-0 Cadrage EPIC Export MP4/H.264** — Audit code : `OutputConfig` (`CapsuleConfig.kt:122-126`) 3 champs pas de `format`, `DistributeCapsuleVideoTask` filtre `*.webm` hardcodé L56, `CapsuleVideoTask.captureDeckSequential/Parallel` WebM final `${deckName}.webm` dans `outDir` post-burnIn L489/L519, `mergeOutputConfig` (L267-273) + `mergeVersioning` (L288-298) pattern à suivre pour `mergeOutputFormat`, `CapsuleExtension`/`CapsuleConventions` pas de `outputFormat`, `pushConfigIntoExtension` pas de push format, 8 `CucumberTaskSpec` existants, `MediaProbeUtil.probeDuration` (économie d'encre MP4) + `StrictModeGuard.requireAvailable` (ffmpeg strict) réutilisables, `CaptureResolver` factory + `CaptureStrategy`/`VersioningStrategy` enums patterns de référence. 10 décisions architecturales : (1) `OutputFormat` enum type-safe 3 valeurs `WEBM`/`MP4`/`BOTH` défaut `WEBM` backward compat, companion `fromString` case-insensitive fallback `WEBM` ; (2) section `output` étendue 4ème champ `format` (pas nouvelle section) ; (3) `mergeOutputFormat` helper dédié pattern `mergeVersioning` ; (4) 4 sources ENV `CAPSULE_OUTPUT_FORMAT` < props `capsule.output.format` < YAML < CLI ; (5) `VideoFormatConverter` object pur domaine `capsule` `convertToMp4` (FFmpeg libx264+aac+faststart) + NoOp + `resolveFormatConverter` factory + StrictModeGuard ; (6) wiring `convertFormatIfEnabled` post-burnIn (MP4 → transcode+supprime WebM, BOTH → garde WebM+MP4, WEBM → no-op) + `CapsuleExtension.outputFormat` + helper Groovy ; (7) `DistributeCapsuleVideoTask` filtre selon format + `task.format` Property ; (8) `CapsuleConventions.outputFormat = WEBM` + push format ; (9) économie d'encre skip transcode si `.mp4` existe ET probe>0 ; (10) runner dédié `CapsuleFormatCucumberRunner` + steps préfixés "format distribution" + `CucumberTaskSpec("cucumberTestFormat")`. 4 US ~5 pts (US-0 ✅, US-1 enum+merger, US-2 converter+wiring, US-3 cucumber). Zéro code modifié, `compileKotlin` VERT. Baseline 742 unit + 52 functional + 3 cucumber préservée. | 9/10
| 096 | 2026-08-11 | feat+ddd+tdd+bdd+cadrage | **CAP-CR3-3 Branchement ScreenshotCapture (EPIC TERMINÉ 5/5 US)** — `CaptureStrategy` enum (PLAYWRIGHT/SCREENSHOT, défaut PLAYWRIGHT backward compat, companion `fromString` case-insensitive) + `CaptureConfig.strategy` 15ème champ + `mergeCaptureConfig` 4 sources (ENV `CAPSULE_CAPTURE_STRATEGY` < props `capsule.capture.strategy` < YAML < CLI) + helper `mergeCaptureStrategy`. `CaptureResolver` object pur (`CaptureResolver.kt`) — factory `resolve(strategy, strict, playwrightFactory, screenshotFactory, noOpCapture, enginePath)` dispatch `when(strategy)` avec `PlaywrightFactory`/`ScreenshotFactory` fun interfaces (testable sans I/O), `StrictModeGuard.requireAvailable` réutilisé. `ScreenshotPlanner` object pur (`ScreenshotPlanner.kt`) — `plan(outputDir, slideDurations)` produit `ScreenshotCapturePlan` (entries pngFile/webmFile/durationSecs + concatListFile + finalWebm), `renderConcatList`, `ffmpegPngToWebmArgs`, `ffmpegConcatArgs` (argv builders). `ScreenshotCaptureImpl.capture()` étendu multi-slide (boucle screenshot PNG par slide + navigate Reveal.next() + ffmpeg png→webm par slide durée exacte + concat webm via ffmpeg concat demuxer `concat-list.txt` → `capsule.webm`). `CapsuleExtension.captureStrategy: Property<CaptureStrategy>` (convention PLAYWRIGHT) + helper Groovy `captureStrategy(String)` (case-insensitive). `CapsuleConventions.captureStrategy = PLAYWRIGHT`. `pushConfigIntoExtension` push strategy (guard convention). `CapsuleVideoTask.resolvePlaywrightCapture` refactorisé → `CaptureResolver.resolve` avec lambdas `playwrightFactory`/`screenshotFactory`, log "Capture: X strategy (N slides, M s total)". Cucumber `capsule_capture_strategy.feature` 3 scénarios `@capture @strategy` + `CapsuleCaptureStrategySteps` (préfixés "capture strategy" — bug S-088) + runner dédié `CapsuleCaptureStrategyCucumberRunner` (pattern S-082 `@SelectClasspathResource` + filter `@capture and @strategy and not @wip`) + `CucumberTaskSpec("cucumberTestCaptureStrategy")` câblé. Baby-step TDD/BDD 3 niveaux : 29 unit RED→GREEN (7 `CaptureStrategyTest` + 8 merger capture.strategy + 6 `CaptureResolverTest` + 8 `ScreenshotPlannerTest`) → 3 functional `CaptureStrategyFunctionalTest` (PLAYWRIGHT default → WebM, SCREENSHOT → WebM, SCREENSHOT + strict + noop manim/ffmpeg → WebM) → 3 cucumber scénarios GREEN. 742 unit VERT (+29) + 52 functional VERT (+3) + 3 cucumber dédiés GREEN. 5 décisions architecturales documentées BACKLOG. Zéro régression, zéro secret. | 9/10
| 095 | 2026-08-11 | feat+ddd+tdd+bdd+rename | **CAP-SPD US-3 Wiring + US-4 Cucumber + renommage SPD→PedagogicalScenario (EPIC CAP-SPD TERMINÉ 5/5 US)** — Renommage sémantique : `SpdContext`→`PedagogicalScenario`, `SpdContextLoader`→`PedagogicalScenarioLoader`, `spdSection`→`scenarioSection`, header `"==== Pedagogical Scenario (scenario)"` (was "SPD Pedagogical Context (spd)"), instruction line `contains("Pedagogical Scenario")` (was `contains("SPD Pedagogical Context")`). 3 fichiers renommés via `git mv`. `metadata.json` validation `type=="SPD"` inchangé (format externe K-2, pas le concept capsule). US-3 Wiring : `ContextConfig.scenarioFile: String?` (8ème champ section context), `mergeContextConfig` étendu `mergeStr`, `loadFromEnvironment` (`CAPSULE_CONTEXT_SCENARIO_FILE`) + `buildConfigFromProperties` (`capsule.context.scenarioFile`), `CapsuleExtension.scenarioFile: Property<String>`, `pushConfigIntoExtension` wiring, `CollectCapsuleAugmentedContextTask.scenarioFile: ConfigurableFileCollection @Optional` + `resolveScenarioSection()` (5% budget = 400 tokens/8000, directory→metadata.json+first.adoc, file→direct adoc), `CapsuleManager` wiring lazy provider. 2 functional `ScenarioContextFunctionalTest` GREEN (scenario fourni → ~140 tokens, absent → ~61 tokens fallback). US-4 Cucumber : `capsule_scenario_context.feature` 3 scénarios `@context @scenario`, `CapsuleScenarioContextSteps` (préfixés "scenario context" — bug S-088), runner dédié `CapsuleScenarioContextCucumberRunner` (pattern S-082), `CucumberTaskSpec("cucumberTestScenarioContext")` câblé. 3/3 scénarios PASSED (1m06s). Baby-step TDD/BDD 3 niveaux : unit (S-094) → functional (S-095) → cucumber (S-095). 713 unit VERT (inchangé) + 49 functional VERT (+2) + 3 cucumber dédiés GREEN. Zéro régression. | 9/10
| 094 | 2026-08-11 | cadrage+gouvernance+feat+tdd+ddd | **CAP-SPD US-0 Cadrage + US-1 SpdContext/SpdContextLoader + US-2 CapsuleContext/Builder/PromptBuilder étendus** — Audit `CapsulePipelineGraph` (11 fichiers pipeline) + `CapsuleContextBuilder` (67 lignes) + `DocContextLoader` (51 lignes, pattern mirror) + `GenerateCapsuleContentTask` (121 lignes) + `DefaultCapsulePromptBuilder` (59 lignes) + `ContextConfig` + 7 `CucumberTaskSpec`. Contrat SPD : producteur = borough privé non nommé publiquement. `metadata.json` K-2 = enveloppe provenance (7 champs), AUCUN champ pédagogique — payload dans AsciiDoc compagnon. Capsule public OSS format-agnostic : valide `type=="SPD"` uniquement, PAS `source`. N0 `ContextChannel` sealed → **extension capsule-local** `CapsuleContext.spdSection` (pas de bump N0). SPD absent → vidéo non bloquée (fallback no-op). US-1 : `SpdContext` data class (6 champs, invariant sessionTitle non-blank) + `SpdContextLoader` (object pur, mirror `DocContextLoader`) — `load(metadataFile, adocFile, maxTokens)`, valide `type=="SPD"`, extrait sections AsciiDoc, rend `==== SPD Pedagogical Context (spd)`, tronque via `ContextChannel.Docs.truncateToTokens`. 10/10 unit GREEN. US-2 : `CapsuleContext` 3ème champ `spdSection: String = ""` (invariant ajusté `rendered.isBlank() == (channels.isEmpty() && spdSection.isBlank())`) + `CapsuleContextBuilder.build(composite, budget, spdSection="")` étendu (append SPD après channels, trim, 4 cas) + `DefaultCapsulePromptBuilder.buildGeneratePrompt` instruction line SPD (conditionnelle `contains("SPD Pedagogical Context")`). 9/9 unit GREEN (6 CapsuleContextBuilder + 3 DefaultCapsulePromptBuilder). 713 unit VERT (+9 net). 7 décisions architecturales documentées BACKLOG. | 9/10
| 093 | 2026-08-11 | feat+ddd+tdd+bdd | **CAP-DOCCONTEXT injection corpus documentaire (EPIC TERMINÉ 5/5 US)** — `ContextConfig` data class (`docsGlobs: List<String> = emptyList()`) 8ème section `CapsuleConfig`. `CapsuleConfigMerger.mergeContextConfig` + nouveau helper `mergeStrList` (List<String> comma-separated, 4 sources ENV < props < YAML < CLI) + `splitCommaList` utilitaire. `loadFromEnvironment` (`CAPSULE_CONTEXT_DOCS_GLOBS`) + `buildConfigFromProperties` (`capsule.context.docsGlobs`). `CapsuleExtension.docsGlobs: ListProperty<String>`. `pushConfigIntoExtension` push context. `DocContextLoader` (object pur, domaine `capsule.context`) — résout fichiers pré-matchés, concatène trié par nom, tronque via `ContextChannel.Docs.truncateToTokens(budget.docsTokens)` (10% = 800 tokens/8000). `CollectCapsuleAugmentedContextTask` étendu : `docsFiles` + `docsGlobs` + `resolveDocsSection` (globs > legacy CLI string rétrocompat). `CapsuleManager` lazy `project.provider { }` + `resolveGlobFiles`. 4 scénarios cucumber `@context @docs` + runner dédié `CapsuleDocContextCucumberRunner` (pattern S-082) + steps préfixés "doc context" (bug S-088). `CucumberTaskSpec("cucumberTestDocContext")` câblé `build.gradle.kts`. Baby-step TDD/BDD 3 niveaux : RED → GREEN → REFACTOR (unit → functional → cucumber). 694 unit VERT (+20) → 47 functional VERT (+3) → 4 cucumber scénarios GREEN. Zéro régression, zéro secret. | 9/10
| 092 | 2026-08-10 | cadrage+gouvernance | **Cadrage 11 EPICs neufs (contexte augmenté + post-production)** — Audit post-CAP-ARCH/CAP-CR3-2. 11 EPICs neufs (47 US, ~88 pts) sur 2 axes. Séquence MVP0 définie. Zéro code modifié. | 9/10
| 091 | 2026-08-10 | feat+ddd+tdd+bdd | **CAP-CR3-2 Mode strict anti-NoOp (EPIC CAP-CR3-2 TERMINÉ 4/4 US)** — `capsule.strictMode` DSL (Boolean, défaut `false`, 4 sources ENV < props < YAML < CLI). `StrictModeGuard` (object pur, domaine `capsule.config`) — `requireAvailable(strict, engineName, isAvailable, path)` lève `IllegalStateException` avec message actionable si `strict && !isAvailable` (nomme engine + path + suggestion `strictMode=false` ou `install <tool>`). 6 `resolve*` branchés : `CapsuleBuildTask.resolveTtsEngine` (piper/espeak absent → guard avant NoOp), `CapsuleVideoTask.resolveTtsEngine` (piper/espeak), `CapsuleVideoTask.resolvePlaywrightCapture` (Playwright), `CapsuleManager.resolveManimEngine` (manim, param `strict`), `CapsuleManager.resolveManimVideoMixer` (ffmpeg, param `strict`), `CapsuleManager.resolveSubtitleBurnInService` (ffmpeg, param `strict`). 3 appelants CapsuleVideoTask passent `strict = capsuleExtension.strictMode.get()`. `noop` explicite (`ttsEngine = "noop"`, `ffmpegPath = "noop"`) reste légitime pour tests, non concerné. `StrictModeConfig` data class (1 champ `enabled: Boolean = false`). `CapsuleConfig` étendu `strictMode: StrictModeConfig`. `CapsuleConfigMerger.mergeStrictModeConfig` (utilise `mergeBoolean`) + `loadFromEnvironment` (`CAPSULE_STRICT_MODE_ENABLED`) + `buildConfigFromProperties` (`capsule.strictMode.enabled`). `CapsuleExtension.strictMode: Property<Boolean>` (convention `false`). `CapsulePlugin.pushConfigIntoExtension` push strictMode. Baby-step TDD/BDD 3 niveaux : RED → GREEN → REFACTOR (unit → functional → cucumber). 674 unit VERT (+19 : 7 `StrictModeGuardTest` + 3 `StrictModeConfigTest` + 9 strictMode merger) → 44 functional VERT (+4 `StrictModeFunctionalTest` : default NoOp preserved, strict + piper absent → FAIL, strict + noop explicite → OK, CLI flag strict → FAIL) → 8 cucumber dédiés GREEN (`capsule_strict_mode.feature` 4 scénarios `@strict @config`, runner `CapsuleStrictModeCucumberRunner` pattern S-082, steps préfixées "strict" pour éviter doublons glue, `writeBuildFile()` privé regénère build.gradle complet au lieu d'appendText double bloc). `CucumberTaskSpec("cucumberTestStrictMode")` câblé `build.gradle.kts`. `check` BUILD SUCCESSFUL 6m54s. Zéro régression, zéro secret. | 9/10
| 090 | 2026-08-10 | feat+bdd+cucumber | **CAP-ARCH-6 Cucumber BDD boundary cross-borough (EPIC CAP-ARCH TERMINÉ 8/8 US)** — `capsule_arch_boundary.feature` (4 scénarios `@cross-borough @architecture @i18n @ai`) : registration task `generateCapsuleContentAndVideos` groupe `capsule`, content pipeline LLM (`generateCapsuleContent` mock Ollama 2 réponses alternées propose/generate → `demo-speaker-notes.adoc` + `demo-script.txt`), video pipeline composite (NoOp engines → `demo_fr.webm`), économie d'encre skip WebM pré-existant valide (ffmpeg 1s, `ffprobe` > 0). Runner dédié `CapsuleArchBoundaryCucumberRunner` (pattern S-082 `@SelectClasspathResource` + filter `@cross-borough and not @wip and not @integration`). `CapsuleArchBoundarySteps` self-contained GradleRunner (fake `translateDeck`, pré-génération artefacts fr `demo_fr-deck.html` + `demo_fr-script.txt` dans dirs attendus). `CucumberTaskSpec("cucumberTestArchBoundary")` câblé `build.gradle.kts`. Décision cadrage : périmètre single-language français (pas 10 langues — CAP-29.5 `multi_language_capsule.feature` garde la responsabilité i18n, éviter test redondant, Single Responsibility). Baby-step TDD/BDD 3 niveaux : RED (8 tests failed steps undefined) → GREEN (4 scénarios PASSED ; itérations : doublons glue `DuplicateStepDefinitionException` avec `MultiLanguageCapsuleSteps`/`CapsuleAiSteps` → steps préfixées "capsule arch boundary", skip `MINIMAL_WEBM` header EBML `ffprobe` 0.0 → ffmpeg WebM valide 1s, feature `demo_fr-speaker-notes.adoc` corrigé en `demo-speaker-notes.adoc` car content pipeline mono-langue sans `_lang`) → REFACTOR (`check` BUILD SUCCESSFUL 6m12s). 655 unit VERT (inchangé) + 40 functional VERT (inchangé) + 4 cucumber dédiés GREEN. Zéro régression, zéro secret. EPIC CAP-ARCH TERMINÉ 8/8 US. | 9/10
| 089 | 2026-08-10 | feat+ddd+tdd+i18n | **CAP-ARCH-5 i18n aligné socle (CapsuleMessages)** — Domaine `capsule.i18n` créé (8ème) : `CapsuleMessages` object (pattern `SliderMessages`/`PlantumlMessages` — `utf8Control` ResourceBundle.Control UTF-8, `forLanguage`/`get`/`format`/`resolveLanguage(project)` cascade `-Pcapsule.language` > DSL `capsule { language }` > default `"en"`). 11 `Messages*.properties` (base + en + 9 langues fr/zh/hi/es/ar/bn/pt/ru/ur) — 23 keys (6 task groups + 17 task descriptions). Wiring `CapsuleManager` (13 tasks) + `CapsuleFeedTaskRegistrar` (4 tasks) → `CapsuleMessages.get("key", lang)`. Fix 3 descriptions françaises hardcoded (deployCapsule "Recadre les capsules...", collectCapsuleContext "Exporte le contexte...", transformCapsuleContext "Parse le fichier..." — violation règle English-only). `CapsuleExtension.language` ajouté (default `"en"`). `MultiLanguageResolver` KDoc corrigé (remove `DocumentI18nBridge` ref supprimé S-030, cite `CapsuleMessages` + `SliderMessages`/`PlantumlMessages`). Décisions : pas de `CapsuleI18nBridge` (anti-pattern S-030), pas de `MessageBundle`/`LocaleResolver` (contrats N0 morts zéro consommateur), `Messages_en.properties` explicite requis (fallback default locale système `fr_FR`). Bug découvert : `Locale.ENGLISH` sans fichier `_en` explicite chargeait `Messages_fr.properties` (default locale système). Baby-step TDD : 655 unit VERT (+87 net : 65 `CapsuleMessagesTest` 23 keys × 10 langs + fallback + missing + 22 `MessagesFrTranslationTest` assertNotEquals). 40 functional VERT (inchangé). `compileKotlin` + `test` + `functionalTest` BUILD SUCCESSFUL. Zéro régression. | 9/10
| 088 | 2026-08-10 | feat+bdd+wiring | **CAP-ARCH-4 Wiring backend + CAP-ARCH-7 US-4 Cucumber distrib** — CAP-ARCH-4 : tâche composite `generateCapsuleContentAndVideos` (groupe `capsule`) chaîne `generateCapsuleContent` → `translateAndGenerateCapsuleVideos` (pattern `DefaultTask` + `dependsOn` par nom string, backend existant intact). `CapsuleFeedTaskDomain.kt` constante `GENERATE_CAPSULE_CONTENT_AND_VIDEOS` + `CONTENT_AND_VIDEOS_DESCRIPTION`. `CapsuleFeedTaskRegistrar.kt` `registerGenerateCapsuleContentAndVideos(project)`. `CapsuleManager.kt` câblage. 3 functional tests (`GenerateCapsuleContentAndVideosFunctionalTest` : registration, description, dependsOn chain `--dry-run` avec dummy deck car `generateCapsuleContent` résout `deckFile` par convention). 3 cucumber `@architecture @wiring` (`capsule_content_and_videos.feature` + `CapsuleContentAndVideosSteps` constants pures + `CapsuleContentAndVideosCucumberRunner` dédié). CAP-ARCH-7 US-4 : feature `capsule_distrib.feature` (5 scénarios `@distrib @versioning`) + `CapsuleDistribSteps` (self-contained GradleRunner pattern `MultiLanguageCapsuleSteps`, steps uniques "distrib" pour éviter doublons `CapsuleStepDefinitions`) + `CapsuleDistribCucumberRunner`. Scénarios : registration tasks --group distribute, copy timestamp, copy incremental v1, second run v2, skip idempotent économie d'encre. 2 `CucumberTaskSpec` câblés (`cucumberTestContentAndVideos` + `cucumberTestDistrib`). 568 unit VERT (inchangé) + 40 functional VERT (+3) + 8 cucumber dédiés GREEN (3 `@architecture @wiring` + 5 `@distrib @versioning`). `check` BUILD SUCCESSFUL. Zéro régression. | 9/10
| 087 | 2026-08-10 | review+fix+tdd+feat | **Code review + Bug burn-in fix + CAP-ARCH-7 Video Destination Versioning (US-1→3)** — Code review post-S-083→086 : 4 points relevés (duplication pipeline koog = dette extraction MEMPHIS future, versionning codebase-contracts 0.1.0 = régularisation, hack buildscript force annotations = acceptable pattern slider, **1 bug réel** = FAIL `@integration` burn-in path mismatch `build/capsule/` vs `outputDir=capsules` connu S-069/S-082 jamais traité). Fix bug TDD : `CapsuleOutputDirResolver` (object pur, package `capsule.support`, résout `outputDir` depuis `build.gradle` du projet Cucumber au lieu de hardcoder `build/capsule/`) + 9 unit tests RED→GREEN + 3 steps Cucumber migrés (`aSubtitleFileIsGeneratedInTheCapsuleOutputDirectory`, `theVideoFileHasAValidWebmEbmlHeader`, `aVideoFileIsGenerated` vers `capsuleOutputDir()` dynamique) + runner `cucumberTestIntegration` câblé dans `build.gradle.kts` (`CucumberTaskSpec` dédié, runner `CucumberIntegrationTestRunner` orphelin désormais exécuté). **BUILD SUCCESSFUL 3m39s** — scénario E2E burn-in `@burnin @integration` VERT (était FAIL depuis S-069). CAP-ARCH-7 (nouvelle feature issue review, video destination versioning) : US-1 `OutputConfig` + `VersioningStrategy` enum (TIMESTAMP|INCREMENTAL) étend `CapsuleConfig` (section `output`, 3 champs `videoDestinationDir`/`versioning`/`versionPrefix`), `CapsuleConfigMerger` 4 sources (ENV < props < YAML < CLI) + `mergeOutputConfig` + `mergeVersioning` + `loadFromEnvironment`/`buildConfigFromProperties` étendus, 10 unit tests (defaults, YAML, props, env, CLI override, blank YAML fallback, versioning, versionPrefix). US-2 `VideoDestinationResolver` (object pur main, `resolveVersionLabel` timestamp ISO-8601 compact ou incremental depuis `collectExistingVersions`, `resolveDestinationDir`, `resolveDestinationFile`, `collectExistingVersions` liste dirs matchant prefix), 10 unit tests. US-3 `DistributeCapsuleVideoTask` (tâche `distributeCapsuleVideo`, groupe `distribute`, copie WebM de `build/<outputDir>/` vers `<videoDestinationDir>/<version>/`, CLI `-Poutput.*` overrides config mergée, économie d'encre skip si destination existe et non-vide, log `CAPSULE DISTRIB → …`) + wiring `CapsuleManager.registerDistributeCapsuleVideoTask()`, 3 functional tests (registration, copy versionné, skip idempotent). 568 unit VERT (+28 net : +9 `CapsuleOutputDirResolverTest` +10 `CapsuleConfigMergerTest` OutputConfig +10 `VideoDestinationResolverTest` -1 debug runner supprimé) + 37 functional VERT (+3 `DistributeCapsuleVideoFunctionalTest`). `./gradlew test` BUILD SUCCESSFUL (2m7s). Zéro régression, zéro secret. | 9/10
| 086 | 2026-08-09 | feat+ddd+tdd | **CAP-ARCH-3 Koog pipeline génération contenu (domaine `capsule.pipeline`)** — `CapsulePipelineGraph` (koog 1.0.0 consommé direct, pattern `DeckPipelineGraph` slider SLD-8.3) : `strategy<CapsuleState, CapsuleState>` 3 nœuds propose-context → validate-context → generate-speaker-notes + edges conditionnels, `execute()` séquentiel (pattern `codebase.KoogAugmentedContextGraph`), `asMermaidDiagram()`. Domaine pur (9 fichiers) : `CapsuleState` (stage machine + invariants croisés fail-fast), `CapsulePromptBuilder`/`DefaultCapsulePromptBuilder` (prompts pédagogiques respectant contrat `capsule.feed` `[NOTE.speaker]`), `CapsuleLlm`/`ChatModelCapsuleLlm` (adapter langchain4j), `ContentPlanValidator` (8 checks ordonnés), `TtsScriptDeriver`. Tâche `generateCapsuleContent` (groupe `generate`) : deck `-Pdeck.file` ou premier `.adoc` de `slides/misc` (`CapsuleAdocDir`), `-Pdeck.language` défaut `fr`, contexte augmenté `build/capsule/augmented-context.txt` `@Optional` (convention résolue à null si absent), `@OutputFile` déclarés → économie d'encre UP-TO-DATE, `@ServiceReference LlmBuildService` + `usesService`, mock `-Pollama.baseUrl` routé propose/generate, log `CAPSULE PIPELINE → …`. Wiring `CapsuleManager.registerGenerateCapsuleContentTask()`. Durcissement API : `check(deck.exists())`, contexte configuré inexistant fail-fast, "No deck found" convention. Baby-step TDD : 540 unit VERT (+49) + 34 functional VERT (+3). README master EN mis à jour (layout ai/context/pipeline, section "LLM content pipeline (CAP-ARCH-3)", Key libraries koog 1.0.0/codebase-plugin 0.0.5/codebase-contracts 0.1.0, totaux, EPIC status CAP-29 ✅ + CAP-ARCH). `check` BUILD SUCCESSFUL (3m39s). Zéro régression. | 9/10
| 085 | 2026-08-09 | feat+ddd+tdd+bdd | **CAP-ARCH-2 Contexte augmenté (RAG + Graphify, domaine `capsule.context`)** — `CapsuleContextBuilder` (object pur, `defaultBudget`/`toChannels`/`channelsWithBudget`/`merge`/`build`) assemble le `CompositeContext` (codebase-contracts 0.1.0, bump 0.0.2→0.1.0 + `implementation(libs.codebase.contracts)` — non transitif en compile, absent BOM 0.0.16) : canaux EAGER→RAG→GRAPHIFY→DOCS→RESOURCE + `ChannelBudget` (8000, 40/30/20/10/0). `CapsuleContext` (data class, invariant fail-fast `rendered.isBlank() == channels.isEmpty()`, `isEmpty`/`nonEmptyCount`/`tokenEstimate`). Tâche `collectCapsuleAugmentedContext` (groupe `collect`) : eagerFiles `.agents/INDEX.adoc` + `PROMPT_REPRISE.adoc` + `AGENT.adoc` (`@PathSensitive(RELATIVE)` — fixtures chemin absolu functional), `-Pcontext.{ragContent,graphifyContent,docsContent,tokenBudget=8000}` → `build/capsule/augmented-context.txt` + log `CAPSULE CONTEXT → …`, no-op si contexte vide. Wiring `CapsuleManager.registerCollectAugmentedContextTask()`. Cucumber `capsule_context.feature` `@context @rag` : runner dédié `CapsuleContextCucumberRunner` (contourne bug S-082 `@ConfigurationParameter(FEATURES_PROPERTY_NAME)` écrase system property) + `cucumberConventions.additionalTasks` `CucumberTaskSpec("cucumberTestContext")` (`import build.CucumberTaskSpec`). Baby-step TDD/BDD 3 niveaux : 491 unit VERT (+14 `CapsuleContextBuilderTest`) → 31 functional VERT (+3 `CapsuleAugmentedContextFunctionalTest` : registration, artefact EAGER+RAG+Graphify, UP-TO-DATE économie d'encre) → cucumber 4/4 GREEN (~31s). `check` BUILD SUCCESSFUL (3m33s : test+functionalTest+kover+validatePlugins, cucumber SKIPPED — CR-10). Zéro régression, zéro secret. | 9/10
| 084 | 2026-08-09 | feat+ddd+tdd+bdd | **CAP-ARCH-1 Socle LLM bridge (pattern SLD-8)** — codebase plugin `implementation` 0.0.5 + koog-agents 1.0.0 (bump 0.8.0 mort, `exclude(org.jetbrains:annotations)` + buildscript `force annotations:26.0.2-1` pattern slider) + langchain4j / langchain4j-ollama (BOM 0.0.16). Domaine `capsule.ai` : `CapsuleLlmService` (object pur — `registerLlmBuildService(project)` sharedServices + convention model, `resolveModel(provider, serviceProvider): ChatModel` via `LlmProviderChatModelAdapter`, `aiProvider(project)` `-Pai.provider` default ollama). Tâche `capsuleAiSmokeTest` (groupe `generate`, `usesService`, prompt minimal + log `CAPSULE AI → …`, mock `-Pollama.baseUrl`) câblée dans `CapsuleManager`. Baby-step TDD/BDD 3 niveaux : 477 unit VERT (+10), 28 functional VERT (+3), cucumber `capsule_ai.feature` `@ai` 3/3 GREEN (mock HTTP, registration + smoke + fail-fast unreachable), `./gradlew check` VERT. 1 FAIL `@integration` burn-in préexistant (hors CAP-ARCH, connu S-069/S-072). | 9/10
| 083 | 2026-08-09 | cadrage | **CAP-ARCH-0 Cadrage EPIC CAP-ARCH refondu (LLM-driven via codebase)** — Audit fondé sur le code réel, pas l'hypothèse S-073 : zéro `Ollama`/`ChatModel`/`langchain4j`/`LLM` dans `src/main` (rien à purger), `koog-agents` 0.8.0 dépendance morte (0 usage, à réactiver 1.0.0), `compileOnly slider` 0 import de classe (seul `dependsOn("translateDeck")` string-based), `i18n-contracts` 0.0.2 déjà consommé (CAP-29.1 MultiLanguageResolver/TtsManager/VoiceMapping). Inventaire contrats N0 : `codebase-contracts` 0.1.0 (CompositeContext EAGER+RAG+Graphify+Docs, ContextChannel, ChannelBudget), pipeline-contracts 0.0.2, agent-contracts 0.1.0. Pattern de référence inventorié : slider `AssistantManager` SLD-8 (`registerLlmBuildService` → `LlmBuildService` codebase → `LlmProvider` → `LlmProviderChatModelAdapter` → `ChatModel` langchain4j). Vision refondue : capsule LLM-driven piloté par codebase (koog orchestration + langchain4j exécution + RAG pgvector + Graphify contexte augmenté), backend d'exécution existant recyclé intact (extractSpeakerNotes, TTS, capture, subtitles, burn-in, multi-lang CAP-29). EPIC redécoupé : 7 US (CAP-ARCH-0 ✅, CAP-ARCH-1 socle LLM bridge → CAP-ARCH-6 cucumber boundary cross-borough, ~21 pts). Décisions : codebase plugin `implementation` (comme slider), i18n reste via `slider.translateDeck` (déjà codebase-LLM SLD-8), génération de contenu enrichi = graphe koog `CapsulePipelineGraph` + CompositeContext + prompts pédagogiques (domaines `capsule.ai`/`capsule.context`/`capsule.pipeline`). Zéro code modifié, `compileKotlin` VERT. MàJ BACKLOG/INDEX/PROMPT_REPRISE/TODO. | 9/10
| 082 | 2026-08-08 | doc+dogfooding+fix+tdd | **CAP-29.6 Documentation + dogfooding + clôture EPIC CAP-29 TERMINÉ 7/7 US** — README master EN + README plugin mis à jour (domaine `capsule.multilang`, diagramme pipeline, table des tâches `extractSpeakerNotes`/`translateAndExtractSpeakerNotes`/`generateCapsuleVideoAllLanguages`/`translateAndGenerateCapsuleVideos`, convention nommage `<deckName>_<lang>-deck.html|script.txt|webm`). Dogfooding réel consommateur composite `includeBuild` : `extractSpeakerNotes` réel → `generateCapsuleVideoAllLanguages` réel (Piper fr/en + espeak, chromium Playwright) → 3 WebM (fr/en/ar dont 1 RTL) en 3m07. Défaut nommage découvert : rendu `<deckName>_<lang>-deck.webm` (dérivé header script) au lieu du plan `<deckName>_<lang>.webm`, brisant l'économie d'encre (skip jamais déclenché). Fix TDD baby-step `CapsuleVideoOutputRenamer` (domaine `capsule.multilang`, 4 unit RED→GREEN) aligne le WebM rendu sur `entry.outputVideo` (contrat plan), wiring post-capture dans `renderEntry`. Re-dogfooding : alignement `-deck.webm` → `_<lang>.webm` (3 langues) + économie d'encre prouvée (`0/3 rendered, 3 skipped: existing valid WebM`). 464 unit VERT (+4 net) + 25 functional VERT + 6 cucumber multilang GREEN (1 FAIL `@integration` burn-in préexistant hors CAP-29). EPIC CAP-29 TERMINÉ 7/7 US, domaine `capsule.multilang` clos (7 fichiers prod). | 9/10
| 081 | 2026-08-08 | bdd+cucumber | **CAP-29.5 Cucumber BDD E2E 10 langues** — `multi_language_capsule.feature` (6 scénarios `@multilang @i18n`) + `MultiLanguageCapsuleSteps` : plan pur `CapsuleVideoPlanner` 10 langues (LanguageCatalog), convention nommage `<deckName>_<lang>.webm`, skip langue sans paire deck+script, fail-fast plan vide, E2E GradleRunner NoOp (ttsEngine=noop, captureEnabled=false) → 10 WebM + log `CAPSULE MULTILANG → n/n languages rendered`, E2E RTL ar/ur. `cucumberTest -PrunCucumber` : 6/6 multilang GREEN (suite 154 tests, 1 FAIL préexistant `@integration` burn-in subtitle path — hors périmètre). 460 unit VERT (inchangé) + 25 functional VERT + kover VERT. `check` BUILD SUCCESSFUL. | 9/10
| 080 | 2026-08-08 | feat+wiring+tdd | **CAP-29.4 Câblage translateAndGenerateCapsuleVideos** — Tâche composite `translateAndGenerateCapsuleVideos` : dependsOn `translateAndExtractSpeakerNotes` + `generateCapsuleVideoAllLanguages`. Constantes `TRANSLATE_AND_GENERATE_CAPSULE_VIDEOS` + description (domaine `capsule.feed`), `registerTranslateAndGenerateCapsuleVideos` (group capsule), câblage dans `CapsuleManager`. 3 functional RED→GREEN (registration, description, dependsOn chain `--dry-run` avec stub translateDeck). 460 unit VERT (inchangé) + 25 functional (+3), cucumber SKIPPED. | 9/10
| 079 | 2026-08-08 | feat+ddd+tdd+functional | **CAP-29.3 GenerateCapsuleVideoAllLanguagesTask** — Domaine `capsule.multilang` : `CapsuleVideoAllLanguagesRunner` (object pur, `run(plan, probeDuration, render): List<CapsuleVideoEntry>` itère le plan + `shouldRender(entry, probeDuration)` = skip si WebM existe ET probe > 0, callback render injecté). Tâche `GenerateCapsuleVideoAllLanguagesTask : CapsuleVideoTask` (réutilise `synthesizeTtsForScript` + `captureDeckSequential/Parallel` + `injectAudio` + `computeSlideDurations` CR-1 ; résolution TTS par langue via `MultiLanguageResolver` : set `ttsLanguage`/`ttsVoice`/`espeakVoice`, reset `ttsEngine=null`). Refactor base `CapsuleVideoTask` zéro changement : `execute()`→`open`, `resolveTtsEngine()`/`injectAudio()`→`internal`. Enregistrement `generateCapsuleVideoAllLanguages` (CapsuleManager). 6 unit `CapsuleVideoAllLanguagesRunnerTest` (RED→GREEN, _correction : LanguageCatalog n'a pas "de_", 10 langues) + 1 functional `GenerateCapsuleVideoAllLanguagesFunctionalTest` (3 langues fr/en/ar NoOp → 3 WebM + log `CAPSULE MULTILANG`). 460 unit VERT (+6 net), functional PASS, cucumber SKIPPED. | 9/10
| 078 | 2026-08-08 | feat+ddd+tdd | **CAP-29.2 CapsuleVideoPlan + CapsuleVideoPlanner** — Domaine `capsule.multilang` enrichi. `CapsuleVideoEntry` (data class immutable 4 champs deckFile/scriptFile/language/outputVideo, invariants fail-fast : deck exists, script exists, language in LanguageCatalog). `CapsuleVideoPlan` (data class immutable, invariant non-empty, size/isEmpty/languages). `CapsuleVideoPlanner` (object pur : `plan(deckDir, scriptDir, outputDir, targetLanguages)`, skip langues sans deck+script, fail-fast plan vide). Nommage aligné slider : `<deckName>_<lang>-deck.html` + `<deckName>_<lang>-script.txt` → `<deckName>_<lang>.webm`. 454 unit VERT (+18 net : +10 `CapsuleVideoPlanTest` + 8 `CapsuleVideoPlannerTest`). `check` BUILD SUCCESSFUL. | 9/10
| 077 | 2026-08-07 | feat+ddd+tdd | **CAP-29.1 MultiLanguageResolver + VoiceMapping 10 langues** — Domaine `capsule.multilang` créé (7ème). `MultiLanguageResolver` (object pur) consomme `LanguageCatalog` (BOM `i18n-contracts` 0.0.2) + `VoiceMapping` étendu à 10 langues (fr/en/es/de/zh/hi/ar/bn/pt/ru/ur, 11 Piper + 11 espeak). `Language` enum supprimé (4 entrées). `ResolvedLanguage` data class (SupportedLanguage + piperModel + espeakVoice). `TtsEngine.language(): SupportedLanguage?`. `CapsuleVideoTask.kt:197` + `CapsuleBuildTask.kt:40` migrés (`Language.fromCode` → `MultiLanguageResolver.resolve`). BOM `workspace-bom:0.0.16` + `i18n-contracts:0.0.2` ajoutés. 436 unit VERT (+15 net : +22 `MultiLanguageResolverTest`, +2 `LanguageVoiceMappingTest` adaptés, -9 anciens tests Language). `check` BUILD SUCCESSFUL. | 9/10
| 076 | 2026-08-07 | cadrage | **CAP-29.0 Cadrage EPIC pipeline multi-langue** — Audit code existant (`Language` enum 4 entrées, `VoiceMapping` 4 Piper+4 espeak, `generateCapsuleVideo` mono-langue, `translateAndExtractSpeakerNotes` SLD-11.4, `slider.translateDeck` 10 langues via `LanguageCatalog`). Décision clé : consommer `LanguageCatalog` (10 langues, contrats N0) au lieu d'étendre enum local — évite dette technique détruite dans CAP-ARCH-1. Domaine cible `capsule.multilang` (7ème). 7 US (CAP-29.0→6), ~20 pts. Backlog/INDEX/PROMPT_REPRISE/AGENTS racine mis à jour. Zéro code modifié. | 9/10
| 075 | 2026-08-07 | refactor+tdd+bdd | **CR-10 CI optimization cucumberTest onlyIf** — Domaine `capsule.ci.CucumberTestGuard` (data class pure, 4 méthodes) + 7 unit tests `CucumberTestGuardTest`. Câblage `onlyIf { shouldRun }` dans `build.gradle.kts` (logique inlinée — script ne peut pas importer classpath `main`). BDD `cucumber_skip.feature` (4 scénarios `@ci`) + `CucumberSkipSteps` (5 step defs). `./gradlew check` VERT (cucumberTest SKIPPED sans flag). 421 unit VERT (+7). Bug préexistant découvert : `CucumberTestRunner` ne lance que `capsule.feature` (à investiguer). EPIC CR TERMINÉ (10/10). | 9/10
| 074 | 2026-08-07 | refactor+tdd+bdd | **CR-9 Robust HTML parsing** — `HtmlSectionParser.findTopLevelSectionOpenTags` (38 lignes, depth-tracking existant réutilisé). `injectAudioSequentialFallback` : 2 lignes regex raw supprimées → 1 ligne `HtmlSectionParser.findTopLevelSectionOpenTags`. DRY (DRY-2 S-031). Bug fix : vertical stacks (sections imbriquées) n'étaient pas skippées → `data-audio` injecté sur sous-sections. +8 unit `HtmlSectionParserTest` (flat, nested, self-closing, attributes, empty, malformed, positions, multiline) + 1 unit `CapsuleVideoTaskTest` (data-audio count = 2, pas 4) + 1 scénario cucumber `@audio @sequential` + 2 step defs. 414 unit VERT (+9). | 9/10
| 073 | 2026-08-07 | refactor+tdd+bdd+cadrage | **CR-8 Logging structuré + EPIC CAP-ARCH cadré** — `CapsulePlugin.kt:40-48` 1 ligne monolithic 13 placeholders → 4 lignes `logger.lifecycle` par section (TTS/Capture/Subtitles/Manim). `CapsuleConfigLogger` object pur extrait (domaine `capsule.config`), `formatConfigLog(config): List<String>`. Tokens préservés verbatim → Cucumber @config existants verts. +9 unit tests `CapsuleConfigLoggerTest`, +1 scénario cucumber `@config @logging` (5 steps). 405 unit VERT (+9). EPIC CAP-ARCH cadré au BACKLOG (cross-borough slider+codebase, 6 US ~19 pts). | 9/10
| 072 | 2026-08-07 | refactor+tdd+bdd | **CR-5 DRY CapsuleConfigMerger** — 10 méthodes `mergeXxxConfig`/`mergeXxxConfigNoYaml` (~76 lignes dupliquées) → 5 méthodes unifiées + 4 helpers génériques (`mergeStr/mergeInt/mergeDouble/mergeBoolean`). `merge()` dispatch sur `yaml: T?` nullable. 322→302 lignes (-19 net). Sémantique préservée : `toBoolean()` lenient, heuristic `isNotBlank()` strings, Bool/Int/Double YAML>Props>ENV. +13 unit tests NoYaml (48 total), +1 scénario cucumber `@config @no-yaml`. 396 unit VERT (+12). | 9/10
| 071 | 2026-08-06 | feat+tdd+bdd+fix | **CR-2 + Fix Cucumber + CR-3/4/6/7** — Thread safety + timeout `captureSlideParallel` (unit 4 + functional 3 + cucumber 2). Fix 5 échecs cucumber consolidated (DuplicateStepDefinitionException doublons title/speakerNote + feature quotes + temp file exact name). CR-3 error handling TTS (retry 1× + accumulation + logger.error). CR-4 HTML escape XSS `injectSubtitleTrack` (HtmlEscape object). CR-6 robust CLI parsing (cliInt/cliDouble/cliBoolean helpers). CR-7 validation YAML (déjà implémenté + test). 384 unit VERT (+23), 64 cucumber 0 FAIL. | 9/10
| 070 | 2026-08-06 | ops+cross-borough | **Sync slider 0.0.15 → 0.0.16** (cross-borough, suite slider 0.0.16 S-066 + BOM 0.0.17 S-019). Bump catalogue `capsule-plugin/gradle/libs.versions.toml` `slider = "0.0.15"` → `"0.0.16"`. `./gradlew compileKotlin` VERT avec slider 0.0.16 (mavenLocal). Zéro code production modifié. Session pure sync consommateur. | 9/10
| 069 | 2026-08-05 | feat+ddd+tdd+bdd | **EPIC CAP-MODEL TERMINÉ** — unification modèle `capsule.feed` ↔ `capsule.CapsuleScript` (7/7 US : cadrage + 6 exécution). `SlideType` enum migré vers `capsule.feed`, `SlideSegment` étendu (type+manimScene), `CapsuleScriptReader` remplace `CapsuleManager.parseScript` (64 lignes supprimées), 6 consommateurs migrés, `capsule.CapsuleModels` purgé (SlideSegment/CapsuleScript/SlideType supprimés, reste CapsuleExtension/CapsuleConventions). 361 unit tests VERT (+124). Cucumber boundary `consolidated_capsule_model.feature` (5 scénarios). Zéro doublon modèle. | 9/10
| 068 | 2026-06-29 | migration | **CNV-6 Migration conventions plugins** — `build.gradle.kts` migré vers 4 conventions plugins (gradle-plugin + publishing + functional-test + cucumber 0.0.1). ~70 lignes supprimées. `compileKotlin` ✅. Régularisation migration déjà faite (conventions S005 dogfood legacy) mais non committée. | 9/10
| 067 | 2026-06-24 | refactor README | Architecture README Plugin + Consommateur — déplacement README.plugin → capsule-plugin/, README.consommateurs → racine, liens i18n recréés (10 langues) | 8/10
| 041 | 2026-06-24 | documentation | README i18n — 20 fichiers README (10 langues × consumer + plugin) dans `README.consommateurs/` et `README.plugin/`. Badges shields.io (Maven Central 0.0.1-SNAPSHOT orange, Plugin Portal, CI, License). Marqueurs : 195 unit + 22 Cucumber PASS. Switcher i18n 10 langues. AR/UR RTL. Non publié. | 8/10
| 040 | 2026-06-16 | refactor | CR-1 Refactor CapsuleVideoTask.execute() — 5 méthodes extraites (synthesizeTtsForScript, renderManimSlides, replaceManimSlidesInDeck, captureDeckSequential, captureDeckParallel), execute() 218→76 lignes orchestration, 9 nouveaux tests unitaires, compile OK, unit tests PASS | 9/10
| 039 | 2026-06-16 | governance | Analyse CODE_REVIEW.adoc — 15 problèmes → 10 EPICs CR (CR-1→CR-10) + 3 EPICs CAP (CAP-26→CAP-28). Enrichissement BACKLOG, création TODO.adoc, MàJ INDEX. | 9/10
| 038 | 2026-06-16 | dev | CAP-25 Pipeline E2E burn-in — Cucumber @subtitles @burnin @integration (deck → TTS → capture → mix → burn-in → WebM final), 1 scénario + 2 step defs (Given real ffmpeg, Then burn-in completed), compile OK, unit tests PASS | 9/10
| 037 | 2026-06-16 | dev | CAP-24 Burn-in sous-titres — SubtitleBurnInService interface + Impl (FFmpeg subtitles filter) + NoOp, DSL subtitleBurnIn 4 sources, wiring CapsuleVideoTask post-mixAudioWithVideo, 10 tests unitaires, 3 Cucumber @subtitles @burnin | 9/10
| 036 | 2026-06-16 | dev | CAP-23 Sous-titres SRT/VTT — SubtitleFormat enum + SubtitleEntry + SubtitleGenerator, SrtGenerator + VttGenerator + SubtitleTimingCalculator, DSL subtitleEnabled/subtitleFormat 4 sources, wiring CapsuleVideoTask, 6 Cucumber @subtitles, 43/43 PASSED | 9/10
| 035 | 2026-06-16 | dev | CAP-22 TTS multi-langue — Language enum + VoiceMapping (FR/EN/ES/DE), PiperTtsEngine + EspeakTtsEngine language-aware, 6 Cucumber @tts, 37/37 PASSED | 9/10
| 034 | 2026-06-16 | diagnostic+bugfix | Bug-fix PlaywrightCaptureImpl.close() avant check vidéo (6 failures Cucumber → 0). Kover: 83.3% line, 51.8% branch. Décision CAP-22 TTS multi-langue. 31/44 Cucumber PASSED. | 9/10
| 033 | 2026-06-15 | bugfix+ci | Bug-fix DSL>merge (CapsuleConventions snapshot + pushConfigIntoExtension guard) + NoOp WebM EBML header (MINIMAL_WEBM ByteArray) + CI Template .github/workflows/test.yml. 211 unit PASS. | 8/10
| 032 | 2026-06-15 | refactor | P1 println→logger + P2 isPlaceholder + P3 lateinit→lazy/concat-cleanup/validate-simplify | 9/10
| 031 | 2026-06-15 | refactor | P0 DRY (AudioConversionUtil + HtmlSectionParser + MediaProbeUtil) + resource leak finally + magic number DSL | 9/10
| 030 | 2026-06-13 | maintenance | Mise à jour BACKLOG (publication + priorités P0→P3) | 8/10
| 029 | 2026-06-13 | publish | Publication Maven Central 0.0.1 — nmcp, 195 unit + 22 Cucumber = 217 PASS | 9/10
| 028 | 2026-06-02 | dev | CAP-20 Manim Parallel Render — ManimParallelRenderer + NoOp + Impl + factory + CapsuleVideoTask refactor + parallelRender DSL 4 sources + 22 tests (195 unit GREEN) | 9/10
| 027 | 2026-06-02 | dev | Cucumber @config @manim ManimConfig — 5 scénarios BDD + 8 step defs + lifecycle log + scaffold outputDir (173 unit = 5 Cucumber PASSED) | 9/10
| 026 | 2026-06-02 | dev | CAP-19 ManimEngine configurable — ManimConfig outputDir + VALID_QUALITIES + validate() + 4 sources merger + 19 tests GREEN → 173 total | 9/10
| 025 | 2026-06-02 | dev | Cucumber @integration @manim E2E complet — NoOp render→mux→replace→deck validé (154 unit GREEN) | 8/10
| 024 | 2026-06-02 | dev | CAP-18 ManimSlideReplacer + probeDuration — ManimSlideReplacer interface/Impl/NoOp + computeSlideDurationsWithManim + wiring CapsuleVideoTask (154 unit GREEN, 21 nouveaux) | 9/10
| 023 | 2026-06-02 | dev | ManimVideoMixer wiring + mux MP4+TTS (133 unit + 16 nouveaux + 2 Cucumber @manim) | 8/10
| 022 | 2026-06-02 | dev | Tag @integration Cucumber + ManimEngine wiring + resolveManimEngine factory (117 unit GREEN) | 8/10
| 021 | 2026-06-02 | dev | Diagnostic Cucumber : 3 failures = environnementaux (espeak/ffmpeg/chromium), @config GREEN | 5/10
| 020 | 2026-06-02 | dev | CapsuleConfigMerger + scaffoldCapsuleContext + afterEvaluate wiring (110 unit + 8 func + 9 cucumber GREEN, 3 @config FAILED) | 7/10
| 019 | 2026-06-02 | dev | createSingleSlideHtml + parallelCaptureEnabled DSL + CapsuleConfig + CapsuleConfigLoader (78 unit + 8 func + 9 cucumber = 95 tests) | 8/10
| 018 | 2026-06-02 | dev | ManimEngine.kt + captureSlideParallel + split CapsuleManager (56 unit + 8 func + 9 cucumber = 73 tests) | 9/10
| 017 | 2026-06-02 | dev | Intégration sélective fork lion92 (5/5+2) | 9/10
| 016 | 2026-06-02 | dev | Intégration sélective fork lion92 (2/5) | 7/10
| 015 | 2026-06-02 | audit | Revert merge fork lion92 + Cleanup | 8/10
| 014 | 2026-06-01 | audit | Audit fork lion92 | 10/10
| 013 | 2026-06-01 | rename | Renommage borough "Brooklyn Tech" → "Détroit" | 10/10
| 012 | 2026-05-19 | doc | EPIC K-5 | ✅
| 011 | 2026-05-19 | migration | EPIC K-1 — Renommage taxonomie | 10/10
| 010 | 2026-05-18 | migration | CCCP — namespace rename | 10/10
| 009 | 2026-05-17 | feat+fix | CAP-9 Pipeline E2E + TTS espeak | 10/10
| 008 | 2026-05-17 | audit | Diagnostic pipeline | 8/10
| 007 | 2026-05-16 | dev | CapsuleParseContextTask + Jackson | 10/10
| 006 | 2026-05-16 | dev | CAP-6 N3 + CAP-7 capsuleDistrib | 10/10
| 005 | 2026-05-16 | dev | CAP-4 CapsuleVideo + CAP-5 Cucumber BDD | 10/10
| 004 | 2026-05-15 | dev | CAP-3 Playwright Java capture | 10/10
| 003 | 2026-05-15 | dev | CAP-2 TTS Piper | 10/10
| 002 | 2026-05-15 | dev | CAP-1 Plugin Gradle capsule | 10/10
| 001 | 2026-05-15 | build | CAP-0.3 Initialisation build | ✅
| 000 | 2026-05-14 | bootstrap | Gouvernance + cadrage architecture | ✅
|===

'''
== Roadmap — Etat Actuel

|===
| EPIC | Description | Statut | Priorite
| DRY-1 | ffprobeDuration dupliqué ×3 → utilitaire partagé | ✅ TERMINE (session 031) | P0
| DRY-2 | extractTopLevelSections dupliqué ×2 → utilitaire partagé | ✅ TERMINE (session 031) | P0
| DRY-3 | wavToMp3 dupliqué ×2 → helper partagé | ✅ TERMINE (session 031) | P0
| println→logger | PlaywrightCaptureImpl l.75 + CapsuleConfigLoader l.37 | ✅ TERMINE (session 032) | P1
| resource-leak | Playwright close() pas dans finally | ✅ TERMINE (session 031) | P1
| isPlaceholder | Code mort à supprimer ou réutiliser | ✅ TERMINE (session 032) | P2
| parallelism | Magic number 4 configurable | ✅ TERMINE (session 031) | P2
| lateinit→lazy | Sécuriser les extensions de tâches | ✅ TERMINE (session 032) | P3
| concat-cleanup | try/finally sur fichier temporaire | ✅ TERMINE (session 032) | P3
| validate-simplify | ManimConfig.validate() → when explicite | ✅ TERMINE (session 032) | P3
| CAP-13 | Config Multi-Source (4 sources) | ✅ TERMINE | P0
| CAP-17 | ManimVideoMixer (mux MP4 + TTS audio) | ✅ TERMINE (session 023) | P1
| CAP-18 | ManimSlideReplacer + probeDuration (remplacement HTML→video, computeSlideDurationsWithManim) | ✅ TERMINE (session 024) | P1
| CAP-19 | ManimEngine configurable (ManimConfig outputDir, VALID_QUALITIES, validate, 4 sources) | ✅ TERMINE (session 026) | P1
| CAP-20 | Manim Parallel Render (ManimParallelRenderer + thread pool + parallelRender config) | ✅ TERMINE (session 028) | P1
| CAP-21 | Parallel capture HTML slides (parallelCaptureEnabled DSL, captureSlideParallel) | ✅ TERMINE (session 019) | P1
| Publication Maven Central 0.0.1 | nmcp, 195 unit + 22 Cucumber = 217 PASS | ✅ 2026-06-13 | P2
| K-1 | Renommage taxonomie (4 verbes) | ✅ TERMINE | P0
| CAP-0 | Bootstrap gouvernance agent | ✅ TERMINE | P0
| CAP-1 | Plugin Gradle capsule | ✅ TERMINE | P0
| CAP-2 | TTS integration | ✅ TERMINE | P1
| CAP-3 | Playwright Java capture | ✅ TERMINE | P1
| CAP-4 | CapsuleVideo task | ✅ TERMINE | P1
| CAP-5 | Tests Cucumber BDD | ✅ TERMINE | P2
| CAP-6 | Integrations N3 | ✅ TERMINE | P3
| CAP-7 | capsuleDistrib | ✅ TERMINE | P3
| CAP-8 | CapsuleParseContext | ✅ TERMINE | P3
| CAP-9 | Fix Pipeline E2E | ✅ TERMINE | P0
| CAP-10 | ManimEngine | ✅ TERMINE | P1
| CAP-11 | Parallel capture | ✅ TERMINE | P1
| CAP-12 | Split CapsuleManager | ✅ TERMINE | P2
| CAP-14 | createSingleSlideHtml | ✅ TERMINE (session 019) | P1
| CAP-15 | parallelCaptureEnabled DSL | ✅ TERMINE (session 019) | P1
| CAP-22 | TTS multi-langue — Language enum + VoiceMapping (FR/EN/ES/DE), PiperTtsEngine + EspeakTtsEngine language-aware, 6 Cucumber @tts | ✅ TERMINE (session 035) | P1
| CAP-23 | Sous-titres SRT/VTT — SubtitleFormat enum + SubtitleEntry + SubtitleGenerator, SrtGenerator + VttGenerator + SubtitleTimingCalculator, DSL subtitleEnabled/subtitleFormat 4 sources, wiring CapsuleVideoTask, 6 Cucumber @subtitles | ✅ TERMINE (session 036) | P1
| CAP-24 | Burn-in sous-titres — SubtitleBurnInService interface + Impl (FFmpeg subtitles filter) + NoOp, DSL subtitleBurnIn 4 sources, wiring CapsuleVideoTask post-mixAudioWithVideo, 10 tests unitaires, 3 Cucumber @subtitles @burnin | ✅ TERMINE (session 037) | P1
| CAP-25 | Pipeline E2E avec burn-in — Cucumber @subtitles @burnin @integration (deck → TTS → capture → mix → burn-in → WebM final), 1 scénario + 2 step defs | ✅ TERMINE (session 038) | P1
| CAP-MODEL | Unification modèle capsule.feed ↔ capsule.CapsuleScript — étendre feed.SlideSegment (type+manimScene), CapsuleScriptReader remplace parseScript, suppression CapsuleModels, 7 US ~14 pts | ✅ TERMINÉ (S-069, 361 unit tests VERT) | P0
| CAP-29 | Pipeline Traduction Vidéo Multi-Langue — domaine `capsule.multilang`, `MultiLanguageResolver` + `VoiceMapping` 10 langues (LanguageCatalog), `CapsuleVideoPlan` + `CapsuleVideoPlanner`, `generateCapsuleVideoAllLanguages`, `translateAndGenerateCapsuleVideos` (composite), Cucumber E2E 10 langues, dogfooding réel 3 WebM + fix nommage `CapsuleVideoOutputRenamer`. 7 US ~20 pts | ✅ TERMINÉ (S-082, 7/7 US, 464 unit + 25 functional + 6 cucumber multilang) | P0
| CAP-ARCH | Capsule LLM-driven : génération de contenu + i18n pilotés par codebase (koog + langchain4j + RAG + graphify) — socle LLM bridge SLD-8 (`capsule.ai`), contexte augmenté CompositeContext (`capsule.context`), graphe koog `CapsulePipelineGraph` (`capsule.pipeline`), wiring backend existant (`generateCapsuleContentAndVideos` composite), i18n via translateDeck, cucumber cross-borough, video destination versioning (`capsule.support` + `OutputConfig` + `VideoDestinationResolver` + `DistributeCapsuleVideoTask`), i18n messages socle (`capsule.i18n` + `CapsuleMessages` + 11 `Messages*.properties` 10 langues). 8 US ~25 pts | ✅ TERMINÉ (CAP-ARCH-0 ✅ S-083, CAP-ARCH-1 ✅ S-084, CAP-ARCH-2 ✅ S-085, CAP-ARCH-3 ✅ S-086, CAP-ARCH-4 ✅ S-088, CAP-ARCH-5 ✅ S-089, CAP-ARCH-6 ✅ S-090, CAP-ARCH-7 ✅ S-087+S-088) | P0
| CAP-CR3-2 | Mode strict anti-NoOp — `capsule.strictMode` DSL (4 sources), `StrictModeGuard` (object pur), 6 `resolve*` branchés, `StrictModeConfig`, cucumber `@strict @config`. 4 US ~10 pts | ✅ TERMINÉ (S-091, 674 unit + 44 functional + 8 cucumber dédiés) | P0
| CAP-DOCCONTEXT | Injection corpus documentaire — `context.docsGlobs` 4 sources, `DocContextLoader` résout globs (`docs/afnor/**/*.adoc`), canal Docs enrichi. 5 US ~12 pts | ✅ TERMINÉ (S-093, 694 unit + 47 functional + 4 cucumber) | P0
| CAP-SPD | Consommation métadonnées SPD (renommé PedagogicalScenario) — `PedagogicalScenarioLoader` parse metadata.json K-2 (enveloppe provenance, valide `type=="SPD"` uniquement — capsule public OSS format-agnostic, pas de couplage borough producteur) + AsciiDoc (payload pédagogique : objectifs/durée/prérequis/modalités). N0 ContextChannel sealed → extension capsule-local (`CapsuleContext.scenarioSection`). `CapsuleContextBuilder.build(composite, budget, scenarioSection)` étendu. `DefaultCapsulePromptBuilder` instruction line scenario. Wiring `collectCapsuleAugmentedContext -Pcapsule.context.scenarioFile` (4 sources). Scenario absent → vidéo non bloquée (fallback no-op). 5 US ~10 pts | ✅ TERMINÉ (S-094 US-0/1/2, S-095 US-3/4 + renommage, 713 unit + 49 functional + 3 cucumber) | P0
| CAP-CR3-3 | Branchement ScreenshotCapture — `capture.strategy: playwright|screenshot` 4 sources, `CaptureStrategy` enum, `ScreenshotCaptureImpl` étendu multi-slide, `resolveCapture` factory + StrictModeGuard, cucumber `@capture @strategy`. 5 US ~13 pts | ✅ TERMINÉ (S-096, 742 unit + 52 functional + 3 cucumber) | P0
| CAP-MP4 | Export MP4/H.264 — `output.format: webm\|mp4\|both` 4 sources, `OutputFormat` enum, `VideoFormatConverter` transcode FFmpeg, `DistributeCapsuleVideoTask` étendu. 4 US ~5 pts | ✅ TERMINÉ (S-097 US-0 cadrage, S-098 US-1 enum+merger, S-099 US-2 converter+wiring, S-100 US-3 cucumber + clôture) | P1
| CAP-AUDIO | Post-production audio — BGM + loudnorm EBU R128 + sidechain ducking, `AudioPostProcessor`, domaine `capsule.audio`. 5 US ~10 pts | ✅ TERMINÉ (S-102 US-0 cadrage, S-103 US-1 config+merger, S-104 US-2 processor+factory, S-105 US-3 wiring, S-106 US-4 cucumber + clôture) | P1
| CAP-GLOSSARY | Injection terminologique — `context.glossaryFile` 4 sources, extension capsule-local `glossarySection` (pattern CAP-SPD-2), prompt "Use official terminology". 4 US ~6 pts | ✅ TERMINÉ (S-109 US-0 cadrage, S-110 US-1 data classes+loader+builder+prompt, S-111 US-2 wiring+functional, S-112 US-3 cucumber + clôture) | P1
| CAP-PROVENANCE | Audit context — `ProvenanceTracker` accumulate canaux → `context-provenance.json`, anti-hallucination. 3 US ~5 pts | ✅ TERMINÉ (S-107 cadrage, S-108 clôture — 3/3 US) | P1
| CAP-CHAPITRE | Intro/outro + chapter markers — `CardRenderer` PNG, `ChapterMarkers` Matroska tags depuis `SlideSegment.titles`, domaines `capsule.cards`/`capsule.chapters`. 5 US ~10 pts | ⬜ TODO | P2
| CAP-TRANSCRIPT | Reverse pipeline capsule → article AsciiDoc — `TranscriptConfig` + `TranscriptStrategy` enum (TEMPLATE/LLM) + `TranscriptPlan` + `TranscriptBuilder` object pur + `TranscriptPromptBuilder` + `TranscriptLlmEnhancer` port/adapter + tâche `generateCapsuleTranscript` (dependsOn `generateCapsuleContent`), domaine `capsule.transcript` (11ème), cross-borough document/bakery. 6 US ~12 pts | ✅ TERMINÉ (S-114 US-0, S-115 US-1, S-116 US-2, S-117 US-3, S-118 US-4, S-119 US-5 — 6/6 US) | P2
| CAP-PODCAST | Extraction audio-only — `PodcastConcatenator` FFmpeg concat MP3 slides, tâche `generateCapsulePodcast`. 3 US ~4 pts | ⬜ TODO | P2
| CAP-PREVIEW | Mode dry-run — `capture.previewOnly` 4 sources, screenshot path s'arrête après PNG. 3 US ~4 pts | ⬜ TODO | P2
|===

'''
== EPIC CR — Code Review Globale (CODE_REVIEW.adoc — 10 EPICs actionnables)

_Source_ : `CODE_REVIEW.adoc` (analyse 2026-06-16, Session 039). 15 problèmes → 10 EPICs.

[cols="1,3,2,2", options="header"]
|===
| EPIC | Description | Sévérité | Statut
| CR-1 | Refactor CapsuleVideoTask.execute() — 218 lignes → 5-6 méthodes privées (<80 lignes, max 3 niveaux) | 🔴 BLOQUANT | ✅ TERMINE (session 040)
| CR-2 | Thread safety + timeout captureSlideParallel — executor.shutdownNow() dans finally, it.get(5, MINUTES) | 🔴 BLOQUANT | ✅ TERMINE (session 071)
| CR-3 | Error handling TTS — dégradation signalée (accumuler skips, logger.error, retry 1×) | 🔴 BLOQUANT | ✅ TERMINE (session 071)
| CR-4 | Sécurité HTML escape — injectSubtitleTrack (StringEscapeUtils.escapeHtml4) | 🟠 MAJEUR | ✅ TERMINE (session 071)
| CR-5 | DRY CapsuleConfigMerger — ~76 lignes dupliquées (mergeXxxConfig / mergeXxxConfigNoYaml) | 🟠 MAJEUR | ✅ TERMINE (session 072)
| CR-6 | Robust CLI parsing — `as? Int` → `toIntOrNull()` uniforme | 🟠 MAJEUR | ✅ TERMINE (session 071)
| CR-7 | Validation YAML — CapsulePlugin.afterEvaluate try/catch gracieux | 🟠 MAJEUR | ✅ TERMINE (session 071, déjà implémenté + test)
| CR-8 | Logging structuré — CapsulePlugin l.36-44 → 4-5 logger.lifecycle par section | 🟡 MOYEN | ✅ TERMINE (session 073)
| CR-9 | Robust HTML parsing — HtmlSectionParser au lieu de regex raw dans injectAudioSequentialFallback | 🟡 MOYEN | ✅ TERMINE (session 074)
| CR-10 | CI optimization — cucumberTest onlyIf { hasProperty("runCucumber") || CI } | 🟡 MOYEN | ✅ TERMINE (session 075) |
|===

'''
== EPIC CAP — Fonctionnalités Restantes

[cols="1,3,2,2", options="header"]
|===
| EPIC | Description | Priorité | Statut
| CAP-26 | Style burn-in configurable — DSL subtitleBurnInStyle (fontSize, fontColor, outlineColor, position) + FFmpeg force_style | P1 | ✅ TERMINE (session 039)
| CAP-27 | VTT burn-in — FFmpeg subtitles filter avec VTT. Constat US-0 S-113 : compat **déjà fonctionnelle** (ffmpeg 6.1.1 auto-détecte `.vtt` par extension). 3 US : US-1 `SubtitleBurnInCommand` object pur + 13 unit, US-2 Cucumber `@subtitles @burnin @vtt` 2 scénarios (non-integration + E2E real ffmpeg). | P2 | ✅ TERMINÉ (S-113 3/3 US)
| CAP-28 | Coverage gaps — PiperTtsEngine 31%, ManimEngineImpl 25%, ScreenshotCaptureImpl 26% | P2 | ⬜ TODO
|===

'''

== ✅ EPIC CAP-ARCH — Capsule LLM-driven : génération de contenu + i18n pilotés par codebase (koog + langchain4j + RAG + graphify) (Session 083 — Cadrage refondu, Session 090 — TERMINÉ 8/8 US)

_Vision_ : La génération de capsules (speaker notes enrichies, scripts TTS, i18n)
est *pilotée par LLM via le hub codebase (N1)* : koog (orchestration graphe),
langchain4j (exécution), RAG pgvector + Graphify (contexte augmenté multi-canal),
bridgé en Gradle par `LlmBuildService` (pattern slider SLD-8). Capsule conserve
*intact* son backend d'exécution existant (extractSpeakerNotes, TTS, capture,
subtitles, burn-in, multi-lang CAP-29) — la couche LLM l'alimente, elle ne le
remplace pas. Rien du code existant n'est perdu.

_Audit CAP-ARCH-0 (S-083)_ : zéro appel LLM dans `src/main` (rien à purger —
cadrage S-073 périmé), `koog-agents` 0.8.0 mort (0 usage) à réactiver en 1.0.0,
`i18n-contracts` 0.0.2 déjà consommé (CAP-29.1), pattern slider `AssistantManager`
(LlmBuildService + LlmProviderChatModelAdapter → ChatModel) réutilisable,
`codebase-contracts` 0.1.0 expose `CompositeContext` (EAGER + RAG + Graphify + Docs).

_Socle CAP-ARCH-1 (S-084)_ : codebase plugin `implementation` 0.0.5 +
koog-agents 1.0.0 (bump 0.8.0 mort, exclude annotations + buildscript force
annotations:26.0.2-1) + langchain4j / langchain4j-ollama (BOM). Domaine
`capsule.ai` : `CapsuleLlmService` (registerLlmBuildService + resolveModel +
aiProvider `-Pai.provider` default ollama) + `LlmProviderChatModelAdapter`.
Tâche `capsuleAiSmokeTest` (groupe `generate`, usesService, mock
`-Pollama.baseUrl`). 477 unit + 28 functional + 3 cucumber `@ai` GREEN.

_Contexte CAP-ARCH-2 (S-085)_ : domaine `capsule.context` — `CapsuleContextBuilder`
(object pur) + `CapsuleContext` (data class, invariant
`rendered.isBlank() == channels.isEmpty()`). Consomme `CompositeContext`
(codebase-contracts 0.1.0, bump 0.0.2→0.1.0 + `implementation(libs.codebase.contracts)`
— non transitif en compile). Tâche `collectCapsuleAugmentedContext` (groupe
`collect`, eagerFiles `.agents/INDEX.adoc` + `PROMPT_REPRISE.adoc` + `AGENT.adoc`
`@PathSensitive(RELATIVE)`, `-Pcontext.*`, output `build/capsule/augmented-context.txt`).
491 unit + 31 functional + 4 cucumber `@context @rag` GREEN (runner dédié
`CapsuleContextCucumberRunner` — contourne bug S-082).

[cols="1,3,2,2", options="header"]
|===
| US | Description | Pts | Statut

| CAP-ARCH-0
| *Cadrage EPIC* — audit fondé sur le code, inventaire contrats N0, refonte des US sur la vision LLM-driven. MàJ INDEX/BACKLOG/PROMPT_REPRISE/TODO.
| 1
| ✅ TERMINÉ (S-083)

| CAP-ARCH-1
| *Socle LLM bridge (pattern SLD-8)* — dépendance codebase plugin `implementation` 0.0.5 + koog-agents 1.0.0 (bump 0.8.0, exclude annotations + buildscript force annotations:26.0.2-1) + langchain4j / langchain4j-ollama (BOM). Domaine `capsule.ai` : `CapsuleLlmService` (object pur — `registerLlmBuildService(project)` + `resolveModel(provider, serviceProvider): ChatModel` via `LlmProviderChatModelAdapter` + `-Pai.provider` default ollama). Purge dépendance morte koog 0.8.0. Tâche `capsuleAiSmokeTest` (groupe `generate`, `usesService`, mock `-Pollama.baseUrl`) câblée dans `CapsuleManager`. Baby-step TDD : 477 unit (+10) → 28 functional (+3) → cucumber `capsule_ai.feature` `@ai` 3/3 (mock HTTP) GREEN.
| 3
| ✅ TERMINÉ (S-084)

| CAP-ARCH-2
| *Contexte augmenté (RAG + Graphify)* — domaine `capsule.context` : consomme `CompositeContext` (codebase-contracts 0.1.0) — canaux EAGER/RAG/Graphify/Docs + `ChannelBudget`. `CapsuleContextBuilder` (object pur) + `CapsuleContext` (data class, invariant fail-fast). Tâche `collectCapsuleAugmentedContext` (groupe `collect`, eagerFiles `.agents/INDEX.adoc` + `PROMPT_REPRISE.adoc` + `AGENT.adoc`, `-Pcontext.*`, output `build/capsule/augmented-context.txt`). Baby-step TDD : 491 unit (+14 `CapsuleContextBuilderTest`) → 31 functional (+3 : registration, artefact, UP-TO-DATE) → cucumber `capsule_context.feature` `@context @rag` 4/4 GREEN (runner dédié, contourne bug S-082).
| 4
| ✅ TERMINÉ (S-085)

| CAP-ARCH-3
| *Koog pipeline génération contenu* — domaine `capsule.pipeline` (9 fichiers prod) : `CapsulePipelineGraph` (koog 1.0.0, pattern `DeckPipelineGraph`) — propose-context → validate-context → generate-speaker-notes + edges conditionnels, `execute()` séquentiel, `asMermaidDiagram()`. `CapsuleState` (invariants), `CapsulePromptBuilder`/`DefaultCapsulePromptBuilder`, `CapsuleLlm`/`ChatModelCapsuleLlm`, `ContentPlanValidator` (8 checks), `TtsScriptDeriver`. Tâche `generateCapsuleContent` : deck (`-Pdeck.file`/`slides/misc`) + contexte (`build/capsule/augmented-context.txt`) → `-speaker-notes.adoc` + `-script.txt` (sorties déclarées, économie d'encre), `@ServiceReference LlmBuildService`, mock routé. Durcissement API (deck.exists, no-deck). Baby-step TDD : 540 unit (+49) → 34 functional (+3).
| 5
| ✅ TERMINÉ (S-086)

| CAP-ARCH-4
| *Wiring exécution existante* — tâche composite `generateCapsuleContentAndVideos` (groupe `capsule`) chaîne `generateCapsuleContent` → `translateAndGenerateCapsuleVideos` (pattern `DefaultTask` + `dependsOn` par nom string). Backend existant intact. 3 functional + 3 cucumber `@architecture @wiring` GREEN.
| 2
| ✅ TERMINÉ (S-088)

| CAP-ARCH-5
| *i18n aligné sur le socle* — domaine `capsule.i18n` (8ème) : `CapsuleMessages` object (pattern `SliderMessages`/`PlantumlMessages` — `utf8Control` ResourceBundle UTF-8, `forLanguage`/`get`/`format`/`resolveLanguage(project)` cascade `-Pcapsule.language` > DSL > `"en"`). 11 `Messages*.properties` (base + en + 9 langues) — 23 keys (6 task groups + 17 task descriptions). Wiring `CapsuleManager` (13 tasks) + `CapsuleFeedTaskRegistrar` (4 tasks). Fix 3 descriptions françaises hardcoded. `CapsuleExtension.language` ajouté. `MultiLanguageResolver` KDoc corrigé. Pas de `CapsuleI18nBridge` (anti-pattern S-030), pas de `MessageBundle`/`LocaleResolver` (contrats N0 morts). Baby-step TDD : 655 unit (+87 : 65 `CapsuleMessagesTest` + 22 `MessagesFrTranslationTest`).
| 3
| ✅ TERMINÉ (S-089)

| CAP-ARCH-6
| *Cucumber BDD boundary cross-borough* — `capsule_arch_boundary.feature` (4 scénarios `@cross-borough @architecture @i18n @ai`) : registration task `generateCapsuleContentAndVideos`, content pipeline LLM (`generateCapsuleContent` mock Ollama 2 réponses alternées propose/generate → speaker notes + TTS script), video pipeline composite (NoOp → 1 WebM fr), économie d'encre skip WebM pré-existant valide (ffmpeg 1s, `ffprobe` > 0). Runner dédié `CapsuleArchBoundaryCucumberRunner` (pattern S-082). `CapsuleArchBoundarySteps` self-contained GradleRunner (fake `translateDeck`, pré-génération artefacts fr). Décision cadrage : single-language français (pas 10 langues — CAP-29.5 garde responsabilité i18n). Baby-step TDD/BDD : RED (8 failed) → GREEN (4 PASSED) → REFACTOR (`check` SUCCESSFUL). 655 unit + 40 functional inchangés + 4 cucumber dédiés GREEN.
| 3
| ✅ TERMINÉ (S-090)

| CAP-ARCH-7
| *Video Destination Versioning (post-capture distribution)* — étend `capsule-context.yml` section `output` (`OutputConfig` : `videoDestinationDir`, `versioning: timestamp|incremental`, `versionPrefix`). `VideoDestinationResolver` (object pur) résout path versionné. Tâche `distributeCapsuleVideo` (groupe `distribute`) copie WebM vers `<videoDestinationDir>/<version>/`. Économie d'encre skip idempotent. Baby-step TDD : US-1 `OutputConfig`+merger 10 unit ✅, US-2 `VideoDestinationResolver` 10 unit ✅, US-3 `DistributeCapsuleVideoTask` 3 functional ✅, US-4 Cucumber `@distrib @versioning` 5 scénarios ✅.
| 4
| ✅ TERMINÉ (S-087 US-1→3, S-088 US-4)
|===

_Total_ : 8 US, ~25 pts. _Statut_ : ✅ TERMINÉ (CAP-ARCH-0 ✅ S-083, CAP-ARCH-1 ✅ S-084,
CAP-ARCH-2 ✅ S-085, CAP-ARCH-3 ✅ S-086, CAP-ARCH-4 ✅ S-088, CAP-ARCH-5 ✅ S-089,
CAP-ARCH-6 ✅ S-090, CAP-ARCH-7 ✅ S-087+S-088). 8/8 US terminés. EPIC clos.

'''
== EPIC CAP-MODEL — Unification modèle capsule.feed ↔ capsule.CapsuleScript (Session 069 — Cadrage)

_Vision_ : L'EPIC SLD-11 (S-058) a expulsé `slider.capsule` côté capsule en créant le
domaine `capsule.feed` (`AsciidocSpeakerNoteParser`, `CapsuleFeedModels`, `CapsuleScriptWriter`,
`CapsuleFeedTaskDomain`, `CapsuleFeedTaskRegistrar`). Mais l'ancien modèle
`capsule.CapsuleModels` (`SlideType` enum, `SlideSegment` 5 champs avec `type`+`manimScene`,
`CapsuleScript` avec field `slides`) reste vivant côté consommateurs vidéo
(`CapsuleVideoTask`, `ManimParallelRenderer`, `SubtitleTimingCalculator`, `CapsuleBuildTask`,
`CapsuleScriptTask`, `CapsuleCompositeContextTask`).

_Doublon divergent_ :
* `capsule.feed.SlideSegment` : 3 champs (index, title, speakerNote), invariants stricts, pas de `type`.
* `capsule.SlideSegment` : 5 champs (+ `type: SlideType`, `manimScene: String?`), pas d'invariants.
* `capsule.feed.CapsuleScript` : field `segments`, invariant `deckName` non-blank, `isEmpty`.
* `capsule.CapsuleScript` : field `slides`, pas d'invariant.

_Round-trip lossy_ : `extractSpeakerNotes` produit un `.txt` via `CapsuleScriptWriter`
(format `=== CAPSULE SCRIPT ===` / `--- SLIDE n : title ---`), puis `CapsuleManager.parseScript`
(64 lignes) reparse ce même `.txt` pour reconstruire un `capsule.CapsuleScript` riche
(extrayant `[manim:SceneName]` et `[html]` du titre). Le write→parse est une perte :
le modèle source `capsule.feed.CapsuleScript` connaît déjà les titres propres, mais
le writer les sérialise avec marqueurs puis le parser les re-extrait.

_Boundary cible_ :
- UN seul modèle : `capsule.feed.SlideSegment` (étendu avec `type` + `manimScene`) +
  `capsule.feed.CapsuleScript` (renommé field `segments` → gardé, invariant `isEmpty`).
- `SlideType` enum migre vers `capsule.feed`.
- `CapsuleScriptWriter` préserve le format `.txt` (rétrocompatibilité contrat slider-side
  + tests Cucumber existants), mais sérialise `type`/`manimScene` via marqueurs
  `[manim:SceneName]` / `[html]` déjà utilisés.
- Nouveau `CapsuleScriptReader` (object pur, `capsule.feed`) remplace `CapsuleManager.parseScript`
  — parse `.txt` → `capsule.feed.CapsuleScript`. Round-trip write→read idempotent.
- `CapsuleManager.parseScript` supprimé (dead code après migration consommateurs).
- `capsule.CapsuleModels.kt` supprimé (`SlideSegment`, `CapsuleScript`, `SlideType` retirés).
- `CapsuleExtension`/`CapsuleConventions` restent dans `capsule` (pas du modèle métier).

_Consommateurs à migrer_ (5 fichiers prod) :
. `CapsuleVideoTask.kt` — 12 références `CapsuleScript`/`SlideSegment`/`SlideType`
. `ManimParallelRenderer.kt` — 3 signatures `List<SlideSegment>`
. `SubtitleTimingCalculator.kt` — 1 signature `List<SlideSegment>`
. `CapsuleBuildTask.kt` — 1 appel `parseScript`
. `CapsuleScriptTask.kt` — 1 appel `parseScript`
. `CapsuleCompositeContextTask.kt` — 1 appel `parseScript`

[cols="1,3,2,2", options="header"]
|===
| US | Description | Pts | Priorité

| CAP-MODEL-0
| *Cadrage EPIC* — vision, inventaire doublon, boundary cible, découpage US. MàJ INDEX/BACKLOG/PROMPT_REPRISE + table AGENTS.adoc racine (capsule + slider suivi)
| ✅ TERMINÉ (S-069) | 1

| CAP-MODEL-1
| *Étendre `capsule.feed.SlideSegment`* — ajouter `type: SlideType = SlideType.HTML` + `manimScene: String? = null` (migrer `SlideType` enum vers `capsule.feed`). Invariants préservés (title/speakerNote non-blank). Baby-step TDD : RED (tests échouent — champs absents) → GREEN (champs ajoutés). Unit tests purs (constructeur defaults, MANIM+scene, HTML sans scene, invariants). ~10 unit
| ✅ TERMINÉ (S-069, 10 unit) | 2

| CAP-MODEL-2
| *`CapsuleScriptReader` + `CapsuleScript` field `segments`* — nouvel object pur `capsule.feed.CapsuleScriptReader.read(file): CapsuleScript` (porte `CapsuleManager.parseScript` 64 lignes vers `capsule.feed`, retourne `capsule.feed.CapsuleScript`). `CapsuleScript` field reste `segments` (pas `slides`). Baby-step TDD : RED (tests `CapsuleParseScriptTest` portés vers `CapsuleScriptReaderTest`) → GREEN. ~8 unit
| ✅ TERMINÉ (S-069, 8 unit + 3 writer) | 3

| CAP-MODEL-3
| *Migration consommateurs* — `CapsuleVideoTask`/`ManimParallelRenderer`/`SubtitleTimingCalculator`/`CapsuleBuildTask`/`CapsuleScriptTask`/`CapsuleCompositeContextTask` : import `capsule.feed.{SlideSegment, CapsuleScript, SlideType, CapsuleScriptReader}` au lieu de `capsule.{...}`. Appels `CapsuleManager.parseScript` → `CapsuleScriptReader.read`. Field `parsed.slides` → `parsed.segments`. Tests existants portés (ManimParallelRendererTest, ManimVideoMixerTest, SubtitleTimingCalculatorTest, CapsulePluginTest). ~15 tests ajustés
| ✅ TERMINÉ (S-069, 6 fichiers prod + 4 tests) | 3

| CAP-MODEL-4
| *Suppression dead code* — `capsule.CapsuleModels.kt` supprimé (SlideSegment, CapsuleScript, SlideType retirés — `CapsuleExtension`/`CapsuleConventions` restent). `CapsuleManager.parseScript` supprimé (64 lignes). `CapsuleManager.readScriptFiles` conservé (utilitaire filesystem). Vérification : `./gradlew :capsule:check` VERT, zéro regression
| ✅ TERMINÉ (S-069, -64 lignes parseScript + 2 classes test doublon) | 2

| CAP-MODEL-5
| *Cucumber BDD boundary* — `consolidated_capsule_model.feature` (nouveau) : scénarios BDD end-to-end (parse .adoc → CapsuleScript feed → write .txt → read .txt →CapsuleScript feed identique, type MANIM préservé, manimScene préservé, round-trip idempotent). Steps dans `capsule.scenarios`. Tags `@model @boundary`. ~5 scénarios
| ✅ TERMINÉ (S-069, 5 scénarios) | 3

| CAP-MODEL-6
| *Nettoyage KDoc + références historiques* — KDoc `CapsuleFeedModels.kt` (supprimer référence "historically consumed by CapsuleManager.parseScript"), `CapsuleFeedTaskRegistrar.kt` (mise à jour). Vérification finale : `./gradlew :capsule:check` VERT, `grep -r "CapsuleManager.parseScript\|capsule.CapsuleScript\|capsule.SlideSegment\|capsule.SlideType" capsule-plugin/src/` → zéro (hors CapsuleExtension/CapsuleConventions)
| ✅ TERMINÉ (S-069) | 1
|===

_Total_ : 7 US (US-0 cadrage + US-1→6), ~14 pts. _Méthode_ : baby-step DDD/TDD/BDD,
3 niveaux tests/US (unit → functional → cucumber), English only code/Gherkin.
_Pattern de référence_ : SLD-6 (expulsion SliderManager → domaines DDD) + SLD-11 (expulsion
slider.capsule → capsule.feed). _Statut_ : ✅ TERMINÉ (S-069, 361 unit tests VERT).

'''
== EPIC CAP-29 — Pipeline Traduction Vidéo Multi-Langue (Session 076 — Cadrage)

_Vision_ : Un deck source → 10 vidéos localisées en une tâche Gradle. Le pipeline
`slider.translateDeck` (10 langues, 425 tests PASS) produit les decks traduits. Le
pipeline `capsule.generateCapsuleVideo` (421 unit tests) capture la vidéo + TTS par
langue. CAP-29 crée le pont : un orchestrateur qui itère sur les langues cibles,
résout la voice TTS par langue, et génère un WebM localisé par langue.

_Domaine cible_ : `capsule.multilang` (nouveau domaine DDD, 7ème après `capsule.feed`,
`capsule.capture`, `capsule.tts`, `capsule.subtitles`, `capsule.manim`, `capsule.config`).

_Dépendances_ : CR-2 ✅ (thread safety), CR-3 ✅ (error handling TTS), SLD-11 ✅
(cross-borough slider→capsule feed pipeline), `i18n-contracts` 0.0.2 (workspace-bom N0).

_Décision d'architecture clé_ : `Language` enum local (4 entrées FR/EN/ES/DE) est
remplacé par consommation directe de `LanguageCatalog` (10 langues, contrats N0 du BOM).
Cela évite la dette technique d'étendre l'enum à 10 pour le détruire dans CAP-ARCH-1.
Pattern de référence : `document-gradle` (`DocumentI18nBridge`), `plantuml-gradle`
(`PlantumlTranslationServiceAdapter`). `VoiceMapping` étendu à 10 langues :
espeak codes ISO 639-1 natifs + Piper models nommés (`zh_CN-huayan-medium`, etc.).

_Boundary_ :
- Capsule consomme les decks traduits produits par `slider.translateDeck` (10 langues
  via `LanguageCatalog`) via le pipeline `translateAndExtractSpeakerNotes` (SLD-11.4).
- Le TTS synthétise un audio MP3 par langue (`ttsLanguage` adapté par langue).
- La capture vidéo produit un WebM par langue (`<deckName>_<lang>.webm`).
- Une tâche `generateCapsuleVideoAllLanguages` orchestre la boucle sur les langues.
- `Language` enum supprimé, `VoiceMapping` consomme `LanguageCatalog.findByCode()`.
- Économie d'encre (AGENT.adoc) : ne re-synthétiser que les langues dont le MP3 manque.

[cols="1,3,2,2", options="header"]
|===
| US | Description | Pts | Statut

| CAP-29.0
| *Cadrage EPIC* — vision, domaine `capsule.multilang`, boundary, découpage US.
Audit code existant (`Language` enum 4 entrées, `VoiceMapping` 4 Piper+4 espeak,
`generateCapsuleVideo` mono-langue, `translateAndExtractSpeakerNotes` SLD-11.4).
Décision : consommer `LanguageCatalog` (10 langues) au lieu d'étendre l'enum local.
MàJ INDEX/BACKLOG/PROMPT_REPRISE + table AGENTS.adoc racine.
| 1
| ✅ TERMINÉ (S-076)

| CAP-29.1
| *`MultiLanguageResolver` + extension `VoiceMapping` 10 langues* — domaine
`capsule.multilang`. `MultiLanguageResolver` (object pur) consomme
`LanguageCatalog.findByCode(code)` → résout Piper model + espeak voice par code.
`VoiceMapping` étendu à 10 langues (espeak codes ISO natifs zh/hi/ar/bn/pt/ru/ur,
Piper models nommés). `Language` enum supprimé, `Language.fromCode()` remplacé
par `LanguageCatalog.findByCode()` dans `CapsuleVideoTask`/`CapsuleBuildTask`.
Baby-step TDD : RED (tests `MultiLanguageResolverTest` — resolve 10 langues,
unknown code → null, RTL flag préservé) → GREEN. `VoiceMappingTest` étendu.
~15 unit
| 3
| ✅ TERMINÉ (S-077, 22 unit)

| CAP-29.2
| *`CapsuleVideoPlan` (domaine `capsule.multilang`)* — data class immutable :
`CapsuleVideoEntry` (deckFile, scriptFile, language: SupportedLanguage,
outputVideo) + invariants (deckFile exists, scriptFile exists, language in
LanguageCatalog, non-empty plan). `CapsuleVideoPlanner` (object pur) construit
le plan à partir des decks traduits (`<deckName>_<lang>-deck.html` +
`<deckName>_<lang>-script.txt` → `<deckName>_<lang>.webm`). Baby-step TDD :
RED → GREEN. ~12 unit
| 3
| ✅ TERMINÉ (S-078, 18 unit)

| CAP-29.3
| *`GenerateCapsuleVideoAllLanguagesTask`* — tâche Gradle qui itère sur le
`CapsuleVideoPlan`, appelle le pipeline TTS+capture par langue avec `ttsLanguage`
adapté. Réutilise `synthesizeTtsForScript` + `captureDeckSequential`/`captureDeckParallel`
existants (CR-1 refactor). Output : `<deckName>_<lang>.webm` par langue.
Économie d'encre : skip langue si WebM existe et est valide (probe duration > 0).
Baby-step TDD : unit (plan iteration mocké) + functional (NoOp engines, 3 langues).
| 4
| ✅ TERMINÉ (S-079, 6 unit + 1 functional, 460 unit VERT)

| CAP-29.4
| *Câblage `translateAndExtractSpeakerNotes` → `generateCapsuleVideoAllLanguages`*
— tâche composite `translateAndGenerateCapsuleVideos` : dependsOn
`translateAndExtractSpeakerNotes` + `generateCapsuleVideoAllLanguages`. Pipeline
complet : deck source → translateDeck (10 langues) → extractSpeakerNotes (10 scripts)
→ generateCapsuleVideoAllLanguages (10 WebM). Baby-step TDD : functional (NoOp,
vérifie dependsOn chain). ~3 functional
| 3
| ✅ TERMINÉ (S-080, 3 functional)

| CAP-29.5
| *Cucumber BDD E2E 10 langues* — `multi_language_capsule.feature` : scénarios
BDD end-to-end (deck source → 10 decks traduits → 10 scripts → 10 WebM, ttsLanguage
adapté par langue, output naming `<deckName>_<lang>.webm`, RTL languages ar/ur
générés). Tags `@multilang @i18n`. NoOp engines (pas de vrais Piper/Playwright).
~6 scénarios + step defs.
| 4
| ✅ TERMINÉ (S-081, 6 scénarios cucumber 0 FAIL)

| CAP-29.6
| *Documentation + dogfooding + clôture EPIC* — README master EN + README plugin mis à jour (domaine `capsule.multilang`, diagramme pipeline, table des tâches `extractSpeakerNotes`/`translateAndExtractSpeakerNotes`/`generateCapsuleVideoAllLanguages`/`translateAndGenerateCapsuleVideos`, convention de nommage `<deckName>_<lang>-deck.html|script.txt|webm`, totaux tests 464 unit + 25 functional + 6 cucumber multilang). Dogfooding réel consommateur composite `includeBuild` : 1 deck demo × 3 langues (fr/en/ar dont 1 RTL) → `extractSpeakerNotes` réel → `generateCapsuleVideoAllLanguages` réel (Piper fr/en + espeak, chromium Playwright) → 3 WebM générés en 3m07. Défaut de nommage découvert : le rendu produisait `<deckName>_<lang>-deck.webm` (dérivé du header script) au lieu de la convention du plan `<deckName>_<lang>.webm`, ce qui brisait aussi l'économie d'encre (skip jamais déclenché). Fix TDD baby-step : `CapsuleVideoOutputRenamer` (domaine `capsule.multilang`, 4 unit) aligne le WebM rendu sur `entry.outputVideo` (contrat plan), wiring post-capture dans `renderEntry`. Re-dogfooding : alignement `-deck.webm` → `_<lang>.webm` pour les 3 langues + économie d'encre prouvée (`0/3 rendered, 3 skipped: existing valid WebM`). 464 unit VERT (+4 net) + 25 functional VERT + 6 cucumber multilang GREEN (1 FAIL `@integration` burn-in préexistant hors CAP-29). EPIC CAP-29 TERMINÉ 7/7 US.
| 2
| ✅ TERMINÉ (S-082, 4 unit + 3 WebM réels dogfooding + fix nommage)
|===

_Total_ : 7 US (US-0 cadrage + US-1→6 exécution), ~20 pts. _Méthode_ : baby-step
DDD/TDD/BDD, 3 niveaux tests/US (unit → functional → cucumber), English only
code/Gherkin. _Pattern de référence_ : SLD-5 (DeckTranslationPipeline one-to-many),
CAP-MODEL (unification domaine). _Statut_ : 🟡 EN COURS (CAP-29.0 ✅ S-076,
CAP-29.1 ✅ S-077, CAP-29.2 ✅ S-078, CAP-29.3 ✅ S-079, CAP-29.4 ✅ S-080,
CAP-29.5 ✅ S-081, CAP-29.6 ✅ S-082). 7/7 US terminés. EPIC clos.

'''
== Fichiers Associes

|===
| Fichier | Role | Type
| `AGENT.adoc` | Directives completes | EAGER
| `.agents/INDEX.adoc` | Dashboard + roadmap + portefeuille | EAGER
| `.agents/SESSION_CHECKLIST.adoc` | Checklist ouverture/fermeture session | EAGER
| `.agents/SESSION_PROCEDURE.adoc` | Procedure 6 etapes fin de session | EAGER
| `PROMPT_REPRISE.adoc` | Mission session en cours | EAGER
| `.agents/SESSIONS_HISTORY.adoc` | Historique complet des sessions | EAGER
| `.agents/sessions/` | Archives par session | LAZY
| `.agents/archives/` | Archives taches terminees | LAZY
| `doc/CAPSULE_ARCHITECTURE.adoc` | Cadrage architecture pipeline capsule | LAZY
