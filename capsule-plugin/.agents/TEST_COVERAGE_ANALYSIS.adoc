= TEST_COVERAGE_ANALYSIS — capsule-gradle
:date: 2026-08-25
:session: 121 Intégration PR #1 + fix ScreenshotCaptureImpl + gate Playwright

== Couverture — 25 août 2026 (après session 121)

.Modifications session 121 — Intégration PR #1 + fix + gate Playwright
|===
| Fichier | Changement | Tests ajoutés
| `capsule/PlaywrightManager.kt` (modifié) | `ScreenshotCaptureImpl.isAvailable()` étendu — `isFfmpegAvailable()` probe `ffmpeg -version` via ProcessBuilder. Fix post-merge PR #1 : `ffmpegPath="noop"` retournait `true` (Chromium seul vérifié) puis `capture()` crash `IOException`. | 0 (couvert par functional `CaptureStrategyFunctionalTest` scénario #3 corrigé)
| `capsule/ci/PlaywrightTestGuard.kt` (nouveau) | Pure domain mirror `CucumberTestGuard` (CR-10). Décide si tests Playwright (Chromium réel) doivent tourner : `-PrunPlaywrightTests` ou `CI=true`. | +7 unit `PlaywrightTestGuardTest`
| `capsule/test/PlaywrightTestAssumption.kt` (nouveau, functionalTest) | JUnit5 helper `assumePlaywrightAvailable()` — `assumeTrue` skip par défaut. | 0 (helper — couvert par `CaptureStrategyFunctionalTest` scénario #1 skipped)
| `CaptureStrategyFunctionalTest.kt` (modifié) | Scénario #1 `assumePlaywrightAvailable()` (skipped par défaut). Scénario #3 corrigé "strictMode fails when ffmpeg unavailable" (`assertThrows`). | 0 (3 tests existants, 1 skipped + 2 PASSED)
| `CapsuleCaptureStrategySteps.kt` (modifié) | Steps `build fails`/`failure message contains` + `runCaptureStrategyBuild` catch `UnexpectedBuildFailure`. | 0 (cucumber — 3/3 PASSED)
| `capsule_capture_strategy.feature` (modifié) | Scénario #3 "strictMode fails when ffmpeg unavailable". | 0 (3 scénarios existants)
| `build.gradle.kts` (modifié) | Gate `cucumberTestIntegration onlyIf { shouldRunPlaywright }` (Playwright + ffmpeg réel). | 0 (gate build)
|===

*PR #1 (lion92, 8 commits merge)* : +13 unit `RemotionPlannerTest` (917 → 930),
fix build/i18n/capture/TTS, feat CAP-ANIM (Remotion).

*Totaux* : 930 unit VERT (+13 PR + 7 PlaywrightTestGuard = 937 total test
sources, 930 main + 7 guard), 67 functional VERT (1 skipped Playwright par
défaut, 66 actifs), cucumber dédiés GREEN. `cucumberTestIntegration` skippé
par défaut (gate Playwright). `check` non complété (integration long —
gates clés VERTS). Zéro régression, zéro secret.
régression, zéro secret, zéro bump BOM. Backward compat : transcript
`enabled=false` défaut → tâche pas de skip forcé (skip économie d'encre
uniquement si output existe ET non-blank).


== Couverture — 16 août 2026 (après session 119 CAP-TRANSCRIPT US-5)

.Modifications session 119 — CAP-TRANSCRIPT US-5 Cucumber BDD
|===
| Fichier | Changement | Tests ajoutés
| `src/test/features/capsule_transcript.feature` (nouveau) | Feature `@transcript` — 4 scénarios : TEMPLATE strategy, LLM strategy mocked, empty plan, skip-on-existing-output. | +4 cucumber
| `test/kotlin/capsule/scenarios/CapsuleTranscriptSteps.kt` (nouveau) | Step defs — préfixés "transcript" (bug S-088), utilisation de TranscriptPlan, TranscriptBuilder, ChatModelTranscriptEnhancer (mock), génération et vérification du transcript AsciiDoc. | +4 scénarios couverts
| `test/kotlin/capsule/scenarios/CapsuleTranscriptCucumberRunner.kt` (nouveau) | Runner dédié pattern S-082 + filter `@transcript and not @wip and not @integration`. | +4 scénarios
| `build.gradle.kts` (modifié) | `CucumberTaskSpec("cucumberTestTranscript")` 15ème câblé dans `cucumberConventions.additionalTasks` (après `cucumberTestGlossaryContext`, avant `cucumberTestIntegration`). | —
|===

*Totaux* : 917 unit VERT (baseline S-118 préservée), 67 functional VERT (+3 net S-118), 4 scénarios cucumber dédiés GREEN (nouveaux). `./gradlew check` BUILD SUCCESSFUL. Cucumber SKIPPED par défaut (CR-10) — `cucumberTestTranscript` exécuté via `-PrunCucumber` (4/4 PASSED en ~8s). Zéro régression, zéro secret.

== Couverture — 13 août 2026 (après session 113 EPIC CAP-27 VTT burn-in)

.Modifications session 113 — EPIC CAP-27 VTT burn-in (3/3 US)
|===
| Fichier | Changement | Tests ajoutés
| `capsule/SubtitleBurnInCommand.kt` (nouveau) | Object pur `buildArgv(videoFile, subtitleFile, outputFile, style, ffmpegPath): List<String>` (pattern `AudioPostCommand`). Produit l'argv exact de l'impl (L66-72). Zéro I/O, fully testable. | +13 unit `SubtitleBurnInCommandTest` (SRT path, VTT path CAP-27 core, SRT/VTT diff only by extension, ffmpegPath + `-y`, exactly one `-i`, `-c:a copy`, output last, 9-element structure, default style, custom style, positions top→8/middle→5, custom ffmpegPath)
| `capsule/SubtitleBurnInService.kt` (modifié -7/+2) | Refactor `SubtitleBurnInServiceImpl.burnIn` L66-72 → `SubtitleBurnInCommand.buildArgv(...)`. Argv identique, backward compat totale, mêmes gardes. | 0 (refactor pur — couvert par 13 unit + cucumber E2E)
| `src/test/features/capsule.feature` (modifié +18) | 2 scénarios `@subtitles @burnin @vtt` : non-integration mirror L284-290 (config VTT burn-in + NoOp capture → service invoqué + `.vtt` généré) + E2E `@integration` mirror L292-302 (VTT + real ffmpeg → `.vtt` + valid WebM EBML + burn-in success). | +2 cucumber (non-integration PASSED 32s + E2E real ffmpeg PASSED 32s)
| `capsule/scenarios/CapsuleStepDefinitions.kt` (modifié +38) | 2 step defs VTT uniques `aGradleProjectWithTheCapsulePluginConfiguredForVttSubtitlesWithBurnIn` + `...AndRealFfmpeg` (textes uniques "VTT subtitles", bug S-088). | 0 (step defs — couvert par 2 scénarios cucumber)
|===

*Totaux* : 866 unit VERT (+9 net vs 857 S-112 — 13 nouveaux
`SubtitleBurnInCommandTest`), 64 functional VERT (inchangé baseline),
2 scénarios cucumber VTT (non-integration + E2E `@integration` real
ffmpeg) PASSED. `check` BUILD SUCCESSFUL 12m44s, Kover VERT. Zéro
régression. EPIC CAP-27 TERMINÉ 3/3 US.

== Couverture — 12 août 2026 (après session 111 CAP-GLOSSARY US-2)

.Modifications session 111 — CAP-GLOSSARY US-2 Wiring
|===
| Fichier | Changement | Tests ajoutés
| `capsule/ContextConfig.kt` (modifié) | 3ème champ `glossaryFile: String? = null` (pattern `scenarioFile`), KDoc étendu (4-source precedence `CAPSULE_CONTEXT_GLOSSARY_FILE` < `capsule.context.glossaryFile` < YAML < CLI). | 0 (wiring pur — couvert par functional)
| `capsule/CapsuleConfigMerger.kt` (modifié) | `loadFromEnvironment` (`CAPSULE_CONTEXT_GLOSSARY_FILE`) + `buildConfigFromProperties` (`capsule.context.glossaryFile`) + `mergeContextConfig` étendu `mergeStr` pour `glossaryFile` (pattern `scenarioFile` L329). | 0 (wiring pur — couvert par functional)
| `capsule/CapsuleModels.kt` (modifié) | `CapsuleExtension.glossaryFile: Property<String>` convention `""` (pattern `scenarioFile`). | 0 (wiring pur — couvert par functional)
| `capsule/CapsulePlugin.kt` (modifié) | `pushConfigIntoExtension` push `glossaryFile` (guard `isPresent || blank`, pattern `scenarioFile` L124-126). | 0 (wiring pur — couvert par functional)
| `capsule/context/CollectCapsuleAugmentedContextTask.kt` (modifié) | `glossaryFile: ConfigurableFileCollection @InputFiles @PathSensitive(RELATIVE) @Optional` + `resolveGlossarySection(budget)` (5% budget = 400 tokens/8000, `GlossaryLoader.load(target, glossaryBudget)`, pattern `resolveScenarioSection` L281-296) + `trackSources` étendu tracking GLOSSARY (1 `ProvenanceSource` si `glossaryTarget` existe, pattern SCENARIO L230-249) + appel `CapsuleContextBuilder.build()` étendu `glossarySection = resolveGlossarySection(budget)`. | +2 functional `GlossaryContextFunctionalTest` (glossary fourni → `==== Official Glossary (glossary)` + termes présents + bullet non-Glossary ignoré ; absent → pas de section glossary backward compat)
| `capsule/CapsuleManager.kt` (modifié) | Wiring lazy provider `task.glossaryFile.from(project.provider {...})` (extension afterEvaluate > CLI `capsule.context.glossaryFile`, pattern `scenarioFile` L229-237). | 0 (wiring pur — couvert par functional)
| `src/functionalTest/kotlin/capsule/context/GlossaryContextFunctionalTest.kt` (nouveau) | 2 functional tests RED → GREEN — pattern `ScenarioContextFunctionalTest` mirror : `writeBuildFile` + `writeEagerFiles` + `writeGlossaryFile` (AsciiDoc `== Glossary` + 3 bullets `- term: definition` + `== Other Section` bullet ignoré) + `GradleRunner` CLI `-Pcapsule.context.glossaryFile`. | +2 functional
|===

*Totaux* : 857 unit VERT (inchangé baseline S-110 — US-2 = wiring pur,
zéro test unit nouveau), 64 functional VERT (+2 net
`GlossaryContextFunctionalTest`), 12 cucumber dédiés (inchangé — US-3 =
cucumber à venir). `./gradlew check` BUILD SUCCESSFUL in 26m25s. Kover
VERT, validatePlugins VERT. Zéro régression, zéro secret. Backward
compat strict (`glossaryFile = null` défaut → `resolveGlossarySection`
retourne `""` → tests existants inchangés).

== Couverture — 12 août 2026 (après session 110 CAP-GLOSSARY US-1)

.Modifications session 107+108 — CAP-PROVENANCE (EPIC TERMINÉ 3/3 US)
|===
| Fichier | Changement | Tests ajoutés
| `capsule/context/ProvenanceSource.kt` (nouveau) | Data class immutable 3 champs `(fileName, chars = 0, tokens = 0)` — origine d'une source de contexte (perso/corpus/IA). | +12 unit `ContextProvenanceTest` (5 : defaults/preserve ProvenanceSource + channel aggregates + toJson)
| `capsule/context/ProvenanceChannel.kt` (nouveau) | Data class immutable `(channel, sources = emptyList())` + `totalChars()`/`totalTokens()` somme. | couvert par `ContextProvenanceTest`
| `capsule/context/ContextProvenance.kt` (nouveau) | `totalChars`/`totalTokens` exposés JSON via `@get:JsonProperty` (les `fun` ne sérialisent pas Jackson), `toJson()` Jackson INDENT_OUTPUT, `SCENARIO_CHANNEL = "SCENARIO"` const. | +12 unit `ContextProvenanceTest` (toJson vide/non-vide/totals)
| `capsule/context/ProvenanceTracker.kt` (nouveau) | Mutable accumulator — `trackChannel` replace semantics + ignore empty list, `retainOnly` prune, `build()` snapshot immutable. | +12 unit `ContextProvenanceTest` (3 : snapshot/ignore empty/replace/retainOnly)
| `capsule/context/CapsuleContextBuilder.kt` (modifié) | `build(composite, budget, scenarioSection="", tracker: ProvenanceTracker? = null)` — quand tracker non-null, prune aux canaux survivants (`it.type.name` + SCENARIO si scenario non-blank) ; backward compat null. | +3 unit `CapsuleContextBuilderTest` (prune canaux survivants, SCENARIO conditionnel, null no-op)
| `capsule/context/CollectCapsuleAugmentedContextTask.kt` (modifié) | `@OutputFile provenanceFile` (`build/capsule/context-provenance.json`) + `trackSources(tracker)` privé — EAGER = eagerFiles `.filter{it.exists()}` (fix : le KDoc disait « missing files skipped » mais le code lisait sans vérifier — FileNotFoundException sur AGENT.adoc absent) + `ContextChannel.estimateTokens` par fichier ; RAG = `rag-injected` ; Graphify = `graphify-injected` ; DOCS = docsFiles (fix même `.filter{it.exists()}`) ou `docs-injected` legacy ; SCENARIO = compagnon `.adoc` résolu depuis répertoire ou fichier direct (fix : `scenarioFile` peut être un répertoire CAP-SPD-3 → crash `ScenarioContextFunctionalTest`). Log 2ème ligne `CAPSULE PROVENANCE → N channels, M sources → <path>`. | +2 functional `CollectProvenanceFunctionalTest` (provenance JSON avec EAGER+RAG+Graphify ; context vide → provenance vide)
| `capsule/CapsuleManager.kt` (modifié) | L243 area — `task.provenanceFile.set(project.layout.buildDirectory.file("capsule/context-provenance.json"))`. | 0 régression
| `test/features/capsule_provenance.feature` (nouveau) | 3 scénarios `@context @provenance` (provenance générée via task, contenu JSON, contexte vide → provenance vide). | +3 cucumber
| `test/kotlin/capsule/scenarios/CapsuleProvenanceSteps.kt` (nouveau) | 14 step defs préfixés "provenance" (bug S-088 glue `capsule.scenarios` partagée). | +3 cucumber
| `test/kotlin/capsule/scenarios/CapsuleProvenanceCucumberRunner.kt` (nouveau) | Runner dédié pattern S-082 + filter `@context and @provenance and not @wip and not @integration`. | +3 cucumber
| `build.gradle.kts` (modifié) | `CucumberTaskSpec("cucumberTestProvenance")` 13ème câblé (12ème cucumber dédié, après `cucumberTestAudioPost`, avant `cucumberTestIntegration`). | —
|===

*Totaux* : 834 unit VERT (+15 net : 12 `ContextProvenanceTest` + 3
`CapsuleContextBuilderTest`), 62 functional VERT (+2
`CollectProvenanceFunctionalTest`), 13 cucumber dédiés (+3 provenance, 13
`CucumberTaskSpec` total). `./gradlew check` BUILD SUCCESSFUL in 15m52s.
Cucumber SKIPPED par défaut (CR-10) — `cucumberTestProvenance` exécuté via
`-PrunCucumber` (3/3 PASSED en ~1m55s). Zéro régression, zéro secret.

.2 fix TDD découverts en GREEN (itérations US-2) :
- `FileNotFoundException` sur AGENT.adoc absent → `.filter { it.exists() }`
  ajouté sur eagerFiles (run + trackSources) et docsFiles.
- `scenarioFile` répertoire (CAP-SPD-3) → résolution du compagnon `.adoc`
  (crash `ScenarioContextFunctionalTest`).

.Session 107 (cadrage, zéro code) : CAP-PROVENANCE US-0 — 6 points audités
  (`CapsuleContextBuilder.build` L60-75 / `CollectCapsuleAugmentedContextTask.run`
  L98-132 / `ContextChannel` N0 5 variants / `ChannelBudget` / `ContextConfig` /
  Jackson + runners) + 11 décisions architecturales (extension capsule-local
  pattern CAP-SPD-2, pas de bump N0, `tracker` optional backward compat,
  runner dédié + steps préfixés). Aucun test ajouté.

== Couverture — 11 août 2026 (après session 106 CAP-AUDIO US-4)

.Modifications session 106 — CAP-AUDIO US-4 Cucumber BDD
|===
| Fichier | Changement | Tests ajoutés
| `test/features/capsule_audio_post.feature` (nouveau) | 4 scénarios `@audio @post` (default NoOp skip économie d'encre, BGM enabled NoOp → degraded, ducking only NoOp → degraded, ducking + BGM NoOp → degraded). Pattern `capsule_capture_strategy.feature` (étape interne `generateCapsuleVideo`, pas tâche standalone). | +4 cucumber
| `test/kotlin/capsule/scenarios/CapsuleAudioPostSteps.kt` (nouveau) | 13 step defs préfixés "audio post" (bug S-088 glue `capsule.scenarios` partagée — step texts uniques). Pattern `CapsuleCaptureStrategySteps` : self-contained GradleRunner, temp project + fake deck + fake script + `generateCapsuleVideo` task run + assertions log output + WebM file. `writeBuildGradle(capsuleBlock)` helper. | +4 cucumber
| `test/kotlin/capsule/scenarios/CapsuleAudioPostCucumberRunner.kt` (nouveau) | Runner dédié pattern S-082 `@SelectClasspathResource("features/capsule_audio_post.feature")` + filter `@audio and @post and not @wip and not @integration` + glue `capsule.scenarios` + plugin pretty/html/json reports `cucumber-audio-post.html/.json`. | +4 cucumber
| `build.gradle.kts` (modifié) | `CucumberTaskSpec("cucumberTestAudioPost")` 12ème câblé dans `cucumberConventions.additionalTasks` (11ème cucumber dédié, 12ème `CucumberTaskSpec` total avec `cucumberTestIntegration`). Position : après `cucumberTestDurationValidation`, avant `cucumberTestIntegration`. | —
|===

*Totaux* : 819 unit VERT (baseline S-105 préservée, inchangé), 60 functional
VERT (baseline S-105 préservée, inchangé), 12 cucumber dédiés (+4 audio post,
12 `CucumberTaskSpec` total). `./gradlew check` BUILD SUCCESSFUL in 24m12s.
Cucumber SKIPPED par défaut (CR-10) — `cucumberTestAudioPost` exécuté via
`-PrunCucumber` (4/4 PASSED en 7m21s).

.Modifications session 105 — CAP-AUDIO US-3 Wiring applyAudioPostIfEnabled
|===
| Fichier | Changement | Tests ajoutés
| `capsule/CapsuleVideoTask.kt` (modifié) | Imports `capsule.audio.AudioPostConfig` + `NoOpAudioPostProcessor`. Câblage `applyAudioPostIfEnabled(finalVideo)` dans `captureDeckSequential` L493 + `captureDeckParallel` L524 (post-burnIn, pre-convertFormat). Nouvelle méthode `internal fun applyAudioPostIfEnabled(finalVideo: File)` (20 lignes, pattern `burnInSubtitlesIfEnabled`) : skip si `!bgmEnabled && !duckingEnabled` (économie d'encre), factory `resolveAudioPostProcessor`, `processor.process(finalVideo, tmpFile, config)` → rename si succès / warn + garde original si échec, `catch (AudioPostException)`, `finally` delete tmpFile. | +3 functional (`AudioPostFunctionalTest` : default skip, BGM enabled NoOp degraded, ducking enabled NoOp degraded)
|===

*Totaux* : 819 unit VERT (inchangé baseline S-104), 60 functional VERT
(+3 net), 11 cucumber dédiés (inchangés). `./gradlew check` BUILD
SUCCESSFUL en 9m20s. Cucumber SKIPPED par défaut (CR-10).

.Modifications session 096 — CAP-CR3-3 Branchement ScreenshotCapture
|===
| Fichier | Changement | Tests ajoutés
| `capsule/CapsuleConfig.kt` (modifié) | `CaptureStrategy` enum (2 valeurs PLAYWRIGHT/SCREENSHOT, companion `fromString` case-insensitive, fallback PLAYWRIGHT). `CaptureConfig.strategy: CaptureStrategy = PLAYWRIGHT` 15ème champ. | +7 unit `CaptureStrategyTest` (null/blank/known/case-insensitive/unknown fallback/2 values)
| `capsule/CapsuleConfigMerger.kt` (modifié) | `loadFromEnvironment` (`CAPSULE_CAPTURE_STRATEGY`) + `buildConfigFromProperties` (`capsule.capture.strategy`) + `mergeCaptureConfig` étendu + helper `mergeCaptureStrategy` (4 sources ENV<props<YAML<CLI) | +8 unit `CapsuleConfigMergerTest` (default PLAYWRIGHT, props playwright/screenshot, invalid fallback, YAML override, CLI override, blank CLI fallback, env default)
| `capsule/CaptureResolver.kt` (nouveau) | Object pur factory `resolve(strategy, strict, playwrightFactory, screenshotFactory, noOpCapture, enginePath)` dispatch `when(strategy)`, `StrictModeGuard.requireAvailable` réutilisé. `PlaywrightFactory`/`ScreenshotFactory` fun interfaces. | +6 unit `CaptureResolverTest` (PLAYWRIGHT/SCREENSHOT returns impl, unavailable + non-strict → NoOp, unavailable + strict → IllegalStateException with engine name + path)
| `capsule/ScreenshotPlanner.kt` (nouveau) | Object pur `plan(outputDir, slideDurations)` → `ScreenshotCapturePlan` (entries + concatListFile + finalWebm). `renderConcatList`, `ffmpegPngToWebmArgs`, `ffmpegConcatArgs` (argv builders). `ScreenshotCapturePlan`/`ScreenshotSlideEntry` data classes. | +8 unit `ScreenshotPlannerTest` (single/multi slide, concat list ordre, ffmpeg argv png→webm + concat, empty throws, paths under outputDir, isEmpty)
| `capsule/PlaywrightManager.kt` (modifié) | `ScreenshotCaptureImpl.capture()` étendu multi-slide — boucle screenshot PNG par slide + navigate Reveal.next() + ffmpeg png→webm par slide durée exacte + concat webm via ffmpeg concat demuxer (`concat-list.txt` → `capsule.webm`). Utilise `ScreenshotPlanner`. | couvert par `ScreenshotPlannerTest` (plan) + functional/cucumber (I/O)
| `capsule/CapsuleModels.kt` (modifié) | `CapsuleExtension.captureStrategy: Property<CaptureStrategy>` (convention PLAYWRIGHT) + helper Groovy `captureStrategy(String)`. `CapsuleConventions.captureStrategy = PLAYWRIGHT`. | couvert par functional `CaptureStrategyFunctionalTest`
| `capsule/CapsulePlugin.kt` (modifié) | `pushConfigIntoExtension` push strategy (guard convention) | couvert par functional `CaptureStrategyFunctionalTest`
| `capsule/CapsuleVideoTask.kt` (modifié) | `resolvePlaywrightCapture` refactorisé → `CaptureResolver.resolve` avec lambdas `playwrightFactory`/`screenshotFactory`, log "Capture: X strategy (N slides, M s total)" | couvert par functional `CaptureStrategyFunctionalTest`
| `functionalTest/kotlin/capsule/CaptureStrategyFunctionalTest.kt` (nouveau) | 3 tests GradleRunner (PLAYWRIGHT default → WebM, SCREENSHOT → WebM, SCREENSHOT + strict + noop manim/ffmpeg → WebM) | +3 functional
| `test/features/capsule_capture_strategy.feature` (nouveau) | 3 scénarios `@capture @strategy` (PLAYWRIGHT default, SCREENSHOT, SCREENSHOT + strict) | +3 cucumber
| `test/kotlin/capsule/scenarios/CapsuleCaptureStrategySteps.kt` (nouveau) | Steps préfixés "capture strategy" (bug S-088), `writeBuildGradle` helper (fix parsing Groovy) | +3 cucumber
| `test/kotlin/capsule/scenarios/CapsuleCaptureStrategyCucumberRunner.kt` (nouveau) | Runner dédié pattern S-082 `@SelectClasspathResource` + filter `@capture and @strategy` | +3 cucumber
| `build.gradle.kts` (modifié) | `CucumberTaskSpec("cucumberTestCaptureStrategy")` câblé | —
|===

- Unitaires : 742 VERT (+29 net, 0 FAIL) — dont 7 `CaptureStrategyTest` + 8 merger + 6 `CaptureResolverTest` + 8 `ScreenshotPlannerTest`
- Functional : 52 VERT (+3, 0 FAIL) — 3 `CaptureStrategyFunctionalTest`
- Cucumber dédiés : 3 scénarios GREEN (`capsule_capture_strategy.feature`)
- `./gradlew test functionalTest cucumberTestCaptureStrategy -PrunCucumber` BUILD SUCCESSFUL — zéro régression
- Secrets : 0 trouvé


== Couverture — 11 août 2026 (après session 094 US-2)

.Modifications session 094 US-2 — CapsuleContext.spdSection + CapsuleContextBuilder + DefaultCapsulePromptBuilder étendus
|===
| Fichier | Changement | Tests ajoutés
| `capsule/context/CapsuleContext.kt` (modifié) | 3ème champ `spdSection: String = ""`, invariant ajusté `rendered.isBlank() == (channels.isEmpty() && spdSection.isBlank())`, KDoc mis à jour | couvert par `CapsuleContextBuilderTest`
| `capsule/context/CapsuleContextBuilder.kt` (modifié) | `build(composite, budget, spdSection="")` étendu — append SPD section après channels, trim sur spdSection, 4 cas (blank+blank, blank+spd, channels+blank, channels+spd) | +6 unit `CapsuleContextBuilderTest` (build avec spdSection, blank drop, empty channels + spd, invariant accept/reject, coexist)
| `capsule/pipeline/DefaultCapsulePromptBuilder.kt` (modifié) | `buildGeneratePrompt` instruction line SPD "Speaker notes must address the SPD objectives. Target duration and prerequisites are specified in the SPD Pedagogical Context section." (conditionnelle `contains("SPD Pedagogical Context")`) | +3 unit `DefaultCapsulePromptBuilderTest` (SPD instruction, empty fallback, no-SPD context)
|===

- Unitaires : 713 VERT (+9 net, 0 FAIL, 0 SKIP) — dont 6 `CapsuleContextBuilderTest` + 3 `DefaultCapsulePromptBuilderTest`
- `./gradlew test` BUILD SUCCESSFUL — zéro régression (704 baseline + 9 nouveaux)
- Secrets : 0 trouvé


== Couverture — 9 août 2026 (après session 086)

.Modifications session 086 — koog pipeline génération contenu (domaine `capsule.pipeline`) + tâche generateCapsuleContent + durcissement API
|===
| Fichier | Changement | Tests ajoutés
| `capsule/pipeline/CapsuleState.kt` (nouveau) | Data class immutable + `CapsuleStage` enum — invariants croisés fail-fast (`init` : erreur non nulle seulement si FAILED, cohérence stage/contentPlanJson/speakerNotesAdoc/ttsScript) | 17 unit `CapsuleStateTest`
| `capsule/pipeline/CapsulePromptBuilder.kt` + `DefaultCapsulePromptBuilder.kt` (nouveaux) | Port + impl pédagogique — propose (plan JSON strict deck + contexte augmenté), generate (speaker notes enrichies, contrat `capsule.feed` `[NOTE.speaker]`) | 0 direct (couvert via `CapsulePipelineGraphTest`)
| `capsule/pipeline/CapsuleLlm.kt` + `ChatModelCapsuleLlm.kt` (nouveaux) | Port suspend + adapter langchain4j `ChatModel` | 0 direct (couvert via `CapsulePipelineGraphTest` + functional)
| `capsule/pipeline/ContentPlanValidator.kt` + `ContentPlanValidationResult.kt` (nouveaux) | Validation pure du plan JSON propose — 8 checks ordonnés (champs requis, types, langue ISO) | 18 unit `ContentPlanValidatorTest`
| `capsule/pipeline/TtsScriptDeriver.kt` (nouveau) | Dérive `*-script.txt` (contrat `capsule.feed`) depuis les speaker notes enrichies | 8 unit `TtsScriptDeriverTest`
| `capsule/pipeline/CapsulePipelineGraph.kt` (nouveau) | Graphe koog `strategy<CapsuleState, CapsuleState>` — 3 nœuds + edges conditionnels, `execute()` séquentiel, `asMermaidDiagram()` | 6 unit `CapsulePipelineGraphTest`
| `capsule/pipeline/GenerateCapsuleContentTask.kt` (nouveau) | Tâche `generateCapsuleContent` (groupe `generate`) — deck + contexte augmenté → `-speaker-notes.adoc` + `-script.txt` (sorties déclarées → UP-TO-DATE), `@ServiceReference LlmBuildService`, mock routé, durcissement API (`check(deck.exists())`, contexte inexistant fail-fast) | 3 functional `GenerateCapsuleContentFunctionalTest`
| `capsule/CapsuleManager.kt` (modifié) | Wiring `registerGenerateCapsuleContentTask()` | 0 régression (check VERT)
|===

- Unitaires : 540 VERT (+49 net, 0 FAIL, 0 SKIP) — dont `CapsulePipelineGraphTest` 6, `CapsuleStateTest` 17, `ContentPlanValidatorTest` 18, `TtsScriptDeriverTest` 8
- Functional : 34 VERT (+3, `GenerateCapsuleContentFunctionalTest` — registration, pipeline E2E mock routé, no-deck fail-fast)
- `./gradlew check` BUILD SUCCESSFUL (3m39s : test + functionalTest + kover + validatePlugins ; cucumberTest SKIPPED par défaut — CR-10)
- Secrets : 0 trouvé


.Modifications session 085 — contexte augmenté CompositeContext (domaine `capsule.context`) + dépendance N0 + tâche collect + cucumber dédié
|===
| Fichier | Changement | Tests ajoutés
| `capsule/context/CapsuleContext.kt` (nouveau) | Data class 2 champs `rendered: String` + `channels: List<ContextChannel>` — invariant fail-fast `rendered.isBlank() == channels.isEmpty()`, `isEmpty()`/`nonEmptyCount()`/`tokenEstimate()` | 14 unit `CapsuleContextBuilderTest`
| `capsule/context/CapsuleContextBuilder.kt` (nouveau) | Object pur — `defaultBudget()` (`ChannelBudget()` 8000 40/30/20/10/0), `toChannels(composite)` (5 canaux EAGER→RAG→GRAPHIFY→DOCS→RESOURCE), `channelsWithBudget(channels, budget)` (troncature par canal + append `\n`), `merge(channels)` (sections `==== <sectionHeader> (<name>)`, canaux vides ignorés), `build(composite, budget=defaultBudget())` | couvert par `CapsuleContextBuilderTest`
| `capsule/context/CollectCapsuleAugmentedContextTask.kt` (nouveau) | Tâche `collectCapsuleAugmentedContext` (groupe `collect`) — eagerFiles `.agents/INDEX.adoc` + `PROMPT_REPRISE.adoc` + `AGENT.adoc` (`@InputFiles @PathSensitive(RELATIVE)`), `-Pcontext.{ragContent,graphifyContent,docsContent,tokenBudget=8000}`, output `build/capsule/augmented-context.txt`, log `CAPSULE CONTEXT → …`, no-op si contexte vide | 3 functional `CapsuleAugmentedContextFunctionalTest`
| `capsule/CapsuleManager.kt` (modifié) | Wiring `registerCollectAugmentedContextTask()` | 0 régression (check VERT)
| `gradle/libs.versions.toml` + `build.gradle.kts` (modifiés) | `codebase-contracts 0.0.2 → 0.1.0` + `implementation(libs.codebase.contracts)` (non transitif en compile, absent BOM 0.0.16) + `CucumberTaskSpec("cucumberTestContext")` (`import build.CucumberTaskSpec`) | N/A (deps + task)
| `src/test/features/capsule_context.feature` (nouveau) | Feature `@context @rag` — 4 scénarios : tâche listée, artefact EAGER+RAG+Graphify, budget par défaut 8000, budget custom `-Pcontext.tokenBudget=5000` | 4 scénarios cucumber
| `src/test/kotlin/capsule/scenarios/CapsuleContextSteps.kt` + `CapsuleContextCucumberRunner.kt` (nouveaux) | Step defs dédiés (phrases uniques — évite DuplicateStepDefinitionException S-071) + runner dédié (contourne bug S-082 `@ConfigurationParameter(FEATURES_PROPERTY_NAME)`) | 4 scénarios couverts
|===

- Unitaires : 491 VERT (+14 net, 0 FAIL, 0 SKIP) — dont `CapsuleContextBuilderTest`
- Functional : 31 VERT (+3, `CapsuleAugmentedContextFunctionalTest` — registration, artefact EAGER+RAG+Graphify écrit, UP-TO-DATE)
- Cucumber : `capsule_context.feature` `@context @rag` 4/4 GREEN (~31s, runner dédié `cucumberTestContext`, zéro LLM réel)
- `./gradlew check` BUILD SUCCESSFUL (3m33s : test + functionalTest + kover + validatePlugins ; cucumberTest SKIPPED par défaut — CR-10)
- Secrets : 0 trouvé

## Couverture — 9 août 2026 (après session 084)

.Modifications session 084 — socle LLM bridge codebase (domaine `capsule.ai`) + dépendances + tâche smoke
|===
| Fichier | Changement | Tests ajoutés
| `capsule/ai/CapsuleLlmService.kt` (nouveau) | Object pur — `registerLlmBuildService(project)` (`sharedServices.registerIfAbsent("capsuleLlmService")` + convention model), `resolveModel(provider, serviceProvider): ChatModel`, `aiProvider(project)` `-Pai.provider` default `ollama` | 10 unit `CapsuleLlmServiceTest`
| `capsule/ai/LlmProviderChatModelAdapter.kt` (nouveau) | Pont `LlmProvider` (codebase) → `ChatModel` (langchain4j) — `chat()` délègue à `llmProvider.call` + trace log | 2 unit `LlmProviderChatModelAdapterTest`
| `capsule/ai/CapsuleAiSmokeTestTask.kt` (nouveau) | Tâche `capsuleAiSmokeTest` (groupe `generate`) — résout `ChatModel` via `LlmBuildService`, prompt minimal, log `CAPSULE AI → <réponse>`, fail si réponse non conforme | 3 functional `CapsuleAiSmokeTestFunctionalTest`
| `capsule/CapsuleManager.kt` (modifié) | Câblage `registerAiSmokeTestTask()` (+12 lignes, `usesService`) | 0 régression (check VERT)
| `build.gradle.kts` + `gradle/libs.versions.toml` (modifiés) | codebase 0.0.5 + koog 1.0.0 (bump 0.8.0 mort) + langchain4j/langchain4j-ollama (BOM) + buildscript force annotations | N/A (deps)
| `src/test/features/capsule_ai.feature` (nouveau) | Feature `@ai` — 3 scénarios : task listée, smoke mock LLM (log `CAPSULE AI` + réponse), fail-fast unreachable | 3 scénarios cucumber
| `src/test/kotlin/capsule/scenarios/CapsuleAiSteps.kt` (nouveau) | Step defs — GradleRunner `capsuleAiSmokeTest` + mock `HttpServer` Ollama local (`-Pollama.baseUrl`) | 3 scénarios couverts
|===

- Unitaires : 477 VERT (+10 net, 0 FAIL) — dont `CapsuleLlmServiceTest` + `LlmProviderChatModelAdapterTest`
- Functional : 28 VERT (+3, `CapsuleAiSmokeTestFunctionalTest` — registration, mock HTTP, unreachable fail)
- Cucumber (`-PrunCucumber`) : `capsule_ai.feature` `@ai` 3/3 GREEN (8.3s, mock HTTP — zéro LLM réel). Suite complète 160 tests, 1 FAIL préexistant `@integration` burn-in (subtitle path `build/capsule/` vs `outputDir=capsules`) — hors CAP-ARCH-1, connu S-069/S-072
- `./gradlew check` BUILD SUCCESSFUL (cucumber SKIPPED par défaut — CR-10)
- Secrets : 0 trouvé

## Couverture — 8 août 2026 (après session 082)

.Modifications session 082 — 4 unit tests + fix nommage + dogfooding réel (domaine `capsule.multilang`)
|===
| Fichier | Changement | Tests ajoutés
| `capsule/multilang/CapsuleVideoOutputRenamer.kt` (nouveau) | Object pur `rename(rendered, target): File` — aligne le WebM rendu (`<scriptDeckName>.webm`, dérivé du header script) sur `entry.outputVideo` (contrat plan `<deckName>_<lang>.webm`). No-op si même path ou si rendu absent. Fallback copy+delete si `renameTo` échoue | 4 unit `CapsuleVideoOutputRenamerTest`
| `capsule/multilang/GenerateCapsuleVideoAllLanguagesTask.kt` (modifié) | Wiring post-capture : après `captureDeckSequential/Parallel`, `CapsuleVideoOutputRenamer.rename(outDir/${parsed.deckName}.webm, entry.outputVideo)` + log d'alignement conditionnel. Répare l'économie d'encre (le probe `shouldRender` matche désormais l'output existant) | 0 régression (functional GREEN)
| `README.md` + `capsule-plugin/README.md` (modifiés) | Doc domaine `capsule.multilang` + table des tâches + convention nommage `<deckName>_<lang>-deck.html|script.txt|webm` + totaux tests 464/25/6 | N/A (doc)
|===

- Unitaires : 464 VERT (+4 net : +4 `CapsuleVideoOutputRenamerTest`, 0 FAIL)
- Functional : 25 VERT (`GenerateCapsuleVideoAllLanguagesFunctionalTest` GREEN — exerce le même path NoOp, rename no-op car deckName header == plan deckName)
- Cucumber multilang : 6/6 GREEN (S-081 baseline, non régressés — runner interrompu par timeout tooling, non par échec)
- Dogfooding réel : 3 WebM générés (fr/en/ar) + économie d'encre prouvée (`--rerun-tasks` → `0/3 rendered, 3 skipped`)
- 1 FAIL `@integration` burn-in préexistant (subtitle path `build/capsule/` vs `outputDir=capsules`) — hors CAP-29, connu S-069/S-072
- Secrets : 0 trouvé

== Couverture — 8 août 2026 (après session 081)

.Modifications session 081 — 6 scénarios cucumber `@multilang @i18n` (domaine `capsule.multilang`)
|===
| Fichier | Changement | Tests ajoutés
| `src/test/features/multi_language_capsule.feature` (nouveau) | Feature Gherkin `@multilang @i18n` — 6 scénarios : plan 10 langues (LanguageCatalog), convention nommage `<deckName>_<lang>.webm`, skip langue sans paire deck+script, fail-fast plan vide, E2E GradleRunner NoOp → 10 WebM + log `CAPSULE MULTILANG → n/n languages rendered`, E2E RTL ar/ur | 6 scénarios cucumber
| `src/test/kotlin/capsule/scenarios/MultiLanguageCapsuleSteps.kt` (nouveau) | Step defs — helpers plan pur `CapsuleVideoPlanner.plan()` (Given/When/Then) + 2 scénarios GradleRunner NoOp (ttsEngine=noop, captureEnabled=false, deck source + 10 scripts par langue dans répertoires conventions, assert WebM `build/capsules/*.webm`) | 6 scénarios couverts
|===

- Cucumber (`-PrunCucumber`) : 6/6 multilang GREEN (0 FAIL). Suite complète 154 tests, 1 FAIL préexistant `@integration` burn-in (subtitle path `build/capsule/` vs `outputDir=capsules`) — hors périmètre CAP-29.5, connu S-069/S-072
- Unitaires : 460 VERT (inchangé, 0 FAIL) + 25 functional VERT (inchangé)
- `./gradlew check` BUILD SUCCESSFUL (cucumber SKIPPED par défaut — CR-10)
- Couverture `multi_language_capsule.feature` : 6/6 (plan 10 langues, naming, skip, fail-fast, E2E NoOp 10 WebM, RTL ar/ur)
- Secrets : 0 trouvé

== Couverture — 8 août 2026 (après session 080)

.Modifications session 080 — 3 functional tests (câblage wiring)
|===
| Fichier | Changement | Tests ajoutés
| `capsule/feed/CapsuleFeedTaskDomain.kt` (modifié) | +2 constantes `TRANSLATE_AND_GENERATE_CAPSULE_VIDEOS` + `TRANSLATE_AND_GENERATE_VIDEOS_DESCRIPTION` | 0 (constantes testées via functional)
| `capsule/feed/CapsuleFeedTaskRegistrar.kt` (modifié) | +`registerTranslateAndGenerateCapsuleVideos` (group capsule, 2 dependsOn) | 3 functional `TranslateAndGenerateCapsuleVideosFunctionalTest`
| `capsule/CapsuleManager.kt` (modifié) | Câblage 3ème appel registrar dans `registerExtractSpeakerNotesTask()` | 0 régression
|===

- Unitaires : 460 VERT (inchangé) + 25 functional (+3, 0 FAIL)
- `./gradlew check` BUILD SUCCESSFUL (cucumber SKIPPED — CR-10)
- Couverture `translateAndGenerateCapsuleVideos` : 3/3 (registration groupe, description mentionne les 2 sous-tâches, dependsOn chain via `--dry-run` avec stub translateDeck)
- Secrets : 0 trouvé

== Couverture — 8 août 2026 (après session 079)

.Modifications session 079 — 6 nouveaux tests unitaires + 1 functional (domaine `capsule.multilang`)
|===
| Fichier | Changement | Tests ajoutés
| `capsule/multilang/CapsuleVideoAllLanguagesRunner.kt` (nouveau) | Object pur `run(plan, probeDuration, render): List<CapsuleVideoEntry>` — itère le plan, invoque `render` pour chaque entrée sans WebM valide, retourne les entrées rendues dans l'ordre du plan. `shouldRender(entry, probeDuration)` : skip si WebM existe ET probe > 0 (ink economy), re-render si probe ≤ 0 (artefact invalide) | 6 unit `CapsuleVideoAllLanguagesRunnerTest`
| `capsule/multilang/GenerateCapsuleVideoAllLanguagesTask.kt` (nouveau) | Tâche Gradle `: CapsuleVideoTask` — `@TaskAction override execute()`, résolution TTS par langue via `MultiLanguageResolver` (set `ttsLanguage`/`ttsVoice`/`espeakVoice`, reset `ttsEngine=null`), pipeline mono-langue réutilisé (CR-1), log `CAPSULE MULTILANG → x/y` | 1 functional `GenerateCapsuleVideoAllLanguagesFunctionalTest` (3 langues fr/en/ar NoOp → 3 WebM)
| `capsule/CapsuleVideoTask.kt` (modifié) | `execute()` → `open`, `resolveTtsEngine()`/`injectAudio()` → `internal` — zéro changement de comportement | 0 régression
| `capsule/CapsuleManager.kt` (modifié) | Enregistrement `generateCapsuleVideoAllLanguages` (groupe generate) | 0 régression
|===

- Unitaires : 460 VERT (was 454, +6 net : +6 `CapsuleVideoAllLanguagesRunnerTest`)
- Functional : +1 PASS (`GenerateCapsuleVideoAllLanguagesFunctionalTest` — 3 langues NoOp → 3 WebM)
- 0 FAIL, 0 SKIP (cucumber SKIPPED par défaut — CR-10)
- `./gradlew check` BUILD SUCCESSFUL
- Couverture `CapsuleVideoAllLanguagesRunner` : 6/6 (renders all 3 langues, skip WebM existant valide, re-render WebM invalide, plan mixte missing+invalid ordre préservé, shouldRender true/false)
- Note : le skip ink-economy n'est pas observable en functional (ffprobe ne parse pas le WebM minimal NoOp — duration vide) → couvert de façon déterministe en unit via `probeDuration` injecté
- Secrets : 0 trouvé

== Couverture — 8 août 2026 (après session 078)

.Modifications session 078 — 18 nouveaux tests (domaine `capsule.multilang`)
|===
| Fichier | Changement | Tests ajoutés
| `capsule/multilang/CapsuleVideoPlan.kt` (nouveau) | `CapsuleVideoEntry` (data class immutable 4 champs deckFile/scriptFile/language/outputVideo, invariants fail-fast deck exists + script exists + language in LanguageCatalog) + `CapsuleVideoPlan` (invariant non-empty, size()/isEmpty()/languages()) | 10 unit `CapsuleVideoPlanTest`
| `capsule/multilang/CapsuleVideoPlanner.kt` (nouveau) | Object pur `plan(deckDir, scriptDir, outputDir, targetLanguages)` — découverte `_<lang>-deck.html` + `_<lang>-script.txt`, skip langues manquantes, fail-fast plan vide, nommage `<deckName>_<lang>.webm` | 8 unit `CapsuleVideoPlannerTest`
|===

- Unitaires : 454 VERT (was 436, +18 net : +10 `CapsuleVideoPlanTest` + 8 `CapsuleVideoPlannerTest`)
- 0 FAIL, 0 SKIP
- `./gradlew check` BUILD SUCCESSFUL
- Couverture `CapsuleVideoPlan` : 10/10 (single/multi entries, plan vide throw, languages, isEmpty, deck manquant, script manquant, language hors catalog, fields préservés)
- Couverture `CapsuleVideoPlanner` : 8/8 (3 langues, skip deck manquant, skip script manquant, plan vide throw, naming, deck/script paths préservés, RTL Arabic)
- Secrets : 0 trouvé

== Couverture — 7 août 2026 (après session 077)

.Modifications session 077 — 22 nouveaux tests + 2 adaptés (domaine `capsule.multilang`)
|===
| Fichier | Changement | Tests ajoutés
| `capsule/multilang/MultiLanguageResolver.kt` (nouveau) | Object pur `MultiLanguageResolver` (`resolve`, `resolveByPiperModel`, `resolveByEspeakVoice`, `piperModel`, `espeakVoice`, `supportedCodes`) + `ResolvedLanguage` data class | 22 unit `MultiLanguageResolverTest`
| `capsule/multilang/VoiceMapping.kt` (nouveau) | 11 Piper models + 11 espeak voices (fr/en/es/de/zh/hi/ar/bn/pt/ru/ur) + reverse maps | couvert indirectement par `MultiLanguageResolverTest`
| `capsule/TtsManager.kt` (modifié) | `TtsEngine.language(): SupportedLanguage?` (was `Language?`). `PiperTtsEngine`/`EspeakTtsEngine` prennent `SupportedLanguage?` | 4 tests existants migrés (zéro net)
| `capsule/CapsuleVideoTask.kt` (l.197) | `Language.fromCode` → `MultiLanguageResolver.resolve` | 0 régression
| `capsule/CapsuleBuildTask.kt` (l.40) | `Language.fromCode` → `MultiLanguageResolver.resolve` | 0 régression
| `capsule/LanguageVoiceMapping.kt` (supprimé) | `Language` enum (4 entrées) + ancien `VoiceMapping` | -9 tests `LanguageVoiceMappingTest` → 2 tests adaptés (délèguent à `MultiLanguageResolver`)
| `build.gradle.kts` + `libs.versions.toml` | BOM `workspace-bom:0.0.16` + `i18n-contracts:0.0.2` | 0 régression
|===

- Unitaires : 436 VERT (was 421, +15 net : +22 nouveaux, +2 adaptés, -9 supprimés)
- 0 FAIL, 0 SKIP
- `./gradlew check` BUILD SUCCESSFUL (53s)
- Couverture `MultiLanguageResolver` : 22/22 tests (resolve 10 codes, unknown→null, RTL ar/ur, piperModel/espeakVoice 11 codes, resolveByPiperModel/resolveByEspeakVoice, supportedCodes 10)
- Secrets : 0 trouvé

== Couverture — 16 juin 2026 (après session 040)

.Modifications session 040 — 9 nouveaux tests unitaires
|===
| Fichier | Changement | Tests ajoutés
| CapsuleVideoTask.kt | 5 méthodes extraites (synthesizeTtsForScript, renderManimSlides, replaceManimSlidesInDeck, captureDeckSequential, captureDeckParallel) | 0 régression
| CapsulePluginTest.kt | 9 nouveaux tests | synthesizeTtsForScript (3), renderManimSlides (2), replaceManimSlidesInDeck (2), captureDeckSequential (1), captureDeckParallel (1)
|===

- Unitaires: tous PASS (aucune régression)
- Compile: BUILD SUCCESSFUL
- Secrets: 0 trouvé

== Couverture — 15 juin 2026 (après session 032)

|===
| Classe | Type | Tests | Couverture
| AudioConversionUtil.kt | main | 6 unitaires | wavToMp3 fallback ffmpeg absent, fallback exit code, parent dirs, integration ffmpeg, isAvailable true/false
| HtmlSectionParser.kt | main | 6 unitaires | Flat sections, nested sections, empty, single, self-closing skip, attributes
| MediaProbeUtil.kt | main | 4 unitaires | probeDuration, ffprobePath, isFfprobeAvailable, unavailable
| CapsulePlugin.kt | main | 7 unitaires | Enregistrement extension + 5 taches + collectCliParams + pushConfigIntoExtension (inc. parallelRender + parallelCaptureThreads + parallelRenderThreads)
| CapsuleModels.kt | main | 5 unitaires | CapsuleExtension 17 propriétés + SlideType enum + SlideSegment defaults + manimScene + manimParallelRender + parallelCaptureThreads + manimParallelRenderThreads
| CapsuleManager.kt | main | 4 unitaires | parseScript + resolveManimEngine + resolveManimVideoMixer + resolveManimSlideReplacer + resolveManimParallelRenderer
| CapsuleScriptTask.kt | main | 0 | (loggé uniquement)
| CapsuleBuildTask.kt | main | 3 unitaires | Parallélisme (Executors+AtomicInteger+Future), failures+fallback, empty scripts
| CapsuleVideoTask.kt | main | 19 unitaires | empty deck/script warn, e2e NoOp + injectAudio + sequential fallback + multi-deck + parallel capture + concatWebmFiles + createSingleSlideHtml + computeSlideDurationsWithManim + captureSlideParallel cap configurable + manimSlideReplacer wiring + manimParallelRenderer wiring + resource leak finally
| CapsuleDistribTask.kt | main | 3 unitaires | no videos warn, ffmpeg unavailable copy, multiple videos
| CapsuleCompositeContextTask.kt | main | 4 unitaires | json context file, multiple decks, distribVideo, valid parseable JSON
| CapsuleParseContextTask.kt | main | 3 unitaires | parse JSON, empty entries, missing file graceful
| ManimEngine.kt | main | 9 unitaires | NoOpManimEngine (3) + ManimEngineImpl (6 : unavailable, ManimException, name, config quality, default, noop path) — probeDuration délégué à MediaProbeUtil
| ManimVideoMixer.kt | main | 11 unitaires | NoOpManimVideoMixer (4) + ManimVideoMixerImpl (4) + resolveManimVideoMixer factory (2) + MixerException — probeDuration délégué à MediaProbeUtil
| ManimSlideReplacer.kt | main | 14 unitaires | NoOpManimSlideReplacer (2) + ManimSlideReplacerImpl (10 : replace at index, first, last, video attrs, preserve structure, out of bounds, negative, single slide, nested sections, sequential, data-capsule-slide) + factory — extractTopLevelSections délégué à HtmlSectionParser
| ManimParallelRenderer.kt | main | 14 unitaires | NoOpManimParallelRenderer (5) + ManimParallelRendererImpl (6) + factory (2)
| TtsManager.kt | main | 5 unitaires | NoOpTtsEngine, PiperTtsEngine, EspeakTtsEngine, TtsException — wavToMp3 délégué à AudioConversionUtil
| PlaywrightManager.kt | main | 6 unitaires | NoOpPlaywrightCapture (3) + ScreenshotCaptureImpl (3)
| CapsuleConfigLoader | main | 5 unitaires | YAML loading, defaults, env vars, missing file, empty file
| CapsuleConfig | main | 5 unitaires | Defaults, capture section (inc. parallelCaptureThreads), distrib section, manim section (inc. parallelRenderThreads), custom values
| CapsuleConfigMerger | main | 29 unitaires | 4 precedence levels, partial overrides, edge cases, manim outputDir, manim parallelRender + parallelRenderThreads, capture parallelCaptureThreads
| CapsuleScaffoldTask | main | 6 unitaires | Creates YAML, all sections, commented defaults, no overwrite, custom path, resolution order, manim section fields + parallelCaptureThreads + parallelRenderThreads
| ManimConfig | main | 16 unitaires | Defaults, VALID_QUALITIES, validate (7 tests), copy, presets, parallelRender + parallelRenderThreads
| ComputeSlideDurationsWithManim | main | 4 unitaires | Manim duration priority, fallback 0.0, mixed slides, empty map defaults
| ManimSlideReplacementWiring | main | 3 unitaires | Replacer wiring, NoOp replacer, factory resolves impl
| ManimEngineWiring | main | 3 unitaires | Engine wiring, NoOp engine, factory resolves
| ManimVideoMixerWiring | main | 5 unitaires | Mixer wiring, NoOp mixer, factory resolves
| AudioQualityConstraint | main | 3 unitaires | Real TTS > placeholder, not text, WebM EBML header
| CapsulePluginWiring | main | 3 unitaires | Plugin wiring, NoOp engines, config merging
| CapsuleParseScript | main | 2 unitaires | Script parsing, manim scene extraction
| ParallelCapture | main | 6 unitaires | Parallel capture enable/disable, cap threads, fallback
| ParallelCaptureSwitch | main | 2 unitaires | DSL switch, default value
| ScreenshotCapture | main | 3 unitaires | Screenshot with viewport, close, NoOp
|===

- Unitaires: 211/211 PASS (+16 vs session 031)
- Cucumber BDD: 22 scénarios (5 @config @manim + 3 @manim + 1 @manim @parallel + 13 standard)
- Full check: BUILD SUCCESSFUL
- Kover coverage: OK

== Modifications session 032 (pas de nouveaux tests, refactor uniquement)

| Fichier | Changement | Impact tests
| PlaywrightManager.kt | println → logger.lifecycle | 0 régression
| CapsuleConfigLoader.kt | println → logger.warn | 0 régression
| CapsuleVideoTask.kt | isPlaceholder() supprimé, concat finally, mixAudio finally | 0 régression
| CapsuleBuildTask.kt | lateinit var → lazy delegation | 0 régression
| CapsuleScriptTask.kt | lateinit var → lazy delegation | 0 régression
| CapsuleDistribTask.kt | lateinit var → lazy delegation | 0 régression
| CapsuleCompositeContextTask.kt | lateinit var → lazy delegation | 0 régression
| CapsuleScaffoldTask.kt | lateinit var → lazy delegation | 0 régression
| CapsuleManager.kt | Suppression capsuleExt field + 6 injections | 0 régression
| CapsuleConfig.kt | validate() when explicite | 0 régression

== Couverture — 11 août 2026 (après session 094)

.Modifications session 094 — CAP-SPD US-0 cadrage + US-1 SpdContext/SpdContextLoader (domaine `capsule.context` étendu)
|===
| Fichier | Changement | Tests ajoutés
| `capsule/context/SpdContext.kt` (nouveau) | Data class immutable 6 champs (objectives, duration, prerequisites, modalities, sessionTitle, module), invariant fail-fast `sessionTitle.isNotBlank()`, derived `isEmpty` | 0 direct (couvert via `SpdContextLoaderTest`)
| `capsule/context/SpdContextLoader.kt` (nouveau) | Object pur mirror `DocContextLoader` — `load(metadataFile, adocFile, maxTokens)`, valide metadata.json K-2 (`type=="SPD"` uniquement, pas `source` — capsule public OSS format-agnostic), parse AsciiDoc (extractTitle/extractAttribute/extractBulletSection/extractTextSection), rend `==== SPD Pedagogical Context (spd)`, tronque `ContextChannel.Docs.truncateToTokens` | 10 unit `SpdContextLoaderTest`
|===

- Unitaires : 704 VERT (+10 net, 0 FAIL, 0 SKIP) — 10 `SpdContextLoaderTest` (null metadata + missing adoc, valid metadata + adoc extracts objectives, missing metadata still parses adoc, type != SPD skips, empty objectives fallback, budget truncation, malformed metadata skipped + adoc still parsed, multi-line objectives, zero budget blank, empty adoc blank)
- Zéro régression (694 baseline + 10 nouveau)
- Domaine `capsule.context` : 5 fichiers (CapsuleContext + CapsuleContextBuilder + DocContextLoader + CollectCapsuleAugmentedContextTask + 2 NEW SpdContext/SpdContextLoader)
- Baby-step TDD : RED (10 tests unresolved `SpdContextLoader`) → GREEN (9/10, 1 test assertion incohérente corrigée) → REFACTOR (`source` retiré `SpdMetadata`)
- Non-blocage : SPD absent → blank → vidéo générée sans ancrage SPD (fallback no-op)