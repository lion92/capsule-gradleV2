= SESSIONS_HISTORY — capsule-gradle

_Date creation_ : 2026-05-14

== Sessions

|===
| # | Date | Type | Sujet | Score
| 122 | 2026-08-25 | cadrage+tdd+bdd+cloture | **EPIC CAP-ANIM TERMINÉ 3/3 US** — cadrage rétroactif code Remotion livré PR #1 (S-121). US-0 cadrage (10 constats + 9 décisions, EPIC "EN COURS" puis TERMINÉ — code fonctionnel mais gaps tests à combler). US-1 tests unit manquants 40 unit : AudioCodecs 7 unit, RemotionConfig 8 unit, RemotionTemplate 7 unit, RemotionCaptureImpl 14 unit, CaptureResolver REMOTION 4 unit. Baby-step TDD RED → GREEN → REFACTOR. US-2 cucumber BDD `capsule_remotion.feature` 3 scénarios `@anim @remotion` + runner dédié 16ème + 2 functional. **Restauration `build.gradle.kts` post-PR #1** : `cucumberConventions` block + 16 `CucumberTaskSpec` + `kover` + `publishing` + gates CR-10/Playwright were stripped by PR #1 — restored. 977 unit VERT (+40 net baseline 937), 69 functional VERT (+2 net baseline 67), cucumber dédiés GREEN. Zéro régression, zéro secret, zéro bump BOM. Domaine `capsule.remotion` (12ème) clos. | 9/10
| 121 | 2026-08-25 | merge+fix+tdd+bdd | **Intégration PR #1 (lion92) — "Fix/build toolchain et pipeline"** — merge branche `pr-1` (8 commits) dans main. Conflit INDEX.adoc résolu (version S-120 conservée). Contenu PR : (1) fix build toolchain conventions 0.0.2→0.0.3 (Java 25) + CI JDK 25 + exclusion Groovy 3 `testRuntimeClasspath { exclude(group = "org.codehaus.groovy") }` ; (2) fix i18n namespace bundle `i18n/Messages` → `capsule/i18n/Messages` (conflit classpath avec slider résolu) + 10 langues complétées (task.group.verification + validateCapsuleVideoDuration.description) ; (3) fix 4 défauts capture (narration double injectSubtitleTrack réinjectait AUDIO_INJECT_SCRIPT, vidéo tronquée SCREENSHOT listFiles .first() prenait slide-N.webm au lieu de capsule.webm, parallelCapture ne produisait rien cherchait "slide.webm" inexistant, ffmpeg hardcodé ignorait ffmpegExecutablePath) ; (4) docs réaligne READMEs + INDEX + session 119 sur code réel ; (5) fix capture fonctionnelle conteneur (Chromium --no-sandbox, navigateur unique par instance pas 1/diapo, encodage PNG→WebM parallèle, reveal.js CDN supprimé mono-diapo, FFmpeg stderr→log file) ; (6) feat CAP-ANIM stratégie `CaptureStrategy.REMOTION` (composition image par image cascade/dérive/fondu/progressbar, React embarqué jar, `RemotionPlanner`/`RemotionCaptureImpl`/`RemotionTemplate`, section `capsule.remotion.*` DSL 4 sources, `RemotionConfig` dans `CapsuleConfig`) ; (7) feat Remotion MP4/H.264 + Manim incrusté `OffthreadVideo`, `PlaywrightCapture.outputExtension()`, `AudioCodecs` codec selon conteneur ; (8) fix TTS économie d'encre generateCapsule ne réécrit plus narration existante. **Fix post-merge baby-step TDD/BDD** : `ScreenshotCaptureImpl.isAvailable()` étendu pour vérifier ffmpeg (`isFfmpegAvailable()` probe `ffmpeg -version`) — avant la PR `isAvailable()` ne vérifiait que Chromium/Playwright, donc `ffmpegPath="noop"` retournait `true` puis `capture()` crash `IOException`. Test functional `CaptureStrategyFunctionalTest` "strictMode succeeds" corrigé → "strictMode fails when ffmpeg is unavailable" (`assertThrows<UnexpectedBuildFailure>` + message "screenshot is not available"). Cucumber `capsule_capture_strategy.feature` scénario #3 corrigé → "strictMode fails when ffmpeg is unavailable" + steps `build fails`/`failure message contains` ajoutés. Gate : `compileKotlin` VERT + `compileTestKotlin` VERT + `test` 930 unit 0 FAIL (+13 net baseline 917 → 930, `RemotionPlannerTest` ajouté par PR) + `functionalTest` 67 tests 0 FAIL (1 test corrigé) + `cucumberTestTranscript` 4/4 PASSED + `cucumberTestCaptureStrategy` 3/3 PASSED. `check` en cours (cucumber integration). EPIC CAP-ANIM (Remotion) à cadrer post-merge S-122. | 9/10 (cross-borough MEMPHIS + Queens) — Catalogue capsule fixé : `slider` → `slider-plugin` (artifactId réel depuis aplatissement S-067), slider 0.0.16 → 0.0.18, codebase 0.0.8 → 0.0.10, BOM 0.0.16 → 0.0.27. Bug discovery : `testImplementation(libs.slider)` (build.gradle.kts:232) amenait Groovy 3.0.17 sur le classpath de test via la chaîne `slider-plugin:0.0.18` → `asciidoctor-gradle-jvm-slides:5.0.0-alpha.1` → `jrubygradle-resolver:2.1.2` → `groovy-nio:3.0.17`. Gradle 9.6.1 ProjectBuilder utilise Groovy 4 — conflit 3.x vs 4.x casse `org.codehaus.groovy.runtime.InvokerHelper` (`NoClassDefFoundError` + `ExceptionInInitializerError`). 79/917 tests unitaires FAILED préexistantes depuis bump codebase 0.0.8 S-119 (documenté S-208 comme "conflit résolution double slider/slider-plugin" — root cause réelle : Groovy 3.x transitif). Fix : retrait `testImplementation(libs.slider)` — aucun test n'importait slider (`grep -rn "import slider\." src/` = zéro). `compileOnly(libs.slider)` conservé (runtime : CapsulePlugin applique slider). Gate : `compileKotlin --rerun-tasks` VERT + `compileTestKotlin` VERT + `test` 917 tests 0 FAIL (baseline restaurée) + `cucumberTestTranscript` 4/4 scénarios PASSED. CAP-TRANSCRIPT EPIC TERMINÉ 6/6 US (sync BACKLOG US-5 `⬜ TODO` → `✅ TERMINÉ S-119`). EPIC MEMPHIS-BOM-FIX TERMINÉ 5/5 US cross-borough (BOM 0.0.27 Central MEMPHIS S-024 + codebase 0.0.10 Central Queens S-209). | 10/10
| 119 | 2026-08-16 | feat+bdd+cucumber+cloture | **CAP-TRANSCRIPT US-5 Cucumber BDD ** (4 scénarios : TEMPLATE strategy, LLM strategy mocked, empty plan, skip-on-existing-output) + runner dédié  + étapes . Tous les scénarios passent : BUILD SUCCESSFUL. EPIC CAP-TRANSCRIPT maintenant à 6/6 US terminées. | 9/10
| 118 | 2026-08-14 | feat+ddd+tdd | **CAP-TRANSCRIPT US-4 tâche `generateCapsuleTranscript` + wiring** — Domaine `capsule.transcript` (11ème) enrichi avec `GenerateCapsuleTranscriptTask.kt` (tâche Gradle `@DisableCachingByDefault` : groupe `generate`, `@InputFile speakerNotesFile` `@PathSensitive(RELATIVE)`, `@Input language`, `@Input strategy: Property<TranscriptStrategy>`, `@OutputFile transcriptOutput`, `@Optional @ServiceReference llmService: Property<LlmBuildService>`. TaskAction : skip si output existe ET non-blank (économie d'encre) ; check speakerNotesFile.exists() message actionable "Run generateCapsuleContent first" ; deckName = filename removeSuffix("-speaker-notes") ; parse `AsciidocSpeakerNoteParser` → `CapsuleScript` → `TranscriptPlan(deckName, segments, language, outputPath)` ; TEMPLATE → `TranscriptBuilder.build(plan)` ; LLM → `project.aiProvider` + `project.resolveModel(provider, llmService)` + `ChatModelTranscriptEnhancer(model).enhance(template, plan)` ; write + log `CAPSULE TRANSCRIPT →`). Wiring `CapsuleManager.registerGenerateTranscriptTask` (après `registerGenerateCapsuleContentTask`, groupe generate, description i18n, `dependsOn("generateCapsuleContent")`, conventions language `deck.language`?:`fr`, strategy extension?:CLI, speakerNotesFile 1er `*-speaker-notes.adoc` dans build/capsule/, transcriptOutput map `capsule/<deckName>-transcript.adoc`, llmService + usesService). Décision GREEN : dependsOn préservé en prod (conforme cadrage S-114), tests utilisent `-x generateCapsuleContent` (pré-écrivent les speaker notes, standalone + zéro LLM involontaire + évite erreur config `deckFile` sans deck). Clé `task.generateCapsuleTranscript.description` 24ème ajoutée 11 `Messages*.properties` (base + en + fr/zh/hi/es/ar/bn/pt/ru/ur). Baby-step TDD strict RED (3 functional → `UnexpectedBuildFailure` task not found) → GREEN (1 fichier prod + wiring + `-x` tests, 3/3 PASSED) → REFACTOR (`:functionalTest` BUILD SUCCESSFUL 4m38s 67 tests 0 fail ; `check` BUILD SUCCESSFUL 26m03s, 917 unit VERT baseline S-117 préservée, 67 functional VERT +3 net baseline 64 → 67, cucumber dédiés GREEN baseline préservée — cucumberTest global SKIPPED par CR-10, 13 FAIL rapport global = artefact NoTestsDiscovered bug S-082 hors périmètre, Kover VERT, validatePlugins VERT, zéro régression, zéro secret, zéro bump BOM). 1 fichier prod créé + 1 wiring modifié + 1 test créé + 11 i18n modifiés. EPIC CAP-TRANSCRIPT 5/6 US (US-0 ✅ S-114, US-1 ✅ S-115, US-2 ✅ S-116, US-3 ✅ S-117, US-4 ✅ S-118, US-5 TODO). | 9/10
| 117 | 2026-08-14 | feat+ddd+tdd | **CAP-TRANSCRIPT US-3 TranscriptLlmEnhancer port + ChatModelTranscriptEnhancer adapter** — Domaine `capsule.transcript` (11ème) enrichi avec `TranscriptLlmEnhancer.kt` (interface port sync `fun enhance(template: String, plan: TranscriptPlan): String`, pattern `CapsuleLlm` — le domaine reste Gradle-free/coroutine-free, unit-testable avec un plain fake, l'adapter bridge langchain4j vit hors du domaine) + `ChatModelTranscriptEnhancer.kt` (adapter `ChatModel` langchain4j pattern `ChatModelCapsuleLlm` — `model.chat(UserMessage.from(prompt)).aiMessage().text()`, prompt construit via `TranscriptPromptBuilder.buildEnhancePrompt`). Fallback degraded pattern `AudioPostProcessor.process` : LLM blank/whitespace → template original (économie d'encre, le caller garde un AsciiDoc valide). Short-circuit si template blank (no LLM call). Baby-step TDD strict RED (6 tests → `Unresolved reference 'ChatModelTranscriptEnhancer'` × 5 sites) → GREEN (2 fichiers prod créés) → REFACTOR (`check` BUILD SUCCESSFUL 23m33s, 917 unit VERT +6 net baseline S-116 911 → 917, 64 functional VERT baseline préservée, 13 cucumber dédiés GREEN baseline préservée, Kover VERT, validatePlugins VERT, zéro régression, zéro secret, zéro bump BOM). 6 unit tests créés (`TranscriptLlmEnhancerTest` + FakeChatModel minimal override `doChat` langchain4j 1.16.3 single abstract entry point). 2 fichiers prod créés + 1 fichier test créé (zéro fichier existant modifié). EPIC CAP-TRANSCRIPT 4/6 US (US-0 ✅ S-114, US-1 ✅ S-115, US-2 ✅ S-116, US-3 ✅ S-117, US-4→5 TODO). | 9/10
| 116 | 2026-08-13 | feat+ddd+tdd | **CAP-TRANSCRIPT US-2 TranscriptPlan + TranscriptBuilder + TranscriptPromptBuilder** — Domaine `capsule.transcript` (11ème) enrichi avec `TranscriptPlan.kt` (data class immutable 4 champs `deckName` + `segments: List<SlideSegment>` + `language` + `outputPath`, invariants fail-fast `isNotBlank` × 3, segments vide autorisé, pattern `CapsuleVideoPlan` CAP-29.2 `capsule.multilang`) + `TranscriptBuilder.kt` (object pur `build(plan): String` — template AsciiDoc déterministe `= <deckName>` level-0 + `:language: <lang>` attribute + `INTRO` boilerplate const + une section `== <title>` par segment HTML avec speakerNote body + `OUTRO` boilerplate const, MANIM segments skipped `if (segment.type == SlideType.MANIM) continue`, constantes `INTRO`/`OUTRO` publiques `const val` testabilité, pattern `CapsuleScriptWriter` object pur `write(script): String`) + `TranscriptPromptBuilder.kt` (object pur `buildEnhancePrompt(plan): String` — prompt pédagogique enrichissement LLM embed `TranscriptBuilder.build(plan)` + rules keep `== ` level-2 headings + preserve speaker note content + add pedagogical transitions + add intro+outro paragraphs + respond AsciiDoc only, pattern `DefaultCapsulePromptBuilder`). Baby-step TDD strict RED (23 tests → `Unresolved reference 'TranscriptPlan'`/`'TranscriptBuilder'`/`'TranscriptPromptBuilder'` 15 errors) → GREEN (3 fichiers prod créés) → REFACTOR (`check` BUILD SUCCESSFUL 22m49s, 911 unit VERT +23 net baseline S-115 888 → 911, 64 functional VERT baseline préservée, 13 cucumber dédiés GREEN baseline préservée, Kover VERT, validatePlugins VERT, zéro régression, zéro secret, zéro bump BOM). 23 unit tests créés (7 `TranscriptPlanTest` + 11 `TranscriptBuilderTest` + 5 `TranscriptPromptBuilderTest`). 3 fichiers prod créés + 3 fichiers test créés (zéro fichier existant modifié). `publishToMavenLocal` VERT. EPIC CAP-TRANSCRIPT 3/6 US (US-0 ✅ S-114, US-1 ✅ S-115, US-2 ✅ S-116, US-3→5 TODO). | 9/10
| 115 | 2026-08-13 | feat+ddd+tdd | **CAP-TRANSCRIPT US-1 TranscriptConfig + TranscriptStrategy enum + merger 4 sources** — Domaine `capsule.transcript` (11ème) créé avec `TranscriptConfig.kt` (data class 2 champs `enabled: Boolean = false` + `strategy: TranscriptStrategy = TEMPLATE`, defaults backward compat) + `TranscriptStrategy` enum type-safe 2 valeurs `TEMPLATE`/`LLM` companion `fromString` case-insensitive fallback `TEMPLATE` (pattern `OutputFormat`/`CaptureStrategy`). `CapsuleConfig` 11ème section `transcript`. `CapsuleConfigMerger` `mergeTranscriptConfig` (pattern `mergeValidationConfig` — `mergeBoolean` enabled + `mergeTranscriptStrategy` strategy) + `loadFromEnvironment` (`CAPSULE_TRANSCRIPT_ENABLED`/`_STRATEGY`) + `buildConfigFromProperties` (`capsule.transcript.enabled`/`.strategy`) + `mergeTranscriptStrategy` helper enum (pattern `mergeVersioning`/`mergeOutputFormat` — CLI > YAML > props). `CapsuleExtension` 2 properties (`transcriptEnabled` convention `false` + `transcriptStrategy` convention `TEMPLATE` + helper Groovy `transcriptStrategy(String)` case-insensitive) + `CapsuleConventions` 2 champs + `pushConfigIntoExtension` transcript block (Variant B `isPresent` + equality) après AudioPost block. Baby-step TDD strict RED (22 tests → `Unresolved reference 'TranscriptStrategy'`/`'TranscriptConfig'` 14 errors) → GREEN (TranscriptConfig.kt + CapsuleConfig + Merger + Models + Plugin) → REFACTOR (`check` BUILD SUCCESSFUL 18m53s, 888 unit VERT +22 net baseline S-113 866 → 888, 64 functional VERT baseline préservée, 13 cucumber dédiés GREEN baseline préservée, Kover VERT, validatePlugins VERT, zéro régression, zéro secret, zéro bump BOM). 22 unit tests créés (7 `TranscriptStrategyTest` + 5 `TranscriptConfigTest` + 10 merger transcript `CapsuleConfigMergerTest`). 5 fichiers prod modifiés + 3 fichiers test (2 nouveaux + 1 modifié). EPIC CAP-TRANSCRIPT 2/6 US (US-0 ✅ S-114, US-1 ✅ S-115, US-2→5 TODO). | 9/10
| 114 | 2026-08-13 | cadrage+gouvernance | **CAP-TRANSCRIPT US-0 Cadrage EPIC** (gouvernance pure, zéro code) — Audit code : `capsule.feed` (`SlideSegment`/`CapsuleScript`/`AsciidocSpeakerNoteParser`/`CapsuleScriptReader` round-trip), `capsule.pipeline` (`CapsuleLlm` port sync + `ChatModelCapsuleLlm` adapter + `DefaultCapsulePromptBuilder` port pattern + `CapsuleState` + `TtsScriptDeriver`), `GenerateCapsuleContentTask` (`@OutputFile speakerNotesOutput` source transcript), `CapsuleConfig` 10 sections, `CapsuleConfigMerger` 5 helpers scalaires + 3 helpers enum, `CapsuleExtension` 48 properties + `CapsuleConventions` mirror, `CapsulePlugin.pushConfigIntoExtension` 10 blocks guards, `CapsuleManager` 14 registrations + 5 `resolve*` factories, `CapsuleMessages` 23 keys + 10 `Messages_*.properties`, 14 `CucumberTaskSpec`. 12 décisions architecturales : (1) domaine `capsule.transcript` 11ème ; (2) `TranscriptConfig` 11ème section 2 champs `enabled=false`/`strategy=TEMPLATE` defaults backward compat ; (3) `TranscriptStrategy` enum 2 valeurs TEMPLATE/LLM défaut TEMPLATE (pas de LLM requis) ; (4) `TranscriptPlan` data class immutable consomme `capsule.feed.SlideSegment` (pas nouveau modèle) ; (5) `TranscriptBuilder` object pur `build(plan): String` template AsciiDoc (titre + intro + sections `== title` + speakerNote body + outro) ; (6) `TranscriptLlmEnhancer` port + `ChatModelTranscriptEnhancer` adapter pattern `CapsuleLlm` + fallback degraded template original si LLM blank ; (7) `TranscriptPromptBuilder` object pur ; (8) tâche `generateCapsuleTranscript` groupe `generate` `@OutputFile` dependsOn `generateCapsuleContent` économie d'encre skip ; (9) 4 sources DSL `transcriptEnabled`/`transcriptStrategy` ; (10) pas de factory `resolve*` (object pur + in-task `resolveModel`) ; (11) runner dédié `CapsuleTranscriptCucumberRunner` 15ème `CucumberTaskSpec` ; (12) `CapsuleMessages` 24ème key groupe `generate` réutilisé. EPIC redécoupé 6 US ~12 pts (US-0 ✅, US-1 config+enum+merger, US-2 plan+builder+prompt, US-3 llm enhancer, US-4 tâche+wiring, US-5 cucumber). MàJ BACKLOG (section CAP-TRANSCRIPT refondue 12 décisions + 6 US détaillées + synthèse 48 US/90 pts), INDEX (en-tête + roadmap + table sessions), PROMPT_REPRISE, AGENTS racine. Pattern S-057/S-063. | 9/10
| 113 | 2026-08-13 | cadrage+feat+tdd+bdd+cloture | **EPIC CAP-27 VTT burn-in TERMINÉ 3/3 US** — Constat prémisse caduque : compat VTT **déjà fonctionnelle** (test réel ffmpeg 6.1.1, filtre `subtitles=` auto-détecte `.vtt` par extension, exit 0 WebM valide). Gap = couverture test. US-0 cadrage (8 décisions). US-1 `SubtitleBurnInCommand` object pur (pattern `AudioPostCommand`) `buildArgv(...)` + refactor `SubtitleBurnInServiceImpl.burnIn` (argv identique backward compat) + 13 unit tests SRT/VTT (RED 13 `Unresolved reference` → GREEN 13/13 → REFACTOR, 866 unit VERT 0 FAIL +9 net). US-2 Cucumber BDD 2 scénarios `@subtitles @burnin @vtt` dans `capsule.feature` (feature existante → pas de nouveau runner S-082) + 2 step defs VTT uniques (S-088) : non-integration PASSED 32s + E2E `@integration` real ffmpeg PASSED 32s (WebM valide EBML + `.vtt` + burn-in success). `cucumberTestIntegration` 256 tests 0 failures. `check` BUILD SUCCESSFUL 12m44s, Kover VERT, 64 functional VERT, zéro régression (13 failures cucumberTest préexistantes NoTestsDiscoveredException dedicated runners bug S-082), zéro secret, zéro bump BOM, zéro changement DSL. 5 fichiers code + 5 gouvernance. EPIC clos en 1 session. | 9/10
| 112 | 2026-08-13 | feat+bdd+cucumber+cloture | **CAP-GLOSSARY US-3 Cucumber BDD + clôture EPIC CAP-GLOSSARY TERMINÉ 4/4 US** — Feature `capsule_glossary_context.feature` `@context @glossary` (3 scénarios : glossary injecté → section `==== Official Glossary (glossary)` + termes `competence transversale`/`evaluation formative` présents + bullet `unrelated` ignoré ; absent → fallback no-op ; malformed sans `== Glossary` → gracefully skipped no-op). Runner dédié `CapsuleGlossaryContextCucumberRunner` (pattern S-082 `@SelectClasspathResource("features/capsule_glossary_context.feature")` + filter `@context and @glossary and not @wip and not @integration` + glue `capsule.scenarios` + plugin pretty/html/json reports). `CapsuleGlossaryContextSteps` (10 step defs préfixés "glossary context", pattern `CapsuleScenarioContextSteps` mirror — bug S-088 glue `capsule.scenarios` partagée, step texts uniques). `CucumberTaskSpec("cucumberTestGlossaryContext")` 14ème câblé `build.gradle.kts` (13ème cucumber dédié, 14ème `CucumberTaskSpec` total avec integration). Baby-step BDD RED → GREEN (3/3 scénarios PASSED en 55s, BUILD SUCCESSFUL) → REFACTOR (`./gradlew check` BUILD SUCCESSFUL in 28m52s, 857 unit VERT baseline S-111 préservée — US-3 pure BDD zéro production modifié, 64 functional VERT baseline S-111 préservée, `cucumberTest` global SKIPPED par CR-10, Kover VERT, validatePlugins VERT, zéro régression, zéro secret). Domaine `capsule.context` clos (13 fichiers prod : 3 loaders `DocContextLoader`/`PedagogicalScenarioLoader`/`GlossaryLoader`). EPIC CAP-GLOSSARY TERMINÉ 4/4 US (US-0 cadrage S-109, US-1 data classes+loader+builder+prompt S-110, US-2 wiring+functional S-111, US-3 cucumber S-112). | 9/10
| 111 | 2026-08-12 | feat+ddd+tdd | **CAP-GLOSSARY US-2 Wiring `CollectCapsuleAugmentedContextTask` + `ContextConfig` + merger 4 sources + `CapsuleExtension` + `CapsuleManager` + provenance tracking** — `ContextConfig` 3ème champ `glossaryFile: String? = null` (pattern `scenarioFile`). `CapsuleConfigMerger.mergeContextConfig` étendu `mergeStr` pour `glossaryFile` + `loadFromEnvironment` (`CAPSULE_CONTEXT_GLOSSARY_FILE`) + `buildConfigFromProperties` (`capsule.context.glossaryFile`). `CapsuleExtension.glossaryFile: Property<String>` convention `""`. `pushConfigIntoExtension` push `glossaryFile` (guard `isPresent || blank`, pattern `scenarioFile` L124-126). `CollectCapsuleAugmentedContextTask.glossaryFile: ConfigurableFileCollection @InputFiles @PathSensitive(RELATIVE) @Optional` + `resolveGlossarySection(budget)` (5% budget = 400 tokens/8000, `GlossaryLoader.load(target, glossaryBudget)`, pattern `resolveScenarioSection` L281-296) + `trackSources` étendu tracking GLOSSARY (1 `ProvenanceSource` si `glossaryTarget` existe, pattern SCENARIO L230-249) + appel `CapsuleContextBuilder.build()` étendu `glossarySection = resolveGlossarySection(budget)`. `CapsuleManager` wiring lazy provider `glossaryFile.from(project.provider {...})` (extension afterEvaluate > CLI, pattern `scenarioFile` L229-237). Baby-step TDD strict RED (1 FAIL + 1 passed par accident) → GREEN (2/2 `GlossaryContextFunctionalTest` PASSED — glossary fourni → `==== Official Glossary (glossary)` + termes `compétence transversale`/`évaluation formative` présents + bullet `unrelated` de section non-Glossary ignoré ; absent → pas de section glossary backward compat) → REFACTOR (`check` BUILD SUCCESSFUL in 26m25s, 857 unit VERT inchangé baseline S-110, 64 functional VERT +2 net, Kover VERT, validatePlugins VERT, zéro régression, zéro secret). Backward compat strict (`glossaryFile = null` défaut → `resolveGlossarySection` retourne `""` → tests existants inchangés). EPIC CAP-GLOSSARY 3/4 US (reste US-3 cucumber 3 scénarios). | 9/10
| 110 | 2026-08-12 | feat+ddd+tdd | **CAP-GLOSSARY US-1 `GlossaryEntry` + `GlossaryContext` + `GlossaryLoader` + `CapsuleContext.glossarySection` + Builder + PromptBuilder étendus** — Domaine `capsule.context` étendu (3 nouveaux fichiers prod : `GlossaryEntry.kt` data class immutable invariant fail-fast blank term/definition, `GlossaryContext.kt` data class immutable liste ordonnée, `GlossaryLoader.kt` object pur mirror `PedagogicalScenarioLoader` — `load(glossaryFile: File, maxTokens: Int): String` parse AsciiDoc `== Glossary` + bullets `- term: definition` split premier `:`, rend `==== Official Glossary (glossary)`, tronque via `ContextChannel.Docs.truncateToTokens(maxTokens)`, missing file → empty, malformed bullet skipped, non-Glossary section ignored, zero budget → empty). `CapsuleContext` 4ème champ `glossarySection: String = ""` invariant ajusté backward compat. `CapsuleContextBuilder.build()` 4ème paramètre `glossarySection` + `buildRendered` refactor `when` 4 cas → `listOf(...).filter { isNotBlank }.joinToString("\n\n")` 3 sections conditionnelles + `tracker.retainOnly` étendu `+= GLOSSARY_CHANNEL`. `DefaultCapsulePromptBuilder` instruction "Use the official terminology" conditionnelle `contains("Official Glossary")` pattern `Pedagogical Scenario`. `ContextProvenance.GLOSSARY_CHANNEL` const. `CollectCapsuleAugmentedContextTask` appel `build()` migré arguments nommés. Baby-step TDD strict RED (23 tests → `Unresolved reference`) → GREEN (857 unit VERT +23 net : 15 `GlossaryLoaderTest` + 7 `CapsuleContextBuilderTest` glossarySection + 2 `DefaultCapsulePromptBuilderTest` glossary) → REFACTOR (`check` BUILD SUCCESSFUL in 18m48s, Kover VERT, validatePlugins VERT, 62 functional VERT inchangé, zéro régression, zéro secret). Backward compat strict. EPIC CAP-GLOSSARY 2/4 US. | 9/10
| 109 | 2026-08-12 | cadrage+gouvernance | **CAP-GLOSSARY US-0 Cadrage EPIC Injection terminologique** — Audit code 16 constats (`CapsuleContextBuilder.build` L69-92 3 params optionnels, `CapsuleContext` 3 champs invariant, `CollectCapsuleAugmentedContextTask` `scenarioFile`/`resolveScenarioSection` 5% budget/`trackSources` 5 canaux, `ContextConfig` 2 champs, `CapsuleConfigMerger.mergeContextConfig` `mergeStr`, `DefaultCapsulePromptBuilder` instruction conditionnelle `Pedagogical Scenario`, `ContextProvenance.SCENARIO_CHANNEL`, `ProvenanceTracker.retainOnly`, `CapsuleExtension.scenarioFile`, `pushConfigIntoExtension` scenarioFile push, `CapsuleManager` scenarioFile provider, 13 `CucumberTaskSpec`, 20 features, 11 `Messages*.properties`) + 12 décisions architecturales (extension capsule-local `glossarySection` pas 6ème canal N0 pattern CAP-SPD-2, `GlossaryEntry`/`GlossaryContext` data classes immutable liste ordonnée, `GlossaryLoader` object pur mirror `PedagogicalScenarioLoader` format AsciiDoc `== Glossary` + bullets, budget 5% = 400 tokens, `CapsuleContextBuilder.build()` 4ème paramètre `glossarySection` + `tracker.retainOnly` étendu `+= GLOSSARY_CHANNEL`, `DefaultCapsulePromptBuilder` instruction `Official Glossary` conditionnelle, provenance `GLOSSARY_CHANNEL` const + `trackSources` étendu, 4 sources `context.glossaryFile`, pas de nouvelle tâche Gradle ni clé CapsuleMessages, économie d'encre `@InputFiles @Optional` UP-TO-DATE, runner dédié `CapsuleGlossaryContextCucumberRunner` + `CucumberTaskSpec("cucumberTestGlossaryContext")` 14ème). 4 US ~6 pts (US-0 ✅, US-1 data classes + loader + builder + prompt builder ~10 unit, US-2 wiring + 2 functional, US-3 cucumber 3 scénarios). Zéro code modifié — gouvernance pure pattern S-057/S-063/S-076/S-083/S-092/S-094/S-097/S-102/S-107. MàJ BACKLOG/INDEX/PROMPT_REPRISE/TODO. | 9/10
| 108 | 2026-08-11 | feat+ddd+tdd+bdd+cucumber+cloture | **CAP-PROVENANCE US-1 + US-2 + Clôture EPIC CAP-PROVENANCE (3/3 US TERMINÉ)** — US-1 : `ProvenanceSource`/`ProvenanceChannel`/`ContextProvenance`/`ProvenanceTracker` (4 fichiers prod domaine `capsule.context`) + `CapsuleContextBuilder.build()` étendu `tracker: ProvenanceTracker? = null` (prune aux canaux survivants `it.type.name` + `SCENARIO_CHANNEL`, backward compat null). Jackson `totalChars`/`totalTokens` `@get:JsonProperty` (les `fun` ne sérialisent pas). 15 unit (12 `ContextProvenanceTest` + 3 builder). US-2 : `CollectCapsuleAugmentedContextTask` `@OutputFile provenanceFile` + `trackSources` 5 canaux (EAGER `.filter{it.exists()}` N sources, RAG/Graphify injected, DOCS files/legacy, SCENARIO compagnon `.adoc`) + log `CAPSULE PROVENANCE →` + 2 functional + 3 cucumber `capsule_provenance.feature` + runner dédié + `CucumberTaskSpec` 13ème. 2 fix TDD : missing files skipped réel + scenarioFile répertoire. `check` BUILD SUCCESSFUL in 15m52s : 834 unit + 62 functional + 3 cucumber GREEN, zéro régression. EPIC CAP-PROVENANCE TERMINÉ 3/3 US, domaine `capsule.context` clos (10 fichiers prod). | 9/10
| 107 | 2026-08-11 | cadrage+gouvernance | **CAP-PROVENANCE US-0 Cadrage EPIC audit context anti-hallucination** — Audit code (`CapsuleContextBuilder.build` L60-75, `CollectCapsuleAugmentedContextTask.run` L98-132, `ContextChannel` N0 sealed pas de source file, `ChannelBudget` pas de per-source, `ContextConfig` 2 champs, `CapsuleConfigMerger`/`CapsuleExtension`/`CapsuleConventions` pas de provenance, `CapsuleMessages` 31 keys, Jackson déjà dépendance, 12 `CucumberTaskSpec`), 11 décisions architecturales (pas de nouveau domaine, data classes immutable, `ProvenanceTracker` mutable accumulator + snapshot, builder tracker optional backward compat, wiring `@OutputFile provenanceFile` always-on cheap pas de config/DSL, économie d'encre UP-TO-DATE, JSON INDENT_OUTPUT, runner dédié + steps préfixés + `CucumberTaskSpec` 13ème, log 2ème ligne). Zéro code modifié — gouvernance pure. INDEX/BACKLOG/PROMPT_REPRISE/TODO mis à jour. | 9/10
| 106 | 2026-08-11 | feat+bdd+cucumber+cloture | **CAP-AUDIO US-4 Cucumber BDD + clôture EPIC CAP-AUDIO (5/5 US)** — Feature `capsule_audio_post.feature` `@audio @post` (4 scénarios : default NoOp skip économie d'encre, BGM enabled NoOp → degraded, ducking only NoOp → degraded, ducking + BGM NoOp → degraded) + `CapsuleAudioPostSteps` (13 step defs préfixés "audio post", pattern `CapsuleCaptureStrategySteps` — étape interne `generateCapsuleVideo`, pas tâche standalone — `applyAudioPostIfEnabled` est câblée dans `CapsuleVideoTask` post-burnIn pre-convertFormat) + `CapsuleAudioPostCucumberRunner` (pattern S-082 `@SelectClasspathResource("features/capsule_audio_post.feature")` + filter `@audio and @post and not @wip and not @integration` + glue `capsule.scenarios` + plugin pretty/html/json reports `cucumber-audio-post.html/.json`) + `CucumberTaskSpec("cucumberTestAudioPost")` 12ème câblé `build.gradle.kts` dans `cucumberConventions.additionalTasks` (11ème cucumber dédié, 12ème `CucumberTaskSpec` total avec `cucumberTestIntegration`). Position : après `cucumberTestDurationValidation`, avant `cucumberTestIntegration`. NoOp processor (pas de vrai FFmpeg — `ffmpegExecutablePath = "noop"` routes through `NoOpAudioPostProcessor` which returns `false`, keeping original video) — valide le wiring + economy-of-ink guard + factory dispatch, pas le rendu audio (couvert par US-2 unit tests `AudioPostProcessorTest` 17 unit). Baby-step BDD RED → GREEN → REFACTOR : RED (feature + steps + runner + CucumberTaskSpec écrits) → GREEN (4/4 scénarios PASSED en 7m21s, BUILD SUCCESSFUL) → REFACTOR (`./gradlew check` BUILD SUCCESSFUL in 24m12s, 819 unit VERT baseline S-105 préservée, 60 functional VERT baseline S-105 préservée, `validatePlugins` VERT, Kover VERT, zéro régression, zéro secret). EPIC CAP-AUDIO TERMINÉ 5/5 US (US-0 cadrage S-102, US-1 config+merger S-103, US-2 processor+factory S-104, US-3 wiring S-105, US-4 cucumber S-106). Domaine `capsule.audio` (10ème) clos (6 fichiers prod : `AudioPostConfig`, `AudioPostProcessor` interface, `AudioPostProcessorImpl`, `NoOpAudioPostProcessor`, `AudioPostCommand`, `AudioPostException`). | 9/10
| 105 | 2026-08-11 | feat+ddd+tdd | **CAP-AUDIO US-3 Wiring `CapsuleVideoTask.applyAudioPostIfEnabled` + câblage** — Nouvelle méthode `internal fun applyAudioPostIfEnabled(finalVideo: File)` (20 lignes, pattern `burnInSubtitlesIfEnabled` + `convertFormatIfEnabled`) : skip si `!bgmEnabled && !duckingEnabled` (économie d'encre), skip si `finalVideo` n'existe pas → warn + return, factory `resolveAudioPostProcessor(ffmpegPath, strict)` (5ème), log lifecycle si non-NoOp, `AudioPostConfig` depuis `capsuleExtension.audioPost*` (5 champs), `processor.process(finalVideo, tmpFile, config)` → si succès rename tmpFile → finalVideo + log, si échec warn + garde original (degraded), `catch (AudioPostException)` warn + garde original, `finally` delete tmpFile. Câblage `captureDeckSequential` L493 (post-burnIn, pre-convertFormat) + `captureDeckParallel` L524 (même position). Imports `capsule.audio.AudioPostConfig` + `NoOpAudioPostProcessor`. Baby-step TDD RED → GREEN → REFACTOR : RED (3 functional, 2 failed: log absent, 1 passed par accident) → GREEN (3/3 `AudioPostFunctionalTest` PASSED 2m37s) → REFACTOR (`check` BUILD SUCCESSFUL 9m20s). 819 unit VERT (inchangé), 60 functional VERT (+3 net), zéro régression, zéro secret. | 9/10
| 104 | 2026-08-11 | feat+ddd+tdd | **CAP-AUDIO US-2 `AudioPostProcessor` + Impl + NoOp + Exception + `AudioPostCommand` + factory** — Domaine `capsule.audio` enrichi (4 fichiers prod + 1 exception). `AudioPostProcessor` interface (`process(video, output, config): Boolean` degraded + `isAvailable()` + `name()`) — pattern `VideoFormatConverter`. `AudioPostProcessorImpl` FFmpeg `loudnorm=I=-16:TP=-1.5:LRA=11` EBU R128 + `volume + amix=inputs=2` BGM + `sidechaincompress=threshold=0.1:ratio=4:attack=5:release=300` ducking (ducking sans BGM ignoré — meaningless). `NoOpAudioPostProcessor` returns `false` (degraded garde original, pattern `NoOpVideoFormatConverter`). `AudioPostCommand` object pur (pattern `FormatConversion`) — `buildFilterGraph(config, hasBgmInput): String` (loudness-only / loudness+BGM / loudness+BGM+ducking / ducking-without-BGM ignored) + `buildArgv(videoFile, bgmFile, outputFile, config, ffmpegPath): List<String>` (BGM = 2ème `-i` + `-filter_complex` + `-map 0:v -map [aout]`, no-BGM = `-af` loudnorm only, `-c:v copy -c:a libvorbis -shortest`). `CapsuleManager.resolveAudioPostProcessor(ffmpegPath, strict)` 5ème factory `resolve*` (pattern `resolveFormatConverter` — noop→NoOp, unavailable+non-strict→NoOp, unavailable+strict→throw via `StrictModeGuard`, available→Impl). Baby-step TDD RED → GREEN → REFACTOR : RED (17 tests → `Unresolved reference 'AudioPostProcessorImpl'`/`'NoOpAudioPostProcessor'`/`'resolveAudioPostProcessor'`) → GREEN (4 fichiers prod + factory `CapsuleManager`) → REFACTOR (2 itérations : `buildArgv` pure ne vérifie pas existence BGM — laissé à Impl, test `assertEquals(1, ...)` corrigé 2→1 `-i` flags no-BGM). 819 unit VERT (+17 net : 5 `buildFilterGraph` + 2 `buildArgv` + 4 Impl + 2 NoOp + 4 factory), zéro régression, zéro secret. | 9/10
| 103 | 2026-08-11 | feat+ddd+tdd | **CAP-AUDIO US-1 `AudioPostConfig` + merger 4 sources** — Nouveau domaine `capsule.audio` (10ème) créé avec `AudioPostConfig` data class 5 champs (`bgmEnabled: Boolean = false`, `bgmFile: String = ""`, `bgmLevel: Double = -18.0`, `loudnessTarget: Double = -16.0`, `duckingEnabled: Boolean = false`), KDoc EBU R128 + backward compat. Section `audioPost` 10ème dans `CapsuleConfig` (import `capsule.audio.AudioPostConfig`). `CapsuleConfigMerger` : `merge()` étendu `audioPost = mergeAudioPostConfig(...)` + `loadFromEnvironment` 5 ENV vars `CAPSULE_AUDIO_POST_*` + `buildConfigFromProperties` 5 props `capsule.audioPost.*` + `mergeAudioPostConfig` (pattern `mergeValidationConfig` — `mergeBoolean` bgmEnabled/duckingEnabled, `mergeStr` bgmFile avec env fallback, `mergeDouble` bgmLevel/loudnessTarget). `CapsuleModels` : `CapsuleExtension` +5 properties + `CapsuleConventions` +5 champs + instance `conventions`. `CapsulePlugin.pushConfigIntoExtension` section AudioPost 5 guards (pattern `durationValidation`). Baby-step TDD RED → GREEN → REFACTOR : RED (4 `AudioPostConfigTest` + 12 merger `audioPost` → `Unresolved reference: AudioPostConfig`) → GREEN (AudioPostConfig.kt + CapsuleConfig + Merger + Models + Plugin) → REFACTOR (802 unit VERT +20 net, `CapsulePluginFunctionalTest` VERT, zéro régression, zéro secret). | 9/10
| 102 | 2026-08-11 | cadrage+gouvernance | **CAP-AUDIO US-0 Cadrage EPIC post-production audio** — Audit code : `CapsuleVideoTask.captureDeckSequential` (L461-498) + `captureDeckParallel` (L500-529) — point d'insertion entre `burnInSubtitlesIfEnabled` (L492/523) et `convertFormatIfEnabled` (L493/524) ; `SubtitleBurnInService` (interface + Impl + NoOp + factory + `BurnInException`) pattern de référence ; `VideoFormatConverter` (interface + Impl + NoOp + factory, retourne Boolean degraded) 2ème pattern ; `CapsuleConfig` 9 sections pas de `audioPost` ; `CapsuleConfigMerger` 9 mergeXxxConfig + 5 helpers scalaires + 3 helpers enum ; `CapsuleExtension`/`CapsuleConventions`/`pushConfigIntoExtension` pas de audioPost ; `CapsuleManager` 4 factories resolve* ; `StrictModeGuard.requireAvailable` 7 branchés ; `MediaProbeUtil.probeDuration` réutilisable ; 11 `CucumberTaskSpec` ; `CapsuleMessages` 31 keys pas de audioPost ; `mixAudioWithVideo` (L891-932) TTS brut — CAP-AUDIO intervient après + après burn-in, ne remplace pas. 12 décisions architecturales : (1) domaine `capsule.audio` 10ème ; (2) `AudioPostConfig` 5 champs defaults backward compat ; (3) section `audioPost` 10ème `CapsuleConfig` ; (4) 4 sources `mergeAudioPostConfig` ; (5) `AudioPostProcessor.process()` retourne Boolean degraded + `AudioPostException` ; (6) `AudioPostProcessorImpl` FFmpeg loudnorm EBU R128 + amix BGM + sidechaincompress ducking ; (7) `NoOpAudioPostProcessor` ; (8) factory `resolveAudioPostProcessor` 5ème ; (9) wiring `applyAudioPostIfEnabled` post-burnIn pre-convertFormat ; (10) `CapsuleExtension` 5 properties + conventions + push ; (11) économie d'encre skip si disabled/missing ; (12) runner dédié `CapsuleAudioPostCucumberRunner` + `CucumberTaskSpec("cucumberTestAudioPost")` 12ème + pas de nouvelle tâche Gradle (CAP-AUDIO est une étape de `CapsuleVideoTask`). 5 US ~12 pts. Zéro code modifié, `compileKotlin` VERT. | 9/10
| 101 | 2026-08-11 | feat+ddd+tdd+bdd+ci+cloture | **CAP-CR3-1 TERMINÉ 5/5 US — CI `full` + probe durée** — US-0 cadrage (10 décisions). US-1 `VideoDurationValidator` (domaine `capsule.validation` 9ème) + `ValidationResult` sealed (Valid/Invalid, pattern `DeckContextValidator` slider SLD-8.3, `message()` Locale.US) — 10 unit GREEN. US-2 `ValidationConfig(durationEnabled=false, toleranceSecs=2.0)` 9ème section `CapsuleConfig` + `mergeValidationConfig` 4 sources + `CapsuleExtension` 2 properties + tâche `validateCapsuleVideoDuration` (groupe `verification`, `@Internal videoFile`, `@InputFiles @PathSensitive(RELATIVE) audioFiles`, skip si disabled/missing, `throw GradleException` si Invalid) + wiring `CapsuleManager` + `CapsuleMessages` — 5 unit + 3 functional GREEN. US-3 Cucumber `capsule_duration_validation.feature` 3 scénarios + runner dédié `CapsuleDurationValidationCucumberRunner` + `CucumberTaskSpec("cucumberTestDurationValidation")` 11ème — 3/3 GREEN. US-4 Workflow CI `full-tests.yml` (installe Playwright+ffmpeg+espeak, invoque validation). 782 unit (+10) + 57 functional (+3) + 11 cucumber dédiés (+1) GREEN. `check` BUILD SUCCESSFUL. | 9/10
| 100 | 2026-08-11 | feat+bdd+cucumber+cloture | **CAP-MP4 US-3 Cucumber BDD + clôture EPIC CAP-MP4 (4/4 US TERMINÉ)** — `capsule_format.feature` 3 scénarios `@distrib @format` (Default WEBM distributes only WebM, MP4 distributes only MP4, BOTH distributes WebM+MP4) + runner dédié `CapsuleFormatCucumberRunner` (pattern S-082 `@SelectClasspathResource("features/capsule_format.feature")` + filter `@distrib and @format and not @wip and not @integration`) + `CapsuleFormatSteps` (6 step defs self-contained GradleRunner, pattern `CapsuleDistribSteps` mirror, préfixés "format distribution" — bug S-088 glue `capsule.scenarios` partagée, step texts uniques "format distribution copies video file" vs distrib "the video file is copied") + `CucumberTaskSpec("cucumberTestFormat")` câblé `build.gradle.kts` (9ème, 10 cucumber dédiés total). NoOp converter (pas de vrai FFmpeg — valide la distribution filter, pas le transcode déjà couvert US-2). Baby-step BDD RED → GREEN → REFACTOR : RED (6 tests failed, undefined steps) → GREEN (3/3 scénarios PASSED, 12 steps, 1m22s) → REFACTOR (`check` BUILD SUCCESSFUL 15m38s). 772 unit VERT (baseline S-099 préservée) + 54 functional VERT (baseline préservée) + 10 cucumber dédiés GREEN (66 tests cucumber dédiés, 0 FAIL). 2 `NoTestsDiscoveredException` dans `cucumberTest` global préexistantes (bug S-082 — runners dédiés ContentAndVideos/Context ne découvrent pas via feature global, hors CAP-MP4, `cucumberTest` skippé en CI par CR-10). EPIC CAP-MP4 TERMINÉ 4/4 US (US-0 cadrage S-097, US-1 enum+merger S-098 17 unit, US-2 converter+wiring S-099 13 unit + 2 functional, US-3 cucumber S-100 3 scénarios). | 9/10
| 099 | 2026-08-11 | feat+ddd+tdd | **CAP-MP4 US-2 `VideoFormatConverter` + `FormatConversion` + wiring** — `VideoFormatConverter.kt` (interface + `VideoFormatConverterImpl` FFmpeg `-c:v libx264 -c:a aac -movflags +faststart -y` + `NoOpVideoFormatConverter` returns `false`, pattern `SubtitleBurnInService`). `FormatConversion.kt` (object pur `convertIfNeeded(finalVideo, format, converter, probeDuration): File` — WEBM → no-op, MP4 → transcode + supprime WebM, BOTH → transcode + garde WebM, skip si `.mp4` existe ET probe>0 économie d'encre, échec transcode → garde WebM degraded). `CapsuleManager.resolveFormatConverter(ffmpegPath, strict)` factory (4ème factory `resolve*`, pattern `resolveSubtitleBurnInService` — "noop"→NoOp, unavailable+non-strict→NoOp, unavailable+strict→throw via `StrictModeGuard`). `CapsuleVideoTask.convertFormatIfEnabled(finalVideo)` post-burnIn (L693-712) câblé `captureDeckSequential` L493 + `captureDeckParallel` L523, log `CAPSULE →` pointe vers fichier produit. `DistributeCapsuleVideoTask.format: Property<String>` + filtre `.webm`/`.mp4`/both selon `OutputFormat.fromString` (CLI `-Poutput.format` override). `registerDistributeCapsuleVideoTask` push `merged.output.format.name`. Baby-step TDD RED → GREEN → REFACTOR : RED (13 tests → `Unresolved reference 'VideoFormatConverterImpl'`/`'resolveFormatConverter'`) → GREEN (converter + FormatConversion + factory + wiring + DistributeCapsuleVideoTask) → REFACTOR (patterns `CaptureResolver`/`SubtitleBurnInService` respectés, 4 factory `resolve*` + 7 `StrictModeGuard` cohérents). 772 unit VERT (+13 : 7 `VideoFormatConverterTest` + 6 `FormatConversionTest`), 54 functional VERT (+2 `FormatConversionFunctionalTest`), zéro régression. | 9/10
| 098 | 2026-08-11 | feat+ddd+tdd | **CAP-MP4 US-1 `OutputFormat` enum + `OutputConfig.format` + merger 4 sources** — `OutputFormat` enum type-safe 3 valeurs `WEBM`/`MP4`/`BOTH` (companion `fromString` case-insensitive fallback `WEBM` backward compat, pattern `CaptureStrategy`/`VersioningStrategy`) — `CapsuleConfig.kt:108-134`. `OutputConfig` étendu 4ème champ `format: OutputFormat = OutputFormat.WEBM` (backward compat) — `CapsuleConfig.kt:148`. `mergeOutputFormat` helper dédié (pattern `mergeVersioning`/`mergeCaptureStrategy`, CLI > YAML > props, fallback `WEBM`) — `CapsuleConfigMerger.kt:312-320`. `loadFromEnvironment` (`CAPSULE_OUTPUT_FORMAT`) + `buildConfigFromProperties` (`capsule.output.format`) étendus. `mergeOutputConfig` étendu avec `format`. `CapsuleExtension.outputFormat: Property<OutputFormat>` (convention `WEBM`) + helper Groovy `outputFormat(String)` (case-insensitive) — `CapsuleModels.kt:143-149`. `CapsuleConventions.outputFormat = OutputFormat.WEBM` (champ data class + instance) — `CapsuleModels.kt:175,214`. `pushConfigIntoExtension` push format (guard `isPresent` + convention, pattern `captureStrategy`) — `CapsulePlugin.kt:102`. Baby-step TDD RED → GREEN → REFACTOR : RED (8 `OutputFormatTest` + 9 merger `output.format` → `Unresolved reference 'OutputFormat'`) → GREEN (enum + OutputConfig.format + merger + Extension + Conventions + push) → REFACTOR (aucun — patterns établis respectés, 3 enums/3 helpers/3 Property/3 push cohérents). 759 unit VERT (+17 : 8 `OutputFormatTest` + 9 merger `output.format`), zéro régression, `compileKotlin` + `compileTestKotlin` VERT. US-1 config pure (pas de tâche Gradle, pas de cucumber — US-2/3 restantes). | 9/10
| 097 | 2026-08-11 | cadrage+gouvernance | **CAP-MP4 US-0 Cadrage EPIC Export MP4/H.264** — Audit code : `OutputConfig` (`CapsuleConfig.kt:122-126`) 3 champs pas de `format`, `DistributeCapsuleVideoTask` filtre `*.webm` hardcodé L56, `CapsuleVideoTask.captureDeckSequential/Parallel` WebM final `${deckName}.webm` dans `outDir` post-burnIn L489/L519, `mergeOutputConfig` (L267-273) + `mergeVersioning` (L288-298) pattern à suivre pour `mergeOutputFormat`, `CapsuleExtension`/`CapsuleConventions` pas de `outputFormat`, `pushConfigIntoExtension` pas de push format, 8 `CucumberTaskSpec` existants, `MediaProbeUtil.probeDuration` (économie d'encre MP4) + `StrictModeGuard.requireAvailable` (ffmpeg strict) réutilisables, `CaptureResolver` factory + `CaptureStrategy`/`VersioningStrategy` enums patterns de référence. 10 décisions architecturales : (1) `OutputFormat` enum type-safe 3 valeurs `WEBM`/`MP4`/`BOTH` défaut `WEBM` backward compat, companion `fromString` case-insensitive fallback `WEBM` ; (2) section `output` étendue 4ème champ `format` (pas nouvelle section) ; (3) `mergeOutputFormat` helper dédié pattern `mergeVersioning` ; (4) 4 sources ENV `CAPSULE_OUTPUT_FORMAT` < props `capsule.output.format` < YAML < CLI ; (5) `VideoFormatConverter` object pur domaine `capsule` `convertToMp4` (FFmpeg libx264+aac+faststart) + NoOp + `resolveFormatConverter` factory + StrictModeGuard ; (6) wiring `convertFormatIfEnabled` post-burnIn (MP4 → transcode+supprime WebM, BOTH → garde WebM+MP4, WEBM → no-op) + `CapsuleExtension.outputFormat` + helper Groovy ; (7) `DistributeCapsuleVideoTask` filtre selon format + `task.format` Property ; (8) `CapsuleConventions.outputFormat = WEBM` + push format ; (9) économie d'encre skip transcode si `.mp4` existe ET probe>0 ; (10) runner dédié `CapsuleFormatCucumberRunner` + steps préfixés "format distribution" + `CucumberTaskSpec("cucumberTestFormat")`. 4 US ~5 pts (US-0 ✅, US-1 enum+merger, US-2 converter+wiring, US-3 cucumber). Zéro code modifié, `compileKotlin` VERT. Baseline 742 unit + 52 functional + 3 cucumber préservée. | 9/10
| 096 | 2026-08-11 | feat+ddd+tdd+bdd+cadrage | **CAP-CR3-3 Branchement ScreenshotCapture (EPIC TERMINÉ 5/5 US)** — `CaptureStrategy` enum (PLAYWRIGHT/SCREENSHOT, défaut PLAYWRIGHT backward compat, companion `fromString` case-insensitive) + `CaptureConfig.strategy` 15ème champ + `mergeCaptureConfig` 4 sources (ENV `CAPSULE_CAPTURE_STRATEGY` < props `capsule.capture.strategy` < YAML < CLI) + helper `mergeCaptureStrategy`. `CaptureResolver` object pur (`CaptureResolver.kt`) — factory `resolve(strategy, strict, playwrightFactory, screenshotFactory, noOpCapture, enginePath)` dispatch `when(strategy)` avec `PlaywrightFactory`/`ScreenshotFactory` fun interfaces (testable sans I/O), `StrictModeGuard.requireAvailable` réutilisé. `ScreenshotPlanner` object pur (`ScreenshotPlanner.kt`) — `plan(outputDir, slideDurations)` produit `ScreenshotCapturePlan` (entries pngFile/webmFile/durationSecs + concatListFile + finalWebm), `renderConcatList`, `ffmpegPngToWebmArgs`, `ffmpegConcatArgs` (argv builders). `ScreenshotCaptureImpl.capture()` étendu multi-slide (boucle screenshot PNG par slide + navigate Reveal.next() + ffmpeg png→webm par slide durée exacte + concat webm via ffmpeg concat demuxer). `CapsuleExtension.captureStrategy: Property<CaptureStrategy>` + helper Groovy `captureStrategy(String)`. `pushConfigIntoExtension` push strategy. `CapsuleVideoTask.resolvePlaywrightCapture` refactorisé → `CaptureResolver.resolve` (log "Capture: X strategy"). Cucumber `capsule_capture_strategy.feature` 3 scénarios `@capture @strategy` + runner dédié `CapsuleCaptureStrategyCucumberRunner` (pattern S-082) + `CucumberTaskSpec("cucumberTestCaptureStrategy")`. Baby-step TDD/BDD 3 niveaux : 29 unit (7 CaptureStrategy + 8 merger + 6 CaptureResolver + 8 ScreenshotPlanner) → 3 functional → 3 cucumber. 742 unit VERT (+29) + 52 functional VERT (+3) + 3 cucumber dédiés GREEN. Zéro régression, zéro secret. | 9/10
| 095 | 2026-08-11 | feat+ddd+tdd+bdd+rename | **CAP-SPD US-3 Wiring + US-4 Cucumber + renommage SPD→PedagogicalScenario (EPIC CAP-SPD TERMINÉ 5/5 US)** — Renommage sémantique : `SpdContext`→`PedagogicalScenario`, `SpdContextLoader`→`PedagogicalScenarioLoader`, `spdSection`→`scenarioSection`, header `"==== Pedagogical Scenario (scenario)"` (was "SPD Pedagogical Context (spd)"), instruction line `contains("Pedagogical Scenario")` (was `contains("SPD Pedagogical Context")`). 3 fichiers renommés via `git mv`. `metadata.json` validation `type=="SPD"` inchangé (format externe K-2, pas le concept capsule). US-3 Wiring : `ContextConfig.scenarioFile: String?` (8ème champ section context), `mergeContextConfig` étendu `mergeStr`, `loadFromEnvironment` (`CAPSULE_CONTEXT_SCENARIO_FILE`) + `buildConfigFromProperties` (`capsule.context.scenarioFile`), `CapsuleExtension.scenarioFile: Property<String>`, `pushConfigIntoExtension` wiring, `CollectCapsuleAugmentedContextTask.scenarioFile: ConfigurableFileCollection @Optional` + `resolveScenarioSection()` (5% budget = 400 tokens/8000, directory→metadata.json+first.adoc, file→direct adoc), `CapsuleManager` wiring lazy provider. 2 functional `ScenarioContextFunctionalTest` GREEN (scenario fourni → ~140 tokens, absent → ~61 tokens fallback). US-4 Cucumber : `capsule_scenario_context.feature` 3 scénarios `@context @scenario`, `CapsuleScenarioContextSteps` (préfixés "scenario context" — bug S-088), runner dédié `CapsuleScenarioContextCucumberRunner` (pattern S-082), `CucumberTaskSpec("cucumberTestScenarioContext")` câblé. 3/3 scénarios PASSED (1m06s). Baby-step TDD/BDD 3 niveaux : unit (S-094) → functional (S-095) → cucumber (S-095). 713 unit VERT (inchangé) + 49 functional VERT (+2) + 3 cucumber dédiés GREEN. Zéro régression. | 9/10
| 094 | 2026-08-11 | cadrage+gouvernance+feat+tdd+ddd | **CAP-SPD US-0 Cadrage + US-1 SpdContext/SpdContextLoader + US-2 CapsuleContext/Builder/PromptBuilder étendus** — Audit `CapsulePipelineGraph` (11 fichiers pipeline) + `CapsuleContextBuilder` (67 lignes) + `DocContextLoader` (51 lignes, pattern mirror) + `GenerateCapsuleContentTask` (121 lignes) + `DefaultCapsulePromptBuilder` (59 lignes) + `ContextConfig` + 7 `CucumberTaskSpec`. Contrat SPD : producteur = borough privé non nommé publiquement (correction S-092 qui citait planner publiquement). `metadata.json` K-2 = enveloppe provenance (7 champs), AUCUN champ pédagogique — payload dans AsciiDoc compagnon. Capsule public OSS format-agnostic : valide `type=="SPD"` uniquement, PAS `source` (pas de couplage borough producteur). N0 `ContextChannel` sealed → **extension capsule-local** `CapsuleContext.spdSection` (pas de bump N0). SPD absent → vidéo non bloquée (fallback no-op). US-1 : `SpdContext` data class (6 champs, invariant sessionTitle non-blank) + `SpdContextLoader` (object pur, mirror `DocContextLoader`) — `load(metadataFile, adocFile, maxTokens)`, valide `type=="SPD"`, extrait sections AsciiDoc, rend `==== SPD Pedagogical Context (spd)`, tronque via `ContextChannel.Docs.truncateToTokens`. 10/10 unit GREEN. US-2 : `CapsuleContext` 3ème champ `spdSection: String = ""` (invariant ajusté `rendered.isBlank() == (channels.isEmpty() && spdSection.isBlank())`) + `CapsuleContextBuilder.build(composite, budget, spdSection="")` étendu (append SPD après channels, trim, 4 cas) + `DefaultCapsulePromptBuilder.buildGeneratePrompt` instruction line SPD (conditionnelle `contains("SPD Pedagogical Context")`). 9/9 unit GREEN (6 CapsuleContextBuilder + 3 DefaultCapsulePromptBuilder). 713 unit VERT (+9 net). 7 décisions architecturales documentées BACKLOG. US-3/4 restantes. | 9/10
| 093 | 2026-08-11 | feat+ddd+tdd+bdd | **CAP-DOCCONTEXT injection corpus documentaire (EPIC TERMINÉ 5/5 US)** — `ContextConfig` data class (`docsGlobs: List<String> = emptyList()`) 8ème section `CapsuleConfig`. `CapsuleConfigMerger.mergeContextConfig` + nouveau helper `mergeStrList` (List<String> comma-separated, 4 sources ENV < props < YAML < CLI) + `splitCommaList` utilitaire. `loadFromEnvironment` (`CAPSULE_CONTEXT_DOCS_GLOBS`) + `buildConfigFromProperties` (`capsule.context.docsGlobs`). `CapsuleExtension.docsGlobs: ListProperty<String>`. `pushConfigIntoExtension` push context. `DocContextLoader` (object pur, domaine `capsule.context`) — résout fichiers pré-matchés, concatène trié par nom `--- filename ---\n<content>`, tronque via `ContextChannel.Docs.truncateToTokens(budget.docsTokens)` (10% = 800 tokens/8000, pas 40% qui est EAGER — correction S-093). `CollectCapsuleAugmentedContextTask` étendu : `docsFiles` (`@InputFiles @PathSensitive(RELATIVE)`) + `docsGlobs` (`@Input`) + `resolveDocsSection` (globs > legacy CLI string rétrocompat). `CapsuleManager` lazy `project.provider { }` (extension afterEvaluate > CLI) + `resolveGlobFiles` (`fileTree().matching { include(glob) }`). 4 scénarios cucumber `@context @docs` (`capsule_doc_context.feature`) + runner dédié `CapsuleDocContextCucumberRunner` (pattern S-082) + steps préfixés "doc context" (bug S-088). `CucumberTaskSpec("cucumberTestDocContext")` câblé `build.gradle.kts`. Baby-step TDD/BDD 3 niveaux : RED → GREEN → REFACTOR (unit → functional → cucumber). 694 unit VERT (+20 : 11 ContextConfig merger + 9 DocContextLoader) → 47 functional VERT (+3 DocContextFunctionalTest : docsContent CLI rétrocompat, docsGlobs résolus, empty no-op) → 4 cucumber scénarios GREEN. Zéro régression, zéro secret. | 9/10
| 092 | 2026-08-10 | cadrage+gouvernance | **Cadrage 11 EPICs neufs (contexte augmenté + post-production)** — Audit post-CAP-ARCH/CAP-CR3-2. 11 EPICs neufs (47 US, ~88 pts) sur 2 axes : contexte augmenté (5 EPICs : CAP-DOCCONTEXT injection corpus documentaire `context.docsGlobs` 4 sources + `DocContextLoader` ~12 pts P0, CAP-SPD consommation métadonnées SPD `SpdContextLoader` parse metadata.json K-2 + canal `spd` dans `CapsulePipelineGraph` ~10 pts P0, CAP-GLOSSARY injection terminologique `context.glossaryFile` + canal `glossary` ~6 pts P1, CAP-PROVENANCE audit context anti-hallucination `ProvenanceTracker` → `context-provenance.json` ~5 pts P1, CAP-TRANSCRIPT reverse pipeline capsule → article AsciiDoc domaine `capsule.transcript` cross-borough document/bakery ~10 pts P2) + post-production (5 EPICs + CAP-CR3-3 : CAP-CR3-3 branchement ScreenshotCapture `capture.strategy: playwright|screenshot` 4 sources + `ScreenshotCaptureImpl` étendu multi-slide + `resolveCapture` factory + StrictModeGuard ~13 pts P0, CAP-MP4 export MP4/H.264 `output.format: webm|mp4|both` + `VideoFormatConverter` ~4 pts P1, CAP-AUDIO post-production audio BGM + loudnorm EBU R128 + sidechain ducking domaine `capsule.audio` ~10 pts P1, CAP-CHAPITRE intro/outro + chapter markers Matroska domaines `capsule.cards`/`capsule.chapters` ~10 pts P2, CAP-PODCAST extraction audio-only `PodcastConcatenator` FFmpeg concat MP3 ~4 pts P2, CAP-PREVIEW mode dry-run `capture.previewOnly` PNGs sans mux ~4 pts P2). Décision architecturale CAP-CR3-3 : brancher ScreenshotCapture (non expulser) — avantages fonctionnels (CI-friendly CAP-CR3-1, 10x plus rapide decks statiques, empreinte CI légère, cohérent chemin Manim, artifact preview/debug). Séquence MVP0 : DOCCONTEXT > SPD > CR3-3 > MP4 > AUDIO > GLOSSARY > PROVENANCE > CHAPITRE > TRANSCRIPT > PODCAST > PREVIEW. MàJ BACKLOG (11 sections EPIC + synthèse + séquence), INDEX (roadmap 12 lignes + correction faux positif CAP-29.6 `⬜ TODO` → `✅ TERMINÉ S-082`), PROMPT_REPRISE (session 092 + contexte reprise 093 CAP-DOCCONTEXT). Zéro code modifié, `compileKotlin` VERT. | 9/10
| 091 | 2026-08-10 | feat+ddd+tdd+bdd | **CAP-CR3-2 Mode strict anti-NoOp (EPIC CAP-CR3-2 TERMINÉ 4/4 US)** — `capsule.strictMode` DSL (Boolean, défaut `false`, 4 sources ENV < props < YAML < CLI). `StrictModeGuard` (object pur, domaine `capsule.config`) — `requireAvailable(strict, engineName, isAvailable, path)` lève `IllegalStateException` avec message actionable si `strict && !isAvailable` (nomme engine + path + suggestion `strictMode=false` ou `install <tool>`). 6 `resolve*` branchés : `CapsuleBuildTask.resolveTtsEngine` (piper/espeak absent → guard avant NoOp), `CapsuleVideoTask.resolveTtsEngine` (piper/espeak), `CapsuleVideoTask.resolvePlaywrightCapture` (Playwright), `CapsuleManager.resolveManimEngine` (manim, param `strict`), `CapsuleManager.resolveManimVideoMixer` (ffmpeg, param `strict`), `CapsuleManager.resolveSubtitleBurnInService` (ffmpeg, param `strict`). 3 appelants CapsuleVideoTask passent `strict = capsuleExtension.strictMode.get()`. `noop` explicite (`ttsEngine = "noop"`, `ffmpegPath = "noop"`) reste légitime pour tests, non concerné. `StrictModeConfig` data class (1 champ `enabled: Boolean = false`). `CapsuleConfig` étendu `strictMode: StrictModeConfig`. `CapsuleConfigMerger.mergeStrictModeConfig` (utilise `mergeBoolean`) + `loadFromEnvironment` (`CAPSULE_STRICT_MODE_ENABLED`) + `buildConfigFromProperties` (`capsule.strictMode.enabled`). `CapsuleExtension.strictMode: Property<Boolean>` (convention `false`). `CapsulePlugin.pushConfigIntoExtension` push strictMode. Baby-step TDD/BDD 3 niveaux : RED → GREEN → REFACTOR (unit → functional → cucumber). 674 unit VERT (+19 : 7 `StrictModeGuardTest` + 3 `StrictModeConfigTest` + 9 strictMode merger) → 44 functional VERT (+4 `StrictModeFunctionalTest` : default NoOp preserved, strict + piper absent → FAIL, strict + noop explicite → OK, CLI flag strict → FAIL) → 8 cucumber dédiés GREEN (`capsule_strict_mode.feature` 4 scénarios `@strict @config`, runner `CapsuleStrictModeCucumberRunner` pattern S-082, steps préfixées "strict" pour éviter doublons glue, `writeBuildFile()` privé regénère build.gradle complet au lieu d'appendText double bloc). `CucumberTaskSpec("cucumberTestStrictMode")` câblé `build.gradle.kts`. `check` BUILD SUCCESSFUL 6m54s. Zéro régression, zéro secret. | 9/10
| 090 | 2026-08-10 | feat+bdd+cucumber | **CAP-ARCH-6 Cucumber BDD boundary cross-borough (EPIC CAP-ARCH TERMINÉ 8/8 US)** — `capsule_arch_boundary.feature` (4 scénarios `@cross-borough @architecture @i18n @ai`) single-language français (pas 10 langues — CAP-29.5 `multi_language_capsule.feature` garde la responsabilité i18n, éviter test redondant, Single Responsibility). Scénarios : registration task `generateCapsuleContentAndVideos` groupe `capsule`, content pipeline LLM (`generateCapsuleContent` mock Ollama 2 réponses alternées propose/generate → `demo-speaker-notes.adoc` + `demo-script.txt`), video pipeline composite (NoOp engines → `demo_fr.webm`), économie d'encre skip WebM pré-existant valide (ffmpeg 1s, `ffprobe` > 0). Runner dédié `CapsuleArchBoundaryCucumberRunner` (pattern S-082 `@SelectClasspathResource` + filter `@cross-borough and not @wip and not @integration`). `CapsuleArchBoundarySteps` self-contained GradleRunner (fake `translateDeck`, pré-génération artefacts fr `demo_fr-deck.html` + `demo_fr-script.txt` dans dirs attendus). `CucumberTaskSpec("cucumberTestArchBoundary")` câblé `build.gradle.kts`. Baby-step TDD/BDD 3 niveaux : RED (8 tests failed steps undefined) → GREEN (4 scénarios PASSED ; itérations : doublons glue `DuplicateStepDefinitionException` avec `MultiLanguageCapsuleSteps`/`CapsuleAiSteps` → steps préfixées "capsule arch boundary", skip `MINIMAL_WEBM` header EBML `ffprobe` 0.0 → ffmpeg WebM valide 1s, feature `demo_fr-speaker-notes.adoc` corrigé en `demo-speaker-notes.adoc` car content pipeline mono-langue sans `_lang`) → REFACTOR (`check` BUILD SUCCESSFUL 6m12s). 655 unit VERT (inchangé) + 40 functional VERT (inchangé) + 4 cucumber dédiés GREEN. Zéro régression, zéro secret. EPIC CAP-ARCH TERMINÉ 8/8 US. | 9/10
| 089 | 2026-08-10 | feat+ddd+tdd+i18n | **CAP-ARCH-5 i18n aligné socle (CapsuleMessages)** — Domaine `capsule.i18n` créé (8ème) : `CapsuleMessages` object (pattern `SliderMessages`/`PlantumlMessages` — `utf8Control` ResourceBundle.Control UTF-8, `forLanguage`/`get`/`format`/`resolveLanguage(project)` cascade `-Pcapsule.language` > DSL `capsule { language }` > default `"en"`). 11 `Messages*.properties` (base + en + 9 langues fr/zh/hi/es/ar/bn/pt/ru/ur) — 23 keys (6 task groups + 17 task descriptions). Wiring `CapsuleManager` (13 tasks) + `CapsuleFeedTaskRegistrar` (4 tasks) → `CapsuleMessages.get("key", lang)`. Fix 3 descriptions françaises hardcoded (deployCapsule "Recadre les capsules...", collectCapsuleContext "Exporte le contexte...", transformCapsuleContext "Parse le fichier..." — violation règle English-only). `CapsuleExtension.language` ajouté (default `"en"`). `MultiLanguageResolver` KDoc corrigé (remove `DocumentI18nBridge` ref supprimé S-030, cite `CapsuleMessages` + `SliderMessages`/`PlantumlMessages`). Décisions : pas de `CapsuleI18nBridge` (anti-pattern S-030), pas de `MessageBundle`/`LocaleResolver` (contrats N0 morts zéro consommateur), `Messages_en.properties` explicite requis (fallback default locale système `fr_FR`). Bug découvert : `Locale.ENGLISH` sans fichier `_en` explicite chargeait `Messages_fr.properties`. Baby-step TDD : 655 unit VERT (+87 net : 65 `CapsuleMessagesTest` 23 keys × 10 langs + fallback + missing + 22 `MessagesFrTranslationTest` assertNotEquals). 40 functional VERT (inchangé). `compileKotlin` + `test` + `functionalTest` BUILD SUCCESSFUL. Zéro régression. | 9/10
| 088 | 2026-08-10 | feat+bdd+wiring | **CAP-ARCH-4 Wiring backend + CAP-ARCH-7 US-4 Cucumber distrib** — CAP-ARCH-4 : tâche composite `generateCapsuleContentAndVideos` (groupe `capsule`) chaîne `generateCapsuleContent` → `translateAndGenerateCapsuleVideos` (pattern `DefaultTask` + `dependsOn` par nom string, backend existant intact). `CapsuleFeedTaskDomain.kt` constante `GENERATE_CAPSULE_CONTENT_AND_VIDEOS` + `CONTENT_AND_VIDEOS_DESCRIPTION`. `CapsuleFeedTaskRegistrar.kt` `registerGenerateCapsuleContentAndVideos(project)`. `CapsuleManager.kt` câblage. 3 functional tests (`GenerateCapsuleContentAndVideosFunctionalTest` : registration, description, dependsOn chain `--dry-run` avec dummy deck car `generateCapsuleContent` résout `deckFile` par convention). 3 cucumber `@architecture @wiring` (`capsule_content_and_videos.feature` + `CapsuleContentAndVideosSteps` constants pures + `CapsuleContentAndVideosCucumberRunner` dédié). CAP-ARCH-7 US-4 : feature `capsule_distrib.feature` (5 scénarios `@distrib @versioning`) + `CapsuleDistribSteps` (self-contained GradleRunner pattern `MultiLanguageCapsuleSteps`, steps uniques "distrib" pour éviter doublons `CapsuleStepDefinitions`) + `CapsuleDistribCucumberRunner`. Scénarios : registration tasks --group distribute, copy timestamp, copy incremental v1, second run v2, skip idempotent économie d'encre. 2 `CucumberTaskSpec` câblés (`cucumberTestContentAndVideos` + `cucumberTestDistrib`). 568 unit VERT (inchangé) + 40 functional VERT (+3) + 8 cucumber dédiés GREEN (3 `@architecture @wiring` + 5 `@distrib @versioning`). `check` BUILD SUCCESSFUL. Zéro régression. | 9/10
| 087 | 2026-08-10 | review+fix+tdd+feat | **Code review + Bug burn-in fix + CAP-ARCH-7 Video Destination Versioning (US-1→3)** — Code review post-S-083→086 : 4 points (duplication pipeline koog = dette extraction MEMPHIS future, versionning codebase-contracts 0.1.0 = régularisation, hack buildscript force annotations = acceptable pattern slider, **1 bug réel** = FAIL `@integration` burn-in path mismatch `build/capsule/` vs `outputDir=capsules` connu S-069/S-082 jamais traité). Fix bug TDD : `CapsuleOutputDirResolver` (object pur, package `capsule.support`, résout `outputDir` depuis `build.gradle` du projet Cucumber) + 9 unit tests RED→GREEN + 3 steps Cucumber migrés (`aSubtitleFileIsGeneratedInTheCapsuleOutputDirectory`, `theVideoFileHasAValidWebmEbmlHeader`, `aVideoFileIsGenerated` vers `capsuleOutputDir()` dynamique) + runner `cucumberTestIntegration` câblé (`CucumberTaskSpec` dédié, runner `CucumberIntegrationTestRunner` orphelin exécuté). **BUILD SUCCESSFUL 3m39s** — scénario E2E burn-in `@burnin @integration` VERT (était FAIL depuis S-069). CAP-ARCH-7 (nouvelle feature issue review) : US-1 `OutputConfig` + `VersioningStrategy` enum (TIMESTAMP|INCREMENTAL) étend `CapsuleConfig` section `output` (3 champs `videoDestinationDir` défaut `office/videos`/`versioning`/`versionPrefix`), `CapsuleConfigMerger` 4 sources + `mergeOutputConfig` + `mergeVersioning` + `loadFromEnvironment`/`buildConfigFromProperties` étendus, 10 unit tests. US-2 `VideoDestinationResolver` (object pur main, `resolveVersionLabel` timestamp ISO-8601 compact ou incremental depuis `collectExistingVersions`, `resolveDestinationDir`, `resolveDestinationFile`, `collectExistingVersions`), 10 unit tests. US-3 `DistributeCapsuleVideoTask` (tâche `distributeCapsuleVideo`, groupe `distribute`, copie WebM de `build/<outputDir>/` vers `<videoDestinationDir>/<version>/`, CLI `-Poutput.*` overrides config mergée, économie d'encre skip si destination existe et non-vide, log `CAPSULE DISTRIB → …`) + wiring `CapsuleManager.registerDistributeCapsuleVideoTask()`, 3 functional tests (registration, copy versionné, skip idempotent). 568 unit VERT (+28 net) + 37 functional VERT (+3). `./gradlew test` BUILD SUCCESSFUL (2m7s). Zéro régression, zéro secret. | 9/10
| 086 | 2026-08-09 | feat+ddd+tdd | **CAP-ARCH-3 Koog pipeline génération contenu (domaine `capsule.pipeline`)** — `CapsulePipelineGraph` (koog 1.0.0 consommé direct via BOM, pattern `DeckPipelineGraph` slider SLD-8.3) : `strategy<CapsuleState, CapsuleState>` 3 nœuds propose-context → validate-context → generate-speaker-notes + edges conditionnels (propose FAILED → stop, validate INVALID → FAILED), `execute()` séquentiel (pattern `codebase.KoogAugmentedContextGraph`), `asMermaidDiagram()`. Domaine pur 9 fichiers : `CapsuleState` (stage machine `CapsuleStage` IDLE→PROPOSE_CONTEXT→CONTEXT_VALIDATED→CONTENT_GENERATED/FAILED + invariants croisés fail-fast `init`), `CapsulePromptBuilder`/`DefaultCapsulePromptBuilder` (ports pédagogiques : propose = plan JSON strict deck+contexte, generate = speaker notes enrichies respectant contrat `capsule.feed` `[NOTE.speaker]`), `CapsuleLlm`/`ChatModelCapsuleLlm` (port suspend + adapter langchain4j ChatModel), `ContentPlanValidator`/`ContentPlanValidationResult` (8 checks ordonnés : champs requis, types, langue ISO), `TtsScriptDeriver` (dérive `*-script.txt` contrat feed). 49 unit RED→GREEN. Tâche `generateCapsuleContent` (groupe `generate`) : deck `-Pdeck.file` ou premier `.adoc` de `slides/misc`, `@OutputFile` speakerNotes + ttsScript (économie d'encre UP-TO-DATE), `@ServiceReference LlmBuildService`, mock `-Pollama.baseUrl`. Wiring `CapsuleManager`. Durcissement API. 3 functional VERT. README master EN mis à jour. `./gradlew check` BUILD SUCCESSFUL (3m39s). Zéro régression. | 9/10
| 085 | 2026-08-09 | feat+ddd+tdd+bdd | **CAP-ARCH-2 Contexte augmenté (RAG + Graphify, domaine `capsule.context`)** — `CapsuleContextBuilder` (object pur : `defaultBudget()` `ChannelBudget` 8000 40/30/20/10/0, `toChannels(composite)` 5 canaux EAGER→RAG→GRAPHIFY→DOCS→RESOURCE, `channelsWithBudget(channels, budget)` troncature + append `\n`, `merge(channels)` sections `==== <sectionHeader> (<name>)` canaux vides ignorés, `build(composite, budget)` orchestration) assemble le `CompositeContext` (codebase-contracts 0.1.0, bump catalogue 0.0.2→0.1.0 + `implementation(libs.codebase.contracts)` — non transitif en compile, absent BOM 0.0.16). `CapsuleContext` (data class 2 champs, invariant fail-fast `rendered.isBlank() == channels.isEmpty()`, `isEmpty`/`nonEmptyCount`/`tokenEstimate`). Tâche `collectCapsuleAugmentedContext` (groupe `collect`) : eagerFiles `.agents/INDEX.adoc` + `PROMPT_REPRISE.adoc` + `AGENT.adoc` `@InputFiles @PathSensitive(PathSensitivity.RELATIVE)` (fixtures chemin absolu functional), `-Pcontext.{ragContent,graphifyContent,docsContent,tokenBudget=8000}`, output `build/capsule/augmented-context.txt` + log `CAPSULE CONTEXT → …`, no-op si contexte vide. Wiring `CapsuleManager.registerCollectAugmentedContextTask()`. Cucumber `capsule_context.feature` `@context @rag` : runner dédié `CapsuleContextCucumberRunner` (contourne bug S-082 — `@ConfigurationParameter(FEATURES_PROPERTY_NAME)` du CucumberTestRunner écrase la system property cucumber.features) + `cucumberConventions.additionalTasks` `CucumberTaskSpec("cucumberTestContext", ..., runnerClass)` (`import build.CucumberTaskSpec`). Baby-step TDD/BDD 3 niveaux : 491 unit VERT (+14 `CapsuleContextBuilderTest`) → 31 functional VERT (+3 `CapsuleAugmentedContextFunctionalTest` : registration, artefact EAGER+RAG+Graphify écrit, second run UP-TO-DATE économie d'encre) → cucumber 4/4 GREEN (~31s). `./gradlew check` BUILD SUCCESSFUL (3m33s : test + functionalTest + kover + validatePlugins ; cucumberTest SKIPPED par défaut — CR-10). Zéro régression, zéro secret. | 9/10
| 084 | 2026-08-09 | feat+ddd+tdd+bdd | **CAP-ARCH-1 Socle LLM bridge (pattern SLD-8, domaine `capsule.ai`)** — codebase plugin `implementation` 0.0.5 + koog-agents 1.0.0 (bump 0.8.0 mort, `exclude(org.jetbrains:annotations)` + buildscript `force annotations:26.0.2-1` pattern slider) + langchain4j / langchain4j-ollama (BOM 0.0.16). Domaine `capsule.ai` : `CapsuleLlmService` (object pur — `registerLlmBuildService(project)` = `gradle.sharedServices.registerIfAbsent("capsuleLlmService", LlmBuildService)` + `spec.parameters.model.convention(project.aiProvider)` ; `resolveModel(provider, serviceProvider): ChatModel` via `LlmProviderChatModelAdapter` ; `aiProvider(project)` `-Pai.provider` default `ollama`). Tâche `capsuleAiSmokeTest` (groupe `generate`, `usesService(llmServiceProvider)`, prompt minimal + log `CAPSULE AI → …` + fail si réponse non conforme, mock `-Pollama.baseUrl` sinon pool rotation) câblée dans `CapsuleManager` (+12 lignes). Baby-step TDD/BDD 3 niveaux : 477 unit VERT (+10 net : `CapsuleLlmServiceTest` + `LlmProviderChatModelAdapterTest`) → 28 functional VERT (+3 `CapsuleAiSmokeTestFunctionalTest` : registration, mock HTTP, unreachable fail) → cucumber `capsule_ai.feature` `@ai` 3/3 GREEN (task listée, smoke mock LLM log + réponse, fail-fast unreachable ; mock HttpServer local, zéro LLM réel). `./gradlew check` VERT. 1 FAIL `@integration` burn-in préexistant (subtitle path `build/capsule/` vs `outputDir=capsules`) hors CAP-ARCH-1, connu S-069/S-072. | 9/10
| 083 | 2026-08-09 | cadrage | **CAP-ARCH-0 Cadrage EPIC CAP-ARCH refondu (capsule LLM-driven via codebase)** — Audit fondé sur le code réel, remplace le cadrage S-073 (périmé). Constats : (1) zéro `Ollama`/`ChatModel`/`ChatLanguageModel`/`langchain4j`/`LLM` dans `src/main` → capsule n'a AUCUN appel LLM à purger ; (2) `koog-agents` `0.8.0` en `implementation` mais 0 usage dans le code → dépendance morte, à réactiver en `1.0.0` pour CAP-ARCH-3 (pattern slider : exclusion koog submodules + forcer `org.jetbrains:annotations:26.0.2-1`) ; (3) `compileOnly slider` mais 0 import de classe slider (seul `dependsOn("translateDeck")` string-based dans `CapsuleFeedTaskRegistrar`) → à re-confirmer en CAP-ARCH-4 ; (4) `i18n-contracts` 0.0.2 déjà consommé (CAP-29.1 MultiLanguageResolver/TtsManager/VoiceMapping) → l'US « migration i18n » S-073 est largement faite. Inventaire contrats N0 : `codebase-contracts` 0.1.0 (CompositeContext EAGER+RAG+Graphify+Docs, ContextChannel, ChannelBudget — la « réalité augmentée complète »), pipeline-contracts 0.0.2, agent-contracts 0.1.0. Pattern de référence inventorié : slider `AssistantManager` SLD-8 (`registerLlmBuildService()` → `LlmBuildService` codebase.koog.llm.service → `LlmProvider` fun interface suspend → `LlmProviderChatModelAdapter` → `ChatModel` langchain4j). Vision refondue : capsule LLM-driven piloté par codebase (koog orchestration graphe + langchain4j exécution + RAG pgvector + Graphify contexte augmenté), backend d'exécution existant recyclé intact (extractSpeakerNotes, TTS Piper/espeak, capture Playwright, subtitles, burn-in, multi-lang CAP-29). EPIC redécoupé : 7 US (CAP-ARCH-0 ✅, CAP-ARCH-1 socle LLM bridge `capsule.ai` → CAP-ARCH-2 contexte augmenté `capsule.context` → CAP-ARCH-3 graphe koog `CapsulePipelineGraph` `capsule.pipeline` → CAP-ARCH-4 wiring backend → CAP-ARCH-5 i18n aligné socle → CAP-ARCH-6 cucumber cross-borough, ~21 pts). Décisions : codebase plugin en `implementation` (comme slider), i18n reste via `slider.translateDeck` (déjà LLM codebase SLD-8), génération de contenu enrichi = LA nouveauté. Zéro code modifié, `compileKotlin` VERT. MàJ BACKLOG/INDEX/PROMPT_REPRISE/TODO/SESSIONS_HISTORY. | 9/10
| 082 | 2026-08-08 | doc+dogfooding+fix+tdd | **CAP-29.6 Documentation + dogfooding + clôture EPIC CAP-29 TERMINÉ 7/7 US** — README master EN + README plugin mis à jour (domaine `capsule.multilang`, diagramme pipeline, table des tâches `extractSpeakerNotes`/`translateAndExtractSpeakerNotes`/`generateCapsuleVideoAllLanguages`/`translateAndGenerateCapsuleVideos`, convention nommage `<deckName>_<lang>-deck.html|script.txt|webm`, totaux tests 464 unit + 25 functional + 6 cucumber multilang). Dogfooding réel consommateur composite `includeBuild` (`/tmp/opencode/dogfood-multilang`) : `extractSpeakerNotes` réel (3 scripts fr/en/ar) → `generateCapsuleVideoAllLanguages` réel (Piper fr/en + espeak, chromium Playwright) → 3 WebM générés en 3m07 (ar 2MB / en 4.7MB / fr 4.3MB). Défaut nommage découvert : le rendu produisait `<deckName>_<lang>-deck.webm` (dérivé du header script `=== CAPSULE SCRIPT : <deckName>_<lang>-deck ===`, flux réel slider SLD-11) au lieu de la convention du plan `<deckName>_<lang>.webm` (`entry.outputVideo`), ce qui brisait aussi l'économie d'encre (le probe `shouldRender` ne matchait jamais l'output existant → re-render systématique). Fix TDD baby-step `CapsuleVideoOutputRenamer` (domaine `capsule.multilang`, object pur, 4 unit RED→GREEN) aligne le WebM rendu sur `entry.outputVideo` (contrat plan), wiring post-capture dans `GenerateCapsuleVideoAllLanguagesTask.renderEntry`. Re-dogfooding : alignement `-deck.webm` → `_<lang>.webm` logué pour les 3 langues + économie d'encre prouvée (`--rerun-tasks` → `0/3 languages rendered, 3 skipped: existing valid WebM`). 464 unit VERT (+4 net : +4 `CapsuleVideoOutputRenamerTest`) + 25 functional VERT (`GenerateCapsuleVideoAllLanguagesFunctionalTest` GREEN) + 6 cucumber multilang GREEN (S-081 baseline, non régressés — 1 FAIL `@integration` burn-in préexistant subtitle path `build/capsule/` vs `outputDir=capsules`, hors CAP-29, connu S-069/S-072). EPIC CAP-29 TERMINÉ 7/7 US, domaine `capsule.multilang` clos (7 fichiers prod : MultiLanguageResolver, VoiceMapping, ResolvedLanguage, CapsuleVideoEntry, CapsuleVideoPlan, CapsuleVideoPlanner, CapsuleVideoAllLanguagesRunner, GenerateCapsuleVideoAllLanguagesTask, CapsuleVideoOutputRenamer). Prochain EPIC : CAP-ARCH (cross-borough slider+codebase, débloqué). | 9/10
| 081 | 2026-08-08 | bdd+cucumber | **CAP-29.5 Cucumber BDD E2E 10 langues** — `multi_language_capsule.feature` (6 scénarios `@multilang @i18n`) + `MultiLanguageCapsuleSteps` : helpers plan pur `CapsuleVideoPlanner` (10 langues LanguageCatalog, nommage `<deckName>_<lang>.webm`, skip langue sans paire, fail-fast plan vide) + 2 scénarios E2E GradleRunner NoOp (ttsEngine=noop, captureEnabled=false, deck source + 10 scripts par langue, assert log `CAPSULE MULTILANG → n/n languages rendered` + WebM `build/capsules/*.webm`, RTL ar/ur). `cucumberTest -PrunCucumber` : 6/6 multilang GREEN (suite 154 tests, 1 FAIL préexistant `@integration` burn-in — subtitle path `build/capsule/` vs `outputDir=capsules`, connu S-069/S-072, hors périmètre). 460 unit VERT (inchangé) + 25 functional VERT + kover VERT. `check` BUILD SUCCESSFUL. | 9/10
| 080 | 2026-08-08 | feat+wiring+tdd | **CAP-29.4 Câblage translateAndGenerateCapsuleVideos** — Tâche composite purement déclarative `translateAndGenerateCapsuleVideos` : dependsOn `translateAndExtractSpeakerNotes` + `generateCapsuleVideoAllLanguages`. `CapsuleFeedTaskNames.TRANSLATE_AND_GENERATE_CAPSULE_VIDEOS` + description ajoutés (domaine `capsule.feed`), `CapsuleFeedTaskRegistrar.registerTranslateAndGenerateCapsuleVideos` (group capsule, 2 dependsOn), câblage 3ème appel registrar dans `CapsuleManager.registerExtractSpeakerNotesTask()`. Pipeline complet : deck source → translateDeck (10 langues) → extractSpeakerNotes (10 scripts) → generateCapsuleVideoAllLanguages (10 WebM). 3 functional tests RED→GREEN (registration groupe, description mentionne les 2 sous-tâches, dependsOn chain via `--dry-run` avec stub `translateDeck` dans projet isolé). 460 unit VERT (inchangé) + 25 functional (+3), cucumber SKIPPED. `check` BUILD SUCCESSFUL. | 9/10
| 079 | 2026-08-08 | feat+ddd+tdd+functional | **CAP-29.3 GenerateCapsuleVideoAllLanguagesTask** — Domaine `capsule.multilang` : `CapsuleVideoAllLanguagesRunner` (object pur, `run(plan, probeDuration, render): List<CapsuleVideoEntry>` itère le plan + `shouldRender(entry, probeDuration)` = skip si WebM existe ET probe > 0, callback render injecté — frontière domaine/adaptateur nette). Tâche `GenerateCapsuleVideoAllLanguagesTask : CapsuleVideoTask` (réutilise `synthesizeTtsForScript` + `captureDeckSequential/Parallel` + `injectAudio` + `computeSlideDurations` CR-1 ; résolution TTS par langue via `MultiLanguageResolver` : set `ttsLanguage`/`ttsVoice`/`espeakVoice`, reset `ttsEngine=null`). Refactor base `CapsuleVideoTask` zéro changement de comportement : `execute()`→`open`, `resolveTtsEngine()`/`injectAudio()`→`internal`. Enregistrement `generateCapsuleVideoAllLanguages` (groupe generate, CapsuleManager). 6 unit `CapsuleVideoAllLanguagesRunnerTest` (RED→GREEN, _correction : LanguageCatalog n'a pas "de", 10 langues_) + 1 functional `GenerateCapsuleVideoAllLanguagesFunctionalTest` (3 langues fr/en/ar NoOp → 3 WebM + log `CAPSULE MULTILANG`). 460 unit VERT (+6 net), functional PASS, cucumber SKIPPED. | 9/10
| 078 | 2026-08-08 | feat+ddd+tdd | **CAP-29.2 CapsuleVideoPlan + CapsuleVideoPlanner** — Domaine `capsule.multilang` enrichi. `CapsuleVideoEntry` (data class immutable 4 champs deckFile/scriptFile/language/outputVideo, invariants fail-fast : deck exists, script exists, language in LanguageCatalog). `CapsuleVideoPlan` (data class immutable, invariant non-empty, size/isEmpty/languages). `CapsuleVideoPlanner` (object pur : `plan(deckDir, scriptDir, outputDir, targetLanguages)`, skip langues sans deck+script, fail-fast plan vide). Nommage aligné slider : `<deckName>_<lang>-deck.html` + `<deckName>_<lang>-script.txt` → `<deckName>_<lang>.webm`. 454 unit VERT (+18 net : +10 `CapsuleVideoPlanTest` + 8 `CapsuleVideoPlannerTest`). `check` BUILD SUCCESSFUL. | 9/10
| 077 | 2026-08-07 | feat+ddd+tdd | **CAP-29.1 MultiLanguageResolver + VoiceMapping 10 langues** — Domaine `capsule.multilang` créé (7ème). `MultiLanguageResolver` (object pur) consomme `LanguageCatalog` (BOM `i18n-contracts` 0.0.2) + `VoiceMapping` étendu à 10 langues (fr/en/es/de/zh/hi/ar/bn/pt/ru/ur, 11 Piper + 11 espeak). `Language` enum supprimé (4 entrées). `ResolvedLanguage` data class (SupportedLanguage + piperModel + espeakVoice). `TtsEngine.language(): SupportedLanguage?`. `CapsuleVideoTask.kt:197` + `CapsuleBuildTask.kt:40` migrés (`Language.fromCode` → `MultiLanguageResolver.resolve`). BOM `workspace-bom:0.0.16` + `i18n-contracts:0.0.2` ajoutés. 436 unit VERT (+15 net : +22 `MultiLanguageResolverTest`, +2 `LanguageVoiceMappingTest` adaptés, -9 anciens tests Language). `check` BUILD SUCCESSFUL. | 9/10
| 076 | 2026-08-07 | cadrage | **CAP-29.0 Cadrage EPIC pipeline multi-langue** — Audit code existant (`Language` enum 4 entrées, `VoiceMapping` 4 Piper+4 espeak, `generateCapsuleVideo` mono-langue, `translateAndExtractSpeakerNotes` SLD-11.4, `slider.translateDeck` 10 langues via `LanguageCatalog`). Décision clé : consommer `LanguageCatalog` (10 langues, contrats N0) au lieu d'étendre enum local — évite dette technique détruite dans CAP-ARCH-1. Domaine cible `capsule.multilang` (7ème). 7 US (CAP-29.0→6), ~20 pts. Backlog/INDEX/PROMPT_REPRISE/AGENTS racine mis à jour. Zéro code modifié. | 9/10
| 075 | 2026-08-07 | refactor+tdd+bdd | **CR-10 CI optimization cucumberTest onlyIf** — Domaine `capsule.ci.CucumberTestGuard` (data class pure, 4 méthodes) + 7 unit tests `CucumberTestGuardTest`. Câblage `onlyIf { shouldRun }` dans `build.gradle.kts` (logique inlinée — script ne peut pas importer classpath `main`). BDD `cucumber_skip.feature` (4 scénarios `@ci`) + `CucumberSkipSteps` (5 step defs). `./gradlew check` VERT (cucumberTest SKIPPED sans flag). 421 unit VERT (+7). Bug préexistant découvert : `CucumberTestRunner` ne lance que `capsule.feature` (à investiguer). EPIC CR TERMINÉ (10/10). | 9/10
| 074 | 2026-08-07 | refactor+tdd+bdd | **CR-9 Robust HTML parsing** — `HtmlSectionParser.findTopLevelSectionOpenTags` (38 lignes, depth-tracking existant réutilisé). `injectAudioSequentialFallback` : 2 lignes regex raw supprimées → 1 ligne `HtmlSectionParser.findTopLevelSectionOpenTags`. DRY (DRY-2 S-031). Bug fix : vertical stacks (sections imbriquées) n'étaient pas skippées → `data-audio` injecté sur sous-sections. +8 unit `HtmlSectionParserTest` (flat, nested, self-closing, attributes, empty, malformed, positions, multiline) + 1 unit `CapsuleVideoTaskTest` (data-audio count = 2, pas 4) + 1 scénario cucumber `@audio @sequential` + 2 step defs. 414 unit VERT (+9). | 9/10
| 073 | 2026-08-07 | refactor+tdd+bdd+cadrage | **CR-8 Logging structuré + EPIC CAP-ARCH cadré** — `CapsulePlugin.kt:40-48` 1 ligne monolithic 13 placeholders → 4 lignes `logger.lifecycle` par section (TTS/Capture/Subtitles/Manim). `CapsuleConfigLogger` object pur extrait (domaine `capsule.config`), `formatConfigLog(config): List<String>`. Tokens préservés verbatim (engine=, quality=, subtitle=, subtitleFormat=, burnIn=, scriptsDir=) → Cucumber @config existants verts. +9 unit tests `CapsuleConfigLoggerTest`, +1 scénario cucumber `@config @logging` (5 steps) + 5 step defs. 405 unit VERT (+9). EPIC CAP-ARCH cadré au BACKLOG (cross-borough slider+codebase pour contenu et i18n, 6 US ~19 pts, inspiration document/bakery/plantuml). | 9/10
| 072 | 2026-08-07 | refactor+tdd+bdd | **CR-5 DRY CapsuleConfigMerger** — 10 méthodes `mergeXxxConfig`/`mergeXxxConfigNoYaml` (~76 lignes dupliquées) → 5 méthodes unifiées + 4 helpers génériques (`mergeStr/mergeInt/mergeDouble/mergeBoolean`). `merge()` dispatch sur `yaml: T?` nullable. 322→302 lignes (-19 net). Sémantique préservée : `toBoolean()` lenient, heuristic `isNotBlank()` strings, Bool/Int/Double YAML>Props>ENV. +13 unit tests NoYaml (48 total ConfigMergerTest), +1 scénario cucumber `@config @no-yaml` + 1 step Given. 396 unit VERT (+12). 1 échec cucumber `Burn-in E2E @integration` préexistant (ffmpeg exit 183, non lié CR-5). | 9/10
| 071 | 2026-08-06 | feat+tdd+bdd+fix | **CR-2 + Fix Cucumber + CR-3/4/6/7** — Thread safety + timeout `captureSlideParallel` (unit 4 + functional 3 + cucumber 2). Fix 5 échecs cucumber consolidated (DuplicateStepDefinitionException doublons title/speakerNote renommés `the read segment` + feature quotes + temp file exact name). CR-3 error handling TTS (retry 1× + accumulation `failedSlides` + logger.error final). CR-4 HTML escape XSS `injectSubtitleTrack` (HtmlEscape object). CR-6 robust CLI parsing (cliInt/cliDouble/cliBoolean helpers tolèrent String). CR-7 validation YAML (déjà implémenté + test couverture). 384 unit VERT (+23), 64 cucumber 0 FAIL, 3 functional VERT. | 9/10
| 070 | 2026-08-06 | ops+cross-borough | **Sync slider 0.0.15 → 0.0.16** (cross-borough, suite slider 0.0.16 S-066 + BOM 0.0.17 S-019). Bump catalogue `capsule-plugin/gradle/libs.versions.toml` `slider = "0.0.15"` → `"0.0.16"`. `./gradlew compileKotlin` VERT avec slider 0.0.16 (mavenLocal). Zéro code production modifié. Session pure sync consommateur. | 9/10
| 069 | 2026-08-05 | feat+ddd+tdd+bdd | **EPIC CAP-MODEL TERMINÉ** — unification modèle `capsule.feed` ↔ `capsule.CapsuleScript` (7/7 US : cadrage + 6 exécution). `SlideType` enum migré vers `capsule.feed`, `SlideSegment` étendu (type+manimScene), `CapsuleScriptReader` remplace `CapsuleManager.parseScript` (64 lignes supprimées), 6 consommateurs migrés, `capsule.CapsuleModels` purgé. 361 unit tests VERT (+124). Cucumber boundary `consolidated_capsule_model.feature` (5 scénarios). Zéro doublon modèle. | 9/10
| 068 | 2026-06-29 | migration | **CNV-6 Migration conventions plugins** — `build.gradle.kts` migré vers 4 conventions plugins (gradle-plugin + publishing + functional-test + cucumber 0.0.1). ~70 lignes supprimées (functionalTest 15 + cucumberTest 25 + test config 10 + toolchain/signing 20). `compileKotlin` ✅. Régularisation migration déjà faite (conventions S005 dogfood legacy) mais non committée. | 9/10
| 067 | 2026-06-24 | refactor README | Architecture README Plugin + Consommateur — déplacement README.plugin → capsule-plugin/, README.consommateurs → racine, liens i18n recréés (10 langues) | 8/10
| 041 | 2026-06-24 | documentation | README i18n — 20 fichiers README (10 langues × consumer + plugin) dans `README.consommateurs/` et `README.plugin/`. Badges shields.io (Maven Central 0.0.1-SNAPSHOT orange, Plugin Portal, CI, License). Marqueurs : 195 unit + 22 Cucumber PASS. Switcher i18n 10 langues. AR/UR RTL. Non publié. | 8/10
| 040 | 2026-06-16 | refactor | CR-1 Refactor CapsuleVideoTask.execute() — 5 méthodes extraites (synthesizeTtsForScript, renderManimSlides, replaceManimSlidesInDeck, captureDeckSequential, captureDeckParallel), execute() 218→76 lignes orchestration, 9 nouveaux tests unitaires, compile OK, unit tests PASS | 9/10
| 039 | 2026-06-16 | governance+dev | Analyse CODE_REVIEW.adoc — 15 problèmes → 10 EPICs CR (CR-1→CR-10) + 3 EPICs CAP (CAP-26→CAP-28). Enrichissement BACKLOG, création TODO.adoc, MàJ INDEX. CAP-26 Style burn-in configurable TERMINE — SubtitleBurnInStyle data class + 4 champs DSL/Config/Merger/Scaffold + FFmpeg force_style. 16 tests unitaires + 2 Cucumber @subtitles @burnin @style. Compile OK, unit tests PASS. | 9/10
| 038 | 2026-06-16 | dev | CAP-25 Pipeline E2E burn-in — Cucumber @subtitles @burnin @integration (deck → TTS → capture → mix → burn-in → WebM final), 1 scénario + 2 step defs (Given real ffmpeg, Then burn-in completed), compile OK, unit tests PASS | 9/10
| 037 | 2026-06-16 | dev | CAP-24 Burn-in sous-titres — SubtitleBurnInService interface + Impl (FFmpeg subtitles filter) + NoOp, DSL subtitleBurnIn 4 sources, wiring CapsuleVideoTask post-mixAudioWithVideo, 10 tests unitaires, 3 Cucumber @subtitles @burnin | 9/10
| 036 | 2026-06-16 | dev | CAP-23 Sous-titres SRT/VTT — SubtitleFormat enum + SubtitleEntry + SubtitleGenerator, SrtGenerator + VttGenerator + SubtitleTimingCalculator, DSL subtitleEnabled/subtitleFormat 4 sources, wiring CapsuleVideoTask, 6 Cucumber @subtitles, 43/43 PASSED | 9/10
| 035 | 2026-06-16 | dev | CAP-22 TTS multi-langue — Language enum + VoiceMapping (FR/EN/ES/DE), PiperTtsEngine + EspeakTtsEngine language-aware, 6 Cucumber @tts, 37/37 PASSED, Kover 83.3% line | 9/10
| 034 | 2026-06-16 | diagnostic+bugfix | Bug-fix PlaywrightCaptureImpl.close() avant check vidéo (6 failures Cucumber → 0). Kover: 83.3% line, 51.8% branch. Décision CAP-22 TTS multi-langue. 31/44 Cucumber PASSED. | 9/10
| 033 | 2026-06-15 | bugfix+ci | Bug-fix DSL>merge (CapsuleConventions snapshot + pushConfigIntoExtension guard) + NoOp WebM EBML header (MINIMAL_WEBM ByteArray) + CI Template .github/workflows/test.yml. 211 unit PASS. | 8/10
| 032 | 2026-06-15 | refactor | P1 println→logger (PlaywrightCaptureImpl + CapsuleConfigLoader) + P2 isPlaceholder dead code + P3 lateinit→lazy (6 tâches) + concat-cleanup try/finally + validate-simplify when explicite. 211 unit PASS. | 9/10
| 031 | 2026-06-15 | refactor | P0 DRY (AudioConversionUtil + HtmlSectionParser + MediaProbeUtil) + resource leak finally + magic number DSL (parallelCaptureThreads + parallelRenderThreads) | 9/10
| 030 | 2026-06-13 | maintenance | Mise à jour BACKLOG (publication + priorités P0→P3) | 8/10
| 029 | 2026-06-13 | publish | Publication Maven Central 0.0.1 — nmcp, 195 unit + 22 Cucumber = 217 PASS | 9/10
| 028 | 2026-06-02 | dev | CAP-20 Manim Parallel Render — ManimParallelRenderer interface + NoOp (sequential) + Impl (thread pool Callable) + factory resolveManimParallelRenderer + CapsuleVideoTask refactor renderAll→mux + Config DSL parallelRender (ManimConfig + CapsuleExtension + CapsuleConfigMerger 4 sources + CapsuleScaffoldTask) + 8 config tests + 3 ManimConfigTest + Cucumber @manim @parallel. 195 unit GREEN. | 9/10
| 027 | 2026-06-02 | dev | Cucumber @config @manim — 5 scénarios BDD (quality YAML override, CLI -P override, defaults, outputDir YAML, scaffold manim fields) + 8 step defs + CapsulePlugin lifecycle log manim config + CapsuleScaffoldTask outputDir field. 173 unit GREEN, 5/5 Cucumber PASSED | 9/10
| 026 | 2026-06-02 | dev | CAP-19 ManimEngine configurable — ManimConfig enrichment (outputDir, VALID_QUALITIES, validate) + ManimEngineImpl(config) + CapsuleExtension manimOutputDir + CapsuleConfigMerger 4 sources outputDir + CapsuleVideoTask ManimConfig résolu + 19 nouveaux tests (13+3+3) → 173 unit tests GREEN | 9/10
| 025 | 2026-06-02 | dev | Cucumber @integration @manim E2E complet — CucumberIntegrationTestRunner (filtre not @wip and (@integration or @manim)), capsule.feature scénario E2E pipeline (NoOp render→mux→replace→deck validé), 4 new steps Manim E2E (noop config, .py script, placeholder MP4, muxed MP4, replaced deck). 154 unit tests GREEN. Cucumber @integration @manim PASSED. | 8/10
| 024 | 2026-06-02 | dev | ManimSlideReplacer + computeSlideDurationsWithManim + probeDuration wiring (154 unit GREEN, 21 nouveaux : ManimSlideReplacerTest 14 + ComputeSlideDurationsWithManimTest 4 + ManimSlideReplacementWiringTest 3 + 1 Cucumber @manim slide replacement) | 9/10
| 023 | 2026-06-02 | dev | ManimVideoMixer wiring + mux MP4+TTS (133 unit GREEN, 16 nouveaux : ManimVideoMixerTest 11 + ManimVideoMixerWiringTest 5 + 2 Cucumber @manim) | 8/10
| 022 | 2026-06-02 | dev | Tag @integration Cucumber + ManimEngine wiring + resolveManimEngine factory (117 unit tests GREEN, ManimEngineWiringTest 3/3, CucumberManimEngineTest 4/4) | 8/10
| 021 | 2026-06-02 | dev | Diagnostic Cucumber : 3 failures = environnementaux (espeak/ffmpeg/chromium absents), @config GREEN | 5/10
| 020 | 2026-06-02 | dev | CapsuleConfigMerger + scaffoldCapsuleContext + afterEvaluate wiring (110 unit + 8 func + 9 cucumber GREEN, 3 @config FAILED) | 7/10
| 019 | 2026-06-02 | dev | createSingleSlideHtml + parallelCaptureEnabled DSL + CapsuleConfig + CapsuleConfigLoader (78 unit + 8 func + 9 cucumber = 95 tests) | 8/10
| 018 | 2026-06-02 | dev | ManimEngine.kt (interface+impl+NoOp, 6 tests TDD) + captureSlideParallel + concatWebmFiles (4-6 tests TDD) + split CapsuleManager.kt 957→195 lignes (8 fichiers). Full check GREEN : 56 unit + 8 func + 9 cucumber = 73 tests. | 9/10
| 017 | 2026-06-02 | dev | Intégration sélective fork lion92 (5/5+2) : Parallélisme TTS + ScreenshotCapture + Jackson + Output build/ + Plugin ID fix (37 unit + 8 func + 9 cucumber PASS) | 9/10
| 016 | 2026-06-02 | dev | Intégration sélective fork lion92 2/5 : CapsuleModels (SlideType, espeak, manim config) + DRY (companion resolveScriptDir/DeckDir, AUDIO_INJECT_SCRIPT, parseScript [manim:Scene]). 6 unit tests TDD. | 7/10
| 015 | 2026-06-02 | audit | Revert merge fork lion92 + Cleanup — retour main à 0d69a94, suppression branche audit/lion92-fork. Leçon : zero merge cross-fork. | 8/10
| 014 | 2026-06-01 | audit | Audit fork lion92 — parallélisme TTS, ScreenshotCapture, Manim, DRY refacto. Commit 17adafb sur audit/lion92-fork. | 10/10
| 013 | 2026-06-01 | rename | Renommage borough "Brooklyn Tech" → "Détroit" (24 fichiers live, gouv + docs + code Kotlin) | 10/10
| 012 | 2026-05-19 | documentation | EPIC K K-5 — INDEX.adoc synchronise taxonomie 4 verbes + metadata.json + convention over config. EPIC K ferme (K-0→K-5). | ✅
| 011 | 2026-05-19 | migration | EPIC K-1 — Renommage taxonomie (7 tâches : generateCapsuleScript, generateCapsule, generateCapsuleVideo, deployCapsule, collectCapsuleContext, transformCapsuleContext, collectCapsuleRetrieve) — 12 fichiers modifiés, Compile OK | 10/10
| 010 | 2026-05-18 | migration | CCCP — namespace rename cccp.education.capsule → capsule (8 fichiers + build) | 10/10
| 009 | 2026-05-17 | feat+fix | CAP-9 Pipeline E2E + TTS espeak + Audio mix FFmpeg + Timing dynamique (WebM 322s, 31+10 tests PASS) | 10/10
| 008 | 2026-05-17 | audit | Diagnostic pipeline (4 bugs : reveal.js paths, example-deck, format string, audio injection) | 8/10
| 007 | 2026-05-16 | dev | CapsuleParseContextTask (Jackson parse JSON) + collectCapsuleRetrieve engine N3 + CapsuleDistrib isValidWebM fix + Jackson dans engine build.gradle.kts (42/42 PASS) | 10/10
| 006 | 2026-05-16 | dev | CAP-6 Integrations N3 (capsuleCompositeContext, export JSON engine) + CAP-7 capsuleDistrib (FFmpeg crop 9:16, fallback copy, multi-deck, 36/36 PASS) | 10/10
| 005 | 2026-05-16 | dev | CAP-4 CapsuleVideo orchestration (sequential fallback, multi-deck, noop bridge, @Tag integration chromium) + CAP-5 Cucumber BDD (5 scenarios, cucumberTest task, 28/28 PASS) | 10/10
| 004 | 2026-05-15 | dev | CAP-3 — Playwright Java (PlaywrightCapture interface/Impl/NoOp, generateCapsuleVideo task, injectAudio HTML, Kover 0.9.1, 19/19 PASS) | 10/10
| 003 | 2026-05-15 | dev | CAP-2 — TTS Piper (TtsEngine interface, PiperTtsEngine CLI, NoOpTtsEngine fallback, 11/11 PASS) | 10/10
| 002 | 2026-05-15 | dev | CAP-1 — Plugin Gradle capsule (compileOnly slider, 2 taches, CapsuleExtension DSL, 5/5 PASS) | 10/10
| 001 | 2026-05-15 | build | CAP-0.3 — Structure Gradle root + plugin indépendant, compileKotlin OK, 2/2 PASS | ✅ TERMINE
| 000 | 2026-05-14 | bootstrap | CAP-0.1 + CAP-0.2 — Gouvernance agent + cadrage architecture | ✅ TERMINE
|===
| 119 | 2026-08-16 | feat+bdd+cucumber+cloture | **CAP-TRANSCRIPT US-5 Cucumber BDD `capsule_transcript.feature`** (4 scénarios : TEMPLATE strategy, LLM strategy mocked, empty plan, skip-on-existing-output) + runner dédié `CapsuleTranscriptCucumberRunner` + étapes `CapsuleTranscriptSteps$. Tous les scénarios passent : BUILD SUCCESSFUL. EPIC CAP-TRANSCRIPT maintenant à 6/6 US terminées. | 9/10
