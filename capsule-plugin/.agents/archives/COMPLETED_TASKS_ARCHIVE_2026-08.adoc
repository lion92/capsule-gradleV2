= COMPLETED_TASKS_ARCHIVE — Août 2026
:date: 2026-08-26

== Session 127 — EPIC CAP-PODCAST TERMINÉ 3/3 US ✅

CAP-PODCAST US-2 tâche + wiring + Cucumber BDD. `GenerateCapsulePodcastTask`
(113 lignes, `capsule/podcast/`) — tâche Gradle `@DisableCachingByDefault` :
`@InputFiles audioFiles`, `@Input deckName`, `@OutputFile podcastOutput`,
`@Input podcastEnabled`. TaskAction : skip si `!enabled` ; skip si output
existe ET `MediaProbeUtil.probeDuration > 0.0` (économie d'encre) ; skip
si `audioFiles` vide → warn ; factory `resolvePodcastConcatenator` +
`concatenate`. Dégradé NoOp → warn + garde rien (backward compat).
`CapsuleManager.registerGeneratePodcastTask` (16ème, L371) — pattern
`registerGenerateTranscriptTask`. `audioFiles` résolus depuis
`build/capsule/<deckName>/slide-*.mp3` (provider lazy), `podcastOutput`
convention `build/capsule/<deckName>-podcast.mp3`, `dependsOn("generateCapsuleVideo")`.
`CapsuleMessages` 25ème key (11 `Messages*.properties`). Cucumber
`capsule_podcast.feature` 3 scénarios `@podcast` + runner dédié 17ème
`CucumberTaskSpec("cucumberTestPodcast")` + `CapsulePodcastSteps` préfixés
"podcast" (bug S-088). `GeneratePodcastFunctionalTest` 3 tests (task
registered, disabled skip, enabled NoOp → degraded no MP3). `check` VERT
4m25s — 1023 unit (+9 i18n), 72 functional (+3), 17 cucumber dédiés GREEN.
Domaine `capsule.podcast` (13ème) clos. EPIC CAP-PODCAST TERMINÉ 3/3 US
(US-0 S-125, US-1 S-126, US-2 S-127).

== Session 126 — CAP-PODCAST US-1 config+concatenator+factory ✅

Domaine `capsule.podcast` (13ème) créé : `PodcastConfig` (2 champs
`enabled=false` + `outputFile=""` defaults backward compat) +
`PodcastConcatenator` interface + `PodcastConcatCommand` object pur +
`PodcastConcatenatorImpl` (FFmpeg concat demuxer) + `NoOpPodcastConcatenator`
(returns false degraded). `CapsuleConfig` 13ème section `podcast` +
`mergePodcastConfig` 4 sources + `CapsuleExtension` 2 properties +
`pushConfigIntoExtension` podcast block. `CapsuleManager.resolvePodcastConcatenator`
8ème factory (pattern `resolveAudioPostProcessor` — noop→NoOp,
unavailable+strict→throw). 1014 unit VERT (+28 net baseline 986).

== Session 125 — CAP-PODCAST US-0 Cadrage ✅

Gouvernance pure (zéro code). 15 constats + 12 décisions. Domaine
`capsule.podcast` 13ème. 3 US ~4 pts. Pattern S-057.

== Session 122 — EPIC CAP-ANIM TERMINÉ 3/3 US ✅

Cadrage rétroactif code Remotion livré PR #1 (S-121) + tests unit manquants
40 unit (AudioCodecs 7 + RemotionConfig 8 + RemotionTemplate 7 +
RemotionCaptureImpl 14 + CaptureResolver REMOTION 4) + cucumber BDD
`capsule_remotion.feature` 3 scénarios + runner dédié 16ème + 2 functional.
Restauration `build.gradle.kts` post-PR #1 (`cucumberConventions` block +
16 `CucumberTaskSpec` + `kover` + `publishing` + gates CR-10/Playwright
stripped by PR #1). 977 unit VERT (+40 net), 69 functional VERT (+2 net),
cucumber dédiés GREEN. Domaine `capsule.remotion` (12ème) clos.

== Session 121 — Intégration PR #1 (lion92) + fix ScreenshotCaptureImpl ✅

Merge branche `pr-1` (8 commits) dans main. Conflit INDEX.adoc résolu.
Contenu PR : fix build (conventions 0.0.3, JDK 25, exclusion Groovy 3),
fix i18n (namespace `capsule/i18n/Messages`), fix 4 défauts capture +
capture conteneur (Chromium --no-sandbox), feat CAP-ANIM
(`CaptureStrategy.REMOTION` + RemotionPlanner/RemotionCaptureImpl/
RemotionTemplate + Manim incrusté OffthreadVideo + AudioCodecs), fix TTS
économie d'encre.

Fix post-merge baby-step TDD/BDD : `ScreenshotCaptureImpl.isAvailable()`
étendu pour vérifier ffmpeg (`isFfmpegAvailable()` probe `ffmpeg -version`).
Test functional + cucumber scénario #3 corrigés → "strictMode fails when
ffmpeg is unavailable".

Gate : 930 unit 0 FAIL (+13 RemotionPlannerTest), 67 functional 0 FAIL,
cucumberTestCaptureStrategy 3/3, cucumberTestTranscript 4/4.

== Session 120 — EPIC MEMPHIS-BOM-FIX US-5 TERMINÉ ✅

Cross-borough MEMPHIS + Queens. Catalogue capsule fixé : `slider` →
`slider-plugin`, slider 0.0.16 → 0.0.18, codebase 0.0.8 → 0.0.10, BOM
0.0.16 → 0.0.27. Bug Groovy 3.x transitif résolu. CAP-TRANSCRIPT EPIC
TERMINÉ 6/6 US.

== Session 118 — CAP-TRANSCRIPT US-4 tâche generateCapsuleTranscript + wiring ✅

EPIC CAP-TRANSCRIPT US-4 terminé (P2, 4ème US sur 6, tâche + wiring 2 pts).

Tâche `generateCapsuleTranscript` (`capsule/transcript/GenerateCapsuleTranscriptTask.kt`) :
groupe `generate`, `@InputFile speakerNotesFile` `@PathSensitive(RELATIVE)`
+ `@Input language` + `@Input strategy: Property<TranscriptStrategy>` +
`@OutputFile transcriptOutput` (`build/capsule/<deckName>-transcript.adoc`)
+ `@Optional @ServiceReference llmService: Property<LlmBuildService>`.
TaskAction : skip si output existe ET non-blank (économie d'encre) ; check
`speakerNotesFile.exists()` (message actionable "Run generateCapsuleContent
first") ; `deckName` = filename `removeSuffix("-speaker-notes")` ; parse
`AsciidocSpeakerNoteParser` → `CapsuleScript` → `TranscriptPlan(deckName,
segments, language, outputPath)` ; TEMPLATE → `TranscriptBuilder.build(plan)` ;
LLM → `project.aiProvider` + `project.resolveModel(provider, llmService)` +
`ChatModelTranscriptEnhancer(model).enhance(template, plan)` ; write + log
`CAPSULE TRANSCRIPT → <file> (N chars, strategy=X)`.

Wiring `CapsuleManager.registerGenerateTranscriptTask` (après
`registerGenerateCapsuleContentTask`) : groupe generate, description i18n,
`dependsOn("generateCapsuleContent")`, conventions language `deck.language`?:`fr`,
strategy extension `transcriptStrategy` ?: CLI `-Pcapsule.transcript.strategy`,
speakerNotesFile 1er fichier `*-speaker-notes.adoc` dans `build/capsule/`,
transcriptOutput map `capsule/<deckName>-transcript.adoc`, llmService +
usesService. Décision GREEN : dependsOn préservé en prod (conforme cadrage
S-114) ; tests utilisent `-x generateCapsuleContent` car la tâche ne dépend
pas du deck mais seulement des speaker notes (pré-écrits dans les tests,
standalone + zéro LLM involontaire + évite erreur de config `deckFile` sans
deck).

Clé `task.generateCapsuleTranscript.description` (24ème) ajoutée dans les 11
`Messages*.properties` (base + en + fr/zh/hi/es/ar/bn/pt/ru/ur).

3 functional tests `GenerateCapsuleTranscriptFunctionalTest` : registration
groupe generate ; TEMPLATE déterministe → `build/capsule/demo-transcript.adoc`
(`= demo`, `:language:`, `== Introduction`, speaker note body, outro) ; LLM
mock HTTP `/api/chat` + `-Pollama.baseUrl` → article enrichi + ≥1 appel.
Baby-step TDD strict RED (3/3 FAILED — task not found) → GREEN (3/3 PASSED)
→ REFACTOR (`check` BUILD SUCCESSFUL in 26m03s).

917 unit VERT (baseline S-117 préservée — US-4 zéro unit nouveau), 67
functional VERT (+3 net baseline 64 → 67), cucumber dédiés GREEN (baseline
préservée — US-5 cucumber à venir, cucumberTest global SKIPPED par CR-10),
Kover VERT, validatePlugins VERT, zéro régression, zéro secret, zéro bump
BOM. Backward compat : transcript `enabled=false` défaut.

1 fichier prod créé + 1 wiring modifié + 1 test créé + 11 i18n modifiés.
EPIC CAP-TRANSCRIPT 5/6 US (US-5 Cucumber BDD TODO).

Le borough Détroit produit désormais l'article AsciiDoc de révision
(`generateCapsuleTranscript`) en plus de la capsule vidéo — double livrable
MVP0. L'article est publiable par document-gradle (WRITE + PUBLISH
multi-format) ou bakery-gradle (site statique).

Rapport : `.agents/sessions/118-cap-transcript-us4-task-wiring.adoc`.

== Session 113 — EPIC CAP-27 VTT burn-in TERMINÉ 3/3 US ✅

EPIC CAP-27 (P2, 3 US, ~5 pts) terminé en 1 session. Constat de
prémisse caduque : la compatibilité VTT **fonctionne déjà** (test réel
ffmpeg 6.1.1, filtre `subtitles=` auto-détecte `.vtt` par extension,
exit 0 WebM valide avec format exact `VttGenerator`). Le backlog
présupposait « SRT seulement » — prémisse caduque. CAP-27 = cible de
couverture test, pas un fix de compat.

US-0 Cadrage : audit 10 points + 8 décisions architecturales
(`SubtitleBurnInCommand` object pur pattern `AudioPostCommand`, refactor
`SubtitleBurnInServiceImpl.burnIn` argv identique, 13 unit SRT/VTT,
2 scénarios cucumber `@subtitles @burnin @vtt` dans `capsule.feature`
feature existante → pas de nouveau runner S-082, step defs uniques S-088,
zéro bump BOM, zéro changement DSL, économie d'encre).

US-1 `SubtitleBurnInCommand` : object pur `buildArgv(videoFile,
subtitleFile, outputFile, style, ffmpegPath): List<String>` (mirror
`AudioPostCommand` L74-106) + refactor `SubtitleBurnInServiceImpl.burnIn`
L66-72 (argv identique, backward compat totale) + 13 unit tests
`SubtitleBurnInCommandTest` (SRT path, VTT path CAP-27 core, SRT/VTT
diff only by extension, ffmpegPath + `-y`, exactly one `-i`, `-c:a copy`,
output last, 9-element structure, default style FontSize=24/white/
Alignment=2, custom style fontSize=36/fontColor/outlineColor/top→8,
top→8, middle→5, custom ffmpegPath). Baby-step TDD RED (13 `Unresolved
reference 'SubtitleBurnInCommand'` + 1 illegal char `:` nom test corrigé
→ `-c:a copy` renommé `preserves audio with copy codec`) → GREEN (13/13
PASSED) → REFACTOR (refactor impl sans régression). 866 unit VERT 0 FAIL
(+9 net vs 857 S-112).

US-2 Cucumber BDD : 2 scénarios dans `capsule.feature` (mirror L284-290
non-integration `@subtitles @burnin @vtt` : config VTT burn-in + deck/
script NoOp capture → service invoqué + `.vtt` généré, PASSED 32.01s ;
mirror L292-302 E2E `@subtitles @burnin @vtt @integration` : VTT + real
ffmpeg → `.vtt` généré + valid WebM EBML header + burn-in success,
PASSED 32.36s) + 2 step defs VTT uniques
`aGradleProjectWithTheCapsulePluginConfiguredForVttSubtitlesWithBurnIn`
+ `...AndRealFfmpeg` dans `CapsuleStepDefinitions.kt` (textes uniques
"VTT subtitles", bug S-088). `cucumberTest -PrunCucumber` : VTT
non-integration PASSED (13 failures préexistantes
NoTestsDiscoveredException dedicated runners bug S-082, hors CAP-27).
`cucumberTestIntegration -PrunCucumber` : VTT E2E PASSED, 256 tests
235 skipped 0 failures. `check` BUILD SUCCESSFUL in 12m44s, Kover VERT,
64 functional VERT baseline préservée, zéro régression, zéro secret,
zéro bump BOM, zéro changement DSL.

5 fichiers code (2 nouveaux `SubtitleBurnInCommand.kt` 50 lignes +
`SubtitleBurnInCommandTest.kt` 167 lignes, 3 modifiés
`SubtitleBurnInService.kt` -7/+2 + `capsule.feature` +18 +
`CapsuleStepDefinitions.kt` +38) + 5 fichiers gouvernance.

Le borough Détroit produit désormais des capsules burn-in VTT testées
(unit + cucumber E2E real ffmpeg) en plus du SRT. La compatibilité VTT
— déjà fonctionnelle — est désormais garantie par régression.

Rapport : `.agents/sessions/113-cap27-vtt-burnin-cadrage.adoc`.

== Session 112 — CAP-GLOSSARY US-3 Cucumber BDD + clôture EPIC CAP-GLOSSARY TERMINÉ 4/4 US ✅

EPIC CAP-GLOSSARY US-3 terminé (P1, 4ème US sur 4, cucumber BDD 1 pt).
EPIC CAP-GLOSSARY TERMINÉ 4/4 US.

Feature `capsule_glossary_context.feature` `@context @glossary` (3
scénarios : glossary injecté → section `==== Official Glossary (glossary)`
+ termes `competence transversale`/`evaluation formative` présents +
bullet `unrelated` de section non-Glossary ignoré ; absent → fallback
no-op pas de section glossary backward compat ; malformed sans
`== Glossary` section → gracefully skipped no-op fallback). Runner
dédié `CapsuleGlossaryContextCucumberRunner` (pattern S-082
`@SelectClasspathResource` + filter `@context and @glossary and not @wip
and not @integration`). `CapsuleGlossaryContextSteps` (10 step defs
préfixés "glossary context", pattern `CapsuleScenarioContextSteps` mirror
— bug S-088 glue `capsule.scenarios` partagée, step texts uniques).
`CucumberTaskSpec("cucumberTestGlossaryContext")` 14ème câblé
`build.gradle.kts` (13ème cucumber dédié, 14ème `CucumberTaskSpec` total
avec integration). Baby-step BDD RED → GREEN (3/3 scénarios PASSED en
55s) → REFACTOR (`check` BUILD SUCCESSFUL in 28m52s, 857 unit VERT
baseline S-111 préservée — pure BDD zéro production modifié, 64 functional
VERT baseline préservée, cucumberTest global SKIPPED par CR-10, Kover VERT,
zéro régression, zéro secret). Domaine `capsule.context` clos (13 fichiers
prod : 3 loaders `DocContextLoader`/`PedagogicalScenarioLoader`/
`GlossaryLoader`).

== Session 111 — CAP-GLOSSARY US-2 Wiring CollectCapsuleAugmentedContextTask + ContextConfig + merger 4 sources + CapsuleExtension + CapsuleManager + provenance tracking ✅

EPIC CAP-GLOSSARY US-2 terminé (P1, 3ème US sur 4, wiring 2 pts).

`ContextConfig` 3ème champ `glossaryFile: String? = null` (pattern
`scenarioFile`). `CapsuleConfigMerger.mergeContextConfig` étendu `mergeStr`
pour `glossaryFile` + `loadFromEnvironment` (`CAPSULE_CONTEXT_GLOSSARY_FILE`)
+ `buildConfigFromProperties` (`capsule.context.glossaryFile`).
`CapsuleExtension.glossaryFile: Property<String>` convention `""`.
`pushConfigIntoExtension` push `glossaryFile` (guard `isPresent || blank`,
pattern `scenarioFile` L124-126). `CollectCapsuleAugmentedContextTask.
glossaryFile: ConfigurableFileCollection @InputFiles @PathSensitive(RELATIVE)
@Optional` + `resolveGlossarySection(budget)` (5% budget = 400 tokens/8000,
`GlossaryLoader.load(target, glossaryBudget)`, pattern `resolveScenarioSection`
L281-296) + `trackSources` étendu tracking GLOSSARY (1 `ProvenanceSource` si
`glossaryTarget` existe, pattern SCENARIO L230-249) + appel
`CapsuleContextBuilder.build()` étendu `glossarySection =
resolveGlossarySection(budget)`. `CapsuleManager` wiring lazy provider
`glossaryFile.from(project.provider {...})` (extension afterEvaluate > CLI,
pattern `scenarioFile` L229-237).

Baby-step TDD strict RED → GREEN → REFACTOR (functional uniquement) :
. **RED** : 1 fichier test (2 `GlossaryContextFunctionalTest`) → 1 FAIL
   (glossary fourni mais pas de section — wiring `glossaryFile` inexistant),
   1 PASSED par accident (absent → fallback actuel).
. **GREEN** : 6 fichiers prod étendus (ContextConfig + Merger + Models +
   Plugin + Task + Manager) + 2/2 functional PASSED.
. **REFACTOR** : `./gradlew check` BUILD SUCCESSFUL in 26m25s, 857 unit
   VERT inchangé baseline S-110, 64 functional VERT +2 net, Kover VERT,
   validatePlugins VERT, zéro régression, zéro secret.

Backward compat strict (`glossaryFile = null` défaut →
`resolveGlossarySection` retourne `""` → tests existants inchangés).
EPIC CAP-GLOSSARY 3/4 US (reste US-3 cucumber 3 scénarios).

== Session 099 — CAP-MP4 US-2 VideoFormatConverter + FormatConversion + wiring ✅

EPIC CAP-MP4 US-2 terminé (P1, 3ème US sur 4, converter + wiring 2 pts).

`VideoFormatConverter.kt` (interface + `VideoFormatConverterImpl` FFmpeg
`-c:v libx264 -c:a aac -movflags +faststart -y` + `NoOpVideoFormatConverter`
returns `false`, pattern `SubtitleBurnInService`). `FormatConversion.kt`
(object pur `convertIfNeeded(finalVideo, format, converter, probeDuration):
File` — WEBM → no-op, MP4 → transcode + supprime WebM, BOTH → transcode +
garde WebM, skip si `.mp4` existe ET probe>0 économie d'encre, échec transcode
→ garde WebM degraded). `CapsuleManager.resolveFormatConverter(ffmpegPath,
strict)` factory (4ème factory `resolve*`, pattern
`resolveSubtitleBurnInService` — "noop"→NoOp, unavailable+non-strict→NoOp,
unavailable+strict→throw via `StrictModeGuard`).
`CapsuleVideoTask.convertFormatIfEnabled(finalVideo)` post-burnIn (L693-712)
câblé `captureDeckSequential` L493 + `captureDeckParallel` L523, log
`CAPSULE →` pointe vers fichier produit. `DistributeCapsuleVideoTask.format:
Property<String>` + filtre `.webm`/`.mp4`/both selon `OutputFormat.fromString`
(CLI `-Poutput.format` override). `registerDistributeCapsuleVideoTask` push
`merged.output.format.name`.

Baby-step TDD RED → GREEN → REFACTOR (unit → functional) :
. **RED** : 2 fichiers test (7 `VideoFormatConverterTest` + 6
   `FormatConversionTest`) → `Unresolved reference 'VideoFormatConverterImpl'`/`'resolveFormatConverter'`
. **GREEN** : converter + FormatConversion + factory + wiring +
   DistributeCapsuleVideoTask + 2 functional
. **REFACTOR** : patterns `CaptureResolver`/`SubtitleBurnInService`
   respectés, 4 factory `resolve*` + 7 `StrictModeGuard` cohérents

772 unit VERT (+13) + 54 functional VERT (+2), zéro régression, zéro secret.

Fichiers modifiés :
. `src/main/kotlin/capsule/VideoFormatConverter.kt` — NOUVEAU (interface +
   Impl + NoOp)
. `src/main/kotlin/capsule/FormatConversion.kt` — NOUVEAU (object pur dispatch)
. `src/test/kotlin/capsule/VideoFormatConverterTest.kt` — NOUVEAU (7 unit)
. `src/test/kotlin/capsule/FormatConversionTest.kt` — NOUVEAU (6 unit)
. `src/functionalTest/kotlin/capsule/FormatConversionFunctionalTest.kt` — NOUVEAU (2 functional)
. `src/main/kotlin/capsule/CapsuleManager.kt` — factory `resolveFormatConverter` + push format
. `src/main/kotlin/capsule/CapsuleVideoTask.kt` — `convertFormatIfEnabled` post-burnIn wiring
. `src/main/kotlin/capsule/DistributeCapsuleVideoTask.kt` — `format` Property + filtre
. `BACKLOG.adoc` — section EPIC CAP-MP4 US-2 statut mis à jour
. `.agents/INDEX.adoc` — header session 099, table sessions (ligne 099), roadmap CAP-MP4
. `.agents/SESSIONS_HISTORY.adoc` — entrée 099 ajoutée
. `PROMPT_REPRISE.adoc` — contexte reprise session 100
. `TODO.adoc` — CAP-MP4 US-2 coché

== Session 098 — CAP-MP4 US-1 OutputFormat enum + OutputConfig.format + merger 4 sources ✅

EPIC CAP-MP4 US-1 terminé (P1, 2ème US sur 4, config pure 1 pt).
`OutputFormat` enum type-safe 3 valeurs `WEBM`/`MP4`/`BOTH` (companion
`fromString` case-insensitive fallback `WEBM` backward compat). `OutputConfig`
étendu 4ème champ `format`. `mergeOutputFormat` helper dédié (pattern
`mergeVersioning`/`mergeCaptureStrategy`). 4 sources ENV `CAPSULE_OUTPUT_FORMAT`
< props `capsule.output.format` < YAML < CLI. `CapsuleExtension.outputFormat`
+ helper Groovy. `CapsuleConventions.outputFormat = WEBM`.
`pushConfigIntoExtension` push format. Baby-step TDD RED → GREEN → REFACTOR.
759 unit VERT (+17 : 8 `OutputFormatTest` + 9 merger `output.format`).

Tâches terminées :
 . `src/main/kotlin/capsule/CapsuleConfig.kt` (MODIFIED) — `OutputFormat` enum + `OutputConfig.format` 4ème champ
 . `src/main/kotlin/capsule/CapsuleConfigMerger.kt` (MODIFIED) — `mergeOutputFormat` helper + `loadFromEnvironment`/`buildConfigFromProperties` étendus + `mergeOutputConfig` étendu
 . `src/main/kotlin/capsule/CapsuleModels.kt` (MODIFIED) — `CapsuleExtension.outputFormat` + helper Groovy + `CapsuleConventions.outputFormat`
 . `src/main/kotlin/capsule/CapsulePlugin.kt` (MODIFIED) — `pushConfigIntoExtension` push format
 . `src/test/kotlin/capsule/OutputFormatTest.kt` (NEW) — 8 unit tests enum
 . `src/test/kotlin/capsule/CapsuleConfigMergerTest.kt` (MODIFIED) — +9 unit tests merger `output.format`
 . `BACKLOG.adoc` — section EPIC CAP-MP4 US-1 statut mis à jour
 . `.agents/INDEX.adoc` — header session 098, table sessions (ligne 098), roadmap CAP-MP4
 . `PROMPT_REPRISE.adoc` — mission session 099 (US-2), contexte reprise
 . `TODO.adoc` — CAP-MP4 US-1 coché
 . `.agents/sessions/098-cap-mp4-us1.adoc` (NEW) — rapport session 098
 . `.agents/SESSIONS_HISTORY.adoc` — ligne 098 ajoutée

== Session 097 — CAP-MP4 US-0 Cadrage EPIC Export MP4/H.264 ✅

EPIC CAP-MP4 démarré (P1, 4 US ~5 pts). Session 097 : US-0 cadrage
uniquement (10 décisions architecturales, zéro code modifié). `OutputFormat`
enum type-safe WEBM/MP4/BOTH défaut WEBM backward compat. Section `output`
étendue 4ème champ `format` (pas nouvelle section). `mergeOutputFormat`
helper dédié pattern `mergeVersioning`. 4 sources ENV<props<YAML<CLI.
`VideoFormatConverter` object pur + NoOp + factory + StrictModeGuard.
Wiring post-burnIn `convertFormatIfEnabled`. `DistributeCapsuleVideoTask`
filtre selon format. Cucumber runner dédié `CapsuleFormatCucumberRunner`.
Baseline 742 unit + 52 functional + 3 cucumber préservée.

== Session 096 — CAP-CR3-3 Branchement ScreenshotCapture (EPIC TERMINÉ 5/5 US) ✅

EPIC CAP-CR3-3 terminé (P0, 3ème EPIC axe post-prod, 5 US ~13 pts).
`ScreenshotCaptureImpl` était jamais câblé dans `CapsuleVideoTask` — désormais
alternative configurable via `capture.strategy: playwright|screenshot` 4 sources.
`CaptureStrategy` enum type-safe (PLAYWRIGHT/SCREENSHOT, défaut PLAYWRIGHT backward
compat). `CaptureResolver` object pur factory dispatch `when(strategy)` avec
fun interfaces (testable sans I/O). `ScreenshotPlanner` object pur (plan + argv
builders). `ScreenshotCaptureImpl` étendu multi-slide (boucle screenshot PNG +
ffmpeg png→webm + concat webm). Cucumber `@capture @strategy` runner dédié
(pattern S-082). 742 unit (+29) + 52 functional (+3) + 3 cucumber dédiés GREEN.

== Session 094 — CAP-SPD US-0 Cadrage + US-1 SpdContext/SpdContextLoader + US-2 CapsuleContext/Builder/PromptBuilder étendus ✅

EPIC CAP-SPD démarré (P0, 2ème EPIC axe contexte augmenté, 5 US ~10 pts).
Session 094 : US-0 cadrage (7 décisions architecturales) + US-1
`SpdContext`/`SpdContextLoader` (10/10 unit GREEN) + US-2
`CapsuleContext.spdSection`/`CapsuleContextBuilder`/`DefaultCapsulePromptBuilder`
étendus (9/9 unit GREEN). US-3/4 restantes.

Correction majeure : capsule public OSS format-agnostic. Le producteur SPD
est un borough privé non nommé publiquement (le cadrage S-092 citait
"planner" publiquement). Capsule valide le *format* SPD (`type=="SPD"`),
pas le *producteur* (`source`). Aucune référence à un borough producteur
dans le code ou les messages publics. SPD absent → vidéo non bloquée
(fallback no-op).

Tâches terminées :
 . `capsule/context/SpdContext.kt` (NEW) — data class immutable 6 champs (objectives, duration, prerequisites, modalities, sessionTitle, module), invariant `sessionTitle.isNotBlank()`
 . `capsule/context/SpdContextLoader.kt` (NEW) — object pur mirror `DocContextLoader`, `load(metadataFile, adocFile, maxTokens)`, valide `type=="SPD"`, parse AsciiDoc (title/attribute/bullet/text sections), rend `==== SPD Pedagogical Context (spd)`, tronque `ContextChannel.Docs.truncateToTokens`
 . `capsule/context/CapsuleContext.kt` (MODIFIED) — 3ème champ `spdSection: String = ""`, invariant ajusté `rendered.isBlank() == (channels.isEmpty() && spdSection.isBlank())`
 . `capsule/context/CapsuleContextBuilder.kt` (MODIFIED) — `build(composite, budget, spdSection="")` étendu, append SPD section après channels, trim, 4 cas
 . `capsule/pipeline/DefaultCapsulePromptBuilder.kt` (MODIFIED) — `buildGeneratePrompt` instruction line SPD (conditionnelle `contains("SPD Pedagogical Context")`)
 . `src/test/kotlin/capsule/context/SpdContextLoaderTest.kt` (NEW) — 10 unit tests `@TempDir` + `jacksonObjectMapper`
 . `src/test/kotlin/capsule/pipeline/DefaultCapsulePromptBuilderTest.kt` (NEW) — 3 unit tests (SPD instruction, empty fallback, no-SPD context)
 . `src/test/kotlin/capsule/context/CapsuleContextBuilderTest.kt` (MODIFIED) — +6 unit tests (build avec spdSection, blank drop, empty channels + spd, invariant accept/reject, coexist)
 . `BACKLOG.adoc` — section EPIC CAP-SPD refondue (7 décisions + 5 US détaillées), US-0 ✅ + US-1 ✅ + US-2 ✅
 . `.agents/INDEX.adoc` — header session 094, roadmap CAP-SPD, table sessions (ligne 094)
 . `PROMPT_REPRISE.adoc` — mission session 095 (US-3 + US-4), contexte reprise
 . `TODO.adoc` — coches CAP-ARCH-5/6 (S-089/090 rétroactif), CAP-DOCCONTEXT ajout, CAP-SPD US-0/1/2 ajout
 . `.agents/sessions/094-cap-spd-cadrage-us1.adoc` (UPDATED) — rapport session 094 (US-0/1/2)
 . `.agents/SESSIONS_HISTORY.adoc` — ligne 094 mise à jour
 . `.agents/TEST_COVERAGE_ANALYSIS.adoc` — entrée session 094 US-2 ajoutée

7 décisions architecturales (BACKLOG) :
 . Producteur SPD = borough privé non nommé publiquement (capsule public OSS format-agnostic)
 . metadata.json K-2 = enveloppe provenance (valide `type=="SPD"` uniquement, pas `source`)
 . N0 `ContextChannel` sealed → extension capsule-local `CapsuleContext.spdSection` (pas de bump N0)
 . Rubriques SPD AFNOR/REAC — `SpdContext` extrait objectifs/durée/prérequis/modalités/titre/module
 . Wiring sur `collectCapsuleAugmentedContext -Pspd.file` (cohérence CAP-DOCCONTEXT)
 . `DefaultCapsulePromptBuilder` étendu — instruction line référençant section SPD (conditionnelle `contains`)
 . Cucumber runner dédié `CapsuleSpdContextCucumberRunner` + steps préfixés "spd context" (bug S-088)

Tests : 713 unit VERT (+9 net : 6 `CapsuleContextBuilderTest` + 3 `DefaultCapsulePromptBuilderTest`), 0 FAIL, 0 SKIP. Zéro
régression (704 baseline préservée). `./gradlew :test` BUILD SUCCESSFUL.

Reste session suivante : US-3 wiring `collectCapsuleAugmentedContext -Pspd.file` + functional, US-4 cucumber BDD.

== Session 093 — CAP-DOCCONTEXT injection corpus documentaire (EPIC TERMINÉ 5/5 US) ✅

EPIC CAP-DOCCONTEXT terminé (5/5 US, ~12 pts). Le canal Docs de `CompositeContext`
(CAP-ARCH-2) était nourri par un seul string CLI `-Pcontext.docsContent`. Désormais,
`context.docsGlobs` (4 sources ENV < props < YAML < CLI) résout des globs Ant-style
et concatène les fichiers documentaires (AFNOR/REAC) via `DocContextLoader` (object
pur). Le LLM reçoit le matériel de formation réel, pas un prompt vide.

Tâches terminées :
. `capsule/ContextConfig.kt` (NEW) — `data class ContextConfig(val docsGlobs: List<String> = emptyList())`
. `capsule/CapsuleConfig.kt` — 8ème section `context: ContextConfig = ContextConfig()`
. `capsule/CapsuleConfigMerger.kt` — `mergeContextConfig` + `mergeStrList` (NEW helper) + `splitCommaList` + `loadFromEnvironment` + `buildConfigFromProperties`
. `capsule/CapsuleModels.kt` — `CapsuleExtension.docsGlobs: ListProperty<String>`
. `capsule/CapsulePlugin.kt` — `pushConfigIntoExtension` push context
. `capsule/context/DocContextLoader.kt` (NEW) — object pur, concatène + tronque `budget.docsTokens`
. `capsule/context/CollectCapsuleAugmentedContextTask.kt` — `docsFiles` + `docsGlobs` + `resolveDocsSection`
. `capsule/CapsuleManager.kt` — lazy `project.provider { }` + `resolveGlobFiles`
. `src/test/kotlin/capsule/CapsuleConfigMergerTest.kt` — +11 unit tests
. `src/test/kotlin/capsule/context/DocContextLoaderTest.kt` (NEW) — 9 unit tests
. `src/functionalTest/kotlin/capsule/context/DocContextFunctionalTest.kt` (NEW) — 3 functional
. `src/test/features/capsule_doc_context.feature` (NEW) — 4 scénarios `@context @docs`
. `src/test/kotlin/capsule/scenarios/CapsuleDocContextCucumberRunner.kt` (NEW) — runner dédié
. `src/test/kotlin/capsule/scenarios/CapsuleDocContextSteps.kt` (NEW) — steps préfixés "doc context"
. `build.gradle.kts` — `CucumberTaskSpec("cucumberTestDocContext")` câblé
. `BACKLOG.adoc` — US-0→4 statuts + correction budget (40% → 10% Docs)

Totaux : 694 unit VERT (+20) + 47 functional VERT (+3) + 4 cucumber GREEN. Zéro FAIL, zéro régression, zéro secret.

== Session 092 — Cadrage 11 EPICs neufs (contexte augmenté + post-production) ✅

Cadrage de 11 EPICs neufs (47 US, ~88 pts) étendant le périmètre capsule sur
2 axes. Axe contexte augmenté (5 EPICs) : CAP-DOCCONTEXT, CAP-SPD, CAP-GLOSSARY,
CAP-PROVENANCE, CAP-TRANSCRIPT. Axe post-production (5 EPICs + CAP-CR3-3) :
CAP-CR3-3, CAP-MP4, CAP-AUDIO, CAP-CHAPITRE, CAP-PODCAST, CAP-PREVIEW. Décision
architecturale CAP-CR3-3 : brancher ScreenshotCapture (non expulser). Séquence
MVP0 définie. Zéro code modifié, `compileKotlin` VERT.

Tâches terminées (gouvernance uniquement) :
. `BACKLOG.adoc` — 11 sections EPIC + synthèse + séquence MVP0 + `:last_update:`
. `.agents/INDEX.adoc` — roadmap 12 lignes + en-tête (session 092 + prochaine 093) + correction faux positif CAP-29.6 (`⬜ TODO` → `✅ TERMINÉ S-082`)
. `PROMPT_REPRISE.adoc` — session 092 + contexte reprise 093 (CAP-DOCCONTEXT) + EPICs restants séquence MVP0
. `.agents/SESSIONS_HISTORY.adoc` — entrée 092
. `.agents/sessions/092-cadrage-epics-neufs.adoc` — rapport de session

== Session 091 — CAP-CR3-2 Mode strict anti-NoOp (EPIC CAP-CR3-2 TERMINÉ 4/4 US) ✅

`capsule.strictMode` DSL (Boolean, défaut `false`, 4 sources ENV < props < YAML
< CLI). `StrictModeGuard` (object pur, domaine `capsule.config`) —
`requireAvailable(strict, engineName, isAvailable, path)` lève
`IllegalStateException` avec message actionable. 6 `resolve*` branchés
(CapsuleBuildTask piper/espeak, CapsuleVideoTask piper/espeak/playwright,
CapsuleManager manim/mixer/subtitle — param `strict` ajouté, défaut `false`
rétrocompatible). `noop` explicite reste légitime, non concerné.
`StrictModeConfig` data class. `CapsuleConfigMerger.mergeStrictModeConfig`.
`CapsuleExtension.strictMode` Property. Cucumber `@strict @config` 4 scénarios
(runner dédié `CapsuleStrictModeCucumberRunner` pattern S-082). 674 unit VERT
(+19) + 44 functional VERT (+4) + 8 cucumber dédiés GREEN. `check` BUILD
SUCCESSFUL 6m54s. Zéro régression, zéro secret.

Tâches terminées :
. `src/main/kotlin/capsule/StrictModeConfig.kt` — data class 1 champ `enabled: Boolean = false`
. `src/main/kotlin/capsule/StrictModeGuard.kt` — object pur, `requireAvailable` lève en strict mode
. `src/main/kotlin/capsule/CapsuleConfig.kt` — étendu `strictMode: StrictModeConfig`
. `src/main/kotlin/capsule/CapsuleConfigMerger.kt` — `mergeStrictModeConfig` + env + props
. `src/main/kotlin/capsule/CapsuleModels.kt` — `CapsuleExtension.strictMode` Property
. `src/main/kotlin/capsule/CapsulePlugin.kt` — `pushConfigIntoExtension` push strictMode
. `src/main/kotlin/capsule/CapsuleBuildTask.kt` — guard piper/espeak
. `src/main/kotlin/capsule/CapsuleVideoTask.kt` — guard piper/espeak/playwright + 3 appelants manim/mixer/subtitle
. `src/main/kotlin/capsule/CapsuleManager.kt` — param `strict` 3 resolve*
. `src/test/kotlin/capsule/StrictModeConfigTest.kt` — 3 unit
. `src/test/kotlin/capsule/StrictModeGuardTest.kt` — 7 unit
. `src/test/kotlin/capsule/CapsuleConfigMergerTest.kt` — +9 strictMode tests
. `src/functionalTest/kotlin/capsule/StrictModeFunctionalTest.kt` — 4 functional
. `src/test/features/capsule_strict_mode.feature` — 4 scénarios `@strict @config`
. `src/test/kotlin/capsule/scenarios/CapsuleStrictModeSteps.kt` — steps self-contained
. `src/test/kotlin/capsule/scenarios/CapsuleStrictModeCucumberRunner.kt` — runner dédié S-082
. `build.gradle.kts` — `CucumberTaskSpec("cucumberTestStrictMode")` câblé

EPIC CAP-CR3-2 TERMINÉ 4/4 US (S-091). Le « faux positif » vidéo est éliminé.

---

== Session 090 — CAP-ARCH-6 Cucumber BDD boundary cross-borough (EPIC CAP-ARCH TERMINÉ 8/8 US) ✅

`capsule_arch_boundary.feature` (4 scénarios `@cross-borough @architecture
@i18n @ai`) single-language français. Runner dédié `CapsuleArchBoundaryCucumberRunner`
(pattern S-082). `CapsuleArchBoundarySteps` self-contained GradleRunner (mock
Ollama 2 réponses alternées propose/generate, fake translateDeck, pré-génération
artefacts fr, ffmpeg WebM valide 1s pour skip). `CucumberTaskSpec("cucumberTestArchBoundary")`
câblé. 655 unit + 40 functional inchangés + 4 cucumber dédiés GREEN. Zéro régression.

Tâches terminées :
. `src/test/features/capsule_arch_boundary.feature` — 4 scénarios (registration, content, video, skip)
. `src/test/kotlin/capsule/scenarios/CapsuleArchBoundaryCucumberRunner.kt` — runner dédié S-082
. `src/test/kotlin/capsule/scenarios/CapsuleArchBoundarySteps.kt` — steps self-contained (mock Ollama alterné, fake translateDeck, ffmpeg WebM skip)
. `build.gradle.kts` — `CucumberTaskSpec("cucumberTestArchBoundary")` câblé `cucumberConventions.additionalTasks`

EPIC CAP-ARCH TERMINÉ 8/8 US (S-083→S-090). Borough Détroit en maintenance.

---

== Session 089 — CAP-ARCH-5 i18n aligné socle (CapsuleMessages) ✅

Domaine `capsule.i18n` créé (8ème) : `CapsuleMessages` object (pattern
`SliderMessages`/`PlantumlMessages`). 11 `Messages*.properties` 10 langues.
Wiring `CapsuleManager` (13 tasks) + `CapsuleFeedTaskRegistrar` (4 tasks).
Fix 3 descriptions françaises hardcoded. 655 unit VERT (+87) + 40 functional.

Tâches terminées :
. `src/main/kotlin/capsule/i18n/CapsuleMessages.kt` — object pur `utf8Control` + `forLanguage`/`get`/`format`/`resolveLanguage(project)`
. `src/main/resources/i18n/Messages.properties` — base EN (23 keys)
. `src/main/resources/i18n/Messages_en.properties` — EN explicite (fallback default locale)
. `src/main/resources/i18n/Messages_{fr,zh,hi,es,ar,bn,pt,ru,ur}.properties` — 9 traductions
. `src/test/kotlin/capsule/i18n/CapsuleMessagesTest.kt` — 65 unit (23 keys × EN/FR + 10 langs + fallback + missing)
. `src/test/kotlin/capsule/i18n/MessagesFrTranslationTest.kt` — 22 unit (assertNotEquals EN/FR)
. `src/main/kotlin/capsule/CapsuleManager.kt` — 13 task registrations i18n'd + import `CapsuleMessages`
. `src/main/kotlin/capsule/CapsuleModels.kt` — `CapsuleExtension.language` ajouté
. `src/main/kotlin/capsule/feed/CapsuleFeedTaskRegistrar.kt` — 4 task registrations i18n'd + import
. `src/main/kotlin/capsule/multilang/MultiLanguageResolver.kt` — KDoc corrigé (remove `DocumentI18nBridge`)

---

== Session 088 — CAP-ARCH-4 Wiring backend + CAP-ARCH-7 US-4 Cucumber distrib ✅

CAP-ARCH-4 : tâche composite `generateCapsuleContentAndVideos` chaîne
`generateCapsuleContent` → `translateAndGenerateCapsuleVideos`. Backend intact.
3 functional + 3 cucumber `@architecture @wiring` GREEN.
CAP-ARCH-7 US-4 : feature `capsule_distrib.feature` (5 scénarios
`@distrib @versioning`) + `CapsuleDistribSteps` + `CapsuleDistribCucumberRunner`.
5 cucumber GREEN. 568 unit + 40 functional VERT.

Tâches terminées :
. `src/main/kotlin/capsule/feed/CapsuleFeedTaskDomain.kt` — constante `GENERATE_CAPSULE_CONTENT_AND_VIDEOS` + `CONTENT_AND_VIDEOS_DESCRIPTION`
. `src/main/kotlin/capsule/feed/CapsuleFeedTaskRegistrar.kt` — `registerGenerateCapsuleContentAndVideos(project)`
. `src/main/kotlin/capsule/CapsuleManager.kt` — câblage `registerGenerateCapsuleContentAndVideos`
. `src/functionalTest/kotlin/capsule/pipeline/GenerateCapsuleContentAndVideosFunctionalTest.kt` — 3 functional (registration, description, dependsOn `--dry-run`)
. `src/test/features/capsule_content_and_videos.feature` — 3 scénarios `@architecture @wiring`
. `src/test/kotlin/capsule/scenarios/CapsuleContentAndVideosSteps.kt` — steps constants pures
. `src/test/kotlin/capsule/scenarios/CapsuleContentAndVideosCucumberRunner.kt` — runner dédié
. `src/test/features/capsule_distrib.feature` — 5 scénarios `@distrib @versioning`
. `src/test/kotlin/capsule/scenarios/CapsuleDistribSteps.kt` — steps self-contained GradleRunner
. `src/test/kotlin/capsule/scenarios/CapsuleDistribCucumberRunner.kt` — runner dédié
. `build.gradle.kts` — 2 `CucumberTaskSpec` (`cucumberTestContentAndVideos` + `cucumberTestDistrib`)

== Session 087 — Code review + Bug burn-in fix + CAP-ARCH-7 Video Destination Versioning (US-1→3) ✅

Code review post-S-083→086, fix bug burn-in, CAP-ARCH-7 US-1→3.
568 unit + 37 functional VERT.

== Session 086 — CAP-ARCH-3 Koog pipeline génération contenu (domaine `capsule.pipeline`) ✅

US-3 de l'EPIC CAP-ARCH. Domaine `capsule.pipeline` : `CapsulePipelineGraph`
(koog 1.0.0, pattern `DeckPipelineGraph` slider) — propose-context →
validate-context → generate-speaker-notes + edges conditionnels. Tâche
`generateCapsuleContent`. 540 unit VERT (+49) + 34 functional VERT (+3).
`check` VERT (3m39s). README master EN mis à jour.

Tâches terminées :
. `src/main/kotlin/capsule/pipeline/CapsuleState.kt` — stage machine `CapsuleStage` + invariants
. `src/main/kotlin/capsule/pipeline/CapsulePromptBuilder.kt` + `DefaultCapsulePromptBuilder.kt` — ports pédagogiques
. `src/main/kotlin/capsule/pipeline/CapsuleLlm.kt` + `ChatModelCapsuleLlm.kt` — port + adapter langchain4j
. `src/main/kotlin/capsule/pipeline/ContentPlanValidator.kt` + `ContentPlanValidationResult.kt` — 8 checks
. `src/main/kotlin/capsule/pipeline/TtsScriptDeriver.kt` — dérivation `*-script.txt`
. `src/main/kotlin/capsule/pipeline/CapsulePipelineGraph.kt` — graphe koog (execute séquentiel + mermaid)
. `src/main/kotlin/capsule/pipeline/GenerateCapsuleContentTask.kt` — tâche (deck + contexte → `.adoc`/`.txt`, UP-TO-DATE, mock routé, durcissement API)
. `src/main/kotlin/capsule/CapsuleManager.kt` — wiring registerGenerateCapsuleContentTask
. `src/test/kotlin/capsule/pipeline/` — 49 unit (CapsuleStateTest, ContentPlanValidatorTest, TtsScriptDeriverTest, CapsulePipelineGraphTest)
. `src/functionalTest/kotlin/capsule/pipeline/GenerateCapsuleContentFunctionalTest.kt` — 3 functional (registration, pipeline E2E, no-deck)
. `README.md` — layout ai/context/pipeline + section "LLM content pipeline (CAP-ARCH-3)" + libs réelles + totaux
. Gouvernance — BACKLOG/INDEX/PROMPT_REPRISE/TODO/SESSIONS_HISTORY/
  TEST_COVERAGE/rapport 086
. Vérifications — `check` BUILD SUCCESSFUL (3m39s), zéro secret,
  marqueurs backlog légitimes, pas de commit (règle 1)

Prochaine session : CAP-ARCH-4 — Wiring exécution existante : sortie pipeline
LLM → `generateCapsuleVideoAllLanguages` sans modifier le backend. Décision
`compileOnly slider` confirmée/remplacée. Baby-step TDD : functional (chaîne
complète NoOp) → cucumber (`@i18n @cross-borough`).

== Session 085 — CAP-ARCH-2 Contexte augmenté (RAG + Graphify, domaine `capsule.context`) ✅

US-2 de l'EPIC CAP-ARCH. Domaine `capsule.context` : `CapsuleContextBuilder`
(object pur) assemble le `CompositeContext` (codebase-contracts 0.1.0) —
canaux EAGER/RAG/Graphify/Docs + `ChannelBudget`. 491 unit VERT (+14) +
31 functional VERT (+3) + 4 cucumber `@context @rag` GREEN. `check` VERT.

Tâches terminées :
. `gradle/libs.versions.toml` + `build.gradle.kts` — codebase-contracts 0.0.2 → 0.1.0 +
  `implementation(libs.codebase.contracts)` + `CucumberTaskSpec("cucumberTestContext")`
. `src/main/kotlin/capsule/context/CapsuleContext.kt` — data class, invariant
  `rendered.isBlank() == channels.isEmpty()`, isEmpty/nonEmptyCount/tokenEstimate
. `src/main/kotlin/capsule/context/CapsuleContextBuilder.kt` — object pur
  (defaultBudget/toChannels/channelsWithBudget/merge/build)
. `src/main/kotlin/capsule/context/CollectCapsuleAugmentedContextTask.kt` — tâche
  `collectCapsuleAugmentedContext` (groupe `collect`, eagerFiles
  `.agents/INDEX.adoc` + `PROMPT_REPRISE.adoc` + `AGENT.adoc`
  `@PathSensitive(RELATIVE)`, `-Pcontext.*`, output `build/capsule/augmented-context.txt`)
. `src/main/kotlin/capsule/CapsuleManager.kt` — wiring registerCollectAugmentedContextTask
. `src/test/kotlin/capsule/context/CapsuleContextBuilderTest.kt` — 14 unit tests
. `src/functionalTest/kotlin/capsule/context/CapsuleAugmentedContextFunctionalTest.kt` — 3 functional (registration, artefact, UP-TO-DATE)
. `src/test/features/capsule_context.feature` + `CapsuleContextSteps.kt` +
  `CapsuleContextCucumberRunner.kt` — 4 scénarios `@context @rag` (runner dédié contourne bug S-082)
. Gouvernance — BACKLOG/INDEX/PROMPT_REPRISE/TODO/SESSIONS_HISTORY/
  TEST_COVERAGE/rapport 085
. Vérifications — `check` BUILD SUCCESSFUL (3m33s), zéro secret,
  marqueurs backlog légitimes, pas de commit (règle 1)

Prochaine session : CAP-ARCH-3 — Koog pipeline génération contenu (domaine
`capsule.pipeline`, `CapsulePipelineGraph` pattern `DeckPipelineGraph` slider,
`generateCapsuleContent`).

== Session 084 — CAP-ARCH-1 Socle LLM bridge (pattern SLD-8, domaine `capsule.ai`) ✅

US-1 de l'EPIC CAP-ARCH. Capsule devient consommatrice du hub codebase (N1)
pour son futur contenu LLM-driven : plugin codebase `implementation` 0.0.5
(`LlmBuildService` + `LlmProvider`), `koog-agents` bump 0.8.0 (dépendance
morte) → 1.0.0 (exclude annotations + buildscript force annotations:26.0.2-1),
langchain4j / langchain4j-ollama (BOM 0.0.16). Domaine `capsule.ai` :
`CapsuleLlmService` (object pur — `registerLlmBuildService` +
`resolveModel(provider, serviceProvider): ChatModel` via
`LlmProviderChatModelAdapter` + `-Pai.provider` default ollama). Tâche
`capsuleAiSmokeTest` (groupe `generate`, `usesService`, mock `-Pollama.baseUrl`,
log `CAPSULE AI → …`) câblée dans `CapsuleManager`. Baby-step TDD/BDD 3 niveaux :
477 unit VERT (+10), 28 functional VERT (+3), cucumber `capsule_ai.feature` `@ai`
3/3 GREEN (mock HTTP, zéro LLM réel). `./gradlew check` VERT. 1 FAIL
`@integration` burn-in préexistant (hors CAP-ARCH, connu S-069/S-072).

Tâches terminées :
. `build.gradle.kts` + `gradle/libs.versions.toml` — codebase 0.0.5 + koog 1.0.0 +
  langchain4j/langchain4j-ollama (BOM) + buildscript force annotations
. `src/main/kotlin/capsule/ai/CapsuleLlmService.kt` — register + resolve + aiProvider
. `src/main/kotlin/capsule/ai/LlmProviderChatModelAdapter.kt` — pont ChatModel
. `src/main/kotlin/capsule/ai/CapsuleAiSmokeTestTask.kt` — tâche smoke test
. `src/main/kotlin/capsule/CapsuleManager.kt` — câblage registerAiSmokeTestTask
. `src/test/kotlin/capsule/ai/` — 12 unit tests (CapsuleLlmService + Adapter)
. `src/functionalTest/kotlin/capsule/ai/` — 3 functional tests GradleTestKit
. `src/test/features/capsule_ai.feature` + `CapsuleAiSteps.kt` — 3 scénarios `@ai`
. Gouvernance — BACKLOG/INDEX/PROMPT_REPRISE/TODO/SESSIONS_HISTORY/
  TEST_COVERAGE/rapport 084
. Vérifications — `check` VERT, zéro secret, marqueurs backlog légitimes,
  pas de commit (règle 1)

Prochaine session : CAP-ARCH-2 — Contexte augmenté (RAG + Graphify, domaine
`capsule.context`, `CapsuleContextBuilder` + `CompositeContext` + `ChannelBudget`).

== Session 083 — CAP-ARCH-0 Cadrage EPIC CAP-ARCH refondu ✅

Cadrage EPIC CAP-ARCH refondu fondé sur le code réel. L'audit corrige le
cadrage S-073 (périmé, fait avant CAP-29) : zéro appel LLM dans capsule (rien
à purger), `koog-agents` 0.8.0 dépendance morte (0 usage), `compileOnly slider`
0 import de classe, `i18n-contracts` déjà consommé (CAP-29.1). Vision refondue
sur la demande product owner : capsule LLM-driven piloté par codebase
(koog + langchain4j + RAG + graphify), backend d'exécution existant recyclé
intact. EPIC redécoupé en 7 US (CAP-ARCH-0 ✅, CAP-ARCH-1→6 TODO, ~21 pts).
Zéro code modifié.

Tâches terminées :
. `BACKLOG.adoc` — section EPIC CAP-ARCH refondue (audit S-083, vision,
  décisions de cadrage, 7 US détaillées), en-tête `last_update` 2026-08-09
. `.agents/INDEX.adoc` — session 083 ajoutée, section EPIC CAP-ARCH refondue,
  roadmap (CAP-ARCH 🟡 EN COURS), métadonnées (prochaine 084)
. `PROMPT_REPRISE.adoc` — réécrit pour session 084 (CAP-ARCH-1, baby-steps 1a-d)
. `TODO.adoc` — CAP-ARCH-0 coché, US 1-6 ajoutées, en-tête mis à jour
. `.agents/SESSIONS_HISTORY.adoc` — entrée 083 ajoutée
. `.agents/sessions/083-caparch0-cadrage-llm-driven.adoc` — rapport de session
. Vérifications — `compileKotlin` VERT, zéro secret, marqueurs backlog légitimes,
  pas de commit (règle 1)

Prochaine session : CAP-ARCH-1 — Socle LLM bridge (pattern SLD-8, domaine
`capsule.ai`, `CapsuleLlmService`, bump koog 1.0.0, purge dépendance morte).

== Session 082 — CAP-29.6 Documentation + dogfooding + clôture EPIC CAP-29 ✅

Documentation du domaine `capsule.multilang` (README master EN + README plugin),
dogfooding réel du pipeline multi-langue (3 WebM fr/en/ar dont 1 RTL), découverte
et fix d'un défaut de nommage (`CapsuleVideoOutputRenamer`), clôture EPIC CAP-29
7/7 US. 464 unit VERT (+4) + 25 functional VERT + 6 cucumber multilang GREEN.

Tâches terminées :
. `README.md` + `capsule-plugin/README.md` — domaine `capsule.multilang`,
  table des tâches, convention nommage `<deckName>_<lang>-deck.html|script.txt|webm`
. Dogfooding réel consommateur composite `includeBuild` — `extractSpeakerNotes`
  réel → `generateCapsuleVideoAllLanguages` réel (Piper fr/en + espeak, chromium
  Playwright) → 3 WebM générés en 3m07
. Défaut nommage découvert — rendu `<deckName>_<lang>-deck.webm` (header script)
  au lieu du plan `<deckName>_<lang>.webm` (`entry.outputVideo`), brisant
  l'économie d'encre (skip jamais déclenché)
. `capsule/multilang/CapsuleVideoOutputRenamer.kt` (nouveau) — object pur
  `rename(rendered, target): File` aligne le WebM rendu sur le contrat plan
. Wiring post-capture dans `GenerateCapsuleVideoAllLanguagesTask.renderEntry`
. Re-dogfooding — alignement `-deck.webm` → `_<lang>.webm` (3 langues) +
  économie d'encre prouvée (`--rerun-tasks` → `0/3 rendered, 3 skipped`)
. Tests — 4 unit `CapsuleVideoOutputRenamerTest` (+4 net, 464 total) +
  `GenerateCapsuleVideoAllLanguagesFunctionalTest` GREEN (25 total) + 6 cucumber
  multilang GREEN (S-081 baseline, non régressés)
. Vérifications — 1 FAIL `@integration` burn-in préexistant (subtitle path
  `build/capsule/` vs `outputDir=capsules`) hors CAP-29, connu S-069/S-072,
  zéro secret

EPIC CAP-29 TERMINÉ 7/7 US. Domaine `capsule.multilang` clos (9 fichiers prod).

Prochaine session : CAP-ARCH (cross-borough slider+codebase, débloqué par
CAP-29 ✅) ou CAP-CR3 (solidité chaîne vidéo — burn-in @integration FAIL).

== Session 081 — CAP-29.5 Cucumber BDD E2E 10 langues ✅

Cucumber BDD E2E multi-langue (CAP-29.5). `multi_language_capsule.feature`
(6 scénarios `@multilang @i18n`) + `MultiLanguageCapsuleSteps` (step defs).
`cucumberTest -PrunCucumber` : 6/6 multilang GREEN. 460 unit VERT (inchangé)
+ 25 functional VERT + kover VERT.

Tâches terminées :
. `src/test/features/multi_language_capsule.feature` (nouveau) — 6 scénarios :
  plan 10 langues (LanguageCatalog), nommage `<deckName>_<lang>.webm`,
  skip langue sans paire deck+script, fail-fast plan vide, E2E GradleRunner NoOp
  → 10 WebM + log `CAPSULE MULTILANG → n/n languages rendered`, E2E RTL ar/ur
. `src/test/kotlin/capsule/scenarios/MultiLanguageCapsuleSteps.kt` (nouveau) —
  helpers plan pur `CapsuleVideoPlanner.plan()` + 2 scénarios GradleRunner NoOp
  (ttsEngine=noop, captureEnabled=false, deck source + 10 scripts par langue,
  assert WebM `build/capsules/*.webm`)
. Vérifications — `compileTestKotlin` VERT, `cucumberTest -PrunCucumber` 6/6
  multilang GREEN (suite 154 tests, 1 FAIL préexistant `@integration` burn-in
  subtitle path — hors périmètre, connu S-069/S-072), `check` BUILD SUCCESSFUL,
  zéro secret

Prochaine session : CAP-29.6 Documentation + dogfooding (1 deck × 3 langues
fr/en/ar) + clôture EPIC CAP-29 (7/7 US).

== Session 080 — CAP-29.4 Câblage translateAndGenerateCapsuleVideos ✅

Tâche composite `translateAndGenerateCapsuleVideos` : câble le pipeline complet
multi-langue (deck source → translateDeck 10 langues → extractSpeakerNotes 10
scripts → generateCapsuleVideoAllLanguages 10 WebM). 460 unit VERT (inchangé)
+ 25 functional (+3) PASS.

Tâches terminées :
. `CapsuleFeedTaskDomain.kt` — +2 constantes `TRANSLATE_AND_GENERATE_CAPSULE_VIDEOS`
  + `TRANSLATE_AND_GENERATE_VIDEOS_DESCRIPTION` (stable task identifiers)
. `CapsuleFeedTaskRegistrar.kt` — +`registerTranslateAndGenerateCapsuleVideos` :
  `tasks.register` + group `capsule` + description + dependsOn
  (`TRANSLATE_AND_EXTRACT_SPEAKER_NOTES` + `generateCapsuleVideoAllLanguages`)
. `CapsuleManager.kt` — câblage 3ème appel registrar dans
  `registerExtractSpeakerNotesTask()`
. Tests — 3 functional `TranslateAndGenerateCapsuleVideosFunctionalTest`
  (RED→GREEN : registration groupe, description mentionne les 2 sous-tâches,
  dependsOn chain via `--dry-run` avec stub `translateDeck` dans projet isolé)
. Vérifications — `check` BUILD SUCCESSFUL (unit 460 + functional + kover,
  cucumber SKIPPED — CR-10), zéro secret

Prochaine session : CAP-29.5 Cucumber BDD E2E 10 langues
(`multi_language_capsule.feature`).

== Session 079 — CAP-29.3 GenerateCapsuleVideoAllLanguagesTask ✅

Tâche `generateCapsuleVideoAllLanguages` : itère le `CapsuleVideoPlan` (S-078),
TTS+capture par langue avec `ttsLanguage` adapté, économie d'encre. Domaine
`capsule.multilang`. 460 unit tests VERT (+6 net) + 1 functional PASS.

Tâches terminées :
. `CapsuleVideoAllLanguagesRunner.kt` (nouveau) — object pur `run(plan,
  probeDuration, render): List<CapsuleVideoEntry>` (callback render injecté —
  frontière domaine/adaptateur nette) + `shouldRender(entry, probeDuration)` :
  skip si WebM existe ET probe > 0, re-render si probe ≤ 0 (artefact invalide)
. `GenerateCapsuleVideoAllLanguagesTask.kt` (nouveau) — tâche Gradle
  `: CapsuleVideoTask`, `@TaskAction override execute()` : build plan via
  `CapsuleVideoPlanner`, itère via runner, réutilise `synthesizeTtsForScript` +
  `captureDeckSequential/Parallel` + `injectAudio` + `computeSlideDurations`
  (CR-1). TTS par langue via `MultiLanguageResolver` : set `ttsLanguage`/
  `ttsVoice`/`espeakVoice`, reset `ttsEngine=null` (engine résolue par champ).
  Log `CAPSULE MULTILANG → x/y languages rendered`
. Refactor base `CapsuleVideoTask.kt` (zéro changement de comportement) —
  `execute()` → `open` (permet override @TaskAction du sous-type),
  `resolveTtsEngine()`/`injectAudio()` → `internal` (réutilisables module)
. `CapsuleManager.kt` — enregistrement `generateCapsuleVideoAllLanguages`
  (groupe generate)
. Tests — 6 unit `CapsuleVideoAllLanguagesRunnerTest` (RED→GREEN : 3 langues
  toutes rendues, skip WebM valide, re-render WebM invalide, plan mixte ordre
  préservé, shouldRender true/false). _Correction RED_ : `LanguageCatalog` n'a
  pas "de" (10 langues en/zh/hi/es/fr/ar/bn/pt/ru/ur) — langues corrigées
  fr/en/ar/ru. 1 functional `GenerateCapsuleVideoAllLanguagesFunctionalTest` :
  3 langues fr/en/ar NoOp → 3 WebM + log `CAPSULE MULTILANG`
. Vérifications — `test` 460 VERT (0 FAIL), functionalTest PASS, `check`
  BUILD SUCCESSFUL (cucumber SKIPPED — CR-10), zéro secret

Note : le skip ink-economy n'est pas observable en functional (ffprobe ne parse
pas le WebM minimal NoOp, duration vide) → couvert de façon déterministe en unit
via `probeDuration` injecté.

Prochaine session : CAP-29.4 câblage `translateAndGenerateCapsuleVideos`
(dependsOn `translateAndExtractSpeakerNotes` + `generateCapsuleVideoAllLanguages`),
functional (NoOp, vérifie dependsOn chain).

== Session 078 — CAP-29.2 CapsuleVideoPlan + CapsuleVideoPlanner ✅

Domaine `capsule.multilang` enrichi. `CapsuleVideoEntry` (data class immutable
4 champs) + `CapsuleVideoPlan` (invariant non-empty) + `CapsuleVideoPlanner`
(object pur). 454 unit tests VERT (+18 net).

Tâches terminées :
. `CapsuleVideoPlan.kt` (nouveau) — `CapsuleVideoEntry` (deckFile, scriptFile,
  language: SupportedLanguage, outputVideo) avec invariants fail-fast (deck
  exists, script exists, language in LanguageCatalog) + `CapsuleVideoPlan`
  (invariant non-empty, accès `size()`/`isEmpty()`/`languages()`)
. `CapsuleVideoPlanner.kt` (nouveau) — object pur `plan(deckDir, scriptDir,
  outputDir, targetLanguages)` : skip silencieux des langues sans deck+script,
  fail-fast si plan vide. Nommage `<deckName>_<lang>-deck.html` +
  `<deckName>_<lang>-script.txt` → `<deckName>_<lang>.webm` (aligné slider)
. Tests — 10 unit `CapsuleVideoPlanTest` (entries, invariants, languages) +
  8 unit `CapsuleVideoPlannerTest` (découverte 3 langues, skip, throw, naming,
  RTL Arabic) avec `@TempDir`
. Vérifications — `test` 454 VERT (0 FAIL), `check` BUILD SUCCESSFUL, zéro secret

Prochaine session : CAP-29.3 `GenerateCapsuleVideoAllLanguagesTask`, baby-step
TDD unit + functional (NoOp engines, 3 langues).

== Session 077 — CAP-29.1 MultiLanguageResolver + VoiceMapping 10 langues ✅

Domaine `capsule.multilang` créé (7ème domaine DDD). `Language` enum supprimé
(4 entrées FR/EN/ES/DE), remplacé par consommation directe de `LanguageCatalog`
(BOM `i18n-contracts` 0.0.2). `VoiceMapping` étendu à 10 langues (fr/en/es/de/
zh/hi/ar/bn/pt/ru/ur). 436 unit tests VERT (+15 net).

Tâches terminées :
. Pré-requis build — BOM `workspace-bom:0.0.16` + `i18n-contracts:0.0.2` ajoutés
  au `libs.versions.toml` + `build.gradle.kts` (`implementation(platform(...))` +
  `implementation(libs.i18n.contracts)`)
. Domaine `capsule.multilang` créé — `VoiceMapping.kt` (62 lignes, 11 Piper +
  11 espeak) + `MultiLanguageResolver.kt` (51 lignes, `ResolvedLanguage` data
  class + resolver object)
. `Language` enum supprimé — `LanguageVoiceMapping.kt` détruit (40 lignes)
. Migration consommateurs — `TtsManager.kt` (`TtsEngine.language(): SupportedLanguage?`,
  `PiperTtsEngine`/`EspeakTtsEngine` prennent `SupportedLanguage?`),
  `CapsuleVideoTask.kt:197` + `CapsuleBuildTask.kt:40` (`Language.fromCode` →
  `MultiLanguageResolver.resolve`)
. Tests — 22 unit `MultiLanguageResolverTest` (resolve 10 codes, unknown→null,
  RTL ar/ur, piperModel/espeakVoice 11 codes, resolveByPiperModel/EspeakVoice,
  supportedCodes 10) + 2 tests `LanguageVoiceMappingTest` adaptés (délèguent
  à `MultiLanguageResolver`) + 4 tests `PiperTtsEngine`/`EspeakTtsEngine` migrés
  (`Language.EN/DE/ES` → `LanguageCatalog.findByCode`)
. Vérifications — `compileKotlin` VERT, `test` 436 VERT (0 FAIL), `check`
  BUILD SUCCESSFUL (53s), zéro secret, zéro référence `Language` résiduelle

Prochaine session : CAP-29.2 `CapsuleVideoPlan` + `CapsuleVideoPlanner`,
baby-step TDD unit.

== Session 076 — CAP-29.0 Cadrage EPIC pipeline multi-langue ✅

Cadrage formel EPIC CAP-29 (Pipeline Traduction Vidéo Multi-Langue). Zéro code
modifié — session de vision et découpage baby-step.

Tâches terminées :
. Audit code existant — `Language` enum 4 entrées (FR/EN/ES/DE), `VoiceMapping`
  4 Piper+4 espeak, `generateCapsuleVideo` mono-langue, `translateAndExtractSpeakerNotes`
  SLD-11.4 déjà câblé, `slider.translateDeck` 10 langues via `LanguageCatalog`
. Décision d'architecture — consommer `LanguageCatalog` (10 langues, contrats N0
  `i18n-contracts`) au lieu d'étendre enum local. Évite dette technique détruite
  dans CAP-ARCH-1. Pattern document-gradre (`DocumentI18nBridge`), plantuml-gradle
  (`PlantumlTranslationServiceAdapter`)
. Domaine cible — `capsule.multilang` (7ème domaine DDD)
. Découpage US — 7 US (CAP-29.0 cadrage + CAP-29.1→6 exécution), ~20 pts
. Gouvernance — BACKLOG/INDEX/PROMPT_REPRISE/AGENTS racine mis à jour
. Vérifications — `compileKotlin` VERT, zéro secret, backlog synchronisé

Prochaine session : CAP-29.1 `MultiLanguageResolver` + extension `VoiceMapping`
10 langues, baby-step TDD unit.

== Session 075 — CR-10 CI optimization cucumberTest onlyIf ✅

CI optimization (CR-10, 🟡 MOYEN). `cucumberTest` skippé sauf demande explicite
(`-PrunCucumber` ou `CI=true`) pour garder `./gradlew check` rapide en local.

Tâches terminées :
. Domaine `capsule.ci.CucumberTestGuard` (data class pure, 4 méthodes :
  shouldRun/shouldSkip/skipReason) — nouveau domaine DDD
. 7 unit tests `CucumberTestGuardTest` (runCucumber active, CI active, both,
  neither, shouldSkip negation, skipReason mention, skipReason blank)
. Câblage `onlyIf { shouldRun }` dans `build.gradle.kts` (logique inlinée —
  script ne peut pas importer classpath `main`)
. BDD `cucumber_skip.feature` (4 scénarios `@ci`) + `CucumberSkipSteps`
  (5 step defs)
. `./gradlew check` VERT — cucumberTest SKIPPED sans flag (garde active)
. `./gradlew cucumberTest -PrunCucumber` s'exécute (garde désactivée)
. Bug préexistant découvert : `CucumberTestRunner` ne lance que `capsule.feature`
  (à investiguer session future)

Métriques : +7 unit tests (421 total), EPIC CR TERMINÉ (10/10), `check` VERT
sans cucumber.

Rapport : `.agents/sessions/075-cr10-ci-optimization-cucumber-onlyif.adoc`.

== Session 074 — CR-9 Robust HTML parsing ✅

Refactor robust HTML parsing (CR-9, 🟡 MOYEN). Regex raw `Regex("""<section\b[^>]*>""")`
dans `injectAudioSequentialFallback` remplacée par `HtmlSectionParser.findTopLevelSectionOpenTags`.

Tâches terminées :
. `HtmlSectionParser.findTopLevelSectionOpenTags(html): List<MatchResult>` (38 lignes)
  — depth-tracking existant réutilisé (skip nested + self-closing)
. `injectAudioSequentialFallback` : 2 lignes regex raw supprimées → 1 ligne
  `HtmlSectionParser.findTopLevelSectionOpenTags`
. DRY — réutilise `HtmlSectionParser` (DRY-2 S-031)
. Bug fix : vertical stacks (sections imbriquées) n'étaient pas skippées par la
  regex raw → `data-audio` injecté sur sous-sections
. +8 unit tests `HtmlSectionParserTest.findTopLevelSectionOpenTags` (flat, nested,
  self-closing, attributes, empty, malformed, positions, multiline)
. +1 unit test `CapsuleVideoTaskTest.sequential fallback skips nested sections in vertical stack`
  (data-audio count = 2, pas 4)
. +1 scénario cucumber `@audio @sequential` + 2 step defs (Given nested, Then exactly N)
. `./gradlew test` VERT — 414 unit tests, 0 FAIL, 0 SKIP

Métriques : +9 unit tests (414 total), regex raw supprimée, HtmlSectionParser réutilisé.

Rapport : `.agents/sessions/074-cr9-robust-html-parsing.adoc`.

== Session 073 — CR-8 Logging structuré + EPIC CAP-ARCH cadré ✅

CR-8 (🟡 MOYEN) — `CapsulePlugin.kt:40-48` 1 ligne monolithic 13 placeholders →
4 lignes `logger.lifecycle` par section (TTS/Capture/Subtitles/Manim).
`CapsuleConfigLogger` object pur extrait (domaine `capsule.config`),
`formatConfigLog(config): List<String>`. +9 unit tests + 1 scénario cucumber
`@config @logging`. EPIC CAP-ARCH cadré au BACKLOG (6 US ~19 pts).

Rapport : `.agents/sessions/073-cr8-logging-structure-cap-arch.adoc`.

== Session 072 — CR-5 DRY CapsuleConfigMerger ✅

Refactor DRY du `CapsuleConfigMerger.kt` (CR-5, 🟠 MAJEUR). 5 paires de méthodes
`mergeXxxConfig`/`mergeXxxConfigNoYaml` (~76 lignes dupliquées) unifiées en
5 méthodes + 4 helpers génériques.

Tâches terminées :
. `merge()` unifié — `yaml: CapsuleConfig? = if (yamlLoaded) yamlConfig else null`
. 10 méthodes `mergeXxxConfig`/`mergeXxxConfigNoYaml` → 5 méthodes unifiées (yaml nullable)
. 4 helpers génériques extraits : `mergeStr/mergeInt/mergeDouble/mergeBoolean`
. Sémantique préservée : `toBoolean()` lenient, `isNotBlank()` heuristic strings
. +13 unit tests NoYaml (48 total ConfigMergerTest, +12 vs S-071)
. +1 scénario cucumber `@config @no-yaml` + 1 step Given
. `./gradlew test` VERT — 396 unit tests
. `./gradlew cucumberTest` : scénario @no-yaml PASS, 1 échec @integration préexistant

Métriques : 322→302 lignes (-19 net), 396 unit VERT (+12), 48/48 ConfigMergerTest PASS.

Rapport : `.agents/sessions/072-cr5-dry-config-merger.adoc`.

== Session 071 — CR-2 + Fix Cucumber + CR-3/4/6/7 ✅

CR-2 (thread safety + timeout `captureSlideParallel`) + Fix 5 échecs cucumber
consolidé (DuplicateStepDefinitionException + feature quotes + temp file) +
CR-3 (error handling TTS retry 1× + accumulation `failedSlides` + logger.error) +
CR-4 (HTML escape XSS `injectSubtitleTrack`, HtmlEscape object) + CR-6 (robust
CLI parsing cliInt/cliDouble/cliBoolean) + CR-7 (validation YAML déjà implémenté
+ test). 384 unit tests VERT (+23), 64 cucumber 0 FAIL, 3 functional VERT.

Rapport : `.agents/sessions/071-cr2-fix-cucumber-cr3-4-6-7.adoc`.

== Session 070 — Sync slider 0.0.15 → 0.0.16 ✅

Synchronisation consommateur capsule-gradle suite à la publication slider
0.0.16 (session 066 slider-gradle) + BOM 0.0.17 (session 019 workspace-bom).
Bump catalogue `capsule-plugin/gradle/libs.versions.toml` `slider = "0.0.15"`
→ `"0.0.16"`. `./gradlew compileKotlin` VERT avec slider 0.0.16 (mavenLocal).
Zéro code production modifié. Session pure sync consommateur (cross-borough).

Tâches terminées :
. Bump `capsule-plugin/gradle/libs.versions.toml` slider 0.0.15 → 0.0.16
. `./gradlew :slider:publishToMavenLocal` côté slider-gradine (sync mavenLocal)
. `./gradlew compileKotlin` capsule-plugin VERT avec slider 0.0.16
. Mise à jour INDEX + SESSIONS_HISTORY + création archive août

1 fichier build bumpé — commit + push cette session (accord pilote donné).

Rapport : `.agents/sessions/070-sync-slider-0.0.16.adoc`.

== Session 069 — EPIC CAP-MODEL TERMINÉ ✅

Unification modèle `capsule.feed` ↔ `capsule.CapsuleScript` (7/7 US :
cadrage + 6 exécution). `SlideType` enum migré vers `capsule.feed`,
`SlideSegment` étendu (type+manimScene), `CapsuleScriptReader` remplace
`CapsuleManager.parseScript` (64 lignes supprimées), 6 consommateurs migrés,
`capsule.CapsuleModels` purgé (SlideSegment/CapsuleScript/SlideType supprimés,
reste CapsuleExtension/CapsuleConventions). 361 unit tests VERT (+124).
Cucumber boundary `consolidated_capsule_model.feature` (5 scénarios). Zéro
doublon modèle.

Rapport : `.agents/sessions/069-cap-model-unification.adoc`.

== Session 073 — CR-8 Logging structuré ✅

Refactor logging structuré `CapsulePlugin.kt:40-48` (CR-8, 🟡 MOYEN). 1 ligne
monolithic avec 13 placeholders `{}` → 4 lignes `logger.lifecycle` par section
(TTS, Capture, Subtitles, Manim).

Tâches terminées :
. `CapsuleConfigLogger` object pur extrait (domaine `capsule.config`, 23 lignes)
. `formatConfigLog(config): List<String>` → 4 lignes structurées
. `formatTtsLine` / `formatCaptureLine` / `formatSubtitleLine` / `formatManimLine`
. Tokens préservés verbatim (engine=, quality=, subtitle=, subtitleFormat=, burnIn=, scriptsDir=)
. `CapsulePlugin.kt` câblage (l.40-48 monolithic → 3 lignes forEach)
. +9 unit tests `CapsuleConfigLoggerTest` (4 sections, tokens, defaults, non-blank)
. +1 scénario cucumber `@config @logging` (5 steps) + 5 step defs
. `./gradlew test` VERT — 405 unit tests (+9), 0 failures, 0 errors

EPIC CAP-ARCH cadré au BACKLOG : collaboration cross-borough slider+codebase pour
contenu et i18n (6 US ~19 pts, inspiration document/bakery/plantuml). ⬜ TODO.

Métriques : 405 unit VERT (+9), 1 scénario cucumber ajouté.

Rapport : `.agents/sessions/073-cr8-logging-structure-cap-arch.adoc`.
== Session 087 — Code review + Bug burn-in fix + CAP-ARCH-7 Video Destination Versioning (2026-08-10)

*Code review post-S-083→086* : 4 points relevés.

*Bug burn-in fix* (CAP-ARCH-7 US-0 implicite) : `CapsuleOutputDirResolver`
(object pur, package `capsule.support`) résout l'`outputDir` depuis le
`build.gradle` du projet Cucumber. 9 unit tests TDD. 3 steps Cucumber migrés.
Runner `cucumberTestIntegration` câblé. **BUILD SUCCESSFUL 3m39s** — scénario
E2E burn-in VERT (était FAIL depuis S-069).

*CAP-ARCH-7 Video Destination Versioning* (nouvelle feature issue review) :
- US-1 : `OutputConfig` + `VersioningStrategy` (TIMESTAMP|INCREMENTAL) étend
  `CapsuleConfig` section `output`. `CapsuleConfigMerger` 4 sources. 10 unit.
- US-2 : `VideoDestinationResolver` (object pur). 10 unit.
- US-3 : `DistributeCapsuleVideoTask` (tâche `distributeCapsuleVideo`). 3 functional.

Métriques : 568 unit VERT (+28 net) + 37 functional VERT (+3). Cucumber
`@burnin @integration` E2E VERT.

Rapport : `.agents/sessions/087-code-review-burn-in-fix-caparch7.adoc`.

== Session 095 — CAP-SPD US-3 Wiring + US-4 Cucumber + renommage SPD→PedagogicalScenario ✅

EPIC CAP-SPD TERMINÉ 5/5 US (S-094 US-0/1/2, S-095 US-3/4 + renommage).
713 unit + 49 functional (+2) + 3 cucumber dédiés GREEN, zéro régression.

*Renommage sémantique* (la sémantique "SPD" n'appartient pas à capsule,
borough public OSS format-agnostic) :
- `SpdContext` → `PedagogicalScenario` (data class)
- `SpdContextLoader` → `PedagogicalScenarioLoader` (object pur)
- `SpdContextLoaderTest` → `CapsuleScenarioLoaderTest`
- `CapsuleContext.spdSection` → `scenarioSection`
- Header `"==== SPD Pedagogical Context (spd)"` → `"==== Pedagogical Scenario (scenario)"`
- Instruction line `contains("SPD Pedagogical Context")` → `contains("Pedagogical Scenario")`
- `metadata.json` validation `type=="SPD"` inchangé (format externe K-2)

*US-3 Wiring* `collectCapsuleAugmentedContext -Pcapsule.context.scenarioFile` :
- `ContextConfig.scenarioFile: String?` (8ème champ section context)
- `mergeContextConfig` étendu `mergeStr` + `loadFromEnvironment` + `buildConfigFromProperties`
- `CapsuleExtension.scenarioFile: Property<String>` + `pushConfigIntoExtension`
- `CollectCapsuleAugmentedContextTask.scenarioFile: ConfigurableFileCollection @Optional`
- `resolveScenarioSection()` (5% budget = 400 tokens/8000, directory→metadata.json+first.adoc, file→direct adoc)
- `CapsuleManager` wiring lazy provider
- 2 functional `ScenarioContextFunctionalTest` GREEN

*US-4 Cucumber BDD* :
- `capsule_scenario_context.feature` 3 scénarios `@context @scenario`
- `CapsuleScenarioContextSteps` (préfixés "scenario context" — bug S-088)
- Runner dédié `CapsuleScenarioContextCucumberRunner` (pattern S-082)
- `CucumberTaskSpec("cucumberTestScenarioContext")` câblé
- 3/3 scénarios PASSED (1m06s)

Tâches terminées :
 . Renommage 3 fichiers via `git mv` (PedagogicalScenario, PedagogicalScenarioLoader, PedagogicalScenarioLoaderTest)
 . `ContextConfig.kt` — `scenarioFile: String? = null`
 . `CapsuleConfigMerger.kt` — `mergeContextConfig` scenarioFile
 . `CapsuleModels.kt` — `CapsuleExtension.scenarioFile: Property<String>`
 . `CapsulePlugin.kt` — `pushConfigIntoExtension` scenarioFile
 . `CollectCapsuleAugmentedContextTask.kt` — scenarioFile + resolveScenarioSection
 . `CapsuleManager.kt` — wiring lazy provider scenarioFile
 . `ScenarioContextFunctionalTest.kt` (NEW) — 2 functional US-3
 . `capsule_scenario_context.feature` (NEW) — 3 scénarios US-4
 . `CapsuleScenarioContextSteps.kt` (NEW) — BDD steps US-4
 . `CapsuleScenarioContextCucumberRunner.kt` (NEW) — runner dédié US-4
 . `build.gradle.kts` — `CucumberTaskSpec("cucumberTestScenarioContext")`

Métriques : 713 unit VERT (inchangé) + 49 functional VERT (+2) + 3 cucumber
dédiés GREEN. `./gradlew test functionalTest` BUILD SUCCESSFUL (1m31s).

Rapport : `.agents/sessions/095-cap-spd-us3-us4-rename.adoc`.

== Session 100 — CAP-MP4 US-3 Cucumber BDD + clôture EPIC CAP-MP4 (2026-08-11)

EPIC : CAP-MP4 — Export MP4/H.264 + format distribution
US : US-3 (4ème et dernière — clôture EPIC 4/4 US)
Méthode : Baby-step BDD RED → GREEN → REFACTOR (niveau cucumber après
unit S-098 + functional S-099)

Livrables :
 . `src/test/features/capsule_format.feature` (NEW) — 3 scénarios
   `@distrib @format` (WEBM default, MP4 only, BOTH)
 . `src/test/kotlin/capsule/scenarios/CapsuleFormatCucumberRunner.kt` (NEW) —
   runner dédié pattern S-082
 . `src/test/kotlin/capsule/scenarios/CapsuleFormatSteps.kt` (NEW) — 6 step
   defs self-contained GradleRunner, préfixés "format distribution" (bug S-088)
 . `build.gradle.kts` — `CucumberTaskSpec("cucumberTestFormat")` câblé
   (9ème CucumberTaskSpec, 10 cucumber dédiés total)

Tâches terminées :
 . Cucumber BDD `capsule_format.feature` 3 scénarios (Default WEBM, MP4 only,
   BOTH) — NoOp converter, validate DistributeCapsuleVideoTask filter
 . Runner dédié `CapsuleFormatCucumberRunner` (pattern S-082
   `@SelectClasspathResource` + filter `@distrib and @format and not @wip
   and not @integration`)
 . Steps `CapsuleFormatSteps` (6 step defs, pattern `CapsuleDistribSteps`
   mirror, préfixés "format distribution" — bug S-088 glue partagée)
 . `CucumberTaskSpec("cucumberTestFormat")` câblé `build.gradle.kts`

Métriques : 772 unit VERT (baseline S-099 préservée) + 54 functional VERT
(baseline préservée) + 10 cucumber dédiés GREEN (66 tests, 0 FAIL dont
`cucumberTestFormat` 6 tests / 3 scénarios). `./gradlew check` BUILD
SUCCESSFUL 15m38s. 2 `NoTestsDiscoveredException` dans `cucumberTest`
global préexistantes (bug S-082, hors CAP-MP4, `cucumberTest` skippé CI
par CR-10). Zéro régression, zéro secret.

EPIC CAP-MP4 TERMINÉ 4/4 US :
 . US-0 cadrage (S-097) — 10 décisions architecturales
 . US-1 enum+merger (S-098) — 17 unit
 . US-2 converter+wiring (S-099) — 13 unit + 2 functional
 . US-3 cucumber (S-100) — 3 scénarios cucumber GREEN

Rapport : `.agents/sessions/100-cap-mp4-us3-cucumber-cloture.adoc`.

== Session 105 — CAP-AUDIO US-3 Wiring applyAudioPostIfEnabled (2026-08-11)

_Borough_ : Détroit (capsule-gradle)
_EPIC_ : CAP-AUDIO (P1) — post-production audio
_US_ : US-3 Wiring `CapsuleVideoTask.applyAudioPostIfEnabled`

Livrables :
 . `CapsuleVideoTask.kt` modifié (+22 lignes : 2 imports + 2 câblages
   + 1 méthode 20 lignes `applyAudioPostIfEnabled`)
 . `AudioPostFunctionalTest.kt` créé (139 lignes, 3 functional tests)
 . Câblage `captureDeckSequential` L493 + `captureDeckParallel` L524
   (post-burnIn, pre-convertFormat)

Méthode `applyAudioPostIfEnabled` (pattern `burnInSubtitlesIfEnabled`) :
 . Skip si `!bgmEnabled && !duckingEnabled` (économie d'encre défaut
   false → no-op backward compat)
 . Skip si `finalVideo` n'existe pas → warn + return
 . Factory `resolveAudioPostProcessor(ffmpegPath, strict)`
 . `AudioPostConfig` depuis `capsuleExtension.audioPost*` (5 champs)
 . `processor.process(finalVideo, tmpFile, config)` → si succès
   rename tmpFile → finalVideo + log, si échec warn + garde original
   (degraded)
 . `catch (AudioPostException)` warn + garde original
 . `finally` delete tmpFile

Métriques : 819 unit VERT (inchangé baseline S-104) + 60 functional
VERT (+3 net `AudioPostFunctionalTest`) + 11 cucumber dédiés (inchangés).
`./gradlew check` BUILD SUCCESSFUL 9m20s. Cucumber SKIPPED par défaut
(CR-10). Zéro régression, zéro secret.

EPIC CAP-AUDIO 4/5 US terminés (10/12 pts) :
 . US-0 cadrage (S-102) — 12 décisions architecturales
 . US-1 config+merger (S-103) — 20 unit
 . US-2 processor+factory (S-104) — 17 unit
 . US-3 wiring (S-105) — 3 functional
 . US-4 cucumber — TODO (S-106)

Rapport : `.agents/sessions/105-cap-audio-us3-wiring.adoc`.

== Session 106 — CAP-AUDIO US-4 Cucumber BDD + clôture EPIC CAP-AUDIO (5/5 US)

_Date_ : 2026-08-11
_Type_ : feat+bdd+cucumber+cloture

Feature `capsule_audio_post.feature` `@audio @post` (4 scénarios : default
NoOp skip économie d'encre, BGM enabled NoOp → degraded, ducking only NoOp
→ degraded, ducking + BGM NoOp → degraded) + `CapsuleAudioPostSteps` (13
step defs préfixés "audio post", pattern `CapsuleCaptureStrategySteps` —
étape interne `generateCapsuleVideo`, pas tâche standalone) +
`CapsuleAudioPostCucumberRunner` (pattern S-082 `@SelectClasspathResource`
+ filter `@audio and @post and not @wip and not @integration`) +
`CucumberTaskSpec("cucumberTestAudioPost")` 12ème câblé `build.gradle.kts`
(11ème cucumber dédié, 12ème `CucumberTaskSpec` total avec
`cucumberTestIntegration`). Position : après `cucumberTestDurationValidation`,
avant `cucumberTestIntegration`. NoOp processor (pas de vrai FFmpeg —
`ffmpegExecutablePath = "noop"` routes through `NoOpAudioPostProcessor`
which returns `false`, keeping original video) — valide le wiring +
economy-of-ink guard + factory dispatch, pas le rendu audio (couvert par
US-2 unit tests `AudioPostProcessorTest` 17 unit).

Baby-step BDD RED → GREEN → REFACTOR : RED (feature + steps + runner +
CucumberTaskSpec écrits) → GREEN (4/4 scénarios PASSED en 7m21s, BUILD
SUCCESSFUL) → REFACTOR (`./gradlew check` BUILD SUCCESSFUL in 24m12s, 819
unit VERT baseline S-105 préservée, 60 functional VERT baseline S-105
préservée, `validatePlugins` VERT, Kover VERT, zéro régression, zéro
secret).

EPIC CAP-AUDIO TERMINÉ 5/5 US (12/12 pts) :
 . US-0 cadrage (S-102) — 12 décisions architecturales
 . US-1 config+merger (S-103) — 20 unit
 . US-2 processor+factory (S-104) — 17 unit
 . US-3 wiring (S-105) — 3 functional
 . US-4 cucumber (S-106) — 4 cucumber scénarios

Domaine `capsule.audio` (10ème) clos (6 fichiers prod : `AudioPostConfig`,
`AudioPostProcessor` interface, `AudioPostProcessorImpl`,
`NoOpAudioPostProcessor`, `AudioPostCommand`, `AudioPostException`).

Rapport : `.agents/sessions/106-cap-audio-us4-cucumber-cloture.adoc`.

== Session 108 — CAP-PROVENANCE US-1+US-2 + clôture EPIC CAP-PROVENANCE (3/3 US) ✅

_Date_ : 2026-08-11
_Type_ : feat+tdd+functional+cucumber+cloture

EPIC CAP-PROVENANCE (P1, 3 US ~5 pts) TERMINÉ — l'artefact de provenance
`build/capsule/context-provenance.json` liste par canal les sources ayant
contribué au contexte augmenté + chars/tokens (ancrage anti-hallucination).

US-1 (data classes + builder étendu) : `ProvenanceSource(fileName, chars,
tokens)` + `ProvenanceChannel(channel, sources)` + `ContextProvenance`
(`totalChars`/`totalTokens` exposés JSON via `@get:JsonProperty` — les `fun`
ne sérialisent pas Jackson, `toJson()` INDENT_OUTPUT, `SCENARIO_CHANNEL`)
+ `ProvenanceTracker` mutable accumulator (`trackChannel` replace + ignore
empty, `retainOnly` prune, `build()` snapshot) — 4 fichiers prod domaine
`capsule.context`. `CapsuleContextBuilder.build()` étendu
`tracker: ProvenanceTracker? = null` (prune aux canaux survivants
`it.type.name` + SCENARIO conditionnel, backward compat null).

US-2 (wiring + functional + cucumber) : `CollectCapsuleAugmentedContextTask`
`@OutputFile provenanceFile` + `trackSources(tracker)` (EAGER = eagerFiles
`.filter{it.exists()}` + estimateTokens ; RAG = `rag-injected` ; Graphify =
`graphify-injected` ; DOCS = docsFiles ou `docs-injected` ; SCENARIO =
compagnon `.adoc`) + log `CAPSULE PROVENANCE → N channels, M sources → <path>`.
`CapsuleManager.registerCollectAugmentedContextTask` : `provenanceFile.set(...)`.

2 fix TDD (itérations GREEN) : (a) FileNotFoundException sur AGENT.adoc
absent → `.filter{it.exists()}` sur eagerFiles (run+trackSources) et
docsFiles ; (b) `scenarioFile` répertoire CAP-SPD-3 → résolution compagnon
`.adoc` (crash `ScenarioContextFunctionalTest`).

Baby-step TDD/BDD : unit → functional → cucumber.
. **RED** : 15 unit (12 `ContextProvenanceTest` + 3 builder) +
   2 functional (`CollectProvenanceFunctionalTest`) + feature/steps/runner
. **GREEN** : 4 fichiers prod + builder étendu + wiring task + fix (a)(b)
. **REFACTOR** : `./gradlew check` BUILD SUCCESSFUL in 15m52s

Totaux : 834 unit VERT (+15 net) + 62 functional VERT (+2) + 3 cucumber
GREEN (`capsule_provenance.feature` via `cucumberTestProvenance`
`CucumberTaskSpec` 13ème, ~1m55s). Zéro régression, zéro secret.

EPIC CAP-PROVENANCE TERMINÉ 3/3 US :
 . US-0 cadrage (S-107) — audit 6 points + 11 décisions architecturales
 . US-1 data classes + builder (S-108) — 15 unit
 . US-2 wiring + functional + cucumber (S-108) — 2 functional + 3 cucumber

Domaine `capsule.context` clos (10 fichiers prod : CapsuleContext +
CapsuleContextBuilder + DocContextLoader + SpdContext + SpdContextLoader +
CollectCapsuleAugmentedContextTask + 4 Provenance*).

Rapport : `.agents/sessions/108-cap-provenance-cloture.adoc`.

== Session 110 — CAP-GLOSSARY US-1 GlossaryEntry + GlossaryContext + GlossaryLoader + CapsuleContext.glossarySection + Builder + PromptBuilder étendus ✅

EPIC CAP-GLOSSARY US-1 terminé (P1, 2ème US sur 4, data classes + loader +
builder + prompt builder étendus 2 pts).

Domaine `capsule.context` étendu (3 nouveaux fichiers prod : `GlossaryEntry.kt`
data class immutable invariant fail-fast blank term/definition,
`GlossaryContext.kt` data class immutable liste ordonnée, `GlossaryLoader.kt`
object pur mirror `PedagogicalScenarioLoader` — `load(glossaryFile: File,
maxTokens: Int): String` parse AsciiDoc `== Glossary` + bullets `- term:
definition` split premier `:`, rend `==== Official Glossary (glossary)`,
tronque via `ContextChannel.Docs.truncateToTokens(maxTokens)`, missing file →
empty, malformed bullet skipped, non-Glossary section ignored, zero budget →
empty). `CapsuleContext` 4ème champ `glossarySection: String = ""` invariant
ajusté backward compat. `CapsuleContextBuilder.build()` 4ème paramètre
`glossarySection` + `buildRendered` refactor `when` 4 cas → `listOf(...).
filter { isNotBlank }.joinToString("\n\n")` 3 sections conditionnelles +
`tracker.retainOnly` étendu `+= GLOSSARY_CHANNEL`. `DefaultCapsulePromptBuilder`
instruction "Use the official terminology" conditionnelle `contains("Official
Glossary")` pattern `Pedagogical Scenario`. `ContextProvenance.GLOSSARY_CHANNEL`
const. `CollectCapsuleAugmentedContextTask` appel `build()` migré arguments
nommés.

Baby-step TDD strict RED (23 tests → `Unresolved reference`) → GREEN
(857 unit VERT +23 net : 15 `GlossaryLoaderTest` + 7 `CapsuleContextBuilderTest`
glossarySection + 2 `DefaultCapsulePromptBuilderTest` glossary) → REFACTOR
(`check` BUILD SUCCESSFUL in 18m48s, Kover VERT, validatePlugins VERT,
62 functional VERT inchangé, zéro régression, zéro secret). Backward compat
strict (`glossarySection = ""` défaut → tests existants inchangés).

Rapport : `.agents/sessions/110-cap-glossary-us1.adoc`.

== Session 120 — EPIC MEMPHIS-BOM-FIX US-5 TERMINÉ (2026-08-25)

Cross-borough MEMPHIS (BOM 0.0.27 Central) + Queens (codebase 0.0.10
Central) + Detroit (capsule fix).

Catalogue capsule fixé : `slider` → `slider-plugin` (artifactId réel depuis
aplatissement slider S-067), slider 0.0.16 → 0.0.18, codebase 0.0.8 → 0.0.10,
BOM 0.0.16 → 0.0.27.

Bug discovery : `testImplementation(libs.slider)` amenait Groovy 3.0.17
(via asciidoctor-gradle-jvm-slides → jrubygradle-resolver) sur le classpath
de test, cassant ProjectBuilder (Groovy 4 vs 3.x). 79/917 tests FAILED
préexistantes. Retrait — aucun test n'importait slider.

Gate : `test` 917 tests 0 FAIL + `cucumberTestTranscript` 4/4 PASSED.
CAP-TRANSCRIPT EPIC TERMINÉ 6/6 US. EPIC MEMPHIS-BOM-FIX TERMINÉ 5/5 US.

== Session 123 — CAP-28 US-0 audit partiel (2026-08-25)

Audit 3 fichiers low-coverage (PiperTtsEngine 31%, ManimEngineImpl 25%,
ScreenshotCaptureImpl 26%). Gaps identifiés par fichier. Décisions
préliminaires (pas de nouveau domaine, baby-step TDD strict, tests sans
I/O uniquement, 3 US ~11 unit).

Clôture anticipée par pilote — US-0 cadrage non finalisé (BACKLOG détaillé
pas MàJ), US-1/2/3 non démarrés. Zéro code modifié — session gouvernance
pure. Baseline 977 unit VERT préservée, 69 functional VERT, cucumber dédiés
GREEN.

Rapport : `.agents/sessions/123-cap-28-audit.adoc`.

== Session 124 — CAP-28 Coverage Gaps (TERMINÉ 4/4 US)

Date : 2026-08-25
EPIC : CAP-28 — Coverage gaps (Kover P2)
Statut : ✅ TERMINÉ 4/4 US

US-0 cadrage BACKLOG détaillé (7 décisions, correction audit S-123 :
ManimEngineImpl n'a pas de cache availabilityProbe — seul ScreenshotCaptureImpl
l'a). US-1 PiperTtsEngine 4 unit (`PiperTtsEngineTest.kt` nouveau — `language()`
2 branches + `resolvedModel` 2 branches). US-2 ManimEngineImpl 2 unit
(`probeDuration` delegation). US-3 ScreenshotCaptureImpl 3 unit (capture empty
durations throws, isAvailable ffmpeg absent, close resets probe).

Baby-step TDD strict RED → GREEN. 986 unit VERT 0 FAIL (+9 net baseline
977 → 986). Zéro prod modifié — pure couverture. Découverte : LanguageCatalog
N0 ne contient pas "de" (10 langues). EPIC CAP-28 clos.

Rapport : `.agents/sessions/124-cap-28-coverage.adoc`.

---

== Session 125 (2026-08-25) — CAP-PODCAST US-0 Cadrage

EPIC : CAP-PODCAST — Extraction audio-only (mode podcast)
Statut : 🟡 EN COURS 1/3 US (US-0 ✅, US-1/2 TODO)

Cadrage formel EPIC CAP-PODCAST — audit 15 constats + 12 décisions
architecturales (gouvernance pure, zéro code). Domaine `capsule.podcast` 13ème.
`PodcastConfig` 13ème section `CapsuleConfig` (2 champs defaults backward
compat). 4 sources DSL. `PodcastConcatenator` object pur FFmpeg concat demuxer
+ Impl + NoOp + factory `resolvePodcastConcatenator` 8ème. Tâche
`generateCapsulePodcast` dependsOn `generateCapsuleVideo` (MP3 slides produits
par `synthesizeTtsForScript`). Économie d'encre skip si `podcastOutput` existe
+ `probeDuration > 0.0`. `CapsuleMessages` 25ème key. Runner dédié 17ème
`CapsulePodcastCucumberRunner` + steps préfixés "podcast" (bug S-088).

3 US ~4 pts (US-0 ✅, US-1 config+concatenator+factory unit TODO S-126, US-2
task+wiring+cucumber TODO S-127). Baseline 986 unit VERT préservée.
Pattern S-057/S-063.

Rapport : `.agents/sessions/125-cap-podcast-cadrage.adoc`.

---

== Session 126 (2026-08-25) — CAP-PODCAST US-1 Config + Concatenator + Factory

EPIC : CAP-PODCAST — Extraction audio-only (mode podcast)
Statut : 🟡 EN COURS 2/3 US (US-0 ✅, US-1 ✅, US-2 TODO)

Domaine `capsule.podcast` (13ème) créé : `PodcastConfig` (2 champs defaults
backward compat) + `PodcastConcatenator` interface (`concatenate`/
`isAvailable`/`name`) + `PodcastConcatCommand` object pur
(`buildConcatListContent` one `file '...'` per line + `buildConcatArgv`
FFmpeg concat demuxer) + `PodcastConcatenatorImpl` (FFmpeg, temp list file,
stream copy, deletes list in finally) + `NoOpPodcastConcatenator` (returns
false degraded). `CapsuleConfig` 13ème section. `mergePodcastConfig` 4
sources. `CapsuleExtension` +2 properties. `pushConfigIntoExtension`
podcast block. `CapsuleManager.resolvePodcastConcatenator` 8ème factory.

Baby-step TDD strict RED → GREEN → REFACTOR. 1014 unit VERT 0 FAIL
(+28 net baseline 986). Zéro régression, zéro secret, zéro bump BOM.
4 fichiers prod créés + 5 modifiés + 2 test créés + 1 modifié.

Rapport : `.agents/sessions/126-cap-podcast-us1-config-concatenator.adoc`.
